---
phase: 05-per-tenant-arcadedb-isolation
plan: 08
type: execute
wave: 5
depends_on: ["05-03", "05-06", "05-07"]
files_modified:
  - tests/test_arcadedb_physical_tenant_isolation.py
  - tests/test_compose_config.py
  - docs/architecture.md
  - docs/configuration.md
  - CHANGELOG.md
autonomous: true
requirements: [ARC-07, TEST-05]
must_haves:
  truths:
    - "A mandatory live test against arcadedata/arcadedb:26.7.1 creates three distinct physical databases for A/B/C and directly proves each contains only its exact tenant's records (D-16, D-17)."
    - "Concurrent identical payloads plus unique canaries remain isolated across representative memory and document write/search/list/get/update/delete paths, including foreign stable-ID attempts; every tenant-bearing operation still binds the exact validated `user_identifier` predicate (D-17, D-19)."
    - "Case variants and Unicode lookalikes route to distinct databases; invalid identities fail before registry/server mutation and raw identities never appear in logs, errors, registry bytes, manifests, cache status, or diagnostics (D-02, D-07, D-19)."
    - "Live lifecycle coverage races same/different tenant first use, exercises cache eviction/reuse, fails closed for a missing previously-ready database, and recovers correctly after ArcadeDB restart (D-20)."
    - "Docker-unavailable local runs skip explicitly, while CI=true turns dependency absence/skip into a hard failure through the existing no-skip-as-green hook (D-16, D-20)."
  artifacts:
    - path: "tests/test_arcadedb_physical_tenant_isolation.py"
      provides: "Pinned live A/B/C physical isolation, foreign-ID, leakage, cache, missing-ready, restart, and real-file async proof"
      contains: "pytest.mark.integration"
    - path: "docs/architecture.md"
      provides: "Database-per-tenant routing/provisioning/manifest/defense-in-depth architecture"
      contains: "agentmem_t_v1_"
    - path: "docs/configuration.md"
      provides: "Naming key, registry, cache, retry, startup, operator correlation, recovery, and non-rotation configuration"
      contains: "AGENTMEMORY_TENANT_NAMING_KEY"
    - path: "CHANGELOG.md"
      provides: "ARC-07 and TEST-05 contract change record"
      contains: "Per-tenant ArcadeDB"
  key_links:
    - from: "tests/test_arcadedb_physical_tenant_isolation.py"
      to: "src/turing_agentmemory_mcp/tenant_router.py"
      via: "real concurrent operations resolve through the production router/provisioner"
      pattern: "TenantRouter"
    - from: "tests/test_arcadedb_physical_tenant_isolation.py"
      to: "ArcadeDB /api/v1/query/<opaque-database>"
      via: "direct per-database inspection of every tenant-bearing type"
      pattern: "list_databases"
    - from: "tests/test_compose_config.py"
      to: "compose.yaml"
      via: "pinned image, durable registry path/env, and no shared database fallback assertions"
      pattern: "AGENTMEMORY_TENANT_REGISTRY_PATH"
---

<objective>
Close ARC-07 and TEST-05 with a live pinned-service proof: concurrent three-tenant physical
separation, defense-in-depth foreign-ID denial, identity/diagnostic leakage checks, cache and
provisioning chaos, restart recovery, and truthful deployment/operator documentation
(D-16 through D-20).

Purpose: Fake tests prove branches; only a live multi-database workload proves that routing,
ArcadeDB lifecycle APIs, manifests, queries, and document workers compose into physical
isolation under concurrency.
Output: Mandatory live integration/chaos tests, Compose contract assertions, and updated docs.
</objective>

<execution_context>
@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-per-tenant-arcadedb-isolation/05-CONTEXT.md
@.planning/phases/05-per-tenant-arcadedb-isolation/05-RESEARCH.md
@.planning/phases/05-per-tenant-arcadedb-isolation/05-PATTERNS.md
@.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
@.planning/phases/05-per-tenant-arcadedb-isolation/05-03-SUMMARY.md
@.planning/phases/05-per-tenant-arcadedb-isolation/05-06-SUMMARY.md
@.planning/phases/05-per-tenant-arcadedb-isolation/05-07-SUMMARY.md
@tests/test_arcadedb_chaos_restart.py
@tests/test_arcadedb_tenant_isolation.py
@tests/conftest.py
</context>

<interfaces>
From completed Phase 05 plans:
- `derive_tenant_database_identity(exact_id, naming_key)` computes expected physical names.
- `TenantRouter.resolve(exact_id)` returns immutable tenant store views.
- `TenantRouter.tenant_status(exact_id)` is non-provisioning and pseudonymous.
- `ArcadeDBClient.list_databases/create_database` and the provisioner implement server lifecycle.
- Foreground tools and document manager use the same StoreResolver.
</interfaces>

<artifacts_this_phase_produces>
- `tests/test_arcadedb_physical_tenant_isolation.py` module-scoped pinned ArcadeDB fixture with cleanup restricted to names derived from an explicit fixed test key
- Live tests for concurrent A/B/C memory/document operations, physical DB inspection, foreign IDs, exact identity variants, leakage, first-use races, cache lifecycle, missing-ready fail-closed, restart recovery, and a real-file async ingest/search/delete path
- Compose policy assertions for pinned `arcadedata/arcadedb:26.7.1`, durable registry path, explicit naming-key injection, and absence of shared tenant-data fallback
- Updated architecture/configuration/changelog contracts
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove concurrent A/B/C physical and predicate isolation on the pinned live service</name>
  <files>tests/test_arcadedb_physical_tenant_isolation.py</files>
  <read_first>
    - .planning/phases/05-per-tenant-arcadedb-isolation/05-CONTEXT.md (D-16 through D-19 exact workload/leakage contract)
    - .planning/phases/05-per-tenant-arcadedb-isolation/05-RESEARCH.md (`Validation Architecture` and `Live proof contract`)
    - .planning/phases/05-per-tenant-arcadedb-isolation/05-PATTERNS.md (`Concurrent and live isolation tests`; combine fake concurrency, live transport, and chaos conventions)
    - tests/test_arcadedb_tenant_isolation.py (ThreadPoolExecutor workload and app-layer isolation assertions)
    - tests/test_arcadedb_chaos_restart.py (pinned Docker service detection/cleanup and local-skip/CI-hard-fail convention)
    - tests/test_arcadedb_client.py (live database-qualified client fixture and server command conventions)
    - tests/conftest.py (existing CI skip-to-failure policy; reuse, do not duplicate/weaken)
  </read_first>
  <behavior>
    - Fixed explicit test key derives distinct expected database names for tenants A/B/C, case variants, and Unicode lookalikes; each appears in live server database listing.
    - Concurrent identical memory/document payloads plus unique canaries exercise memory add/store/search/list/get/update/delete and document ingest/search/reindex/delete with no foreign result/mutation.
    - Tenant A using B resource IDs in get/update/delete/document filters returns absence/denial and leaves B data unchanged.
    - Every tenant-bearing operation binds the exact validated `user_identifier`; direct query of every physical database shows all tenant-bearing records carry only that exact tenant and no foreign canary, while manifest fields match opaque identity and omit raw identifiers.
    - Captured registry bytes, logs, errors, view reprs, global/tenant statuses, and diagnostics contain none of the exact A/B/C identifiers.
    - Invalid identifiers fail before database listing changes or registry bytes change.
  </behavior>
  <action>
    Create the marked integration module using the existing compose `arcadedb` service and exact
    pinned image contract. Use an explicit fixed 32+ byte naming key and temporary registry; derive
    every cleanup name and delete only databases created by this fixture. If Docker/service is
    unavailable locally, emit one explicit `pytest.skip` with the dependency reason; do not catch
    or skip assertion/product failures, and rely on `tests/conftest.py` for CI hard failure. Build
    production router/provisioner/store dependencies with deterministic test embed/extract/rerank
    fakes so database behavior, not GPU providers, is under test. Execute A/B/C operations through
    the public store/router surface concurrently, use identical collision-prone content plus unique
    canaries, attempt foreign IDs, then inspect each physical database through its bound client.
    Check every tenant-bearing type present, manifest, registry raw bytes, captured logging/errors,
    and status payloads. Assert the exact validated `user_identifier` binding on every representative
    tenant-bearing query/write path. Include exact case/Unicode distinctions and invalid-input before/after
    mutation counts. Keep test cleanup in `finally`/fixture teardown.
  </action>
  <verify>
    <automated>python -m pytest tests/test_arcadedb_physical_tenant_isolation.py -q -k "physical and not lifecycle_chaos"</automated>
  </verify>
  <acceptance_criteria>
    - Live test uses the running/pinned 26.7.1 service and three actual top-level databases, not a multi-database fake.
    - Every representative memory/document operation and foreign-ID path is asserted under concurrency.
    - Direct inspection plus exact `user_identifier` binding assertions prove database-level and record-predicate isolation simultaneously.
    - Raw tenant identifiers are absent from manifest/registry/log/error/cache/diagnostic surfaces.
    - Product/assertion failures never convert to skip; cleanup touches only fixture-derived databases.
  </acceptance_criteria>
  <done>TEST-05 has a real A/B/C physical-isolation and defense-in-depth proof under concurrency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise first-use, cache, missing-ready, restart, and real-file lifecycle chaos</name>
  <files>tests/test_arcadedb_physical_tenant_isolation.py</files>
  <read_first>
    - tests/test_arcadedb_physical_tenant_isolation.py (Task 1 fixture, exact-key cleanup boundary, and production router assembly)
    - .planning/phases/05-per-tenant-arcadedb-isolation/05-CONTEXT.md (D-20 lifecycle-chaos gate)
    - .planning/phases/05-per-tenant-arcadedb-isolation/05-RESEARCH.md (live cache/restart/missing-ready contract)
    - tests/test_arcadedb_chaos_restart.py (container restart/reconnect wait and CI dependency policy)
    - tests/test_document_ingest_file.py (real staged-file async manager/status flow)
    - src/turing_agentmemory_mcp/document_job_manager.py (resolved per-job store and truthful terminal cleanup semantics from 05-07)
  </read_first>
  <behavior>
    - Concurrent same-tenant first use produces one ready database/manifest; different-tenant first use overlaps and creates independent databases.
    - Capacity/TTL eviction followed by reuse yields a fresh local immutable view of the same durable database and existing data; an active evicted reference remains usable.
    - Deleting a fixture tenant database after its registry row is ready causes the next resolve to fail closed without recreating it.
    - Force-restarting the ArcadeDB service makes tenant operations/health degrade visibly, then existing tenant data becomes reachable again after server readiness/re-resolution without manual database recreation.
    - A real temporary document file follows async stage -> job -> tenant-resolved ingest -> truthful success -> cited scoped search -> staged-byte removal, and remains absent from another tenant.
  </behavior>
  <action>
    Extend the live module with lifecycle tests using barriers for same/different first-use races,
    injected clock/small cache bounds for deterministic eviction, and the existing Docker restart
    helper. Simulate missing-ready only inside the fixture by dropping one fixture database through
    the privileged test client after registry-ready; assert the router does not recreate it. For
    restart, assert an observable unavailable/degraded period and then successful exact-tenant
    retrieval after the service returns. Add one real `.md`/supported file through the actual
    upload/document manager path and poll its durable job to a truthful terminal state; verify
    canonical chunks/cited search/tenant separation and staged-byte cleanup. Do not weaken leases,
    timeouts, or skip policy, and do not introduce production database deletion/offboarding APIs.
  </action>
  <verify>
    <automated>python -m pytest tests/test_arcadedb_physical_tenant_isolation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Same/different first-use, eviction, active-reference, missing-ready, and restart behaviors run against live ArcadeDB.
    - Missing-ready resolve fails and the database remains absent until fixture cleanup; production has no drop API.
    - Restart shows a real degraded interval and returns correct existing data, not an empty replacement.
    - Real-file async path reaches success, cited scoped retrieval, and staged cleanup.
    - Under CI=true, Docker/service absence or an integration skip is a hard failure through existing conftest policy.
  </acceptance_criteria>
  <done>Lifecycle chaos proves readiness, cache, restart, missing-data, and background file routing do not break isolation or durability.</done>
</task>

<task type="auto">
  <name>Task 3: Lock deployment/configuration contracts, document operations, and run the full gate</name>
  <files>tests/test_compose_config.py, docs/architecture.md, docs/configuration.md, CHANGELOG.md</files>
  <read_first>
    - compose.yaml (final Wave-4 tenant registry/key/cache/retry env and persistent-volume wiring)
    - .env.example (operator-facing values and no-fallback naming-key comment)
    - tests/test_compose_config.py (existing parsed-compose policy assertions)
    - docs/architecture.md (current single-store architecture/data flow to replace)
    - docs/configuration.md (ArcadeDB/application environment sections and secret guidance)
    - CHANGELOG.md (current milestone entry style)
    - .planning/phases/05-per-tenant-arcadedb-isolation/05-CONTEXT.md (deferred key rotation/offboarding/OIDC boundaries must remain explicit)
  </read_first>
  <action>
    Add Compose policy assertions for the pinned ArcadeDB image, durable registry path/volume,
    explicit naming-key env injection, bounded cache/retry settings, and absence of
    `ARCADEDB_DATABASE` as a production tenant-data fallback. Update architecture with the
    exact validator -> HMAC identity -> registry -> provisioner/manifest -> router -> immutable
    store view flow, foreground/background resolver paths, and physical-plus-predicate defense
    layers. Update configuration with secure 32+ byte base64 key generation/injection, immutable
    non-rotation warning, registry backup/corruption/missing-ready recovery posture, bounded cache/
    retry settings, pseudonymous operator correlation, and layered health. Explicitly keep OIDC,
    rotation/migration, offboarding/delete, cross-tenant reporting, and fleet rollout deferred.
    Add CHANGELOG entries for ARC-07/TEST-05. Run focused/live/full pytest, ruff, Compose config,
    file-size gate, and deterministic E2E score; fix product regressions rather than relaxing tests.
  </action>
  <verify>
    <automated>python -m pytest -q && python -m ruff check src tests scripts && docker compose config --quiet && bash scripts/check-file-size.sh && python scripts/e2e_score.py --out e2e-results.json</automated>
  </verify>
  <acceptance_criteria>
    - Compose contract tests and `docker compose config --quiet` pass with no shared tenant-data database fallback.
    - Architecture/configuration docs match implemented symbols/env/state semantics and identify every deferred boundary.
    - Full pytest, ruff, file-size, live isolation, and deterministic E2E gates are green.
    - No test count/score claim is hardcoded; verification uses command exit status and generated output.
  </acceptance_criteria>
  <done>Deployment and operator contracts are truthful, and the complete repository/live isolation gate proves Phase 5 ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant router/workers to multiple live ArcadeDB databases | Full application behavior crosses the physical isolation boundary under concurrency. |
| Operator/CI environment to live dependency and lifecycle controls | Test credentials/key, Docker availability, restart, and cleanup have privileged effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08-01 | Information Disclosure | cross-database result/log/error/diagnostic channels | high | mitigate | Task 1 A/B/C canaries, foreign IDs, direct DB inspection, and raw-identity scans cover data and side channels. |
| T-05-08-02 | Elevation of Privilege | tenant A uses tenant B resource IDs | high | mitigate | Public-path get/update/delete/filter adversarial tests plus direct post-state inspection prove denial and no mutation. |
| T-05-08-03 | Tampering | manifest/client/cache mismatch under lifecycle changes | high | mitigate | Task 2 live first-use, eviction, missing-ready, and restart tests exercise immutable binding and fail-closed reconciliation. |
| T-05-08-04 | Denial of Service | unavailable Docker/ArcadeDB hidden by skip | high | mitigate | Explicit integration mark/skip only for local dependency absence; existing CI=true conftest turns the skip into failure. |
| T-05-08-05 | Repudiation / Data loss | restart or missing-ready silently creates empty tenant | high | mitigate | Live restart must recover existing data; missing-ready database remains absent and resolve fails without create. |
| T-05-08-06 | Tampering | overbroad privileged test cleanup | medium | mitigate | Fixture derives and records exact opaque names from its fixed key and drops only those names in teardown. |
</threat_model>

<verification>
- `python -m pytest tests/test_arcadedb_physical_tenant_isolation.py -q`
- `python -m pytest -q`
- `python -m ruff check src tests scripts`
- `docker compose config --quiet`
- `bash scripts/check-file-size.sh`
- `python scripts/e2e_score.py --out e2e-results.json`
</verification>

<success_criteria>
- ARC-07: every exact tenant has a separate ready/manifest-verified database plus mandatory query predicates.
- TEST-05: live concurrent A/B/C workloads and adversarial foreign IDs show no leakage or mutation.
- D-16 through D-20 pass across deterministic unit and live lifecycle layers with no skip-as-green.
- Operator/deployment documentation is complete and deferred features remain out of scope.
</success_criteria>

<output>
Create `.planning/phases/05-per-tenant-arcadedb-isolation/05-08-SUMMARY.md` when done.
</output>
