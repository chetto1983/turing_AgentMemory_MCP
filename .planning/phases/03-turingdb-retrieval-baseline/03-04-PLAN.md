---
phase: 03-turingdb-retrieval-baseline
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements: [ARC-01]
must_haves:
  truths:
    - "the Turing AgentMemory MCP is installed into Claude Code and reachable via the turing-agentmemory skill"
    - "a human confirms an Italian corpus file ingests and returns tenant-scoped, cited retrieval through the live MCP tools (D-12 supplemental confidence)"
  artifacts: []
  key_links:
    - from: "Claude Code MCP client"
      to: "the running Dockerized MCP (127.0.0.1:8095)"
      via: "installed MCP server config + skills/turing-agentmemory operations"
      pattern: "turing-agentmemory"
---

<objective>
Supplemental, hands-on validation (D-12): install this MCP into Claude Code and use the
existing `skills/turing-agentmemory` skill to drive ingest + tenant-scoped cited retrieval
on the Italian corpus BY HAND, so the baseline is confidence-checked interactively, not
only trusted as a script number.

Purpose: D-12 is explicitly SUPPLEMENTAL — the committed, comparable baseline is the two
scripts' numeric output (plans 03-02/03-03), NOT this interactive session. This plan
produces NO committed artifact; it is a human-verify gate that builds confidence and can
surface gross correctness/tenant-isolation problems the aggregate score would hide.

Output: a human confirmation (recorded in SUMMARY) that ingest + cited retrieval work
hands-on against the current TuringDB stack.
</objective>

<artifacts_produced>
None committed. This plan records a human confirmation only (D-12 is supplemental).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-turingdb-retrieval-baseline/03-CONTEXT.md
@skills/turing-agentmemory/SKILL.md
@skills/turing-agentmemory/references/operations.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the MCP into Claude Code and confirm the skill can reach it</name>
  <read_first>
    - skills/turing-agentmemory/SKILL.md (the installed-skill entry point + tool map)
    - skills/turing-agentmemory/references/mcp-tools.md (the memory/document tool names + user_identifier scoping contract)
    - skills/turing-agentmemory/references/operations.md (ingest + search operating steps)
  </read_first>
  <files>none (client-side MCP registration in Claude Code; no repo source modified)</files>
  <action>
    With the Dockerized stack from 03-02 up (or brought back up via `docker compose up -d`; the TuringDB
    volume is durable so prior ingested data persists), register the running MCP as an MCP server in Claude
    Code pointing at the exposed endpoint (`http://127.0.0.1:8095/mcp/`), following the skill's own
    installation guidance in SKILL.md. Confirm the client lists the memory + document tools. Do not modify
    any repo source — this is client-side wiring only; `files_modified` is empty.
  </action>
  <verify>
    <human-check>Claude Code lists the turing-agentmemory MCP tools (e.g. memory_search, document_ingest_file, document_search) and a trivial call succeeds against 127.0.0.1:8095.</human-check>
  </verify>
  <acceptance_criteria>
    - The MCP is registered in Claude Code and its tools are discoverable.
    - A trivial scoped call (e.g. an empty-result document_search under a test user_identifier) returns without transport/auth error.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (CHECKPOINT): Hands-on validate ingest + cited retrieval on the Italian corpus</name>
  <files>none (interactive validation — commits nothing)</files>
  <action>
    Pause for the human to drive one real ingest + tenant-scoped cited-retrieval round-trip on the
    Italian corpus through the installed MCP tools (see how-to-verify), and record the outcome. This is
    the D-12 supplemental confidence check — it does not gate the committed numeric baseline.
  </action>
  <what-built>
    The MCP is installed in Claude Code (Task 1) against the live TuringDB stack. The 03-02 benchmark
    run already ingested the Italian corpus under its timestamped scope; alternatively a single corpus
    file can be freshly ingested via the skill's document_ingest_file flow.
  </what-built>
  <how-to-verify>
    Using the skill's operations, the human drives one real round-trip on the Italian corpus:
    1. Pick one Italian document (PDF, XLSX, EPUB, or webpage). Either search the scope 03-02 already
       ingested, or ingest the file fresh via `document_ingest_file` and poll `document_ingest_status`
       to a truthful terminal `succeeded`.
    2. Run a document_search in Italian scoped to that `user_identifier`; confirm the results are
       relevant AND carry citations (document/chunk locator) back to the source.
    3. Confirm tenant scoping: the same query under a DIFFERENT `user_identifier` returns nothing
       from that document (no cross-tenant leakage) — a quick sanity check of invariant #1.
    Report "approved" if ingest + cited, tenant-scoped retrieval behave correctly, or describe the
    discrepancy. Because D-12 is supplemental, a discrepancy here is a signal to investigate/record,
    not necessarily a blocker on the committed numeric baseline (which is 03-02/03-03).
  </how-to-verify>
  <resume-signal>Type "approved" (with a one-line note on what you searched + that citations + tenant scoping held), or describe the issue observed.</resume-signal>
  <acceptance_criteria>
    - A human confirms at least one Italian document ingested to a truthful `succeeded` state (or was already present) and returned tenant-scoped, cited retrieval.
    - The cross-tenant sanity check showed no leakage.
    - The confirmation (query used, formats exercised, outcome) is recorded in SUMMARY.md.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code MCP client → running MCP | An interactive client drives the existing memory/document tools; no new server surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Information Disclosure | interactive session surfacing Italian corpus content on screen | low | accept | The session is local + operator-driven; no content is committed by this plan. Retrieved content re-entering the agent prompt is treated as untrusted evidence (CLAUDE.md invariant #7). |
| T-03-07 | Elevation of Privilege | client-supplied `user_identifier` selecting a tenant | low | mitigate | The hands-on check explicitly exercises tenant scoping (invariant #1) — a cross-tenant query must return nothing; a leak is reported at the checkpoint. |
| T-03-SC | Tampering | package installs | — | N/A | No package installs this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Claude Code lists the MCP tools and a scoped call succeeds.
- Human confirms ingest → truthful terminal state → tenant-scoped cited retrieval on ≥1 Italian document, with no cross-tenant leakage.
</verification>

<success_criteria>
- D-12 supplemental hands-on validation performed and its outcome recorded (confidence-building; not the committed baseline).
</success_criteria>

<output>
Create `.planning/phases/03-turingdb-retrieval-baseline/03-04-SUMMARY.md` when done — record what was searched, which formats were exercised, and the human approval note.
</output>
