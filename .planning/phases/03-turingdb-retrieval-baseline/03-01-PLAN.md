---
phase: 03-turingdb-retrieval-baseline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/real_document_benchmark_scoring.py
  - scripts/real_document_benchmark.py
  - tests/test_real_document_benchmark.py
autonomous: true
requirements: [ARC-01]
must_haves:
  truths:
    - "load_frozen_questions parses a valid frozen-questions file into a questions_by_document mapping"
    - "load_frozen_questions raises ValueError on a malformed, empty, or wrong-shape frozen file (fails loud, never silent)"
    - "resolve_questions returns the frozen questions for a filename WITHOUT invoking the generator when a frozen set is loaded"
    - "real_document_benchmark.py accepts --frozen-questions and every tracked *.py stays at or under 600 LOC"
  artifacts:
    - path: "scripts/real_document_benchmark_scoring.py"
      provides: "load_frozen_questions + resolve_questions helpers (D-08 frozen-questions load contract)"
      contains: "def load_frozen_questions"
    - path: "scripts/real_document_benchmark.py"
      provides: "additive --frozen-questions CLI flag + resolve_questions call-site branch"
      contains: "--frozen-questions"
    - path: "tests/test_real_document_benchmark.py"
      provides: "3 new tests covering the loader + skip-generation contract"
      contains: "load_frozen_questions"
  key_links:
    - from: "scripts/real_document_benchmark.py"
      to: "scripts/real_document_benchmark_scoring.py"
      via: "re-export import block (both try/except branches) imports load_frozen_questions + resolve_questions"
      pattern: "load_frozen_questions"
    - from: "scripts/real_document_benchmark.py run()"
      to: "resolve_questions"
      via: "per-file loop calls resolve_questions(frozen, path.name, generate=...) instead of generator.generate directly"
      pattern: "resolve_questions"
---

<objective>
Add the D-08 frozen-questions load path to `real_document_benchmark.py` so Phase 6
can replay this phase's exact generated questions (no regeneration) and measure
retrieval drift instead of question drift. This is the ONLY permitted code touch in
this snapshot phase (per CONTEXT.md `<domain>` — do not change generation/scoring
for the baseline run itself; the additive path is inert unless `--frozen-questions`
is passed).

Purpose: Frozen questions become a cross-phase input contract (this phase → Phase 6).
The loader is validation logic with a defined I/O contract, so it is built TDD:
RED (failing tests + interface stubs) → GREEN (implementations pass). D-08.

Output: `load_frozen_questions` + `resolve_questions` in
`real_document_benchmark_scoring.py`, an additive `--frozen-questions` flag + call-site
branch in `real_document_benchmark.py`, and 3 new tests in
`tests/test_real_document_benchmark.py`.
</objective>

<artifacts_produced>
This plan produces (Phase-3 artifact inventory, code portion):
- `scripts/real_document_benchmark_scoring.py::load_frozen_questions(path: Path) -> dict[str, list[dict[str, str]]]`
- `scripts/real_document_benchmark_scoring.py::resolve_questions(frozen, filename, *, generate) -> tuple[list, dict]`
- `scripts/real_document_benchmark.py` — new `--frozen-questions` argparse flag + `resolve_questions` call-site branch in `run()`
- `tests/test_real_document_benchmark.py` — `test_load_frozen_questions_round_trips`, `test_load_frozen_questions_rejects_malformed`, `test_resolve_questions_skips_generation_when_frozen`

(The committed `baseline/03-turingdb/` directory + its files are produced by plans 03-02/03-03.)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-turingdb-retrieval-baseline/03-CONTEXT.md
@.planning/phases/03-turingdb-retrieval-baseline/03-PATTERNS.md
@.planning/phases/03-turingdb-retrieval-baseline/03-RESEARCH.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Failing tests + interface stubs for load_frozen_questions / resolve_questions</name>
  <read_first>
    - scripts/real_document_benchmark_scoring.py (lines 1-30 imports/header; lines ~141-210 `file_digest`, `select_passages`, `parse_generated_questions` — the validate-and-raise-ValueError convention to mirror)
    - scripts/real_document_benchmark.py (lines 30-55 re-export import blocks; lines 73-95 `parse_args`; lines 460-473 the per-file question-generation call-site)
    - tests/test_real_document_benchmark.py (lines 1-70 import block + existing arrange/act/assert test style; `test_parse_generated_questions_rejects_paraphrased_answers` for the `pytest.raises(ValueError)` shape)
    - .planning/phases/03-turingdb-retrieval-baseline/03-PATTERNS.md (the "add load_frozen_questions" and "additive --frozen-questions branch" sections carry the exact designed signature + minimal-diff shape)
  </read_first>
  <files>tests/test_real_document_benchmark.py, scripts/real_document_benchmark_scoring.py, scripts/real_document_benchmark.py</files>
  <behavior>
    - test_load_frozen_questions_round_trips: given a valid payload {"schema_version":1,"questions_by_document":{"file.pdf":[{source_id,question,answer,evidence_quote}]}} written to tmp_path, load_frozen_questions returns a dict equal to questions_by_document.
    - test_load_frozen_questions_rejects_malformed: a payload missing `questions_by_document`, one with an empty mapping, and one whose rows drop a required key each raise ValueError (parametrize or three asserts via pytest.raises(ValueError)).
    - test_resolve_questions_skips_generation_when_frozen: resolve_questions({"f.pdf":[rows]}, "f.pdf", generate=<callable that raises AssertionError if invoked>) returns (rows, usage-with-frozen-flag) WITHOUT calling generate; and with frozen=None it returns whatever generate() yields.
  </behavior>
  <action>
    Establish the interface, then write failing tests against it.

    1. In `scripts/real_document_benchmark_scoring.py` add two STUB functions that raise
       NotImplementedError (bodies filled in Task 2), matching the file's existing
       type-hinted, keyword-only style: `load_frozen_questions(path: Path) -> dict[str, list[dict[str, str]]]`
       and `resolve_questions(frozen: dict[str, list[dict[str, str]]] | None, filename: str, *, generate)`.
       Add `from collections.abc import Callable` to the imports and type `generate` as
       `Callable[[], tuple[list[dict[str, str]], dict[str, Any]]]`. `Path`/`json`/`Any` are
       already imported — reuse them.
    2. In `scripts/real_document_benchmark.py` wire the re-export: add `load_frozen_questions`
       and `resolve_questions` alphabetically to BOTH try/except import lists (keep the two
       lists in lockstep, matching existing sort order). Add `parser.add_argument("--frozen-questions", default="")`
       in `parse_args`. In `run()`, immediately after the `generator = QuestionGenerator(...)`
       construction, add `frozen = load_frozen_questions(Path(args.frozen_questions)) if args.frozen_questions else None`.
       Replace the unconditional per-file `select_passages(...)` + `generator.generate(...)` block
       (current lines ~463-473) with a single `resolve_questions(frozen, path.name, generate=<lambda that does select_passages + generator.generate>)`
       call wrapped in the existing `await asyncio.to_thread(...)`, so passage selection and the
       LLM call are deferred inside the lambda and never run in frozen mode. Do NOT change any
       other behavior — the non-frozen path must call generate exactly as before.
    3. Add the 3 tests in `tests/test_real_document_benchmark.py`, importing `load_frozen_questions`
       and `resolve_questions` from `scripts.real_document_benchmark` (they resolve via the re-export
       chain), following the existing inline-JSON arrange/act/assert style. Use `tmp_path` for the
       round-trip file. The malformed cases assert `pytest.raises(ValueError)`.
    4. Run the tests — they MUST fail on NotImplementedError from the stubs (the intended RED signal,
       not an ImportError). Commit RED.

    Keep the net diff to `real_document_benchmark.py` minimal (currently 582/600, no allowlist) —
    all real logic lives in the scoring module. Do not inline implementation code here; Task 2 fills the bodies.
  </action>
  <verify>
    <automated>python -m pytest tests/test_real_document_benchmark.py -q 2>&1 | grep -E "NotImplementedError|3 (failed|error)"</automated>
  </verify>
  <acceptance_criteria>
    - The 3 new tests exist and FAIL because the stub bodies raise NotImplementedError (not ImportError, not SyntaxError).
    - `scripts/real_document_benchmark.py` imports `load_frozen_questions` + `resolve_questions` in both try/except blocks and exposes `--frozen-questions`.
    - RED commit landed: `test(03-01): add failing tests for frozen-questions loader`.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement load_frozen_questions + resolve_questions; pass tests; verify LOC cap</name>
  <read_first>
    - scripts/real_document_benchmark_scoring.py (the stubs added in Task 1 + the `parse_generated_questions` validate-and-raise pattern to mirror)
    - .planning/phases/03-turingdb-retrieval-baseline/03-PATTERNS.md ("Exact new function" excerpt gives the verbatim load_frozen_questions body to implement)
    - scripts/check-file-size.sh (the 600-LOC, no-allowlist cap enforced pre-commit)
  </read_first>
  <files>scripts/real_document_benchmark_scoring.py, scripts/real_document_benchmark.py</files>
  <action>
    Fill the two stub bodies (minimal code to pass, no cleverness):

    1. `load_frozen_questions(path)`: `json.loads(path.read_text(encoding="utf-8"))`; read
       `questions_by_document`; raise ValueError if it is not a non-empty dict; for each
       filename→rows entry, raise ValueError unless rows is a list of dicts each containing
       all of `{"source_id","question","answer","evidence_quote"}`; return the mapping. Match the
       descriptive-ValueError style of the sibling helpers exactly (fail loud on any malformed
       input — this is the V5 input-validation control and the T-03-01 tampering mitigation).
    2. `resolve_questions(frozen, filename, *, generate)`: if `frozen is not None` return
       `frozen[filename]` paired with `{"frozen": True, "prompt_tokens": 0, "completion_tokens": 0, "attempt": 0}`;
       otherwise return `generate()`. Do not call `generate` in the frozen branch.
    3. Run the full new-test file green, then run `bash scripts/check-file-size.sh` and confirm ZERO
       files exceed 600 LOC. If `real_document_benchmark.py` is over cap, tighten by moving more logic
       into the scoring module rather than dropping the branch — never exceed the cap.
    4. Run ruff. Commit GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_real_document_benchmark.py -q && bash scripts/check-file-size.sh && python -m ruff check scripts tests</automated>
  </verify>
  <acceptance_criteria>
    - All 3 new tests pass; the pre-existing tests in the file still pass.
    - `bash scripts/check-file-size.sh` reports no file over 600 LOC (`real_document_benchmark.py` ≤ 600, `real_document_benchmark_scoring.py` ≤ 600).
    - `python -m ruff check scripts tests` is clean.
    - GREEN commit landed: `feat(03-01): implement frozen-questions loader (D-08)`.
    - No change to generation/scoring for the non-frozen (baseline) path.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen-questions file → loader | An on-disk JSON file (authored this phase, re-read by Phase 6) crosses into `load_frozen_questions`; a corrupted or tampered freeze is untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `load_frozen_questions` parsing a frozen-questions JSON | medium | mitigate | Schema validation raises ValueError on any missing key / wrong shape / empty mapping (tested by `test_load_frozen_questions_rejects_malformed`), so a corrupted/adversarial freeze fails loudly at Phase 6 replay instead of silently producing an empty/partial question set. ASVS V5. |
| T-03-01b | Information Disclosure | new loader code | low | accept | The loader reads a local file only; introduces no new endpoint, auth path, or credential handling. No installs (supply-chain N/A this phase). |
</threat_model>

<verification>
- `python -m pytest tests/test_real_document_benchmark.py -q` green (3 new + existing tests).
- `bash scripts/check-file-size.sh` passes with no allowlist.
- `python -m ruff check scripts tests` clean.
- Git log shows RED (`test(03-01):`) then GREEN (`feat(03-01):`) commits in order.
</verification>

<success_criteria>
- `load_frozen_questions` + `resolve_questions` exist, are re-exported through `real_document_benchmark.py`, and are covered by passing tests.
- `--frozen-questions` flag present; baseline (non-frozen) generation path unchanged.
- 600-LOC cap holds across all tracked `*.py`.
</success_criteria>

<output>
Create `.planning/phases/03-turingdb-retrieval-baseline/03-01-SUMMARY.md` when done.
</output>
