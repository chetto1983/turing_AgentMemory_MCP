---
phase: 03-turingdb-retrieval-baseline
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - baseline/03-turingdb/real-document-benchmark.json
  - baseline/03-turingdb/e2e-results.json
  - baseline/03-turingdb/capture-provider-env.txt
autonomous: false
requirements: [ARC-01]
user_setup:
  - service: italian-corpus
    why: "D-05 open input — the absolute --root path to the user's Italian PDF/XLSX/EPUB/HTML corpus was never supplied; the benchmark cannot run without it."
    dashboard_config:
      - task: "Provide the absolute path to the Italian corpus directory (D-05)"
        location: "operator supplies at the Task 1 checkpoint"
  - service: gpu-provider-stack
    why: "D-01/D-02 require REAL GPU embed + rerank sidecars (not stubs); the run must execute on a host where `docker compose` can start agentmemory-embed / agentmemory-rerank with GPU visibility."
    env_vars:
      - name: PROVIDER_API_KEY
        source: ".env (OpenRouter key for question generation, D-08 baseline run)"
must_haves:
  truths:
    - "real_document_benchmark.py ran against the live Dockerized TuringDB MCP with real GPU embed+rerank providers on the Italian corpus and produced a real-document-benchmark.json with succeeded jobs + per-question results"
    - "e2e_score.py ran INSIDE Docker with real embed+rerank providers (E2E_USE_EXTERNAL_* + explicit EMBED_/RERANK_ overrides) and produced e2e-results.json with a full checks[] array (check_count present; if not 19 the collapse is caught, not committed as a real baseline)"
    - "the LIVE embed/rerank model IDs, dimensions, and endpoints actually in effect were captured verbatim (not the compose defaults) for the D-11 provider-config metadata"
  artifacts:
    - path: "baseline/03-turingdb/real-document-benchmark.json"
      provides: "raw D-01 real-document benchmark output (SC#1) — documents[], results[], summary, frozen questions source"
      contains: "summary"
    - path: "baseline/03-turingdb/e2e-results.json"
      provides: "raw D-02 real-provider e2e score output (SC#1) — verdict/score/check_count/checks[]"
      contains: "checks"
    - path: "baseline/03-turingdb/capture-provider-env.txt"
      provides: "verbatim live EMBED_/RERANK_ env in effect + git SHA at capture time (D-11 provider config + snapshot SHA source)"
      contains: "EMBED_"
  key_links:
    - from: "scripts/real_document_benchmark.py (host)"
      to: "http://127.0.0.1:8095/mcp/"
      via: "fastmcp.Client over the exposed MCP HTTP port"
      pattern: "127.0.0.1:8095"
    - from: "docker compose run --rm e2e"
      to: "agentmemory-embed:8080 / agentmemory-rerank:8080"
      via: "explicit -e EMBED_BASE_URL / -e RERANK_BASE_URL overrides (the e2e service does NOT inherit them)"
      pattern: "agentmemory-(embed|rerank)"
---

<objective>
Capture the two raw baseline numbers (SC#1) against the CURRENT TuringDB stack with
REAL GPU providers, in one operator session while the stack is up. Produces
`real-document-benchmark.json` (D-01, host-run) and `e2e-results.json` (D-02,
Docker-run) plus a verbatim record of the live provider config + git SHA for D-11.

Purpose: These are the actual measured yardsticks Phase 6 (ARC-09) diffs against.
Three landmines make this easy to get silently wrong — a discarded results file, a
non-inheriting e2e env, and a fail-fast collapse to 1 check — all guarded below.

Output: `baseline/03-turingdb/{real-document-benchmark.json, e2e-results.json,
capture-provider-env.txt}`. (This plan does NOT commit — plan 03-03 assembles the
full artifact and force-adds it; these files land uncommitted in the working tree.)
</objective>

<artifacts_produced>
- `baseline/03-turingdb/real-document-benchmark.json` — verbatim `--output` of `real_document_benchmark.py`
- `baseline/03-turingdb/e2e-results.json` — verbatim `/work/e2e-results.json` from `docker compose run --rm e2e`
- `baseline/03-turingdb/capture-provider-env.txt` — live `EMBED_*`/`RERANK_*` env + `git rev-parse --short HEAD` at capture time
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-turingdb-retrieval-baseline/03-CONTEXT.md
@.planning/phases/03-turingdb-retrieval-baseline/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (CHECKPOINT): Obtain corpus --root and confirm real-provider preconditions</name>
  <read_first>
    - .planning/phases/03-turingdb-retrieval-baseline/03-RESEARCH.md (§"Environment Availability", §"Common Pitfalls" 3 & 4 — the four execution preconditions with no fallback)
  </read_first>
  <files>none (precondition gate — collects inputs; writes nothing)</files>
  <action>
    Pause and collect the four no-fallback execution preconditions from the operator before any run
    starts (see how-to-verify). Do NOT assume a corpus path or silently fall back to stub providers —
    D-01/D-02 require real GPU embed+rerank and D-05's --root is an open input. Record the supplied
    --root path for reuse in Task 2 and BASELINE.md, then resume.
  </action>
  <what-built>
    Nothing yet — this gate collects the inputs the benchmark run literally cannot proceed without.
    D-01/D-02 mandate REAL GPU providers (no stub fallback); D-05's corpus --root is an OPEN INPUT
    that was never supplied; question generation needs PROVIDER_API_KEY.
  </what-built>
  <how-to-verify>
    Operator must supply / confirm all four before resume:
    1. The ABSOLUTE path to the Italian corpus directory (PDF + XLSX + EPUB + webpage/HTML present) — this becomes `--root`.
    2. This host can run the GPU stack: `docker compose up -d turingdb agentmemory-embed agentmemory-rerank agentmemory-gliner turing-agentmemory-mcp` then `docker compose ps` shows agentmemory-embed AND agentmemory-rerank as `healthy` (their healthchecks per compose.yaml). If either is not healthy, STOP — a collapsed run is not a baseline (Pitfall 3).
    3. `.env` contains a working `PROVIDER_API_KEY` (OpenRouter) for `--question-model`.
    4. A host Python env with the project installed (`pip install -e ".[dev]"`) so `real_document_benchmark.py` can import fastmcp/markitdown/pypdfium2 and run from the host.
  </how-to-verify>
  <resume-signal>Provide the absolute --root path and confirm items 2-4 are satisfied (or report which precondition is missing).</resume-signal>
  <acceptance_criteria>
    - A concrete absolute `--root` path is recorded (used verbatim in Task 2 and in BASELINE.md's reproduction section).
    - agentmemory-embed and agentmemory-rerank are confirmed `healthy` before any run starts.
    - PROVIDER_API_KEY and the host venv are confirmed present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the real-document benchmark (D-01) → real-document-benchmark.json</name>
  <read_first>
    - scripts/real_document_benchmark.py (lines 73-95 parse_args — every flag D-11 pins; lines 406-567 run() — upload/poll/search flow; the BENCHMARK_COMPLETE/BENCHMARK_FAILED terminal lines)
    - .planning/phases/03-turingdb-retrieval-baseline/03-RESEARCH.md (§"Exact invocation against the Dockerized stack" — the exact host command with pinned run params; Pitfall 4 — never rely on the default --root)
  </read_first>
  <files>baseline/03-turingdb/real-document-benchmark.json, baseline/03-turingdb/capture-provider-env.txt</files>
  <action>
    With the stack up and healthy (Task 1), create `baseline/03-turingdb/` and run
    `scripts/real_document_benchmark.py` FROM THE HOST (it is an external MCP client, NOT a
    container — there is no compose service for it) against `--mcp-url http://127.0.0.1:8095/mcp/`,
    writing `--output baseline/03-turingdb/real-document-benchmark.json`. Pass the D-11 run params
    explicitly so they are pinned in the artifact: `--root "<path from Task 1>"`, `--top-k 20`,
    `--chunk-bytes 524288`, `--poll-seconds 10`, `--search-concurrency 4`,
    `--question-model inclusionai/ling-2.6-flash`,
    `--question-url https://openrouter.ai/api/v1/chat/completions`, `--scope` left default
    (records the timestamped benchmark_id as user_identifier), `--env-file .env`. Expect a
    ~10-30 min wall-clock run (LLM question generation + GPU ingestion of 4+ documents).

    Confirm SUCCESS from the process output: a terminal `BENCHMARK_COMPLETE {summary}` line and a
    written JSON whose `documents[]` all show `job.status == "succeeded"` and whose `summary` has
    non-null `mrr_at_20` / `recall_at_k`. If the run prints `BENCHMARK_FAILED ...` (e.g. an
    ingestion failure), do NOT proceed to build a baseline from a partial file — diagnose (provider
    health, corpus format) and re-run. Do not modify `real_document_benchmark.py` — it was finalized in 03-01.

    Also append to `baseline/03-turingdb/capture-provider-env.txt`: `git rev-parse --short HEAD`
    (the D-11 snapshot SHA) labeled clearly.
  </action>
  <verify>
    <automated>python -c "import json,pathlib; d=json.loads(pathlib.Path('baseline/03-turingdb/real-document-benchmark.json').read_text(encoding='utf-8')); jobs=[x['job'].get('status') for x in d['documents']]; assert d['documents'] and all(s=='succeeded' for s in jobs), jobs; assert d['summary'].get('question_count',0)>0; print('OK', d['summary']['question_count'])"</automated>
  </verify>
  <acceptance_criteria>
    - `baseline/03-turingdb/real-document-benchmark.json` exists, is valid JSON, and every document's job status is `succeeded`.
    - `summary.question_count > 0` and per-cutoff recall/mrr fields are present.
    - The corpus spans the four D-04 formats (PDF, XLSX, EPUB, webpage/HTML) — confirm the `documents[].suffix` set covers them (or note in SUMMARY which formats the supplied corpus actually contained).
    - `capture-provider-env.txt` records the snapshot short git SHA.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run the e2e score gate with real providers (D-02) → e2e-results.json</name>
  <read_first>
    - src/turing_agentmemory_mcp/e2e_score.py (lines ~57-97 — E2E_USE_EXTERNAL_* handling, start_infra fail-fast, the `if store is not None` guard that skips all 18 scenario checks on infra failure)
    - .planning/phases/03-turingdb-retrieval-baseline/03-RESEARCH.md (§"Exact invocation" e2e block; Pitfalls 1, 2, 3; Assumption A2 — capture LIVE model IDs, not compose defaults)
    - compose.yaml (the `e2e` service block: entrypoint writes /tmp/e2e-results.json; its environment: does NOT define EMBED_/RERANK_)
  </read_first>
  <files>baseline/03-turingdb/e2e-results.json, baseline/03-turingdb/capture-provider-env.txt</files>
  <action>
    First capture the LIVE provider config (Assumption A2 — `.env` may override compose defaults):
    run `docker compose exec turing-agentmemory-mcp env` and record the actual `EMBED_BASE_URL`,
    `EMBED_MODEL`, `EMBED_DIMENSIONS`, `RERANK_BASE_URL`, `RERANK_MODEL` values into
    `baseline/03-turingdb/capture-provider-env.txt`. Use THOSE live values in the run below and in
    D-11 metadata — do not hard-code the compose-file defaults.

    Then run the e2e gate INSIDE Docker (D-03) with real providers (D-02), guarding all three landmines:
    - Landmine 1 (discarded output): append a SECOND `--out /work/e2e-results.json` after the `e2e`
      service name so argparse keeps the last value and the file lands in the bind-mounted repo root
      (`./e2e-results.json` on the host), NOT the container's /tmp.
    - Landmine 2 (no env inheritance): pass every provider var explicitly on the command line —
      `-e E2E_USE_EXTERNAL_EMBED=1 -e E2E_USE_EXTERNAL_RERANK=1 -e EMBED_BASE_URL=http://agentmemory-embed:8080
      -e EMBED_DIMENSIONS=<live> -e EMBED_MODEL=<live> -e RERANK_BASE_URL=http://agentmemory-rerank:8080
      -e RERANK_MODEL=<live>` (values from the live env just captured).
    - Landmine 3 (collapse to 1 check): agentmemory-embed/rerank were confirmed healthy at Task 1;
      re-confirm `docker compose ps` still shows them healthy immediately before invoking, so
      `start_infra()` does not raise and skip all scenario checks.

    Do NOT gate capture on the process exit code — a `FAILED_SCORE_GATE` verdict is expected and
    accepted (D-07 freezes the inflated number as-is); parse and keep the JSON body regardless.
    Move/copy the resulting `./e2e-results.json` to `baseline/03-turingdb/e2e-results.json`.
  </action>
  <verify>
    <automated>python -c "import json,pathlib; d=json.loads(pathlib.Path('baseline/03-turingdb/e2e-results.json').read_text(encoding='utf-8')); cc=d.get('check_count'); print('check_count',cc,'score',d.get('score')); assert cc is not None; assert cc!=1, 'COLLAPSED to 1 check (Pitfall 3) - providers were not reachable; re-run'; print('checks',len(d['checks']))"</automated>
  </verify>
  <acceptance_criteria>
    - `baseline/03-turingdb/e2e-results.json` exists, is valid JSON, and `check_count` is present and NOT 1 (no fail-fast collapse).
    - The run used real providers: the JSON's provider signature points at `agentmemory-embed`/`agentmemory-rerank`, not a `127.0.0.1:<random-port>` stub server.
    - `capture-provider-env.txt` records the LIVE `EMBED_*`/`RERANK_*` values (model IDs, dims, endpoints) actually in effect at run time.
    - The `checks[]` array is captured verbatim (per-check name/ok/points/detail|error) — no extraction/reduction performed (D-07 per-check granularity is already emitted).
    - Capture was NOT gated on the process exit code; a `FAILED_SCORE_GATE` verdict is recorded as-is (D-07).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Italian corpus (private, licensed) → captured JSON artifacts | The real-document benchmark reads private source files and emits retrieved chunk snippets + short evidence quotes into `real-document-benchmark.json`. |
| OpenRouter / provider credentials → run environment | `PROVIDER_API_KEY` and provider endpoints are read from `.env`; they must not leak into any captured artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Information Disclosure | `PROVIDER_API_KEY` / bearer credentials leaking into `real-document-benchmark.json` or `capture-provider-env.txt` | high | mitigate | The benchmark artifact records `question_usage` token counts only, never the API key or raw request/response payloads; `capture-provider-env.txt` records ONLY the `EMBED_*`/`RERANK_*` provider config keys, never `PROVIDER_API_KEY` or any `AGENTMEMORY_AUTH_TOKEN*`. Grep the two written files for `PROVIDER_API_KEY`/`Authorization`/`sk-` before handing off to 03-03 and abort if matched. (ASVS V6 handling / SEC-03 header masking.) |
| T-03-03 | Information Disclosure | whole-file corpus bytes copied into captured artifacts | high | mitigate | Nothing in this plan copies source file bytes into `baseline/`; the benchmark output holds only retrieved chunk snippets + short evidence quotes (an accepted D-08/D-10 consequence, dispositioned in 03-03), and whole documents stay outside the repo (D-06). |
| T-03-SC | Tampering | package installs | — | N/A | This phase installs no npm/pip/cargo packages (RESEARCH Package Legitimacy Audit: not applicable). No supply-chain checkpoint required. |
</threat_model>

<verification>
- Both JSON artifacts exist under `baseline/03-turingdb/`, are valid JSON, and pass the two automated `python -c` checks above.
- `e2e-results.json` `check_count` is present and not 1; provider signature confirms real providers.
- `real-document-benchmark.json` shows all jobs `succeeded` and a populated `summary`.
- `capture-provider-env.txt` holds live provider config + snapshot git SHA and contains NO credential (grep clean).
</verification>

<success_criteria>
- SC#1 (numeric results captured): both scripts ran against the current TuringDB stack with real providers and their raw JSON outputs are on disk under `baseline/03-turingdb/`.
- The live provider config and snapshot SHA needed for D-11 reproducibility are captured verbatim.
- No credential or whole-file corpus content is present in any captured file.
</success_criteria>

<output>
Create `.planning/phases/03-turingdb-retrieval-baseline/03-02-SUMMARY.md` when done — record the exact `--root` used, the live provider model IDs/dims/endpoints, the two run commands verbatim, the resulting score/check_count, and the snapshot git SHA.
</output>
