---
phase: 03-turingdb-retrieval-baseline
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - baseline/03-turingdb/corpus-manifest.json
  - baseline/03-turingdb/frozen-questions.json
  - baseline/03-turingdb/BASELINE.md
autonomous: true
requirements: [ARC-01]
must_haves:
  truths:
    - "baseline/03-turingdb/ contains all five files: BASELINE.md, e2e-results.json, real-document-benchmark.json, frozen-questions.json, corpus-manifest.json"
    - "corpus-manifest.json contains only manifest fields + sha256 per document (filename, suffix, bytes, sha256, page_count) and NEVER whole-file bytes (D-06)"
    - "frozen-questions.json keys questions by filename in the exact schema load_frozen_questions consumes (Phase 6 replay contract, D-08)"
    - "BASELINE.md records every D-11 metadata field and documents the AS-OBSERVED known-false-passing e2e checks with the inflation caveat (D-07), the two inflation phenomena kept separate"
    - "the whole baseline/03-turingdb/ tree is force-added (it is gitignored) and committed before any ArcadeDB code (SC#3, D-09)"
  artifacts:
    - path: "baseline/03-turingdb/corpus-manifest.json"
      provides: "D-06 manifest-only corpus record (no bytes)"
      contains: "sha256"
    - path: "baseline/03-turingdb/frozen-questions.json"
      provides: "D-08 frozen questions in load_frozen_questions schema"
      contains: "questions_by_document"
    - path: "baseline/03-turingdb/BASELINE.md"
      provides: "D-10/D-11 human-readable manifest: provider config, corpus, run params, git SHA, per-check e2e results + inflation caveats, reproduction command"
      contains: "## Reproduction"
  key_links:
    - from: "baseline/03-turingdb/corpus-manifest.json"
      to: "baseline/03-turingdb/real-document-benchmark.json documents[]"
      via: "field-subset extraction (filename/suffix/bytes/sha256/conversion.page_count) — no re-hashing"
      pattern: "sha256"
    - from: "baseline/03-turingdb/frozen-questions.json"
      to: "baseline/03-turingdb/real-document-benchmark.json documents[].questions"
      via: "re-key by filename into questions_by_document"
      pattern: "questions_by_document"
    - from: "baseline/03-turingdb/BASELINE.md per-check section"
      to: "baseline/03-turingdb/e2e-results.json checks[]"
      via: "as-observed ok/fail transcription + false-passing candidate confirmation"
      pattern: "check_count"
---

<objective>
Assemble the committed, reproducible baseline artifact from the raw captures produced
by 03-02, then force-add and commit it (D-06, D-07, D-08, D-09, D-10, D-11). This is
the cross-phase contract Phase 6 (ARC-09) reads to run the meet-or-exceed comparison,
and it must be committed BEFORE any ArcadeDB code touches the stack (SC#3).

Purpose: SC#2 (reproducible + directly comparable) + SC#3 (committed before drift).
Every D-11 field is already a byproduct of the two scripts' JSON or a one-line git
command — this is assembly + careful documentation, not new tooling.

Output: `baseline/03-turingdb/{corpus-manifest.json, frozen-questions.json, BASELINE.md}`
force-added alongside the two raw JSONs from 03-02, in one commit.
</objective>

<artifacts_produced>
Full committed Phase-3 artifact inventory (`baseline/03-turingdb/`, force-added):
- `BASELINE.md` (this plan) — human-readable D-11 manifest
- `corpus-manifest.json` (this plan) — D-06 manifest-only corpus record
- `frozen-questions.json` (this plan) — D-08 Phase-6 replay contract
- `e2e-results.json` (from 03-02) — raw D-02 real-provider e2e output
- `real-document-benchmark.json` (from 03-02) — raw D-01 benchmark output
Also references the code artifacts from 03-01 (`--frozen-questions` flag, `load_frozen_questions`).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-turingdb-retrieval-baseline/03-CONTEXT.md
@.planning/phases/03-turingdb-retrieval-baseline/03-RESEARCH.md
@.planning/phases/03-turingdb-retrieval-baseline/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive corpus-manifest.json + frozen-questions.json from the raw benchmark output</name>
  <read_first>
    - baseline/03-turingdb/real-document-benchmark.json (the captured raw output — source of both derived files)
    - .planning/phases/03-turingdb-retrieval-baseline/03-PATTERNS.md ("corpus-manifest.json - field-subset extraction" + "frozen-questions.json - re-keying extraction" sections with exact field lists + target schemas; JSON serialization convention)
    - scripts/real_document_benchmark_scoring.py (load_frozen_questions — the schema frozen-questions.json MUST satisfy: questions_by_document mapping, each row has source_id/question/answer/evidence_quote)
  </read_first>
  <files>baseline/03-turingdb/corpus-manifest.json, baseline/03-turingdb/frozen-questions.json</files>
  <action>
    Build both derived files by EXTRACTION from `real-document-benchmark.json` — do NOT re-walk or
    re-hash the corpus, and do NOT copy any whole-file bytes.

    1. `corpus-manifest.json` (D-06): iterate `documents[]` and emit per document only
       `{filename, suffix, bytes, sha256, page_count}` (page_count from `conversion.page_count`).
       XLSX sheet-count is NOT present in the source output and no existing code computes it — record
       it as a documented gap (`"sheet_count": null` with a top-level note), per RESEARCH Open Question 1;
       do not hand-roll an openpyxl extractor unless the user confirms it is a hard requirement.
       Include a top-level `{schema_version, source_benchmark_id, root, generated_at}` header.
    2. `frozen-questions.json` (D-08): build `{schema_version: 1, source_benchmark_id: <benchmark_id>,
       questions_by_document: {<filename>: documents[i].questions}}`. Key by `filename` (unique under the
       fixed --root), matching what `load_frozen_questions` consumes. Each question row already carries
       source_id/question/answer/evidence_quote from the run.
    3. Write both with the file's existing serialization convention (`json.dumps(..., ensure_ascii=False,
       indent=2, sort_keys=True)`). A short one-off assembly script is fine; it is not committed product code
       (keep it out of scripts/ or delete after) and must not touch the two LOC-capped benchmark files.
    4. Round-trip-validate: load `frozen-questions.json` back through `load_frozen_questions` — it must
       return without raising (proves the freeze is Phase-6-replayable and the schema matches).
  </action>
  <verify>
    <automated>python -c "import sys,json,pathlib; sys.path.insert(0,'scripts'); from real_document_benchmark_scoring import load_frozen_questions; fq=load_frozen_questions(pathlib.Path('baseline/03-turingdb/frozen-questions.json')); man=json.loads(pathlib.Path('baseline/03-turingdb/corpus-manifest.json').read_text(encoding='utf-8')); assert fq, 'empty frozen set'; docs=man.get('documents',man if isinstance(man,list) else []); assert docs, 'empty manifest'; print('frozen files', len(fq), 'manifest docs', len(docs))"</automated>
  </verify>
  <acceptance_criteria>
    - `corpus-manifest.json` lists every ingested document with `{filename, suffix, bytes, sha256, page_count}` and NO field carrying whole-file content.
    - `frozen-questions.json` loads cleanly through `load_frozen_questions` (schema-valid, non-empty).
    - Both files use `ensure_ascii=False, indent=2, sort_keys=True`.
    - XLSX sheet-count recorded as a null + documented gap (no openpyxl hand-roll).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author BASELINE.md with all D-11 metadata + as-observed D-07 per-check caveats</name>
  <read_first>
    - baseline/03-turingdb/e2e-results.json (the checks[] array — transcribe ok/fail as observed)
    - baseline/03-turingdb/real-document-benchmark.json (summary + run params for D-11)
    - baseline/03-turingdb/capture-provider-env.txt (live provider config + snapshot git SHA from 03-02)
    - .planning/phases/03-turingdb-retrieval-baseline/03-RESEARCH.md (§"D-07 candidate false-positive checks" [ASSUMED] list; Pitfall 5 — keep the TWO inflation phenomena separate; §"Code Examples" for the e2e-results.json field map)
    - .planning/phases/03-turingdb-retrieval-baseline/03-PATTERNS.md (BASELINE.md structural convention modeled on PROJECT.md)
  </read_first>
  <files>baseline/03-turingdb/BASELINE.md</files>
  <action>
    Write `baseline/03-turingdb/BASELINE.md` as the human-readable manifest (D-10), with a `##` section
    per D-11 metadata group. It MUST pin, as concrete values (not prose gestures):
    - Provider config: embed + rerank MODEL IDs, DIMENSIONS, and ENDPOINTS actually in effect (from
      capture-provider-env.txt), and an explicit note that this D-02 run is real-embed + real-rerank,
      GLiNER-OFF (GLiNER is never wired for the e2e container).
    - Corpus: reference corpus-manifest.json + the exact absolute `--root` path used (so Phase 6 can re-point).
    - Frozen questions: reference frozen-questions.json + how Phase 6 replays via `--frozen-questions`.
    - Run params: `--top-k`, `--chunk-bytes`, `--poll-seconds`, `--scope`, `--question-model`,
      `--question-url`, `--search-concurrency` (the values actually used).
    - Snapshot git SHA (from capture-provider-env.txt).
    - Per-check e2e results: a table transcribing every `checks[].name` + `ok` AS OBSERVED from
      e2e-results.json, plus `score`/`check_count`/`verdict`.
    - Inflation caveats (D-07): CONFIRM against the actual run output which checks are known-false-passing
      — cross-reference the RESEARCH [ASSUMED] candidates (the three vacuous-`all()` checks:
      memory_search_does_not_leak_bob, memory_delete_hides_memory_from_get_and_search,
      document_reindex_text_replaces_old_chunks_and_metadata; and the two hardcoded chunk_count checks:
      document_ingest_text_writes_chunks==3, document_reindex...==2) against what the run actually shows,
      and document the false-passing set AS OBSERVED, not by blindly asserting the researcher's list.
      Keep the TWO inflation phenomena SEPARATE (Pitfall 5): (a) the stub-mode HashingEmbedder limitation
      fixed by real embeddings, vs (b) the D-07 harness `check()`/chunk-count inflation that real providers
      do NOT fix. Explicitly flag that the chunk_count checks may legitimately change under Phase 4
      re-chunking (they will not "cancel out"), so Phase 6 must diff PER-CHECK, not just the aggregate.
    - Reproduction: the two exact commands (host benchmark + docker e2e run) to reproduce, as fenced blocks.

    Optionally corroborate (a) vs (b) by running one stub-mode e2e (`docker compose run --rm e2e
    --out /work/e2e-results-stub.json`, NO `-e` overrides, CPU-only) and comparing which checks flip —
    but the PRIMARY source is the committed real-provider e2e-results.json; do not commit the stub file
    unless it adds provenance value, and if committed, label it clearly as corroboration-only.
  </action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('baseline/03-turingdb/BASELINE.md').read_text(encoding='utf-8'); need=['Provider','Corpus','Frozen','Run param','git','per-check','Reproduction']; miss=[w for w in need if w.lower() not in t.lower()]; assert not miss, miss; assert 'chunk' in t.lower(), 'D-07 chunk-count caveat missing'; print('BASELINE.md OK', len(t),'chars')"</automated>
  </verify>
  <acceptance_criteria>
    - BASELINE.md cites every D-11 field with concrete values (model IDs/dims/endpoints, run params, git SHA).
    - The per-check table reflects e2e-results.json as observed; false-passing checks are documented as-observed, not blindly copied from RESEARCH.
    - Both inflation phenomena are described separately (Pitfall 5); the chunk-count non-cancellation caveat is present.
    - A runnable reproduction section (both commands) is included.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Force-add and commit the full baseline before any ArcadeDB code (SC#3, D-09)</name>
  <read_first>
    - .gitignore (lines 13-15 confirm `.benchmarks/` + `e2e-results.json` are ignored → force-add required)
    - .planning/phases/03-turingdb-retrieval-baseline/03-02-SUMMARY.md (the credential/whole-file grep result from 03-02's threat mitigation)
  </read_first>
  <files>baseline/03-turingdb/ (git add -f the five files), CHANGELOG.md</files>
  <action>
    Final safety scrub, then commit. First re-grep the five files under `baseline/03-turingdb/` for any
    credential or whole-file leak (`PROVIDER_API_KEY`, `Authorization`, `sk-`, bearer tokens) and abort
    if matched (T-03-04). Confirm the five expected files are present and each `*.json` is valid JSON.
    Then `git add -f baseline/03-turingdb/` (the tree is gitignored via `.benchmarks/`/`e2e-results.json`
    patterns — a plain `git add` would silently skip it). Commit with a conventional-prefix subject and
    the project `Co-Authored-By` trailer, body explaining this is the ARC-01 yardstick captured before
    any ArcadeDB change (SC#3). Do NOT force-add `.benchmarks/*` or any raw question-generation payload
    file — only the five curated `baseline/03-turingdb/` files. Update CHANGELOG.md noting the new
    committed baseline contract (a cross-phase contract is being introduced).
  </action>
  <verify>
    <automated>git ls-files baseline/03-turingdb/ | sort && python -c "import json,glob; [json.loads(open(p,encoding='utf-8').read()) for p in glob.glob('baseline/03-turingdb/*.json')]; print('all json valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files baseline/03-turingdb/` lists exactly: BASELINE.md, corpus-manifest.json, e2e-results.json, frozen-questions.json, real-document-benchmark.json (tracked).
    - No credential / whole-file content present in any committed file (grep clean).
    - One atomic commit (`docs(03): ...` or `chore(03): ...`) with the Co-Authored-By trailer; CHANGELOG.md updated.
    - Commit lands before any ArcadeDB-touching commit (SC#3 — verified at Phase 4 kickoff).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured artifacts → committed git history | Force-adding gitignored files into `baseline/` crosses into permanent, shared history — anything leaked here is durable. |
| Italian corpus → corpus-manifest.json | Private source documents reduce to a manifest; whole bytes must not cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information Disclosure | `git add -f baseline/03-turingdb/` committing a credential or whole-file corpus content | high | mitigate | Force-add ONLY the five curated files; corpus-manifest.json is built by field-subset extraction (never bytes); Task 3 re-greps all five files for `PROVIDER_API_KEY`/`Authorization`/`sk-`/bearer before commit and aborts on match. |
| T-03-05 | Information Disclosure | retrieved chunk snippets + short evidence quotes inside committed real-document-benchmark.json / frozen-questions.json | medium | accept | Accepted as a direct, sanctioned consequence of D-08 (freeze questions) + D-10 (commit the scripts' raw output) — comparability requires these snippets. Whole-file bytes remain excluded (D-06). Documented in BASELINE.md so the tradeoff is explicit, not silent. |
| T-03-SC | Tampering | package installs | — | N/A | No package installs this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Five files tracked under `baseline/03-turingdb/`; all `*.json` valid.
- `frozen-questions.json` round-trips through `load_frozen_questions`.
- BASELINE.md cites every D-11 field + as-observed D-07 caveats.
- Single atomic commit with force-add; CHANGELOG.md updated; no credential/whole-file leak.
</verification>

<success_criteria>
- SC#2: the committed artifact records provider config, corpus manifest, frozen questions, run params, git SHA, and per-check e2e results with inflation caveats — reproducible + directly comparable by Phase 6.
- SC#3: the baseline is committed (force-added) before any ArcadeDB code.
</success_criteria>

<output>
Create `.planning/phases/03-turingdb-retrieval-baseline/03-03-SUMMARY.md` when done — record the commit SHA, the five tracked files, and the confirmed as-observed false-passing e2e checks.
</output>
