---
phase: 01-ci-git-hook-discipline
plan: 08
type: tdd
wave: 4
depends_on: [01-07]
files_modified:
  - tests/test_no_skip_as_green_guard.py
  - tests/conftest.py
autonomous: true
requirements: [CI-07]
must_haves:
  truths:
    - "Under CI=true, a marked (integration/gpu) test that skips is converted to a FAILURE, not a green pass (D-03)"
    - "Without CI=true, the same marked skip still passes green (guard is inert off-CI)"
    - "The negative self-test proves the guard fires (D-04) and fails when the guard is absent"
    - "Adding conftest.py does not change the full-suite green count (guard is inert for unmarked tests)"
  artifacts:
    - tests/conftest.py
    - tests/test_no_skip_as_green_guard.py
  key_links:
    - "conftest.py's pytest_runtest_makereport hookwrapper reads CI env + item markers (slow/integration/gpu registered in pyproject by plan 07)"
    - "CI arms the guard by setting env CI=true (plan 09 unit-tests self-test step + dockerized-integration job)"
---

<objective>
Ship the no-skip-as-green guard (CI-07) test-first: a central `tests/conftest.py`
`pytest_runtest_makereport` hookwrapper that, under `CI=true`, converts a skip on a marked
integration/gpu tier into a failure (D-03), plus a negative self-test that proves the guard fires
(D-04). This is the defining discipline of the phase — a skipped GPU/integration tier must never
pass green in CI.

Purpose: The guard is the one piece of genuinely custom code this phase writes (no library exists
for it — RESEARCH "Don't Hand-Roll"). TDD fits perfectly: the negative self-test IS the RED test
that must fail before the guard exists, then pass after. Markers were registered in pyproject by
plan 07.
Output: A verified, self-tested no-skip-as-green guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write the negative self-test that proves the guard fires</name>
  <files>tests/test_no_skip_as_green_guard.py</files>
  <read_first>
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"Negative self-test proving the guard fires (D-04)" (the finished pytester probe to write verbatim)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"tests/conftest.py and tests/test_no_skip_as_green_guard.py" (use RESEARCH code verbatim; no in-repo analog)
    - tests/test_admin_repair.py (general test-file style reference)
  </read_first>
  <behavior>
    - Test 1: with CI=true, a `@pytest.mark.integration` test that calls `pytest.skip(...)` is reported as failed=1, and output matches the guard's message marker.
    - Test 2: without CI in the env, the same marked skip is reported as skipped=1 (guard inert off-CI).
  </behavior>
  <action>
    Write `tests/test_no_skip_as_green_guard.py` verbatim from the RESEARCH "Negative self-test"
    code (D-04): `pytest_plugins = ["pytester"]`, a `_PROBE` module string with a marked skipping
    test, `_REPO_CONFTEST = Path(__file__).resolve().parent / "conftest.py"`, and two cases —
    `runpytest_subprocess()` asserting `failed=1` (+ guard-message match) under `monkeypatch.setenv("CI","true")`,
    and `skipped=1` with CI unset. Run it now; it MUST fail/error because `tests/conftest.py` does
    not yet exist (RED). Do not create conftest.py in this task.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_no_skip_as_green_guard.py; test $? -ne 0 && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_no_skip_as_green_guard.py` exits non-zero (RED — conftest.py absent) and prints RED-CONFIRMED
    - The file uses the `pytester` fixture and reads the repo conftest via `_REPO_CONFTEST.read_text()`
  </acceptance_criteria>
  <done>The negative self-test exists and fails because the guard does not yet exist (verified RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Create the conftest.py no-skip-as-green guard</name>
  <files>tests/conftest.py</files>
  <read_first>
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"No-skip-as-green conftest.py guard (D-03)" (the finished hookwrapper to write verbatim)
    - pyproject.toml (confirm slow/integration/gpu markers registered by plan 07 — the guard's `_CI_ENFORCED_MARKERS` = {"integration","gpu"})
    - tests/test_no_skip_as_green_guard.py (the RED test that must now go green)
  </read_first>
  <action>
    Write `tests/conftest.py` verbatim from the RESEARCH "No-skip-as-green conftest.py guard" code
    (D-03): a `@pytest.hookimpl(hookwrapper=True) def pytest_runtest_makereport(...)` that yields,
    returns early unless `os.environ.get("CI") == "true"`, returns unless `report.skipped`, and —
    when the item carries a marker in `_CI_ENFORCED_MARKERS = {"integration", "gpu"}` — sets
    `report.outcome = "failed"` with the "no-skip-as-green" longrepr message. Then run the self-test
    (now GREEN) and the FULL suite to confirm the inert guard did not change the baseline count.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_no_skip_as_green_guard.py && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_no_skip_as_green_guard.py` passes (both cases green — guard fires under CI, inert without)
    - `CI=true python -m pytest -q tests/test_no_skip_as_green_guard.py` still passes (self-test is robust to the CI env)
    - `python -m pytest -q` reports 362 passed (adding conftest.py did not change the baseline)
    - `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>tests/conftest.py converts a CI=true marked skip into a failure and is inert otherwise; the negative self-test passes; the full-suite count is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI env (CI=true) → test outcome policy | The env var arms a test-outcome mutation; same code path runs identically if a developer sets CI=true locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Repudiation | the guard silently stops firing (e.g. marker set drifts, hookwrapper regressed) so a skipped integration/gpu tier passes green unnoticed | high | mitigate | The D-04 negative self-test asserts the guard converts a marked skip to failed=1 under CI=true; it runs in CI (plan 09) so a regressed guard fails the gate |
| T-08-02 | Tampering | conftest.py alters outcomes for unmarked/off-CI tests, corrupting the normal suite | low | mitigate | Guard returns early unless CI=true AND report.skipped AND an enforced marker present; acceptance asserts the full-suite count is unchanged |
</threat_model>

<verification>
- RED: `pytest tests/test_no_skip_as_green_guard.py` fails before conftest exists
- GREEN: after conftest, the self-test passes (failed=1 under CI=true; skipped=1 without)
- `python -m pytest -q` → 362 passed (baseline unchanged)
- `ruff check` clean
</verification>

<success_criteria>
The no-skip-as-green guard exists, is proven to fire by its negative self-test, and is inert for
the normal suite (satisfies CI-07; honors D-03/D-04).
</success_criteria>

## Artifacts this phase produces (this plan)

- New files: `tests/conftest.py` (the `pytest_runtest_makereport` no-skip-as-green hookwrapper), `tests/test_no_skip_as_green_guard.py` (the D-04 negative self-test)
- New symbol: `pytest_runtest_makereport` hook + `_CI_ENFORCED_MARKERS` in conftest.py

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-08-SUMMARY.md` when done.
</output>
