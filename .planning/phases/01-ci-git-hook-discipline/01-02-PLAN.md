---
phase: 01-ci-git-hook-discipline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/server.py
  - src/turing_agentmemory_mcp/document_jobs.py
  - src/turing_agentmemory_mcp/gliner_provider.py
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "from turing_agentmemory_mcp.server import create_mcp_app, auth_from_env still imports unchanged"
    - "from turing_agentmemory_mcp.document_jobs import DocumentJobStore, DocumentIngestJob still imports unchanged"
    - "from turing_agentmemory_mcp.gliner_provider import GLiNERProvider, start_server still imports unchanged"
    - "The full pytest suite stays at its green baseline (362) after the splits"
    - "No tracked server*/document_jobs*/gliner_provider*.py file exceeds 600 LOC"
  artifacts:
    - src/turing_agentmemory_mcp/server.py
    - src/turing_agentmemory_mcp/document_jobs.py
    - src/turing_agentmemory_mcp/gliner_provider.py
  key_links:
    - "server.py's create_mcp_app still wires every @app.tool() after tool-group extraction"
    - "document_job_manager.py's import of DocumentIngestJob/DocumentJobStore stays valid"
    - "tests/test_gliner_provider.py's import of GLiNERProvider/start_server stays valid"
---

<objective>
Decompose three over-cap `src/` modules — `server.py` (762), `document_jobs.py` (666),
`gliner_provider.py` (658) — into ≤600-LOC concern siblings per D-08/D-09, preserving every
public import path the rest of the codebase and tests depend on (invariant #4).

Purpose: Bring three more tracked files under the no-allowlist 600-LOC cap without touching
behavior. These are lower-risk than the store.py split (clean concern boundaries), but they
carry public symbols imported by tests and `document_job_manager.py`, so import-path
preservation is the load-bearing constraint.
Output: Three slimmed modules + their concern siblings, all ≤600 LOC, importers unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split server.py by MCP tool group</name>
  <files>src/turing_agentmemory_mcp/server.py, plus new server_&lt;group&gt;_tools.py siblings (executor names them)</files>
  <read_first>
    - src/turing_agentmemory_mcp/server.py (the file being split — read in full)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"server.py ... splits" (split by tool-group behind create_mcp_app / *_from_env factory convention)
    - src/turing_agentmemory_mcp/entity_extraction.py (Protocol + factory shape reference)
  </read_first>
  <action>
    Per D-08/D-09, split `server.py` by cohesive MCP tool-group concern (e.g.
    `server_memory_tools.py`, `server_document_tools.py`, `server_admin_tools.py`) following the
    existing `*_from_env()` "factory lives near what it constructs" convention. Keep `create_mcp_app`
    and `auth_from_env` importable from `turing_agentmemory_mcp.server` unchanged (7 test files
    import `create_mcp_app`; `test_auth.py` imports `auth_from_env`). If tool registration is
    factored into helper modules, `server.py`'s `create_mcp_app` must still register every
    `@app.tool()` (call the extracted registrar functions) so the assembled app is identical.
    Do not change any tool signature, validation, or tenant-scope handling. Run the behavior gate
    after the split.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_auth.py tests/test_server_batch_tool.py tests/test_governance.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from turing_agentmemory_mcp.server import create_mcp_app, auth_from_env"` exits 0
    - `git ls-files 'src/turing_agentmemory_mcp/server*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m pytest -q` reports 362 passed
    - `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>server.py ≤600 LOC via tool-group siblings; create_mcp_app/auth_from_env import path unchanged; full suite + ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Split document_jobs.py and gliner_provider.py</name>
  <files>src/turing_agentmemory_mcp/document_jobs.py, src/turing_agentmemory_mcp/gliner_provider.py, plus new concern siblings (executor names them)</files>
  <read_first>
    - src/turing_agentmemory_mcp/document_jobs.py (read in full)
    - src/turing_agentmemory_mcp/gliner_provider.py (read in full)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"document_jobs.py ... gliner_provider.py splits" (boundaries: schema/dataclass vs session/query methods; HTTP-handler plumbing vs extraction/label-schema logic)
    - src/turing_agentmemory_mcp/sparse_index.py (SQLite schema-constants→connection→query module boundary reference)
    - src/turing_agentmemory_mcp/entity_extraction.py (Protocol-vs-HTTP-impl separation reference)
  </read_first>
  <action>
    Split `document_jobs.py` by pulling the dataclass/schema (`DocumentIngestJob`, schema/status
    constants) apart from the SQLite `@contextmanager` session + lease/heartbeat query methods,
    matching `sparse_index.py`'s module boundary. Keep `DocumentJobStore` and `DocumentIngestJob`
    importable from `turing_agentmemory_mcp.document_jobs` unchanged (`document_job_manager.py` and
    4 test files import them). Split `gliner_provider.py` by separating the
    `BaseHTTPRequestHandler`/`ThreadingHTTPServer` plumbing (and its `MAX_BODY_BYTES`/`MAX_TEXTS`
    constants) from the extraction/label-schema logic, matching `entity_extraction.py`'s
    Protocol-vs-HTTP-impl separation. Keep `GLiNERProvider` and `start_server` importable from
    `turing_agentmemory_mcp.gliner_provider` unchanged (`tests/test_gliner_provider.py` imports both).
    Re-export moved public symbols from the original module so no importer changes. Per D-08/D-09,
    both original files must land ≤600 LOC. Run the behavior gate after each file.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_document_jobs.py tests/test_document_job_manager.py tests/test_gliner_provider.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from turing_agentmemory_mcp.document_jobs import DocumentJobStore, DocumentIngestJob"` exits 0
    - `python -c "from turing_agentmemory_mcp.gliner_provider import GLiNERProvider, start_server"` exits 0
    - `git ls-files 'src/turing_agentmemory_mcp/document_jobs*.py' 'src/turing_agentmemory_mcp/gliner_provider*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m pytest -q` reports 362 passed; `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>document_jobs.py and gliner_provider.py each ≤600 LOC via concern siblings; DocumentJobStore/DocumentIngestJob/GLiNERProvider/start_server import paths unchanged; full suite + ruff green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→MCP tool (server.py) | Existing untrusted-input boundary; unchanged by this refactor (tool signatures/validation preserved) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | a tool accidentally not re-registered on the app after tool-group extraction | medium | mitigate | `create_mcp_app` must call every extracted registrar; the 7 create_mcp_app-based tests exercise the assembled tool set and must stay green |
| T-02-02 | Information Disclosure | a public symbol dropped from its original module path breaks an importer (server/document_jobs/gliner_provider) | low | mitigate | Re-export moved public symbols; import-smoke acceptance checks assert each original path still resolves |
</threat_model>

<verification>
- Public import smokes for server / document_jobs / gliner_provider all exit 0
- `python -m pytest -q` → 362 passed
- `python -m ruff check src tests scripts` → clean
- All server*/document_jobs*/gliner_provider*.py ≤ 600 LOC
</verification>

<success_criteria>
Three more over-cap src modules are decomposed to ≤600 LOC with public import paths and full-suite
behavior preserved (contributes to CI-01, D-08/D-09).
</success_criteria>

## Artifacts this phase produces (this plan)

- Slimmed modules: `server.py`, `document_jobs.py`, `gliner_provider.py`
- New concern siblings (names per executor judgment, e.g. `server_memory_tools.py`, `server_document_tools.py`, `server_admin_tools.py`, `document_jobs_schema.py`/`document_jobs_store.py`, `gliner_provider_http.py`/`gliner_provider_extraction.py`)
- Preserved public symbols: `create_mcp_app`, `auth_from_env`, `DocumentJobStore`, `DocumentIngestJob`, `GLiNERProvider`, `start_server`

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-02-SUMMARY.md` when done.
</output>
