---
phase: 01-ci-git-hook-discipline
plan: 09
type: execute
wave: 4
depends_on: [01-07]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-03, CI-04, CI-05, CI-06, CI-08, CI-09]
must_haves:
  truths:
    - "CI runs lint (ruff 0.15.21 + file-size cap), unit-tests (pytest, pythonpath=src, single Python 3.12), compose-validation, pip-audit 2.10.1, and a dockerized-integration job on every push/PR to master"
    - "The unit-test job enforces --cov-fail-under at a floor MEASURED against the post-decomposition suite (not guessed)"
    - "The dockerized-integration job runs the E2E score gate in stub embed/rerank mode, visibly labelled as the GPU-less floor (never a skip)"
    - "The no-skip-as-green guard self-test runs under CI=true and a skipped integration/gpu tier fails the gate"
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - "unit-tests job runs pytest --cov=src/turing_agentmemory_mcp --cov-fail-under=<measured floor> (coverage omit for e2e_score comes from pyproject set in plan 07)"
    - "dockerized-integration job sets env CI=true to arm the guard and runs `docker compose run --rm e2e` (the in-process stub floor, D-05)"
    - "real-document E2E is the deterministic in-process path (D-10) — real_document_benchmark.py is NOT wired into CI"
---

<objective>
Author `.github/workflows/ci.yml` — the GitHub Actions gate (L-04) — with lint, unit-tests +
coverage floor, compose-validation, supply-chain (pip-audit), and a dockerized-integration job,
plus the no-skip-as-green arming (D-03/CI-07) and the GPU-less stub floor (D-05/D-06/CI-08). The
coverage floor is MEASURED against the post-decomposition suite (D-07/CI-09), never guessed. CI-05
is satisfied by the deterministic in-process real-file path (D-10); the live
`real_document_benchmark.py` stays operator-run and is NOT wired here. Single Python 3.12 (D-11).

Purpose: This is the heavy gate that protects every downstream phase. Hooks stay fast (plan 07);
the full E2E, coverage, and supply-chain gates live in CI (L-05). Modeled on Aura's discipline,
adapted to this much smaller Python service surface.
Output: A committed CI workflow whose every job's underlying command is verified to run locally.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md
@.planning/phases/01-ci-git-hook-discipline/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the coverage floor against the post-decomposition suite (D-07/CI-09)</name>
  <files>.planning/phases/01-ci-git-hook-discipline/01-09-SUMMARY.md (records the measured percent + chosen floor; this task writes no source)</files>
  <read_first>
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"Coverage Floor" and Assumptions A3 (measured 74% incl e2e_score / 78% excl; recommend excluding e2e_score, which plan 07 configured via [tool.coverage.run] omit)
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"Pitfall 3" (install graspologic-native so the 6 community-detection tests run and don't drag the baseline down)
    - pyproject.toml (confirm [tool.coverage.run] omit for e2e_score is present from plan 07)
  </read_first>
  <action>
    With `pip install -e ".[dev]"` fully installed (graspologic-native present, Pitfall 3), MEASURE
    the real coverage of the current post-decomposition suite: `python -m pytest
    --cov=src/turing_agentmemory_mcp -q` and read the total percent (with the pyproject omit for
    e2e_score applied, expected ~78%). Set the CI floor to the measured integer at/just-below the
    baseline (D-07 — the floor only ever ratchets up; never guess and never silently lower). Record
    the measured number and the chosen floor in the SUMMARY so future phases ratchet from a real
    value. This measured floor is the `<FLOOR>` used in Task 2's `--cov-fail-under`.
  </action>
  <verify>
    <automated>python -m pytest --cov=src/turing_agentmemory_mcp -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest --cov=src/turing_agentmemory_mcp -q` prints a total coverage percent (recorded in SUMMARY)
    - The chosen `--cov-fail-under` value is ≤ the measured percent (ratchet-safe) and > 0 (a real gate, not advisory)
    - `python -m pytest --cov=src/turing_agentmemory_mcp --cov-fail-under=<chosen floor> -q` exits 0 on the current tree
  </acceptance_criteria>
  <done>The coverage floor is measured against the actual post-decomposition suite and a ratchet-safe --cov-fail-under value is chosen and recorded (not guessed).</done>
</task>

<task type="auto">
  <name>Task 2: Author .github/workflows/ci.yml (lint, unit-tests+coverage, compose, pip-audit, dockerized-integration)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"ci.yml skeleton" (the finished job matrix to adapt) and §"Standard Stack" (exact pins)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §".github/workflows/ci.yml" (top-block on/permissions/concurrency for master; the `env: { CI: "true" }` no-skip-as-green arming convention)
    - .planning/phases/01-ci-git-hook-discipline/01-CONTEXT.md §"D-05/D-06/D-10/D-11" (stub floor visibly labelled; deterministic CI-05; single Python 3.12)
    - compose.yaml lines around the `e2e` service (profiles: ["e2e"]) — the dockerized-integration job runs `docker compose run --rm e2e`
  </read_first>
  <action>
    Write `.github/workflows/ci.yml` per the RESEARCH skeleton, on `push`/`pull_request` to
    `master`, `permissions: contents: read`, `concurrency` with `cancel-in-progress: true`. Jobs
    (L-04), all on `ubuntu-latest`, Python 3.12 only (D-11), `actions/checkout@v7` +
    `actions/setup-python@v6`:
    (1) lint — `pip install "ruff==0.15.21"`, then `ruff format --check src tests scripts`, `ruff
    check src tests scripts`, `bash scripts/check-file-size.sh` (CI-03 + the file-size cap).
    (2) unit-tests (CI-04, CI-09) — `pip install -e ".[dev]"`, then `python -m pytest
    --cov=src/turing_agentmemory_mcp --cov-fail-under=<measured floor from Task 1> -q`; add a step
    with `env: { CI: "true" }` running `python -m pytest tests/test_no_skip_as_green_guard.py -q`
    with an inline comment naming that it arms the no-skip-as-green guard (D-03/CI-07).
    (3) compose-validate (CI-06) — `docker compose config --quiet`.
    (4) supply-chain (CI-06) — `pip install -e ".[dev]" "pip-audit==2.10.1"`, then `pip-audit`.
    (5) dockerized-integration (CI-05, CI-08) — `env: { CI: "true" }`, step NAMED to make the
    stub-mode floor visibly distinct from a real-GPU run (D-06), running `docker compose run --rm
    e2e` (the in-process stub embed/rerank E2E score gate = the GPU-less degrade floor, D-05, a real
    pass/fail, never a skip). CI-05's real-document E2E is the deterministic in-process path (D-10):
    it is exercised by the E2E score gate's document flow + the existing deterministic doc test in
    the unit-tests job — do NOT add a `real_document_benchmark.py` step, a PROVIDER_API_KEY secret,
    or any live-LLM call (D-10). Never hardcode a real credential (proportional V14 mitigation).
    Validate the YAML and run each job's underlying command locally to confirm they work.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML-OK')" && ruff check src tests scripts && docker compose config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML and defines the five jobs (lint, unit-tests, compose-validate, supply-chain, dockerized-integration)
    - The workflow triggers on push + pull_request to `master`, sets `permissions: contents: read`, uses Python 3.12 only, and pins `ruff==0.15.21` + `pip-audit==2.10.1`
    - The unit-tests job includes `--cov-fail-under=<measured floor>` and a `CI: "true"` guard self-test step; the dockerized-integration job sets `CI: "true"` and its stub-floor step is explicitly named (D-06)
    - No `real_document_benchmark.py` step and no live-LLM/PROVIDER_API_KEY reference exists in the file (D-10); `grep -n "real_document_benchmark\|PROVIDER_API_KEY" .github/workflows/ci.yml` prints nothing
    - Underlying commands verified locally: `ruff check src tests scripts` exits 0, `docker compose config --quiet` exits 0, `docker compose run --rm e2e` reaches VALIDATED_10_10, `pip-audit` runs
  </acceptance_criteria>
  <done>ci.yml defines the full L-04 job matrix with a measured coverage floor, the no-skip-as-green guard armed under CI=true, and the visibly-labelled stub E2E floor; the deterministic in-process real-file path (not real_document_benchmark.py) satisfies CI-05; every job's underlying command runs locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → PyPI (installs) | CI installs the resolved dependency set + pip-audit; pip-audit scans it for known CVEs (V14) |
| CI logs → public visibility | Workflow output may be world-readable on a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-SC | Tampering | a known-CVE dependency reaching CI/production undetected | high | mitigate | supply-chain job runs `pip-audit==2.10.1` against the resolved env on every push/PR (CI-06); exact pin verified in RESEARCH §"Package Legitimacy Audit" |
| T-09-01 | Information Disclosure | a secret (e.g. a future PROVIDER_API_KEY) hardcoded into ci.yml and leaked in public CI logs | high | mitigate | D-10 keeps the live real-document path out of CI entirely — no secret is introduced; acceptance greps assert no PROVIDER_API_KEY/real_document_benchmark reference exists in the workflow |
| T-09-02 | Repudiation | a skipped GPU/integration tier passing green (silent no-skip-as-green failure) | high | mitigate | dockerized-integration + the unit-tests self-test step run under `CI: "true"`, arming the guard; a skip becomes a failing gate (CI-07/CI-08), and the stub floor is a real pass/fail run, never a skip |
</threat_model>

<verification>
- `.github/workflows/ci.yml` is valid YAML with the five jobs, master triggers, py3.12, pinned ruff/pip-audit
- Coverage floor is measured; `--cov-fail-under=<floor>` present and passing locally
- dockerized-integration arms CI=true and runs the labelled stub E2E floor; no real_document_benchmark/PROVIDER_API_KEY reference
- Local command checks: ruff, docker compose config, docker compose run e2e, pip-audit all succeed
</verification>

<success_criteria>
The CI workflow runs lint + unit-tests(+coverage floor) + compose-validation + pip-audit +
dockerized-integration on every push/PR to master, arms the no-skip-as-green guard, and provides
the visibly-labelled GPU-less stub floor — satisfying CI-03, CI-04, CI-05, CI-06, CI-08, CI-09 and
honoring L-04/D-05/D-06/D-07/D-10/D-11.
</success_criteria>

## Artifacts this phase produces (this plan)

- New file: `.github/workflows/ci.yml` (jobs: lint, unit-tests, compose-validate, supply-chain, dockerized-integration)
- A measured, recorded coverage floor value wired as `--cov-fail-under`

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-09-SUMMARY.md` when done.
</output>
