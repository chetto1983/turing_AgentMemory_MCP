---
phase: 01-ci-git-hook-discipline
plan: 06
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - src/turing_agentmemory_mcp/store.py
  - src/turing_agentmemory_mcp/server.py
  - src/turing_agentmemory_mcp/e2e_score.py
  - scripts/real_document_benchmark.py
  - tests/test_gliner_provider.py
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "ruff format --check src tests scripts exits 0 across the whole tree (no drift remains)"
    - "Every tracked *.py file is ≤600 LOC (all 10 decompositions landed)"
    - "The full pytest suite is green (362) on the merged, reformatted tree"
    - "scripts/e2e_score.py prints VALIDATED_10_10 on the merged tree"
  artifacts:
    - e2e-results.json
  key_links:
    - "This is the bootstrap gate: format + LOC compliance + behavior must ALL be green BEFORE hooks/CI are wired (phase-critical constraint #2)"
    - "The merged tree from all five Wave-1 splits is validated together here — catches merge interactions unit tests alone miss"
---

<objective>
Run the one-time repo-wide `ruff format` bootstrap pass (D-09a) across the fully-decomposed tree,
then execute the full behavior-preservation gate on the MERGED result of all five Wave-1
decompositions. This is the mandatory bootstrap checkpoint (phase-critical constraint #2): the
tree must be format-clean, fully ≤600-LOC compliant, and behaviorally green BEFORE any pre-commit
hook or CI is wired — otherwise the hook-installing commit blocks itself on pre-existing drift.

Purpose: `ruff format --check` fails on 49/78 files today (D-09a); enabling the format hook
without a bootstrap pass would block the first real commit. This plan lands that pass and proves
the decomposed + reformatted tree still passes pytest (362) and the E2E score gate
(VALIDATED_10_10) — the last green light before guardrails go on.
Output: A fully format-clean, ≤600-LOC-compliant, behaviorally-verified tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide ruff format bootstrap pass (D-09a)</name>
  <files>src/ (all *.py), tests/ (all *.py), scripts/ (all *.py) — repo-wide ruff format</files>
  <read_first>
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"Pitfall 2: ruff format --check fails on 49 files today" (the second bootstrap gate)
    - .planning/phases/01-ci-git-hook-discipline/01-CONTEXT.md §"D-09a" (one-time ruff format pass before the pre-commit hook is enabled)
    - pyproject.toml (current ruff config: line-length 100, E501 ignored, target py311 — the format pass must run under the pinned ruff 0.15.21 behavior)
  </read_first>
  <action>
    With the pinned ruff (0.15.21) installed, run `python -m ruff format src tests scripts` ONCE
    across the whole tree (D-09a). This is a formatting-only change — it must not alter behavior.
    Do not touch the ruff rule set, line-length, or E501-ignore. If the pinned ruff is not yet in
    the venv, install it (`pip install "ruff==0.15.21"`) before formatting so the bootstrap matches
    exactly what CI and the pre-commit hook will enforce.
  </action>
  <verify>
    <automated>python -m ruff format --check src tests scripts</automated>
  </verify>
  <acceptance_criteria>
    - `python -m ruff format --check src tests scripts` exits 0 (no "Would reformat" lines)
    - `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>The entire tracked tree is ruff-format clean under ruff 0.15.21; no formatting drift remains.</done>
</task>

<task type="auto">
  <name>Task 2: Full behavior-preservation gate on the merged, reformatted tree</name>
  <files>e2e-results.json (proof artifact; this task runs gates and writes no source)</files>
  <read_first>
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"Behavior-preservation gate" and §"store.py Decomposition Plan" (the three gate commands)
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"Pitfall 3: graspologic-native ... local-environment gap" (run `pip install -e ".[dev]"` fresh so the 6 community-detection tests are not silently skipped/failed and coverage isn't dragged down)
  </read_first>
  <action>
    Prove the merged result of all five Wave-1 decompositions plus the format pass preserves
    behavior (phase-critical constraint #3 applied at the merge boundary). Ensure the full dev
    dependency set is installed (`pip install -e ".[dev]"` — includes graspologic-native==1.3.1, per
    Pitfall 3) so no test is spuriously failing. Then run the full pytest suite, the deterministic
    E2E score gate, and both ruff checks. Confirm every tracked `*.py` is now ≤600 LOC (all 10
    files decomposed — no allowlist, D-08/D-09). If any file is still over cap or any gate is red,
    STOP and report which Wave-1 split regressed; do not proceed to the hooks/CI waves.
  </action>
  <verify>
    <automated>python -m pytest -q && python scripts/e2e_score.py --out e2e-results.json</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q` reports 362 passed (green baseline preserved on the merged tree)
    - `python scripts/e2e_score.py --out e2e-results.json` prints VALIDATED_10_10 (score ≥ 9.8)
    - `git ls-files '*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing (zero files over cap)
    - `python -m ruff check src tests scripts` and `python -m ruff format --check src tests scripts` both exit 0
  </acceptance_criteria>
  <done>Merged decomposed + reformatted tree: zero files over 600 LOC, full suite green (362), E2E VALIDATED_10_10, ruff check + format --check clean — the bootstrap gate is green and hooks/CI may now be wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Formatting + verification only; no runtime surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | a Wave-1 merge interaction (two individually-green splits combine into a red tree) slips through undetected | high | mitigate | This plan re-runs the FULL suite + E2E gate on the merged tree; a red gate blocks progression to the hooks/CI waves |
| T-06-02 | Information Disclosure | graspologic-native missing from the venv drags coverage/tests down and masks a real regression | medium | mitigate | `pip install -e ".[dev]"` fresh before the gate (Pitfall 3) so all 362 tests run |
</threat_model>

<verification>
- `python -m ruff format --check src tests scripts` → exit 0
- `python -m ruff check src tests scripts` → exit 0
- `python -m pytest -q` → 362 passed
- `python scripts/e2e_score.py` → VALIDATED_10_10
- zero tracked *.py over 600 LOC
</verification>

<success_criteria>
The decomposed tree is fully format-clean and ≤600-LOC compliant with behavior preserved (full
suite + E2E gate green) — the bootstrap prerequisite for turning hooks/CI on (contributes to CI-01;
honors D-08/D-09/D-09a).
</success_criteria>

## Artifacts this phase produces (this plan)

- A repo-wide `ruff format` pass applied to `src/`, `tests/`, `scripts/`
- `e2e-results.json` (proof artifact of VALIDATED_10_10 on the merged tree)
- No new source symbols — this plan is the bootstrap verification gate

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-06-SUMMARY.md` when done.
</output>
