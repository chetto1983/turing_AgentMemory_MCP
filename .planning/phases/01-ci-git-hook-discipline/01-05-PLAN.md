---
phase: 01-ci-git-hook-discipline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_gliner_provider.py
  - tests/test_batch_memory.py
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "The same set of test cases still collect and pass — no test is lost or renamed away"
    - "The full pytest suite stays at its green baseline (362) after the splits"
    - "No tracked tests/test_gliner_provider*.py or tests/test_batch_memory*.py exceeds 600 LOC"
  artifacts:
    - tests/test_gliner_provider.py
    - tests/test_batch_memory.py
  key_links:
    - "pytest still collects every case from the split-out test files (testpaths=tests picks them up)"
    - "Split test files keep importing the public symbols (GLiNERProvider/start_server, batch write APIs) unchanged"
---

<objective>
Decompose two over-cap test files — `tests/test_gliner_provider.py` (1076) and
`tests/test_batch_memory.py` (749) — into ≤600-LOC files along their internal fixture/scenario
groupings per D-08/D-09 (the cap has no allowlist and scans `tests/` too).

Purpose: Bring the last two over-cap files under the 600-LOC cap. Test files are split by moving
whole test cases into concern-named siblings — never by deleting or weakening assertions
(CLAUDE.md: never modify tests to make them pass). The total collected/passing count must be
identical before and after.
Output: Four ≤600-LOC test files (2 originals slimmed + 2+ new siblings), same green count.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split tests/test_gliner_provider.py by concern</name>
  <files>tests/test_gliner_provider.py, plus new tests/test_gliner_provider_&lt;concern&gt;.py siblings (executor names them)</files>
  <read_first>
    - tests/test_gliner_provider.py (the file being split — read in full; identify the fixture/class/scenario groups)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"tests/test_gliner_provider.py ... splits" (flat one-file-per-concern test naming convention)
    - tests/test_admin_repair.py (general test-file style: from __future__ import annotations, function-based, monkeypatch)
  </read_first>
  <action>
    Per D-08/D-09, split `test_gliner_provider.py` along its internal grouping into concern-named
    siblings following the flat `test_<concern>.py` convention (e.g. `test_gliner_provider_http.py`
    for the HTTP-server/handler cases and `test_gliner_provider_extraction.py` for the
    extraction/label-schema cases), keeping the original ≤600 LOC (or empty of moved cases). Move
    whole test functions and any fixtures/imports they need verbatim — copy the
    `from turing_agentmemory_mcp.gliner_provider import GLiNERProvider, start_server` import into
    the sibling that uses it. Do NOT alter, weaken, or drop any assertion. pytest's
    `testpaths=["tests"]` auto-collects the new files. Run the affected files after the split, then
    the full suite to confirm the count is unchanged.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_gliner_provider.py tests/test_gliner_provider_http.py tests/test_gliner_provider_extraction.py</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files 'tests/test_gliner_provider*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m pytest -q` reports 362 passed (unchanged total — no test lost)
    - `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>test_gliner_provider.py split into ≤600-LOC concern files; every case still collected and passing; full total unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Split tests/test_batch_memory.py by scenario</name>
  <files>tests/test_batch_memory.py, plus new tests/test_batch_memory_&lt;concern&gt;.py siblings (executor names them)</files>
  <read_first>
    - tests/test_batch_memory.py (the file being split — read in full; identify scenario groups)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"tests/... test_batch_memory.py splits" (split by scenario group, e.g. write vs dedup)
    - tests/test_admin_repair.py (style reference)
  </read_first>
  <action>
    Per D-08/D-09, split `test_batch_memory.py` along its scenario grouping into concern-named
    siblings (e.g. `test_batch_memory_write.py` / `test_batch_memory_dedup.py`), keeping the
    original ≤600 LOC. Move whole test functions plus their fixtures/imports verbatim; do not
    weaken or drop assertions. Run the affected files, then the full suite to confirm the count is
    unchanged.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_batch_memory.py tests/test_batch_memory_write.py tests/test_batch_memory_dedup.py</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files 'tests/test_batch_memory*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m pytest -q` reports 362 passed (unchanged total)
    - `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>test_batch_memory.py split into ≤600-LOC scenario files; every case still collected and passing; full total unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-file layout change only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Repudiation | a test case silently lost during the move (collected count drops) weakens the safety net that guards every other split | medium | mitigate | Assert `python -m pytest -q` reports the SAME total (362) after the split; move whole cases verbatim, never delete/weaken assertions |
</threat_model>

<verification>
- Split test files collect and pass; `python -m pytest -q` → 362 passed (unchanged)
- `python -m ruff check src tests scripts` → clean
- All tests/test_gliner_provider*.py and tests/test_batch_memory*.py ≤ 600 LOC
</verification>

<success_criteria>
Both over-cap test files are decomposed to ≤600 LOC with the identical collected/passing count
preserved (contributes to CI-01, D-08/D-09).
</success_criteria>

## Artifacts this phase produces (this plan)

- Slimmed test files: `tests/test_gliner_provider.py`, `tests/test_batch_memory.py`
- New concern-split test siblings (executor judgment, e.g. `test_gliner_provider_http.py`, `test_gliner_provider_extraction.py`, `test_batch_memory_write.py`, `test_batch_memory_dedup.py`)

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-05-SUMMARY.md` when done.
</output>
