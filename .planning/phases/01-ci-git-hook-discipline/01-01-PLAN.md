---
phase: 01-ci-git-hook-discipline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/store.py
  - src/turing_agentmemory_mcp/store_core.py
  - src/turing_agentmemory_mcp/store_utils.py
  - src/turing_agentmemory_mcp/store_chunking.py
  - src/turing_agentmemory_mcp/store_memory_write.py
  - src/turing_agentmemory_mcp/store_memory_read.py
  - src/turing_agentmemory_mcp/store_search.py
  - src/turing_agentmemory_mcp/store_evidence.py
  - src/turing_agentmemory_mcp/store_documents.py
  - src/turing_agentmemory_mcp/store_rebuild.py
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "from turing_agentmemory_mcp.store import TuringAgentMemory still imports unchanged"
    - "The full pytest suite stays at its green baseline (362) after the split"
    - "scripts/e2e_score.py still prints VALIDATED_10_10 (score >= 9.8) after the split"
    - "No tracked src/turing_agentmemory_mcp/store*.py file exceeds 600 LOC"
  artifacts:
    - src/turing_agentmemory_mcp/store.py
    - src/turing_agentmemory_mcp/store_core.py
    - src/turing_agentmemory_mcp/store_utils.py
    - src/turing_agentmemory_mcp/store_chunking.py
    - src/turing_agentmemory_mcp/store_memory_write.py
    - src/turing_agentmemory_mcp/store_memory_read.py
    - src/turing_agentmemory_mcp/store_search.py
    - src/turing_agentmemory_mcp/store_evidence.py
    - src/turing_agentmemory_mcp/store_documents.py
    - src/turing_agentmemory_mcp/store_rebuild.py
  key_links:
    - "TuringAgentMemory composes every _*Mixin via MRO; self.<method>/self.<attr> resolve across siblings"
    - "Tenant scoping (_require_user / user_identifier) survives every method move — invariant #1"
---

<objective>
Decompose `src/turing_agentmemory_mcp/store.py` (3891 LOC) into cohesive ≤600-LOC
`store_<concern>.py` mixin modules behind a thin `store.py` facade, preserving the
`TuringAgentMemory` public class and its import path exactly (D-06/D-08/D-09/L-06). This is
the headline split of the phase — the file-size cap (D-08, no allowlist) cannot go green until
`store.py` is under 600 LOC.

Purpose: Remove the last "large central exception" so the 600-LOC cap applies to every tracked
`*.py` with no allowlist. Behavior MUST be preserved — the existing pytest suite plus the
deterministic E2E score gate are the safety net (invariant #8, phase-critical constraint #3).
Output: A slim `store.py` facade + 9 `store_*.py` mixin modules, all ≤600 LOC, with the
`TuringAgentMemory` import path and public API unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md
@.planning/phases/01-ci-git-hook-discipline/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract leaf + write/read mixins (store_utils, store_chunking, store_core, store_memory_write, store_memory_read)</name>
  <files>src/turing_agentmemory_mcp/store.py, src/turing_agentmemory_mcp/store_utils.py, src/turing_agentmemory_mcp/store_chunking.py, src/turing_agentmemory_mcp/store_core.py, src/turing_agentmemory_mcp/store_memory_write.py, src/turing_agentmemory_mcp/store_memory_read.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store.py (the file being split — read in full; cut real method bodies, do not guess)
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"store.py Decomposition Plan" (authoritative method→module mapping table + the mixin-composed-facade pattern)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"store.py decomposition" (header/import-block convention + nearest sibling analogs: hybrid.py, ids.py, document_jobs.py, retrieval_fusion.py)
    - src/turing_agentmemory_mcp/ids.py and src/turing_agentmemory_mcp/hybrid.py (pure-helper module shape for store_utils.py / store_chunking.py)
    - src/turing_agentmemory_mcp/document_jobs.py (docstring→constants→class ordering for store_core.py)
  </read_first>
  <action>
    Cut method bodies out of `store.py` into new sibling modules, one concern at a time, running
    the behavior gate after EACH extraction (phase-critical constraint #3 — the E2E gate is the
    only signal that catches a cross-mixin wiring slip). Use mixin classes composed via MRO per
    the RESEARCH "Mixin-composed facade" pattern; no mixin imports another mixin's class; shared
    `self.<attr>` are the ones assigned in `_StoreCore.__init__`.
    Create, in this order, moving the exact methods named in the RESEARCH decomposition table:
    `store_utils.py` (`_UtilsMixin`: value/ID/vector/datetime helpers + `_PAGE_MARKER`-independent
    pure helpers listed for store_utils), `store_chunking.py` (`_ChunkingMixin`: `_chunk_document_text`,
    `_chunk_text`, `_pack_text`, `_chunk_context` + the `_PAGE_MARKER_PATTERN` constant),
    `store_core.py` (`_StoreCore`: `__init__`, `bootstrap`, `load_graph_after_restart`,
    `runtime_status`, and the low-level `_ensure_*`/`_query`/`_write`/`_write_many`/`_records`/
    `_span`/`_audit`/`_now_iso`/`_json_*`/`_require_user`/`_ensure_user` infra),
    `store_memory_write.py` (`_MemoryWriteMixin`: `store_message`, `store_messages`, `add_entity`,
    `add_preference`, `add_fact` + their `_write_memory`/`_create_memories_batch`/`_plan_memory_projections`
    helpers and the `_MISSING` module constant), `store_memory_read.py` (`_MemoryReadMixin`:
    `get_memory`, `list_memories`, `update_memory`, `delete_memory` + row/filter helpers).
    After each file, add its mixin to the `TuringAgentMemory` base-class list in `store.py` so the
    class stays importable and green throughout (never leave `store.py` in a non-importing state
    between extractions). Preserve the `from turing_agentmemory_mcp.store import TuringAgentMemory`
    path (invariant #4) and every keyword-only public signature. The `check-file-size.sh` script
    (built later this phase) is the authoritative LOC check; if `store_memory_write.py` lands over
    600, pull a helper into `store_utils.py` per the RESEARCH sub-split note.
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from turing_agentmemory_mcp.store import TuringAgentMemory"` exits 0
    - `python -m pytest -q` stays at the pre-split green count (362 passed)
    - `python -m ruff check src tests scripts` exits 0
    - `wc -l src/turing_agentmemory_mcp/store_utils.py src/turing_agentmemory_mcp/store_chunking.py src/turing_agentmemory_mcp/store_core.py src/turing_agentmemory_mcp/store_memory_write.py src/turing_agentmemory_mcp/store_memory_read.py` each reports ≤600
    - `python scripts/e2e_score.py --out e2e-results.json` prints VALIDATED_10_10 (score ≥ 9.8)
  </acceptance_criteria>
  <done>The 5 leaf/write/read mixins exist ≤600 LOC each, composed into TuringAgentMemory; full suite + E2E gate + ruff check all green; import path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extract retrieval mixins (store_search, store_evidence, store_documents, store_rebuild) and slim the facade</name>
  <files>src/turing_agentmemory_mcp/store.py, src/turing_agentmemory_mcp/store_search.py, src/turing_agentmemory_mcp/store_evidence.py, src/turing_agentmemory_mcp/store_documents.py, src/turing_agentmemory_mcp/store_rebuild.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store.py (current state after Task 1 — read the remaining retrieval/document/rebuild method bodies before cutting)
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"store.py Decomposition Plan" (store_search/store_evidence/store_documents/store_rebuild rows + the store_documents ~640 sub-split note)
    - src/turing_agentmemory_mcp/rerank.py (apply_rerank_guard/assemble_rerank_document — store_search calls these; keep the import, do not reimplement)
    - src/turing_agentmemory_mcp/retrieval_fusion.py and src/turing_agentmemory_mcp/community_detection.py (multi-signal aggregation / rebuild shape for store_evidence and store_rebuild)
  </read_first>
  <action>
    Continue the concern-by-concern extraction (gate after each), moving the exact methods named
    in the RESEARCH table into: `store_search.py` (`_SearchMixin`: `search_memory`,
    `_search_memory_fused`, `_rerank_memory`, `_annotate_memory_rerank`, `_memory_rerank_document`,
    `_fused_rerank_score_details`), `store_evidence.py` (`_EvidenceMixin`: the `_collect_retrieval_evidence`
    + per-channel `_*_dense_evidence`/`_sparse_evidence`/`_query_graph_evidence`/`_expand_entity_evidence`
    + `_*_sources_by_ids`/`_memory_rows_for_ids` helpers), `store_documents.py` (`_DocumentMixin`:
    `get_context`, `ingest_document_text`, `get_document`, `reindex_document_text`, `delete_document`,
    `search_documents` + document/chunk helpers and the `_DocumentChunkGraphUnit` dataclass), and
    `store_rebuild.py` (`_RebuildMixin`: `rebuild_sparse_projection`, `rebuild_vector_projection`,
    `rebuild_communities` + projection helpers). If `store_documents.py` exceeds 600 LOC, apply the
    RESEARCH sub-split (move `_rerank_documents`/`_reranked_score_details` into `store_search.py`, or
    `_document_chunk_batch_query` into `store_chunking.py`) — check-file-size.sh is authoritative.
    Finally reduce `store.py` to the facade: `class TuringAgentMemory(_MemoryWriteMixin,
    _MemoryReadMixin, _SearchMixin, _EvidenceMixin, _DocumentMixin, _ChunkingMixin, _RebuildMixin,
    _UtilsMixin, _StoreCore)` plus any re-export imports consumers rely on. Keep the module
    docstring pointing at docs/architecture.md. Preserve tenant scoping in every moved method
    (invariant #1) — a dropped `_require_user` call is the highest-risk regression of this split.
  </action>
  <verify>
    <automated>python scripts/e2e_score.py --out e2e-results.json</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/e2e_score.py --out e2e-results.json` prints VALIDATED_10_10 (score ≥ 9.8)
    - `python -m pytest -q` reports 362 passed
    - `python -c "from turing_agentmemory_mcp.store import TuringAgentMemory"` exits 0
    - `git ls-files 'src/turing_agentmemory_mcp/store*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>store.py is a ≤60-LOC facade composing 9 mixins; all store*.py ≤600 LOC; full suite (362) + E2E gate (VALIDATED_10_10) + ruff check green; public import path and API unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure behavior-preserving refactor; no new input, network, or tenant surface is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | tenant scoping lost during a method move (`_require_user`/`user_identifier` dropped from an extracted method) | high | mitigate | Preserve every scoping call verbatim during extraction; the full pytest suite + deterministic E2E score gate re-run after EACH extraction assert tenant-scoped behavior is unchanged (invariant #1) |
| T-01-02 | Tampering | silent behavior drift from a mis-resolved cross-mixin `self.<method>` call | medium | mitigate | Run `scripts/e2e_score.py` after each extraction (unit tests use fakes and can miss wiring slips); require VALIDATED_10_10 before proceeding |
</threat_model>

<verification>
- `python -m pytest -q` → 362 passed (green baseline preserved)
- `python scripts/e2e_score.py --out e2e-results.json` → VALIDATED_10_10, score ≥ 9.8
- `python -m ruff check src tests scripts` → clean
- All `src/turing_agentmemory_mcp/store*.py` ≤ 600 LOC
- `from turing_agentmemory_mcp.store import TuringAgentMemory` unchanged
</verification>

<success_criteria>
store.py is decomposed into a facade + 9 mixin modules, each ≤600 LOC, with the full test suite
and E2E score gate green and the public import path/API unchanged (contributes to CI-01, ROADMAP SC#5).
</success_criteria>

## Artifacts this phase produces (this plan)

- New modules: `store_core.py`, `store_utils.py`, `store_chunking.py`, `store_memory_write.py`, `store_memory_read.py`, `store_search.py`, `store_evidence.py`, `store_documents.py`, `store_rebuild.py`
- Slimmed facade: `store.py`
- New mixin classes: `_StoreCore`, `_UtilsMixin`, `_ChunkingMixin`, `_MemoryWriteMixin`, `_MemoryReadMixin`, `_SearchMixin`, `_EvidenceMixin`, `_DocumentMixin`, `_RebuildMixin`
- Preserved public symbol: `TuringAgentMemory` (import path unchanged)

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-01-SUMMARY.md` when done.
</output>
