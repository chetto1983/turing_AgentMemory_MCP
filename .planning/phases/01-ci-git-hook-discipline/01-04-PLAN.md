---
phase: 01-ci-git-hook-discipline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval_backboard_locomo_mcp.py
  - scripts/real_document_benchmark.py
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "from scripts.real_document_benchmark import (its tested helpers) still imports unchanged"
    - "The scripts/eval_backboard_locomo_mcp public symbols its test imports still resolve"
    - "The full pytest suite stays at its green baseline (362) after the splits"
    - "No tracked scripts/eval_backboard_locomo_mcp*.py or scripts/real_document_benchmark*.py exceeds 600 LOC"
  artifacts:
    - scripts/eval_backboard_locomo_mcp.py
    - scripts/real_document_benchmark.py
  key_links:
    - "tests/test_real_document_benchmark.py imports helpers from scripts.real_document_benchmark"
    - "tests/test_backboard_locomo_runner.py imports from scripts.eval_backboard_locomo_mcp"
    - "Sibling scripts import each other via the script directory on sys.path when run as `python scripts/<name>.py`"
---

<objective>
Decompose two over-cap operator scripts — `scripts/eval_backboard_locomo_mcp.py` (936) and
`scripts/real_document_benchmark.py` (827) — into ≤600-LOC concern siblings per D-08/D-09 (the cap
scans ALL tracked `*.py`, including `scripts/`, with no allowlist).

Purpose: These scripts are decomposed for the cap only. Per D-10, `real_document_benchmark.py`
stays an operator-run tool and is NOT wired into CI — this plan changes neither script's runtime
behavior, only its file layout. Their deterministic helper functions are unit-tested today
(`tests/test_real_document_benchmark.py`, `tests/test_backboard_locomo_runner.py`), so those
import paths must be preserved (invariant #4).
Output: Two slimmed scripts + concern siblings in `scripts/`, all ≤600 LOC, tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split scripts/real_document_benchmark.py (preserve tested helpers)</name>
  <files>scripts/real_document_benchmark.py, plus new scripts/real_document_benchmark_&lt;concern&gt;.py siblings (executor names them)</files>
  <read_first>
    - scripts/real_document_benchmark.py (the file being split — read in full)
    - tests/test_real_document_benchmark.py (the `from scripts.real_document_benchmark import (...)` list — every named symbol must stay importable from that path)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"benchmark.py, e2e_score.py, scripts/... splits" (flat CLI script + sibling-module directory convention; no package split)
    - scripts/eval_locomo_answers.py (nearest smaller sibling script — arg-parse vs scoring layout reference)
  </read_first>
  <action>
    Per D-08/D-09, split `real_document_benchmark.py` into flat `scripts/` siblings by concern
    (e.g. `real_document_benchmark_scoring.py` for the deterministic helpers vs the live-MCP
    runner/CLI in the original), keeping the original ≤600 LOC. The deterministic helper functions
    that `tests/test_real_document_benchmark.py` imports (e.g. `select_passages`,
    `parse_generated_questions`, `evidence_rank`, `summarize_results`) MUST remain importable from
    `scripts.real_document_benchmark` — re-import them into the original module if their bodies
    move to a sibling, so the test's import list does not change. Do NOT alter script behavior and
    do NOT wire this into CI (D-10). Confirm sibling imports resolve when run as `python
    scripts/real_document_benchmark.py`. Run the affected test after the split.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_real_document_benchmark.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_real_document_benchmark.py` passes (unchanged import list)
    - `git ls-files 'scripts/real_document_benchmark*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m pytest -q` reports 362 passed; `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>real_document_benchmark.py ≤600 LOC via scripts siblings; tested helper import path unchanged; not wired into CI; full suite + ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Split scripts/eval_backboard_locomo_mcp.py (preserve tested symbols)</name>
  <files>scripts/eval_backboard_locomo_mcp.py, plus new scripts/eval_backboard_locomo_mcp_&lt;concern&gt;.py siblings (executor names them)</files>
  <read_first>
    - scripts/eval_backboard_locomo_mcp.py (the file being split — read in full)
    - tests/test_backboard_locomo_runner.py (its `from scripts.eval_backboard_locomo_mcp import (...)` list — must stay importable)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"scripts/... splits"
    - scripts/eval_locomo_answers.py (sibling layout reference)
  </read_first>
  <action>
    Per D-08/D-09, split `eval_backboard_locomo_mcp.py` into flat `scripts/` siblings by concern
    (e.g. dataset/scoring helpers vs the async MCP-bridge runner/CLI), keeping the original ≤600
    LOC. Re-import any moved symbol that `tests/test_backboard_locomo_runner.py` imports back into
    `scripts.eval_backboard_locomo_mcp` so the test import list is unchanged. Do not alter runner
    behavior. Confirm sibling imports resolve when run as `python scripts/eval_backboard_locomo_mcp.py`.
    Run the affected test after the split.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_backboard_locomo_runner.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_backboard_locomo_runner.py` passes (unchanged import list)
    - `git ls-files 'scripts/eval_backboard_locomo_mcp*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m pytest -q` reports 362 passed; `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>eval_backboard_locomo_mcp.py ≤600 LOC via scripts siblings; tested symbol import paths unchanged; full suite + ruff green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Operator-run benchmark scripts; not part of the server request path or CI gate (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | a tested helper removed from its `scripts.<name>` module path breaks its unit test | low | mitigate | Re-import moved helpers into the original module; the affected test asserts the import list still resolves |
| T-04-02 | Tampering | a sibling script fails to import a moved helper at runtime (script-dir not on sys.path) | low | mitigate | Confirm `python scripts/<name>.py` resolves sibling imports after the split |
</threat_model>

<verification>
- `tests/test_real_document_benchmark.py` and `tests/test_backboard_locomo_runner.py` pass unchanged
- `python -m pytest -q` → 362 passed; `ruff check` clean
- All scripts/eval_backboard_locomo_mcp*.py and scripts/real_document_benchmark*.py ≤ 600 LOC
</verification>

<success_criteria>
Both over-cap scripts are decomposed to ≤600 LOC with tested import paths and behavior preserved,
and neither is added to CI (contributes to CI-01; honors D-08/D-09/D-10).
</success_criteria>

## Artifacts this phase produces (this plan)

- Slimmed scripts: `scripts/eval_backboard_locomo_mcp.py`, `scripts/real_document_benchmark.py`
- New concern siblings in `scripts/` (executor judgment, e.g. `real_document_benchmark_scoring.py`)
- Preserved public helpers on `scripts.real_document_benchmark` and `scripts.eval_backboard_locomo_mcp`

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-04-SUMMARY.md` when done.
</output>
