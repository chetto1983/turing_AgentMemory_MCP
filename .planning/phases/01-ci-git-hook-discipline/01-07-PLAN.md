---
phase: 01-ci-git-hook-discipline
plan: 07
type: execute
wave: 3
depends_on: [01-06]
files_modified:
  - scripts/check-file-size.sh
  - lefthook.yml
  - Makefile
  - pyproject.toml
  - CLAUDE.md
autonomous: true
requirements: [CI-01, CI-02]
user_setup:
  - service: lefthook (local git hooks)
    why: "Fresh clones must run `lefthook install` (surfaced as `make hooks`) once to wire the pre-commit/pre-push hooks; hooks are intentionally NOT auto-installed (D-02)."
must_haves:
  truths:
    - "bash scripts/check-file-size.sh exits 0 on the compliant tree and exits 1 when any tracked *.py exceeds 600 LOC"
    - "lefthook.yml defines pre-commit (ruff format --check, ruff check, file-size cap) and pre-push (compile smoke, fast pytest subset, docker compose config --quiet)"
    - "pip install -e \".[dev]\" installs lefthook==2.1.10, ruff==0.15.21, pytest-cov==7.1.0"
    - "make hooks runs lefthook install"
    - "CLAUDE.md no longer calls store.py a >600-LOC exception"
  artifacts:
    - scripts/check-file-size.sh
    - lefthook.yml
    - Makefile
    - pyproject.toml
    - CLAUDE.md
  key_links:
    - "lefthook pre-commit file-size command invokes scripts/check-file-size.sh (scans ALL tracked *.py, no allowlist — D-08)"
    - "pyproject markers (slow/integration/gpu) back the pre-push fast subset AND the conftest guard in plan 08"
    - "pyproject [tool.coverage.run] omit backs the coverage floor in plan 09"
---

<objective>
Install the local git-hook layer and its config wiring (L-01/L-02/L-03/D-01/D-02/D-08): the
`scripts/check-file-size.sh` cap (no allowlist), `lefthook.yml` (pre-commit + pre-push),
`pyproject.toml` dev-dependency + marker + coverage-omit additions, a `make hooks` target, and the
CLAUDE.md rewrite that removes the store.py 600-LOC exception (D-09 follow-up). This plan is the
single owner of `pyproject.toml` for the infra waves so no two plans edit it concurrently.

Purpose: Encode the fast local gate that guards every downstream commit/push, adapted from Aura's
discipline (mirrored, not copied). Heavy gates (full E2E, coverage) stay in CI (L-05). Runs only
after the bootstrap gate (plan 06) is green, so the very first hooked commit already complies.
Output: A working local hook stack + the config it depends on + a corrected CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md
@.planning/phases/01-ci-git-hook-discipline/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check-file-size.sh (600-LOC cap, no allowlist, MSYS-safe)</name>
  <files>scripts/check-file-size.sh</files>
  <read_first>
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"check-file-size.sh (adapted from Aura, allowlist dropped per D-08)" (the finished script to write verbatim)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"scripts/check-file-size.sh" (the load-bearing process-substitution Windows/Git-Bash workaround — port, do not re-derive)
    - D:\Repo\Aura\scripts\check-file-size.sh (the original discipline reference)
  </read_first>
  <action>
    Write `scripts/check-file-size.sh` exactly per the RESEARCH finished code: `set -euo pipefail`,
    default cap 600 (overridable via `$1`), `TARGETS=$(git ls-files '*.py' || true)` with NO
    allowlist/exemption grep pipeline (D-08 scans ALL tracked `*.py`), and the process-substitution
    `while` loop (NOT a `<<<` here-string — this is the load-bearing MSYS/Git-Bash fix, this repo is
    Windows-primary). Skip tracked-but-deleted paths (`[ -f "$f" ] || continue`) so a mid-split
    worktree does not false-fail. On any file over cap, print the offender and exit 1 with the
    "no allowlist, refactor on touch: split <name>_<concern>.py" message; exit 0 otherwise.
  </action>
  <verify>
    <automated>bash scripts/check-file-size.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-file-size.sh` exits 0 (tree is compliant post-plan-06)
    - `bash scripts/check-file-size.sh 50` exits 1 and prints at least one `OVER CAP` line (proves the cap fires)
    - The script contains no allowlist/exemption grep and uses process substitution (not `<<<`)
  </acceptance_criteria>
  <done>check-file-size.sh scans all tracked *.py with no allowlist, is MSYS-safe, exits 0 on the compliant tree and non-zero on a violation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire lefthook.yml, pyproject.toml (deps + markers + coverage-omit), and the Makefile hooks target</name>
  <files>lefthook.yml, pyproject.toml, Makefile</files>
  <read_first>
    - .planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md §"lefthook.yml skeleton" and §"pyproject.toml — register the markers" and §"Standard Stack" (exact pins) and §"Coverage Floor" (omit e2e_score recommendation)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"lefthook.yml", §"pyproject.toml (modified)", §"Makefile (modified)" (edit-in-place analogs)
    - pyproject.toml (current dev extra and [tool.pytest.ini_options] — edit in place)
    - Makefile (current .PHONY targets — add `hooks` in the same style)
    - D:\Repo\Aura\lefthook.yml (parallel-commands / bypass-note / heavy-gates-stay-in-CI structure)
  </read_first>
  <action>
    Write `lefthook.yml` per the RESEARCH skeleton: top comment block documenting the install command
    (`lefthook install` / `make hooks`), the `--no-verify` bypass, and why heavy gates stay in CI
    (L-05). `pre-commit` (parallel, L-02): `ruff-format` = `python -m ruff format --check
    {staged_files}` glob `*.py`; `ruff-check` = `python -m ruff check {staged_files}` glob `*.py`;
    `file-size` = `bash scripts/check-file-size.sh` (NOT staged-file glob-gated — it scans ALL
    tracked `*.py` per D-08). `pre-push` (parallel, L-03): `compile-smoke` = `python -m compileall
    -q src tests scripts`; `fast-tests` = `python -m pytest -q -m "not slow and not integration and
    not gpu"`; `compose-config` = `docker compose config --quiet`.
    Edit `pyproject.toml` in place: in `[project.optional-dependencies].dev` bump `"ruff>=0.9"` →
    `"ruff==0.15.21"` and add `"lefthook==2.1.10"` (D-01) and `"pytest-cov==7.1.0"`; in
    `[tool.pytest.ini_options]` add the `markers` key registering `slow`, `integration`, `gpu` per
    the RESEARCH descriptions; add a `[tool.coverage.run]` block with `omit = ["*/e2e_score*.py"]`
    (exercised by its own `make e2e`, per RESEARCH Coverage Floor recommendation) so plan 09 can
    ratchet from the ~78% baseline. Do NOT touch the stale `description`/license fields (deferred to
    Phase 7). Add a `hooks` target to the `Makefile` (`lefthook install`) registered in `.PHONY`,
    matching the existing tab-indented style (D-02 — documented, not auto-installed).
    Verify `pip install -e ".[dev]"` resolves the new pins and `lefthook run pre-commit` executes.
  </action>
  <verify>
    <automated>python -m pip install -e ".[dev]" && python -m lefthook run pre-commit</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pip install -e ".[dev]"` installs lefthook==2.1.10, ruff==0.15.21, pytest-cov==7.1.0 (check `pip show`)
    - `lefthook run pre-commit` runs ruff-format/ruff-check/file-size and exits 0 on the compliant tree
    - `lefthook run pre-push` runs compile-smoke/fast-tests/compose-config and exits 0
    - `python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); m=d['tool']['pytest']['ini_options']['markers']; assert any('integration' in x for x in m) and any('gpu' in x for x in m) and any('slow' in x for x in m)"` exits 0
    - `make hooks` runs `lefthook install` (git hooks wired)
  </acceptance_criteria>
  <done>lefthook.yml wires the L-02 pre-commit and L-03 pre-push commands; pyproject has the exact dev pins + slow/integration/gpu markers + coverage omit; `make hooks` installs the hooks; the fast pre-push subset runs green.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the store.py 600-LOC exception from CLAUDE.md (D-09 follow-up)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md §"DEEP REFACTOR ON TOUCH" (the bullet containing "store.py is the large central exception … NO >600 loc")
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"CLAUDE.md (modified)" (exact clause to remove; keep the small-modules-by-concern guidance)
    - .planning/phases/01-ci-git-hook-discipline/01-CONTEXT.md §"Follow-up status" (CLAUDE.md rewrite is in-scope for this phase, D-09)
  </read_first>
  <action>
    Edit CLAUDE.md §"DEEP REFACTOR ON TOUCH": remove the "`store.py` is the large central exception —
    extend it deliberately, don't grow it casually" clause and the trailing "NO >600 loc" carve-out
    that framed store.py as exempt. Keep the "prefer small modules split by concern
    (`<name>_<concern>.py`)" guidance intact and restate the cap as applying to every tracked `*.py`
    with no allowlist (D-08/D-09), consistent with the file-size hook this phase installs. Do not
    edit unrelated CLAUDE.md sections.
  </action>
  <verify>
    <automated>grep -riq "large central exception" CLAUDE.md && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ri "large central exception" CLAUDE.md` prints nothing (clause removed)
    - `grep -ri "name>_<concern" CLAUDE.md` still matches (small-modules-by-concern guidance retained)
    - CLAUDE.md states the 600-LOC cap applies with no allowlist
  </acceptance_criteria>
  <done>CLAUDE.md no longer exempts store.py from the 600-LOC cap; the no-allowlist rule is stated; the concern-split guidance is retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → PyPI (pip install of dev tooling) | New dev dependencies (lefthook, ruff, pytest-cov) are fetched and their entry points executed locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | a typosquatted/compromised dev-tooling package (lefthook/ruff/pytest-cov) executing at install time | high | mitigate | RESEARCH §"Package Legitimacy Audit" verified all three as mature, org-backed packages (all OK/Approved, none [ASSUMED]/[SUS]/[SLOP]); pin exact versions (lefthook==2.1.10, ruff==0.15.21, pytest-cov==7.1.0) in the dev extra rather than open ranges |
| T-07-01 | Tampering | the file-size cap accidentally narrowed to a subdirectory/staged files, reintroducing an implicit allowlist | medium | mitigate | check-file-size.sh scans `git ls-files '*.py'` unconditionally and the lefthook `file-size` command is NOT glob-gated to staged files (D-08); acceptance asserts the cap fires on a low `$1` |
</threat_model>

<verification>
- `bash scripts/check-file-size.sh` exits 0; `bash scripts/check-file-size.sh 50` exits 1
- `pip install -e ".[dev]"` installs the exact pins; `lefthook run pre-commit` and `lefthook run pre-push` exit 0
- markers registered in pyproject; `[tool.coverage.run] omit` present
- `make hooks` installs hooks; CLAUDE.md exception clause removed
</verification>

<success_criteria>
The local hook stack (pre-commit + pre-push) is wired and runs green on the compliant tree, the dev
tooling is pinned and installable, and CLAUDE.md reflects the no-allowlist cap (satisfies CI-01
pre-commit + CI-02 pre-push; honors L-01/L-02/L-03/D-01/D-02/D-08/D-09).
</success_criteria>

## Artifacts this phase produces (this plan)

- New files: `scripts/check-file-size.sh`, `lefthook.yml`
- Modified: `pyproject.toml` (dev pins lefthook==2.1.10 / ruff==0.15.21 / pytest-cov==7.1.0, `slow`/`integration`/`gpu` markers, `[tool.coverage.run] omit`), `Makefile` (`hooks` target), `CLAUDE.md` (store.py exception removed)

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-07-SUMMARY.md` when done.
</output>
