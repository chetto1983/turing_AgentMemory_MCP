---
phase: 01-ci-git-hook-discipline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/benchmark.py
  - src/turing_agentmemory_mcp/e2e_score.py
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "from turing_agentmemory_mcp.e2e_score import main still imports unchanged (scripts/e2e_score.py depends on it — it IS the E2E gate)"
    - "from turing_agentmemory_mcp.benchmark import main, REQUIRED_FIELDS, make_result_row, _git_commit still imports unchanged"
    - "scripts/e2e_score.py still prints VALIDATED_10_10 (score >= 9.8) after the split"
    - "No tracked benchmark*/e2e_score*.py file (in src) exceeds 600 LOC"
  artifacts:
    - src/turing_agentmemory_mcp/benchmark.py
    - src/turing_agentmemory_mcp/e2e_score.py
  key_links:
    - "scripts/e2e_score.py imports `main` from turing_agentmemory_mcp.e2e_score — must not break"
    - "scripts/benchmark.py imports `main`; agent_quality_eval.py imports `_git_commit`; test_benchmark imports REQUIRED_FIELDS/make_result_row"
---

<objective>
Decompose two over-cap `src/` modules — `benchmark.py` (1044) and `e2e_score.py` (873) — into
≤600-LOC concern siblings per D-08/D-09, preserving the public symbols their CLI wrappers and
tests import (invariant #4).

Purpose: `e2e_score.py` is special — `scripts/e2e_score.py` (the deterministic E2E gate that
EVERY decomposition plan uses as its verify) does `from turing_agentmemory_mcp.e2e_score import
main`. If the split drops that export the whole phase loses its safety net. Preserving
`e2e_score.main` is therefore the load-bearing constraint of this plan.
Output: Two slimmed modules + concern siblings, all ≤600 LOC, the E2E gate still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ci-git-hook-discipline/01-RESEARCH.md
@.planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split src/turing_agentmemory_mcp/e2e_score.py (preserve `main` export)</name>
  <files>src/turing_agentmemory_mcp/e2e_score.py, plus new e2e_score_&lt;concern&gt;.py siblings (executor names them)</files>
  <read_first>
    - src/turing_agentmemory_mcp/e2e_score.py (the file being split — read in full)
    - scripts/e2e_score.py (the 12-line wrapper that imports `main` — confirms the export contract)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"benchmark.py, e2e_score.py ... splits" (scenario data vs runner vs stub-server concern boundaries)
    - src/turing_agentmemory_mcp/gliner_provider.py (HTTP-server-plumbing vs logic separation reference for the stub-server concern)
  </read_first>
  <action>
    Per D-08/D-09, split `e2e_score.py` along its natural concerns — scenario/corpus data, the
    deterministic runner/scorer, and the in-process stub embed/rerank HTTP servers — into siblings
    (e.g. `e2e_score_scenarios.py`, `e2e_score_stubs.py`) while keeping `e2e_score.py` ≤600 LOC.
    `main` MUST remain importable from `turing_agentmemory_mcp.e2e_score` (re-export from the
    facade if the entrypoint logic moves) because `scripts/e2e_score.py` imports it — this gate is
    the phase's behavior-preservation safety net. Do not change scoring thresholds, scenario
    content, or stub behavior. Run the gate immediately after the split.
  </action>
  <verify>
    <automated>python scripts/e2e_score.py --out e2e-results.json</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from turing_agentmemory_mcp.e2e_score import main"` exits 0
    - `python scripts/e2e_score.py --out e2e-results.json` prints VALIDATED_10_10 (score ≥ 9.8)
    - `git ls-files 'src/turing_agentmemory_mcp/e2e_score*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m pytest -q` reports 362 passed; `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>e2e_score.py ≤600 LOC via siblings; `main` import path unchanged; E2E gate VALIDATED_10_10; full suite + ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Split src/turing_agentmemory_mcp/benchmark.py (preserve public helpers)</name>
  <files>src/turing_agentmemory_mcp/benchmark.py, plus new benchmark_&lt;concern&gt;.py siblings (executor names them)</files>
  <read_first>
    - src/turing_agentmemory_mcp/benchmark.py (the file being split — read in full)
    - tests/test_benchmark.py (imports REQUIRED_FIELDS, make_result_row, benchmark module)
    - scripts/benchmark.py (imports `main`) and src/turing_agentmemory_mcp/agent_quality_eval.py (imports `_git_commit`)
    - .planning/phases/01-ci-git-hook-discipline/01-PATTERNS.md §"benchmark.py ... splits" (pure-function module + pipeline-stage concern boundaries)
  </read_first>
  <action>
    Per D-08/D-09, split `benchmark.py` along cohesive concerns (e.g. result-row schema/formatting
    helpers vs the corpus runner) into siblings while keeping `benchmark.py` ≤600 LOC. Keep
    `main`, `REQUIRED_FIELDS`, `make_result_row`, and `_git_commit` importable from
    `turing_agentmemory_mcp.benchmark` unchanged (re-export moved symbols). Do not alter benchmark
    logic or output schema. Run the behavior gate after the split.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_benchmark.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from turing_agentmemory_mcp.benchmark import main, REQUIRED_FIELDS, make_result_row, _git_commit"` exits 0
    - `git ls-files 'src/turing_agentmemory_mcp/benchmark*.py' | while read f; do n=$(wc -l < "$f"); [ "$n" -le 600 ] || echo "OVER:$f:$n"; done` prints nothing
    - `python -m pytest -q` reports 362 passed; `python -m ruff check src tests scripts` exits 0
  </acceptance_criteria>
  <done>benchmark.py ≤600 LOC via siblings; public helper import paths unchanged; full suite + ruff green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Refactor of benchmark/E2E tooling; no production request path or tenant surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `e2e_score.main` export dropped by the split, silently disabling the phase's E2E safety net | high | mitigate | Re-export `main`; run `python scripts/e2e_score.py` immediately after the split and require VALIDATED_10_10 before the plan is considered done |
| T-03-02 | Information Disclosure | benchmark public helper removed from its module path breaks an importer | low | mitigate | Re-export moved symbols; import-smoke acceptance check asserts the path resolves |
</threat_model>

<verification>
- `from turing_agentmemory_mcp.e2e_score import main` and benchmark helper import smokes exit 0
- `python scripts/e2e_score.py` → VALIDATED_10_10
- `python -m pytest -q` → 362 passed; `ruff check` clean
- All src benchmark*/e2e_score*.py ≤ 600 LOC
</verification>

<success_criteria>
benchmark.py and e2e_score.py are decomposed to ≤600 LOC with public import paths and the E2E gate
preserved (contributes to CI-01, D-08/D-09).
</success_criteria>

## Artifacts this phase produces (this plan)

- Slimmed modules: `benchmark.py`, `e2e_score.py`
- New concern siblings (executor judgment, e.g. `e2e_score_scenarios.py`, `e2e_score_stubs.py`, `benchmark_report.py`)
- Preserved public symbols: `e2e_score.main`, `benchmark.main`, `REQUIRED_FIELDS`, `make_result_row`, `_git_commit`

<output>
Create `.planning/phases/01-ci-git-hook-discipline/01-03-SUMMARY.md` when done.
</output>
