---
phase: 04-arcadedb-direct-port
plan: 05
type: tdd
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/turing_agentmemory_mcp/store_memory_write.py
  - src/turing_agentmemory_mcp/store_memory_read.py
  - src/turing_agentmemory_mcp/store_memory_queries.py
  - tests/test_store_arcadedb_memory.py
autonomous: true
requirements: [ARC-04, ARC-05, ARC-08, PERF-01, PERF-02]
must_haves:
  truths:
    - "memory (and entity/fact) writes CREATE ArcadeDB vertices with the embedding inline as a record property and stable_id as the id — no vector_id property is written"
    - "a batch memory write embeds all items in one round-trip and runs memory extraction in one round-trip per batch (D-08/PERF-01/PERF-02), inside one managed transaction"
    - "every write and read is user_identifier-scoped and fails closed on empty"
    - "the memory kind update (store_memory_read) no longer sets a vector_id"
  artifacts:
    - "src/turing_agentmemory_mcp/store_memory_write.py — ported memory/entity/fact write via sqlscript+LET"
    - "src/turing_agentmemory_mcp/store_memory_read.py — ported reads/kind update, no vector_id"
    - "src/turing_agentmemory_mcp/store_memory_queries.py — extracted ArcadeDB query builders (keeps the two files under 600 LOC)"
    - "tests/test_store_arcadedb_memory.py — write/read behavior + batching + tenant-scope tests"
  key_links:
    - "vectors correlate to records via stable_id (ARC-08), never a synthetic vector_id int-join"
    - "batched embed/extract call the existing embedder.embed_many / memory extractor batch API — single round-trip"
  prohibitions:
    - "no vector_id property write and no _*_vector_id() call in these files"
    - "no per-item embed or per-item extraction HTTP call in the batch write path"
    - "no quote()-interpolated data literal — bound params only"
    - "store_memory_write.py and store_memory_read.py must not exceed 600 LOC"
---

<objective>
Port the memory write/read paths to ArcadeDB (ARC-04): CREATE Memory/Entity/Fact vertices with the embedding inline as a record property and `stable_id()` as the id (ARC-08), deleting the `vector_id` int-join (ARC-05). Collapse the batch write into one managed transaction with batched embedding and batched memory extraction — a single round-trip per batch (D-08/PERF-01/PERF-02). Extract the verbose sqlscript+LET builders into a new `store_memory_queries.py` sibling so neither 599-LOC file crosses the cap.

Purpose: The memory write path is the primary ingest surface and the one where per-item HTTP calls (PERF-01/02) and the vector_id int-join (ARC-05) both live.
Output: Ported, tested memory write/read with batched providers and no vector_id.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-arcadedb-direct-port/04-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-PATTERNS.md
@.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
</context>

<artifacts_this_phase_produces>
New module `src/turing_agentmemory_mcp/store_memory_queries.py` (excluded from drift): the extracted ArcadeDB write/read query builders for memories/entities/facts (functions building sqlscript+LET bodies + param dicts). Ports existing symbols: `_create_memory`, the batch-write method, `store_memory_read` kind-update. Consumes `embedder.embed_many` and the memory extractor batch API (existing) for single-round-trip batching.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Port memory/entity/fact writes to inline-vector sqlscript+LET, stable_id id, no vector_id</name>
  <files>src/turing_agentmemory_mcp/store_memory_write.py, src/turing_agentmemory_mcp/store_memory_queries.py, tests/test_store_arcadedb_memory.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_memory_write.py (lines 260-300 batch embed + vector_row building; 395-406 _create_memory CREATE literal with vector_id; 460-530 batch CREATE with entities/facts)
    - src/turing_agentmemory_mcp/store_utils.py (lines 22-41 _projection_edge_literals/_cypher_value — the double-quote value-quoting being retired; lines 199-216 the _*_vector_id helpers the ports stop calling)
    - src/turing_agentmemory_mcp/ids.py (stable_id — the sole identifier)
    - src/turing_agentmemory_mcp/store_core.py (ported _write_many/_query seam + managed transaction — 04-04)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (store_documents/store_memory_write section — sqlscript+LET, retire _cypher_value, bound params; LOC-budget extraction to a sibling)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Pattern 1 sqlscript+LET; State of the Art — vector_id deletion; Pattern 4 bound params)
  </read_first>
  <behavior>
    - Test 1: a single memory write CREATEs a Memory vertex whose `id` equals `stable_id("mem", ...)` and whose embedding is stored inline as a record property; no `vector_id` property is set
    - Test 2: a memory write with extracted entities/facts CREATEs the Entity/Fact vertices + SUBJECT_OF/SUPPORTED_BY/MENTIONS edges in one managed transaction (assert one commit)
    - Test 3: a value containing a single quote in content/metadata is stored intact (bound param, not interpolated)
    - Test 4: a write with empty user_identifier raises ValueError (fail-closed)
    - Test 5: a query for tenant A never returns tenant B's memory (both written, scoped read)
  </behavior>
  <action>
    Rewrite `_create_memory` and the batch-write method to build their graph writes as sqlscript+LET bodies via new builders in `store_memory_queries.py`: `LET $m = CREATE VERTEX Memory CONTENT {id, user_identifier, ..., embedding: [<inlined vector>]}` (stable_id as `id`, embedding inline, NO `vector_id`), plus `CREATE EDGE HAS_MEMORY/SUBJECT_OF/SUPPORTED_BY/MENTIONS` statements, with all non-vector data values passed as bound params (retire `_cypher_value`/`quote` interpolation). Stop calling `_memory_vector_id`/`_entity_vector_id`/`_fact_vector_id` entirely. Route the whole batch through the ported `_write_many` single managed transaction (04-04). Keep the existing `_require_user`/`_ensure_user` fail-closed scoping on every path. Move enough builder code into `store_memory_queries.py` that store_memory_write.py stays under 600 LOC.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/store_memory_write.py src/turing_agentmemory_mcp/store_memory_queries.py tests/test_store_arcadedb_memory.py && python -m pytest tests/test_store_arcadedb_memory.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/store_memory_write.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_store_arcadedb_memory.py` exits 0
    - `grep -cE "vector_id|_memory_vector_id|_entity_vector_id|_fact_vector_id" src/turing_agentmemory_mcp/store_memory_write.py src/turing_agentmemory_mcp/store_memory_queries.py` is 0
    - Test 1 asserts the stored Memory `id` == `stable_id(...)`
    - store_memory_write.py under 600 LOC (check-file-size.sh exits 0)
    - `grep -c "user_identifier" src/turing_agentmemory_mcp/store_memory_queries.py` is >= 1 (scope carried into builders)
  </acceptance_criteria>
  <done>Memory/entity/fact writes are ArcadeDB inline-vector CREATEs keyed on stable_id with bound params, no vector_id, tenant-scoped.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Batched embedding + batched extraction in one transaction (PERF-01/PERF-02); port kind-update read</name>
  <files>src/turing_agentmemory_mcp/store_memory_write.py, src/turing_agentmemory_mcp/store_memory_read.py, tests/test_store_arcadedb_memory.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_memory_write.py (the batch loop — where per-item embed/extract would occur; embedder.embed_many is the batch API)
    - src/turing_agentmemory_mcp/store_memory_read.py (lines 145-180 the kind-update SET m.vector_id path to strip)
    - src/turing_agentmemory_mcp/embeddings.py (embed_many — single-round-trip batching)
    - src/turing_agentmemory_mcp/memory_extraction.py (the extractor batch entry point)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (D-08 batched embed/extract bundle)
  </read_first>
  <behavior>
    - Test 1: a batch of N memory writes calls the embedder exactly once (embed_many over N texts), not N times (assert call count on a fake embedder)
    - Test 2: memory extraction over the batch is invoked once per batch, not per item (assert call count on a fake extractor)
    - Test 3: the kind-update read updates the Memory kind without writing a vector_id property
    - Test 4: batch write remains within one managed transaction across the embed→extract→write sequence
  </behavior>
  <action>
    Ensure the batch write path embeds all item texts in a single `embedder.embed_many(...)` call and runs memory extraction once over the batch (PERF-01/PERF-02) before opening the single managed transaction that writes all records — no per-item HTTP call. Port `store_memory_read`'s kind-update to an ArcadeDB `UPDATE ... SET kind = :kind` with bound params, removing the `SET m.vector_id = {vid}` clause and the `_memory_vector_id` call.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/store_memory_read.py && python -m pytest tests/test_store_arcadedb_memory.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/store_memory_read.py</automated>
  </verify>
  <acceptance_criteria>
    - Test 1/2 assert a single embed and single extract call for a multi-item batch
    - `grep -cE "vector_id|_memory_vector_id" src/turing_agentmemory_mcp/store_memory_read.py` is 0
    - store_memory_read.py under 600 LOC
  </acceptance_criteria>
  <done>Batches embed and extract in one round-trip each within one transaction; the kind update no longer touches vector_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP write tool → memory write path | untrusted content/metadata + user_identifier written to the shared DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure / Elevation | tenant scope in rewritten writes/reads | high | mitigate | user_identifier carried into every builder + fail-closed _require_user; cross-tenant read test (Test 5) |
| T-04-05-02 | Tampering | injection via content/metadata in single-quote dialect | high | mitigate | Bound params for all data values; single-quote-in-content test (Test 3) |
| T-04-05-03 | Tampering (data integrity) | vector/record correlation drift after removing vector_id | high | mitigate | stable_id is the inline record id + vector key; Test 1 asserts id == stable_id |
</threat_model>

<verification>
- `python -m pytest tests/test_store_arcadedb_memory.py -q` green
- `python -m ruff check src tests` clean
- `grep -rc "vector_id" src/turing_agentmemory_mcp/store_memory_write.py src/turing_agentmemory_mcp/store_memory_read.py` is 0
- both files under 600 LOC
</verification>

<success_criteria>
Memory writes/reads run on ArcadeDB with inline vectors keyed on stable_id, batched single-round-trip embed/extract in one transaction, no vector_id, tenant-scoped and injection-safe — both files under the LOC cap.
</success_criteria>

<output>
Create `.planning/phases/04-arcadedb-direct-port/04-05-SUMMARY.md` when done.
</output>
