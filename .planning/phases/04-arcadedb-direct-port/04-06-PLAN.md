---
phase: 04-arcadedb-direct-port
plan: 06
type: tdd
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/turing_agentmemory_mcp/store_documents.py
  - src/turing_agentmemory_mcp/store_chunking.py
  - src/turing_agentmemory_mcp/store_documents_queries.py
  - tests/test_store_arcadedb_documents.py
autonomous: true
requirements: [ARC-04, ARC-05, ARC-06, PERF-01]
must_haves:
  truths:
    - "document ingest CREATEs a Document + Chunk vertices + HAS_CHUNK/NEXT_CHUNK edges in one managed transaction via sqlscript+LET, with chunk embeddings inline and stable_id ids"
    - "document search runs native HNSW vector search AND native Lucene full-text, keeping the adaptive over-fetch-then-filter default (D-03), with no vector_id int-join"
    - "chunk context (NEXT_CHUNK neighbors) resolves by graph traversal keyed on chunk_id, not vector_id"
    - "chunk embeddings are computed in one batched round-trip (PERF-01); the validate to chunk to hash-dedup to stable_id sequence is preserved"
  artifacts:
    - "src/turing_agentmemory_mcp/store_documents.py — ported ingest + search"
    - "src/turing_agentmemory_mcp/store_chunking.py — ported NEXT_CHUNK context, no vector_id"
    - "src/turing_agentmemory_mcp/store_documents_queries.py — extracted sqlscript+LET/search builders (keeps store_documents.py under 600 LOC)"
    - "tests/test_store_arcadedb_documents.py — ingest + search + dedup + tenant-scope tests"
  key_links:
    - "the Lucene full-text channel uses the analyzer chosen in 04-01 (FTS5-approximating) so ranking stays parity-comparable"
    - "chunk-to-document linkage and NEXT_CHUNK ordering survive on stable_id keys"
  prohibitions:
    - "no vector_id property write, no _document_vector_id() call, no c.vector_id read"
    - "no SQLite FTS5 outbox call in the document write path (native Lucene is ACID)"
    - "no quote()-interpolated data literal — bound params only"
    - "store_documents.py must not exceed 600 LOC"
---

> ## ⚠ EXECUTION UPDATE — read `04-EXECUTION-STATE.md` first (post-spike reconciliation)
> - **Both channels:** populate `lexical_tokens`/`lexical_weights` (sparse) on each **Chunk** using the shared sparse-encoder module 04-05 established; Chunk's raw-text property is **`text`** (not `content`) and Lucene auto-indexes it. Reuse the encoder verbatim — write/query tokenization must match.
> - Extract query builders into `store_documents_queries.py` to stay < 600 LOC. No `vector_id`; `stable_id` ids; bound params; every path tenant-scoped + fail-closed.
> - Migrate the document-side transiently-red tests (document_search filters, ingest spans, document-expiry) as your port makes them migratable. NO skip-as-green.
> - Use the settled ArcadeDB facts from 04-EXECUTION-STATE.md / 04-SPIKE-FINDINGS.md, not the plan's pre-spike wording.

<objective>
Port document ingestion, chunking, and document search to ArcadeDB (ARC-04): multi-node `_create_document`/`_document_graph_queries` become sqlscript+LET batches (Document + Chunk vertices + HAS_CHUNK/NEXT_CHUNK edges) with chunk embeddings inline and `stable_id()` ids (ARC-05/ARC-08); document search runs native HNSW + native Lucene full-text (ARC-06) keeping the adaptive over-fetch default (D-03); chunk embeddings batch in one round-trip (PERF-01). Extract the verbose builders into `store_documents_queries.py` so the 597-LOC file stays under the cap.

Purpose: This is the §1.3 full-scan the port fixes for free, and the document side of the FTS5-to-Lucene retirement.
Output: Ported, tested document ingest + chunking + search.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-arcadedb-direct-port/04-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-PATTERNS.md
@.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
</context>

<artifacts_this_phase_produces>
New module `src/turing_agentmemory_mcp/store_documents_queries.py` (excluded from drift): extracted ArcadeDB document/chunk write + search query builders. Ports existing symbols: `ingest_document_text`, `_create_document`, `_document_graph_queries`, `search_documents`, `_chunk_context`. Uses `embedder.embed_documents/embed_many` for batched chunk embedding.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Port document + chunk ingest to sqlscript+LET with inline vectors and NEXT_CHUNK on stable_id</name>
  <files>src/turing_agentmemory_mcp/store_documents.py, src/turing_agentmemory_mcp/store_chunking.py, src/turing_agentmemory_mcp/store_documents_queries.py, tests/test_store_arcadedb_documents.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_documents.py (lines 74-110 ingest_document_text validate/dedup/stable_id sequence to preserve; 247-346 _create_document + 348-468 _document_graph_queries multi-node CREATE with vector_id; 283-292 the chunk vector-row build)
    - src/turing_agentmemory_mcp/store_chunking.py (lines 81-130 _chunk_context NEXT_CHUNK MATCH + c.vector_id read)
    - src/turing_agentmemory_mcp/store_core.py (ported _write_many managed transaction — 04-04)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (store_documents section — sqlscript+LET, preserve ingest sequence, LOC extraction; store_chunking section — small file, safe to inline)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Pattern 1 sqlscript+LET; §1.3 full-scan)
  </read_first>
  <behavior>
    - Test 1: ingesting a 3-chunk document CREATEs one Document + 3 Chunk vertices + 3 HAS_CHUNK + 2 NEXT_CHUNK edges in one managed transaction (assert single commit)
    - Test 2: each Chunk chunk_id is a stable_id and its embedding is stored inline; no vector_id property is written
    - Test 3: re-ingesting the same title+text is deduped by text hash (existing document returned, not duplicated)
    - Test 4: _chunk_context(chunk_id) returns the NEXT_CHUNK neighbors resolved by chunk_id traversal (no vector_id)
    - Test 5: an empty user_identifier raises ValueError; a tenant-A ingest is not visible to tenant B
  </behavior>
  <action>
    Rewrite `_create_document`/`_document_graph_queries` to build sqlscript+LET bodies (via `store_documents_queries.py`): a document vertex CREATE bound to a LET var, then per chunk a Chunk vertex CREATE (with chunk_id, document_id, and the embedding inlined as a numeric array property), a HAS_CHUNK edge from the document var to the chunk var, and a NEXT_CHUNK edge from the previous chunk var to the current — stable_id ids, embeddings inline, NO vector_id, all data as bound params. Preserve the exact `ingest_document_text` validate to chunk to hash-dedup to stable_id sequence; only the terminal graph-write changes. Batch chunk embeddings in one `embed_documents`/`embed_many` call (PERF-01). Change `_chunk_context` to resolve NEXT_CHUNK neighbors by `chunk_id` traversal (drop the vector-int parameter and the `c.vector_id` read). Extract enough builder code into `store_documents_queries.py` to keep store_documents.py under 600 LOC.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/store_documents.py src/turing_agentmemory_mcp/store_chunking.py src/turing_agentmemory_mcp/store_documents_queries.py tests/test_store_arcadedb_documents.py && python -m pytest tests/test_store_arcadedb_documents.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/store_documents.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_store_arcadedb_documents.py` exits 0
    - `grep -cE "vector_id|_document_vector_id" src/turing_agentmemory_mcp/store_documents.py src/turing_agentmemory_mcp/store_chunking.py src/turing_agentmemory_mcp/store_documents_queries.py` is 0
    - Test 2 asserts a chunk id equals a stable_id and the embedding is inline
    - store_documents.py under 600 LOC (check-file-size.sh exits 0)
  </acceptance_criteria>
  <done>Document + chunk ingest is an ArcadeDB sqlscript+LET batch with inline vectors keyed on stable_id, NEXT_CHUNK on chunk_id, tenant-scoped, no vector_id.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Port document search to native HNSW + native Lucene full-text, adaptive over-fetch (D-03)</name>
  <files>src/turing_agentmemory_mcp/store_documents.py, src/turing_agentmemory_mcp/store_documents_queries.py, tests/test_store_arcadedb_documents.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_documents.py (lines 470-570 search_documents — VECTOR SEARCH ... FOR max(limit*4,limit) + c.vector_id join + _chunk_context call)
    - src/turing_agentmemory_mcp/store_documents_queries.py (the builders from Task 1)
    - .planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md (resolved vector function spelling; Lucene full-text score exposure vs boolean CONTAINSTEXT; D-03 pushdown verdict)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (store_search section — keep the over-fetch guard, D-03; DocumentHit citation shape preserved)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Pattern 3 filtered-ANN over-fetch; Pitfall 3 k-underfill; Pitfall 4 analyzer drift)
  </read_first>
  <behavior>
    - Test 1: document vector search returns DocumentHit citations (chunk_id/document_id/locator/text/score/context) ordered by native HNSW score — no c.vector_id join
    - Test 2: a status/expires_at filter keeps the adaptive over-fetch-then-filter default (assert the over-fetch multiplier is applied unless the spike marked pushdown safe)
    - Test 3: the full-text channel matches an exact keyword/phrase query via native Lucene (analyzer from 04-01)
    - Test 4: a tenant-A query never returns tenant-B chunks (user_identifier bound on every channel)
  </behavior>
  <action>
    Rewrite `search_documents` to run the native HNSW vector search (resolved function spelling, record+score together — no `c.vector_id = ids` join) and the native Lucene full-text query, both `user_identifier`-bound, keeping the `max(limit*4, limit)` adaptive over-fetch-then-filter for status/expires_at unless 04-SPIKE-FINDINGS.md proved predicate pushdown does not under-fill k (D-03). Preserve the DocumentHit citation shape and the `_chunk_context` context enrichment (now chunk_id-keyed). Put the query builders in `store_documents_queries.py`.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/store_documents.py src/turing_agentmemory_mcp/store_documents_queries.py && python -m pytest tests/test_store_arcadedb_documents.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/store_documents.py</automated>
  </verify>
  <acceptance_criteria>
    - Test 1-4 pass
    - `grep -c "c.vector_id\|VECTOR SEARCH IN" src/turing_agentmemory_mcp/store_documents.py` is 0
    - the over-fetch guard (or a spike-justified pushdown) is present in the search builder — verify Test 2 asserts the returned count is not silently short
    - store_documents.py under 600 LOC
  </acceptance_criteria>
  <done>Document search runs on native HNSW + Lucene with adaptive over-fetch, citation-preserving, tenant-scoped, no vector_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP document tool → ingest/search | untrusted document text + query + user_identifier cross into the shared DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Information Disclosure / Elevation | tenant scope on ingest + both search channels | high | mitigate | user_identifier bound on every write and both search channels; cross-tenant test (Task1 Test5, Task2 Test4) |
| T-04-06-02 | Tampering | injection via document text/title in single-quote dialect | high | mitigate | Bound params for all data; embeddings inline as numeric arrays only |
| T-04-06-03 | Tampering (data integrity) | chunk/vector correlation drift after removing vector_id | high | mitigate | stable_id chunk_id is the inline id + vector key; Test 2 asserts id == stable_id |
| T-04-06-04 | Information Disclosure | full-text analyzer ranking drift masking missing results | low | accept | Analyzer chosen in 04-01 to approximate FTS5; parity captured in 04-09 for the Phase 6 gate |
</threat_model>

<verification>
- `python -m pytest tests/test_store_arcadedb_documents.py -q` green
- `python -m ruff check src tests` clean
- `grep -rc "vector_id" src/turing_agentmemory_mcp/store_documents.py src/turing_agentmemory_mcp/store_chunking.py` is 0
- store_documents.py under 600 LOC
</verification>

<success_criteria>
Document ingest, chunking, and search run on ArcadeDB with inline vectors keyed on stable_id, native HNSW + Lucene channels, adaptive over-fetch, no vector_id, tenant-scoped and injection-safe — under the LOC cap.
</success_criteria>

<output>
Create `.planning/phases/04-arcadedb-direct-port/04-06-SUMMARY.md` when done.
</output>
