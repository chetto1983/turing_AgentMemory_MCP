---
phase: 04-arcadedb-direct-port
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/turing_agentmemory_mcp/arcadedb_client.py
  - tests/test_arcadedb_client.py
autonomous: true
requirements: [ARC-03]
must_haves:
  truths:
    - "ArcadeDBClient exposes the full transaction surface: query, command, sqlscript, begin, commit, rollback, and a readiness probe"
    - "a single managed transaction can be opened, run a multi-statement sqlscript body, and commit — with a bounded MVCC-conflict commit-retry-N wrapper (D-08)"
    - "the client raises RuntimeError on transport/HTTP exhaustion (raise-hard, no silent degrade) and reserves a soft path only for the readiness probe"
    - "data values bind as ?/:named params; vector literals are inlined; no double-quote Cypher-style escaping is used"
  artifacts:
    - "src/turing_agentmemory_mcp/arcadedb_client.py — full ArcadeDBClient with commit-retry-N and probe/is_ready"
    - "tests/test_arcadedb_client.py — unit tests (mocked urlopen) + the live smoke test from 04-01"
  key_links:
    - "the commit-retry-N wrapper catches ONLY the ArcadeDB MVCC-conflict signal resolved by the spike, layered on top of the generic transport retry — the two retry loops are distinct"
    - "the endpoint path prefix + auth form come from 04-SPIKE-FINDINGS.md, not from a doc guess"
  prohibitions:
    - "no httpx/requests/arcadedb-python import — stdlib urllib only"
    - "no ArcadeDB native RID (#12:34 / getIdentity) captured or returned as an identifier"
    - "no generic exponential-backoff retry framework invented — wrap ArcadeDB's own conflict signal only"
---

<objective>
Grow the spike-minimal `arcadedb_client.py` into the full thin stdlib-`urllib` client the ported store routes every graph/vector/full-text op through (ARC-03): `query`/`command`/`sqlscript`/`begin`/`commit`/`rollback`, a bounded MVCC commit-retry-N wrapper (D-08 infra), and a readiness probe (D-10 infra). Bound `?`/`:named` params for data, inlined vector literals.

Purpose: This is the single HTTP/JSON seam the entire port depends on. Its transaction + retry + probe surface is consumed by the core seam (04-04), the schema bootstrap (04-03), and every store mixin (04-05..08).
Output: A tested, complete ArcadeDBClient.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-arcadedb-direct-port/04-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-PATTERNS.md
@.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
</context>

<artifacts_this_phase_produces>
Extends `ArcadeDBClient` (04-01) with net-new methods (excluded from drift verification): `sqlscript`, `rollback`, `command_in_tx`/managed-transaction helper, the commit-retry-N wrapper, and `probe`/`is_ready`. New env vars introduced here: `ARCADEDB_COMMIT_RETRIES` (MVCC retry budget), `ARCADEDB_TIMEOUT_SECONDS`, `ARCADEDB_MAX_ATTEMPTS`, `ARCADEDB_RETRY_BASE_SECONDS` (if not already added in 04-01).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Managed-transaction surface + commit-retry-N wrapper (D-08 infra)</name>
  <files>src/turing_agentmemory_mcp/arcadedb_client.py, tests/test_arcadedb_client.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/arcadedb_client.py (the 04-01 spike-minimal client — extend, do not rewrite)
    - src/turing_agentmemory_mcp/embeddings.py (lines 120-162 — the transport retry loop shape to layer under the MVCC retry)
    - src/turing_agentmemory_mcp/rerank.py (lines 271-334 — the status-field/predicate-function convention, e.g. retryable_provider_code, for a clean conflict-detection predicate)
    - .planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md (resolved: whether `COMMIT RETRY N` is valid SQL vs a Python-side retry loop is needed; the MVCC-conflict HTTP status/error code)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Assumptions A3; Don't Hand-Roll — MVCC retry row; Pattern 1 sqlscript+LET)
  </read_first>
  <behavior>
    - Test 1: `begin()` then `command(...)` then `commit(tx)` issues the resolved begin/command/commit endpoint calls in order (assert against a mocked urlopen capturing request paths/bodies)
    - Test 2: `sqlscript(body, params=...)` posts one multi-statement body to the resolved sqlscript/command endpoint and returns parsed rows
    - Test 3: a commit that returns the MVCC-conflict signal is retried up to `ARCADEDB_COMMIT_RETRIES` times, then raises RuntimeError; a commit that conflicts once then succeeds returns the success result (assert retry count)
    - Test 4: a non-conflict HTTP 500 is NOT retried by the MVCC wrapper (only by the transport loop) — the two loops are distinct
    - Test 5: params bind as ?/:named in the request payload; a value containing a single-quote does not corrupt the statement (bound, not interpolated)
  </behavior>
  <action>
    Add `sqlscript(body, *, params=None)`, `rollback(tx)`, and a managed-transaction helper that runs `begin` → `command`/`sqlscript` → `commit` and wraps the commit in a bounded retry keyed on the spike-resolved MVCC-conflict signal (either emitting `COMMIT RETRY N` if the spike confirmed the SQL clause, OR a Python-side retry loop of at most `ARCADEDB_COMMIT_RETRIES` attempts catching the conflict HTTP status/error — implement whichever the spike proved). Keep this MVCC retry as a SECOND, narrower loop layered on the existing transport-retry loop from 04-01; use a small predicate function (mirroring `retryable_provider_code`) to classify the conflict signal rather than inlining the check. Wire `ARCADEDB_COMMIT_RETRIES` into `from_env()`. Never log credential or full-statement values on retry.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/arcadedb_client.py tests/test_arcadedb_client.py && python -m pytest tests/test_arcadedb_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_arcadedb_client.py` exits 0 including the new transaction/retry tests
    - `grep -c "def sqlscript\|def rollback\|def begin\|def commit" src/turing_agentmemory_mcp/arcadedb_client.py` is >= 4
    - the MVCC retry budget is bounded by `ARCADEDB_COMMIT_RETRIES` (assert the retry count in a test, not an unbounded loop)
    - `grep -cE "import (httpx|requests)|arcadedb_python" src/turing_agentmemory_mcp/arcadedb_client.py` is 0
  </acceptance_criteria>
  <done>The client can run one managed transaction with a bounded MVCC-conflict retry, tested with mocked HTTP, raising-hard on exhaustion.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Readiness probe / reconnect primitive (D-10 infra)</name>
  <files>src/turing_agentmemory_mcp/arcadedb_client.py, tests/test_arcadedb_client.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/arcadedb_client.py (the probe()/is_ready() stub from 04-01)
    - src/turing_agentmemory_mcp/store_core.py (lines 226-239 — the TuringDB `_ensure_graph_loaded` dance that D-10 REPLACES; do not port the except-Exception:pass swallow)
    - src/turing_agentmemory_mcp/observability.py (RuntimeSignals.configure_stage — the readiness abstraction to extend, not replace)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Don't Hand-Roll — reconnect row; D-10 readiness)
  </read_first>
  <behavior>
    - Test 1: `is_ready()` returns True when the probe query succeeds against a mocked-reachable endpoint
    - Test 2: `is_ready()` returns False (does NOT raise) when the endpoint is unreachable (URLError/OSError) — the ONE soft path
    - Test 3: `probe()` after a transient failure then success reflects recovery (reconnect without a manual load step)
  </behavior>
  <action>
    Finalize `probe()`/`is_ready()` as a cheap reachability check (a trivial SELECT/ready call resolved by the spike) that returns a boolean and NEVER raises into a caller — the single soft/degraded path in an otherwise raise-hard client. It must not carry any TuringDB `load_graph`/`list_loaded_graphs`/`set_graph` semantics (ArcadeDB databases do not need an explicit load-after-restart step — invariant #6 retires). This is the primitive 04-04 wires into `RuntimeSignals.configure_stage("graph", ...)` and `/health`.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/arcadedb_client.py && python -m pytest tests/test_arcadedb_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `is_ready()` returns a bool and does not raise on an unreachable endpoint (asserted in a test)
    - `grep -c "load_graph\|list_loaded_graphs\|set_graph" src/turing_agentmemory_mcp/arcadedb_client.py` is 0
    - `python -m pytest tests/test_arcadedb_client.py -q` green
  </acceptance_criteria>
  <done>A boolean readiness probe exists that reconnects without a manual runbook step and never raises into request handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP process → ArcadeDB HTTP API | statement text + bound params + credentials cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Tampering | statement construction in client | high | mitigate | Client transmits bound ?/:named params separately from statement text; vector literals inlined only as numeric arrays; no string interpolation of caller data (Test 5) |
| T-04-02-02 | Information Disclosure | credentials/statements in retry logs | medium | mitigate | Retry paths log base_url only, never credential or full-statement values |
| T-04-02-03 | Denial of Service | unbounded MVCC retry loop | medium | mitigate | Retry budget bounded by `ARCADEDB_COMMIT_RETRIES`; asserted finite in Test 3 |
</threat_model>

<verification>
- `python -m pytest tests/test_arcadedb_client.py -q` green (mocked-HTTP unit tests + live smoke)
- `python -m ruff check src tests` clean
- No new pip dependency (stdlib urllib only)
</verification>

<success_criteria>
`ArcadeDBClient` is the complete, tested HTTP/JSON seam: full transaction surface, bounded MVCC commit-retry, and a non-raising readiness probe — ready for the core seam and mixins to route through.
</success_criteria>

<output>
Create `.planning/phases/04-arcadedb-direct-port/04-02-SUMMARY.md` when done.
</output>
