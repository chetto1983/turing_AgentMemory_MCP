---
phase: 04-arcadedb-direct-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yaml
  - src/turing_agentmemory_mcp/arcadedb_client.py
  - tests/test_arcadedb_client.py
  - scripts/arcadedb_spike.py
  - .planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
autonomous: true
requirements: [ARC-02, ARC-03]
must_haves:
  truths:
    - "docker compose up -d arcadedb starts the arcadedata/arcadedb:26.7.1 service and it reaches a healthy state with a persistent volume"
    - "tests/test_arcadedb_client.py runs a committed smoke test against the live 26.7.1 container and resolves all five §3 unknowns (vector function name, filtered-ANN k-underfill, full-text analyzer + score exposure, vector DDL dims/COSINE, intra-transaction read-your-writes)"
    - "04-SPIKE-FINDINGS.md records the resolved HTTP endpoint paths, auth requirement, vector function spelling, and the D-03/D-04/D-05 decisions with evidence"
    - "the D-04/D-05 bake-off ran the baseline/03-turingdb frozen questions plus hand-authored lexical-stress queries against both candidate lexical channels and both graph-query surfaces, emitting a machine-readable JSON artifact"
  artifacts:
    - "compose.yaml — new hardened arcadedb service block + persistent named volume"
    - "src/turing_agentmemory_mcp/arcadedb_client.py — minimal ArcadeDBClient (spike scope only: query/command/begin/commit + reachability probe)"
    - "tests/test_arcadedb_client.py — committed smoke test (the D-02 hard gate)"
    - "scripts/arcadedb_spike.py — re-runnable bake-off harness"
    - ".benchmarks/arcadedb-spike.json — machine-readable bake-off results"
    - ".planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md — resolved-unknowns + decision record"
  key_links:
    - "arcadedb_client.py base-URL construction must use the endpoint path prefix the smoke test empirically confirmed (not the divergent doc-sourced guesses)"
    - "the bake-off scores must be captured in a shape directly diffable against baseline/03-turingdb/ (parity-aligned with the Phase 6 gate)"
  prohibitions:
    - "no store_*.py query builder is written or modified in this plan — the spike gates all query-builder work"
    - "no ArcadeDB syntax is committed to a store path before the smoke test proves it against the pinned 26.7.1 image"
    - "the turingdb compose service and turingdb==1.35 dependency are NOT removed (add-only; removal is Phase 7/ARC-10)"
    - "ArcadeDB's native RID (#12:34) is never captured as an identifier in the spike client or smoke test"
---

<objective>
Resolve the five ArcadeDB 26.7.1 capability unknowns empirically before any store query builder is written (D-02 hard gate). Stand up a hardened `arcadedb` Compose service on a persistent volume (ARC-02), write a minimal stdlib-`urllib` `arcadedb_client.py` plus a committed smoke test that exercises the real container (ARC-03 partial), and run the D-04/D-05 bake-off against the Phase-3 yardstick to record the D-03/D-04/D-05 decisions.

Purpose: The RESEARCH doc, the project's `arcadedb` skill, and the Context7-derived capabilities doc DISAGREE on vector function naming, HTTP endpoint paths, and vector-index DDL. Only a live smoke test against the pinned image can close these gaps. Every downstream wave depends on this plan's resolved answers.
Output: A healthy ArcadeDB service, a spike client + smoke test, a bake-off artifact, and a written findings/decision record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-arcadedb-direct-port/04-CONTEXT.md
@.planning/phases/04-arcadedb-direct-port/04-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 1) creates these net-new symbols (excluded from drift/source-grounding verification by later reviews):
- Module `src/turing_agentmemory_mcp/arcadedb_client.py` → class `ArcadeDBClient` (spike-minimal: methods `query`, `command`, `begin`, `commit`, `probe`/`is_ready`, `from_env`)
- Compose service `arcadedb` + named volume `arcadedb-data`
- Env vars introduced here: `ARCADEDB_URL`, `ARCADEDB_DATABASE`, `ARCADEDB_USER`, `ARCADEDB_PASSWORD`
- Script `scripts/arcadedb_spike.py`; artifact `.benchmarks/arcadedb-spike.json`
- Doc `.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md`
- Test `tests/test_arcadedb_client.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Stand up the hardened arcadedb Compose service + resolve auth/endpoint/healthcheck</name>
  <files>compose.yaml, .env.example</files>
  <read_first>
    - compose.yaml (lines 1-160 — the turingdb/turingdb-volume-init service pair is the hardening analog: non-root user "10001:10001", tmpfs /tmp,/run, security_opt no-new-privileges, init, restart, deploy.resources.limits, healthcheck-with-retries)
    - .claude/skills/arcadedb/references/admin-reference.md (Security section: `arcadedb.server.rootPassword`, `arcadedb.server.rootPasswordPath`, first-run password prompt) and api-reference.md (endpoint paths `/query/graph/<db>`, `/command/<db>`, port 2480, HTTP Basic Auth)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (compose.yaml section — inherit hardening verbatim; add-do-not-remove turingdb)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Runtime State Inventory: coexistence with turingdb; Open Question 5: default credentials; Security Domain: no default-open instance)
  </read_first>
  <action>
    Add an `arcadedb` service to compose.yaml pinned to `arcadedata/arcadedb:26.7.1` with a persistent named volume `arcadedb-data` mounted at ArcadeDB's data directory. Inherit the turingdb hardening posture: non-root `user: "10001:10001"`, `security_opt: no-new-privileges:true`, `init: true`, `restart: unless-stopped`, `tmpfs: /tmp,/run`, and a `deploy.resources.limits` block (tune cpus/memory for ArcadeDB — do not copy turingdb's numbers blind). Pass the root credential via env from `${ARCADEDB_PASSWORD}` using the image's `arcadedb.server.rootPassword`/`rootPasswordPath` mechanism (resolve the exact JAVA_OPTS/env form from admin-reference.md) — do NOT ship a default-open instance even inside the private compose network. Publish the port only on `127.0.0.1`. Define a `healthcheck` that probes ArcadeDB's own native readiness endpoint (the arcadedb container is a stock Java image with no Python — use the HTTP form the image supports, e.g. a `wget`/`curl`/CMD-SHELL probe of the ready endpoint resolved in Task 2), with `interval`/`timeout`/`retries`/`start_period` mirroring the turingdb shape. Add `ARCADEDB_URL`, `ARCADEDB_DATABASE`, `ARCADEDB_USER`, `ARCADEDB_PASSWORD` to `.env.example` using the existing `${VAR:-default}` templating. Do NOT remove or edit the turingdb service, turingdb-volume-init, or any `TURINGDB_*` env vars (coexistence through Phase 6).
  </action>
  <verify>
    <automated>docker compose config --quiet && docker compose up -d arcadedb && sleep 30 && docker compose ps arcadedb | grep -qi healthy</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exits 0 with the new arcadedb service present
    - `docker compose ps arcadedb` reports `healthy` within the start_period
    - `grep -c "turingdb:" compose.yaml` is unchanged from pre-plan (turingdb service still present)
    - `.env.example` contains `ARCADEDB_URL` and `ARCADEDB_PASSWORD`
    - the arcadedb service has `user: "10001:10001"`, `security_opt`, and a `healthcheck` block
  </acceptance_criteria>
  <done>The arcadedb service starts healthy on a persistent volume with the same non-root/read-only hardening as the rest of the stack, no default-open credentials, and turingdb untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Minimal ArcadeDBClient + committed smoke test resolving the five §3 unknowns</name>
  <files>src/turing_agentmemory_mcp/arcadedb_client.py, tests/test_arcadedb_client.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/embeddings.py (lines 1-101 — the exact stdlib-urllib client convention: imports, `@dataclass(frozen=True)` config, `from_env()` factory, `Request`/`urlopen` with timeout, exponential-backoff retry loop, `RuntimeError` on exhaustion)
    - src/turing_agentmemory_mcp/rerank.py (the same urllib request+retry shape for a second reference)
    - src/turing_agentmemory_mcp/provider_config.py (provider_env, provider_secret helpers)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Open Questions 1-5 — the literal spike test plan; Assumptions Log A1-A9; Code Examples — the three competing vector function spellings; Pitfall 5 read-your-writes test)
    - .claude/skills/arcadedb/references/api-reference.md and sql-reference.md (endpoint paths, CONTAINSTEXT, BEGIN/COMMIT, parameter binding)
  </read_first>
  <action>
    Create `arcadedb_client.py` with a spike-minimal `ArcadeDBClient` dataclass mirroring `OpenAICompatibleEmbedder`'s shape: `from_env()` reading `ARCADEDB_URL`/`ARCADEDB_DATABASE`/`ARCADEDB_USER`/`ARCADEDB_PASSWORD`/`ARCADEDB_TIMEOUT_SECONDS`/`ARCADEDB_MAX_ATTEMPTS`/`ARCADEDB_RETRY_BASE_SECONDS`; HTTP Basic Auth header from user:password; methods `query(statement, *, params=None, language="sql")`, `command(statement, *, params=None)`, `begin()`, `commit(tx)`, and `probe()`/`is_ready()` (a cheap reachability query). Use `urllib.request` only, exponential backoff on `HTTPError`/`URLError`/`OSError`, raise `RuntimeError` on exhaustion (embeddings.py raise-hard convention — ArcadeDB is canonical, no soft degrade). Bind data values as `?`/`:named` params; inline vector literals (not bindable). Write `tests/test_arcadedb_client.py` as the committed smoke test that runs against the live container and records, into the returned/asserted values, the resolution of: (1) which of `vectorNeighbors` / `` `vector.neighbors` `` / `vector_search` resolves for HNSW; (2) whether a `status`/`expires_at` predicate combined with the vector function under-fills k (populate active/inactive/expired fixtures, count returned vs expected at several k); (3) the Lucene analyzer + whether full-text relevance is exposed as an orderable score column vs boolean CONTAINSTEXT filter; (4) `CREATE VECTOR INDEX`/`LSM_VECTOR` DDL that takes dims from a value + COSINE, full-precision (D-01, no quantization); (5) intra-transaction read-your-writes — CREATE VERTEX A then SELECT A by property filter (not just $var) in the same tx before COMMIT. Skip the test cleanly (pytest skip with a visible reason, never silent green) when the arcadedb container is not reachable, so the suite stays runnable without Docker but the gate requires the live run.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/arcadedb_client.py tests/test_arcadedb_client.py && docker compose up -d arcadedb && ARCADEDB_URL=http://127.0.0.1:2480 python -m pytest tests/test_arcadedb_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_arcadedb_client.py` exits 0 against the live container (not skipped)
    - `arcadedb_client.py` imports only stdlib `urllib` for HTTP (no httpx/requests/arcadedb-python) — verify: `grep -cE "import (httpx|requests)|arcadedb_python" src/turing_agentmemory_mcp/arcadedb_client.py` is 0
    - the smoke test asserts a concrete winning vector-function spelling and a concrete read-your-writes result (test body references the resolved names, not all three candidates)
    - `grep -c "getIdentity\|#[0-9]*:[0-9]*" src/turing_agentmemory_mcp/arcadedb_client.py` is 0 (no RID captured as identifier)
  </acceptance_criteria>
  <done>The five unknowns are resolved by asserting concrete behavior against the pinned image; the client is a thin stdlib-urllib wrapper that raises-hard.</done>
</task>

<task type="auto">
  <name>Task 3: D-04/D-05 bake-off against the Phase-3 yardstick + committed decision record</name>
  <files>scripts/arcadedb_spike.py, .planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md</files>
  <read_first>
    - baseline/03-turingdb/ (BASELINE.md, frozen-questions.json, corpus-manifest.json, e2e-results.json/real-document-benchmark.json — the parity yardstick shape the bake-off must be diffable against)
    - src/turing_agentmemory_mcp/real_document_benchmark_scoring.py (deterministic scoring helpers to reuse)
    - src/turing_agentmemory_mcp/sparse_index.py (the FTS5 lexical-channel shape — conceptual analog only, for how a lexical channel feeds RRF)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Validation Architecture: Additional Validation Surface #1 spike bake-off spec; D-06 yardstick; Pitfall 4 analyzer drift)
    - .planning/phases/04-arcadedb-direct-port/04-CONTEXT.md (D-03/D-04/D-05/D-06 decisions)
  </read_first>
  <action>
    Create `scripts/arcadedb_spike.py` — a re-runnable harness that: indexes a small fixture corpus in ArcadeDB with BOTH candidate lexical channels (native Lucene full-text via CONTAINSTEXT/SEARCH with an analyzer chosen to approximate FTS5 `unicode61`, and native `LSM_SPARSE_VECTOR` BM25 via the sparse-neighbors function); runs `baseline/03-turingdb/frozen-questions.json` PLUS a handful (author 5-8) of hand-authored pure-lexical stress queries (keyword / error-code / exact-phrase) against both; and prototypes the 2-hop `entity→fact→memory` traversal in BOTH ArcadeDB SQL MATCH/TRAVERSE and openCypher, recording which binds params cleanly and composes with the resolved vector/full-text functions. Emit `.benchmarks/arcadedb-spike.json` in the existing machine-readable benchmark convention. Write `04-SPIKE-FINDINGS.md` recording: the resolved HTTP endpoint prefix, auth requirement, winning vector function spelling, vector DDL form, read-your-writes verdict (all from Task 2); and the D-03 (over-fetch-then-filter stays unless pushdown proven no-k-underfill), D-04 (chosen lexical channel + analyzer), D-05 (chosen graph-query surface) decisions with the bake-off evidence. Do NOT change RRF weights or retrieval_fusion.py — both channels feed the existing Python RRF unchanged.
  </action>
  <verify>
    <automated>python -m ruff check scripts/arcadedb_spike.py && docker compose up -d arcadedb && python scripts/arcadedb_spike.py --out .benchmarks/arcadedb-spike.json && test -f .benchmarks/arcadedb-spike.json && test -f .planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.benchmarks/arcadedb-spike.json` exists and contains per-channel and per-graph-surface scores against the frozen questions
    - `04-SPIKE-FINDINGS.md` contains explicit `D-03:`, `D-04:`, `D-05:` decision lines each with a one-line evidence pointer
    - the stress queries (>=5) are present in the harness and scored
    - `grep -c "weights\s*=" src/turing_agentmemory_mcp/retrieval_fusion.py` is unchanged (fusion untouched)
  </acceptance_criteria>
  <done>A committed, re-runnable bake-off produced a parity-diffable artifact and a written decision record that unblocks the query-builder waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP process → ArcadeDB HTTP API | store/client credentials + query text cross here over the compose network |
| compose network → arcadedb container | an unauthenticated/default-credential ArcadeDB would be reachable by any co-networked service |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-01 | Spoofing/Elevation | arcadedb container default credentials | high | mitigate | Require `ARCADEDB_PASSWORD` (no default-open); resolve Open Question 5 in Task 1 before the service ships; 127.0.0.1-only port publish |
| T-04-01-02 | Information Disclosure | credentials in logs | medium | mitigate | Basic-auth header built from env; never log credential values from arcadedb_client.py (raise messages reference base_url only, per embeddings.py) |
| T-04-01-SC | Tampering | arcadedata/arcadedb:26.7.1 image integrity | high | mitigate | Pin exact tag; Task 1 records the resolved image digest/build date in 04-SPIKE-FINDINGS.md (A8). No pip packages installed (stdlib urllib), so no PyPI supply-chain surface |
</threat_model>

<verification>
- `docker compose config --quiet` passes; `docker compose up -d arcadedb` healthy
- `python -m pytest tests/test_arcadedb_client.py -q` green against the live container
- `python -m ruff check src tests scripts` clean for the new files
- 04-SPIKE-FINDINGS.md records all five resolved unknowns + D-03/D-04/D-05
</verification>

<success_criteria>
The D-02 hard gate is satisfied: ArcadeDB 26.7.1 runs healthy on a persistent volume, the five capability unknowns are resolved by a committed live smoke test, and the D-03/D-04/D-05 decisions are recorded with bake-off evidence — no store query builder has been written.
</success_criteria>

<output>
Create `.planning/phases/04-arcadedb-direct-port/04-01-SUMMARY.md` when done, and ensure 04-SPIKE-FINDINGS.md captures the resolved syntax the downstream waves consume.
</output>
