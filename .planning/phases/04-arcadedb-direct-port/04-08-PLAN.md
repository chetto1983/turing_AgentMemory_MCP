---
phase: 04-arcadedb-direct-port
plan: 08
type: tdd
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/turing_agentmemory_mcp/store_rebuild.py
  - src/turing_agentmemory_mcp/store_rebuild_queries.py
  - tests/test_store_arcadedb_rebuild.py
autonomous: true
requirements: [ARC-04, ARC-05, INFRA-03]
must_haves:
  truths:
    - "vector-projection rebuild builds a NEW versioned/namespaced index, populates it fully, atomically swaps the active-version pointer, then drops the old — searches never see a half-built index (D-07/INFRA-03)"
    - "a rebuild removes stale vectors — re-running does not accumulate duplicates (fixes the known memory_rebuild_vector_projection bug)"
    - "the community-graph replace runs as one sqlscript+LET managed transaction; vectors are inline, keyed on stable_id, with no vector_id property"
    - "every rebuild path is user_identifier-scoped"
  artifacts:
    - "src/turing_agentmemory_mcp/store_rebuild.py — ported rebuild with D-07 atomic swap"
    - "src/turing_agentmemory_mcp/store_rebuild_queries.py — extracted sqlscript+LET/rebuild query builders"
    - "tests/test_store_arcadedb_rebuild.py — atomic-swap, no-stale, community-graph, tenant-scope tests"
  key_links:
    - "the active-version pointer is tracked as store state (Config vertex or well-known property) so search always resolves the current version (04-03 versioned naming)"
    - "vectors written inline via CREATE/UPDATE record properties — no CSV LOAD, no vector_id"
  prohibitions:
    - "no in-place index mutation during rebuild (that is the stale-accumulation bug)"
    - "no vector_id property write, no _community_vector_id()/_*_vector_id() call, no CSV LOAD VECTOR"
    - "no quote()-interpolated data literal — bound params only"
    - "store_rebuild.py must not exceed 600 LOC"
---

<objective>
Port vector-projection and community-graph rebuild to ArcadeDB (ARC-04/ARC-05) and pull forward full vector-index versioning with atomic swap (D-07/INFRA-03): a rebuild builds a NEW versioned/namespaced index, populates it, atomically swaps the active-version pointer, then drops the old — eliminating the known `memory_rebuild_vector_projection` stale-vector-accumulation bug and enabling A/B/canary/rollback. The multi-node `_replace_community_graph` CREATE becomes a sqlscript+LET managed transaction with inline vectors keyed on stable_id. Extract builders into `store_rebuild_queries.py` so the 566-LOC file stays under the cap.

Purpose: In-place rebuild was the exact bug this phase pulls D-07 forward to fix — this plan replaces it, it does not re-port it.
Output: Ported, tested rebuild with atomic-swap versioning and no stale vectors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-arcadedb-direct-port/04-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-PATTERNS.md
@.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
</context>

<artifacts_this_phase_produces>
New module `src/turing_agentmemory_mcp/store_rebuild_queries.py` (excluded from drift): extracted ArcadeDB rebuild/community-graph query builders + the active-version-pointer read/write helpers. Ports existing symbols: `rebuild_vector_projection`, `rebuild_communities`, `_replace_community_graph`. Consumes `arcadedb_schema.versioned_vector_index` (04-03) for the new-version index names.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: D-07 versioned atomic-swap vector rebuild with no stale accumulation (INFRA-03)</name>
  <files>src/turing_agentmemory_mcp/store_rebuild.py, src/turing_agentmemory_mcp/store_rebuild_queries.py, tests/test_store_arcadedb_rebuild.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_rebuild.py (lines 122-260 rebuild_vector_projection — the _load_vectors CSV + _*_vector_id table + in-place index mutation being replaced)
    - src/turing_agentmemory_mcp/arcadedb_schema.py (versioned_vector_index + bootstrap — 04-03; the new-version index-name seam)
    - src/turing_agentmemory_mcp/store_core.py (ported seam/managed transaction — 04-04; note _load_vectors is deleted there)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (store_rebuild section + No Analog Found — D-07 atomic swap is NEW logic, no in-repo analog; RESEARCH Patterns 1-4 + capabilities doc are the reference)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Pattern 2 versioned naming; Don't Hand-Roll — vector-index rebuild-in-place row; State of the Art — in-place → atomic swap)
  </read_first>
  <behavior>
    - Test 1: rebuild_vector_projection builds a NEW versioned index name (version+1), populates it with the current records' inline vectors, then flips the active-version pointer to it and drops the previous version (assert order: populate-before-swap-before-drop)
    - Test 2: a search issued mid-rebuild resolves the OLD active version until the swap completes (never a half-built index)
    - Test 3: running rebuild twice does not leave duplicate/stale vectors — the vector count equals the live record count, not 2x (no accumulation)
    - Test 4: vectors are written inline (no CSV file, no vector_id property, no _*_vector_id call)
    - Test 5: rebuild is user_identifier-scoped (tenant A rebuild does not touch tenant B vectors)
  </behavior>
  <action>
    Rewrite `rebuild_vector_projection` (and `rebuild_communities`' vector step) to the D-07 atomic-swap flow via `store_rebuild_queries.py`: compute the next version name with `arcadedb_schema.versioned_vector_index(base, user_identifier, version+1)`, CREATE that new index, populate it by writing the current records' embeddings inline (bound params; no CSV, no vector_id, no `_*_vector_id`), then atomically flip the tracked active-version pointer (a Config-type vertex or well-known store property) to the new version and drop the old index. Searches must resolve the active version only, so a half-populated new index is never served. Stop calling `_load_vectors` (deleted in 04-04) and the `(_memory_vector_id, ...)` helper table.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/store_rebuild.py src/turing_agentmemory_mcp/store_rebuild_queries.py tests/test_store_arcadedb_rebuild.py && python -m pytest tests/test_store_arcadedb_rebuild.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/store_rebuild.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_store_arcadedb_rebuild.py` exits 0
    - Test 3 asserts no vector accumulation across two rebuilds (count == live records)
    - `grep -cE "vector_id|_memory_vector_id|_community_vector_id|LOAD VECTOR|_load_vectors" src/turing_agentmemory_mcp/store_rebuild.py src/turing_agentmemory_mcp/store_rebuild_queries.py` is 0
    - `grep -c "versioned_vector_index" src/turing_agentmemory_mcp/store_rebuild_queries.py` is >= 1
    - store_rebuild.py under 600 LOC
  </acceptance_criteria>
  <done>Rebuild builds+swaps a versioned index atomically with no stale accumulation, inline vectors keyed on stable_id, tenant-scoped.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Port _replace_community_graph multi-node CREATE to sqlscript+LET, no vector_id</name>
  <files>src/turing_agentmemory_mcp/store_rebuild.py, src/turing_agentmemory_mcp/store_rebuild_queries.py, tests/test_store_arcadedb_rebuild.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_rebuild.py (lines 372-430 _replace_community_graph — multi-node CREATE + vector_id property + IN_COMMUNITY edges)
    - src/turing_agentmemory_mcp/store_rebuild_queries.py (builders from Task 1)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Pattern 1 multi-node CREATE → sqlscript+LET)
  </read_first>
  <behavior>
    - Test 1: _replace_community_graph CREATEs the Community vertices + IN_COMMUNITY edges as one sqlscript+LET managed transaction (single commit)
    - Test 2: community vectors are inline, keyed on stable_id; no vector_id property is written
    - Test 3: replacing an existing community graph removes the prior communities (no orphan accumulation) and is user_identifier-scoped
  </behavior>
  <action>
    Rewrite `_replace_community_graph` to a sqlscript+LET body (Community vertex CREATEs bound to LET vars + IN_COMMUNITY edge CREATEs) through the ported managed transaction, with the community embedding inline and stable_id as id (drop the `_community_vector_id` call and the `vector_id` property), all data as bound params, user_identifier-scoped. Ensure the prior community graph for the tenant is cleared before/within the replace so replays do not accumulate.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/store_rebuild.py src/turing_agentmemory_mcp/store_rebuild_queries.py && python -m pytest tests/test_store_arcadedb_rebuild.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/store_rebuild.py</automated>
  </verify>
  <acceptance_criteria>
    - Test 1-3 pass
    - `grep -cE "vector_id|_community_vector_id" src/turing_agentmemory_mcp/store_rebuild.py` is 0
    - the community replace runs in one managed transaction (Test 1 asserts single commit)
    - store_rebuild.py under 600 LOC
  </acceptance_criteria>
  <done>Community-graph replace is one ArcadeDB sqlscript+LET transaction with inline vectors keyed on stable_id, no vector_id, tenant-scoped, no accumulation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rebuild/admin op → ArcadeDB indexes | index lifecycle + projection rewrites for a tenant's vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08-01 | Tampering (data integrity) | half-built index served during rebuild | high | mitigate | Populate-before-swap; searches resolve the active version only (Test 2) |
| T-04-08-02 | Tampering (data integrity) | stale/duplicate vectors from in-place rebuild | high | mitigate | Versioned atomic swap + drop-old; no-accumulation test (Test 3) |
| T-04-08-03 | Information Disclosure / Elevation | tenant scope in rebuild + community replace | high | mitigate | user_identifier-scoped rebuild + versioned tenant-namespaced index names (Test 5 / T2.3) |
| T-04-08-04 | Tampering | injection via community/projection data | medium | mitigate | Bound params for all data values in the sqlscript+LET builders |
</threat_model>

<verification>
- `python -m pytest tests/test_store_arcadedb_rebuild.py -q` green
- `python -m ruff check src tests` clean
- `grep -rcE "vector_id|LOAD VECTOR" src/turing_agentmemory_mcp/store_rebuild.py` is 0
- store_rebuild.py under 600 LOC
</verification>

<success_criteria>
Vector-projection rebuild uses versioned atomic swap with no stale accumulation, and community-graph replace is a single ArcadeDB transaction with inline vectors keyed on stable_id — both tenant-scoped, no vector_id, no CSV load, under the LOC cap.
</success_criteria>

<output>
Create `.planning/phases/04-arcadedb-direct-port/04-08-SUMMARY.md` when done.
</output>
