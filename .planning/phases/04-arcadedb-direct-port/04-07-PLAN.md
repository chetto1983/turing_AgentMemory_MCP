---
phase: 04-arcadedb-direct-port
plan: 07
type: tdd
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/turing_agentmemory_mcp/store_search.py
  - src/turing_agentmemory_mcp/store_evidence.py
  - src/turing_agentmemory_mcp/store_retrieval_queries.py
  - tests/test_store_arcadedb_retrieval.py
autonomous: true
requirements: [ARC-04, ARC-05, ARC-06]
must_haves:
  truths:
    - "fused memory search builds its per-channel seed candidates (episode/fact/entity dense via native HNSW; lexical/bm25 via native Lucene) and feeds them to the UNCHANGED Python weighted RRF"
    - "the lexical channel reads native ArcadeDB full-text — the SQLite FTS5 outbox is not consulted (ARC-06 retired)"
    - "the 2-hop entity to fact to memory traversal (and community reads) runs on the D-05 chosen graph surface with bound params and no vector_id join"
    - "multi-id lookups use a bound IN :ids array param, not a string-built OR-list; over-fetch-then-filter default kept (D-03); every channel is user_identifier-scoped"
  artifacts:
    - "src/turing_agentmemory_mcp/store_search.py — ported fused search seed-channel queries"
    - "src/turing_agentmemory_mcp/store_evidence.py — ported entity/fact/community traversal + dense evidence"
    - "src/turing_agentmemory_mcp/store_retrieval_queries.py — extracted channel/traversal query builders"
    - "tests/test_store_arcadedb_retrieval.py — fusion + traversal + tenant-scope + FTS5-retired tests"
  key_links:
    - "retrieval_fusion.py weighted RRF and rerank.py guard/blend are UNCHANGED — only the seed-candidate fetch is re-expressed in ArcadeDB"
    - "the lexical channel analyzer matches 04-01's FTS5-approximating choice to keep ranking parity-comparable"
  prohibitions:
    - "no change to retrieval_fusion.py RRF weights or the rerank guard/blend math"
    - "no vector_id join (m/f/e/c.vector_id = ids) and no VECTOR SEARCH IN ... syntax"
    - "no sparse_index/FTS5 read in the lexical channel"
    - "no string-built OR-list with quote()-escaped ids — bound IN :ids array param"
---

> ## ⚠ EXECUTION UPDATE — read `04-EXECUTION-STATE.md` first (post-spike reconciliation)
> - **Lexical channel = BOTH (user decision):** the plan's "native Lucene" wording is incomplete. Query BOTH `vector.sparseNeighbors` (encode the query with the shared sparse-encoder from 04-05) AND `SEARCH_INDEX(...) ORDER BY $score`, feeding both into the existing Python RRF (keep `retrieval_fusion.py` logic; decide + document how the two lexical channels map to weights). **Escape Lucene special chars** (`?`,`*`,`(`,`)`…) before `SEARCH_INDEX` — unescaped input raises `IndexException` (spike Unknown 4).
> - Dense seed channels on `vectorNeighbors` with **over-fetch-then-filter** (D-03); 2-hop `entity→fact→memory` on **SQL MATCH** (D-05); delete the `vector_id` int-join; replace string-built OR-lists with bound `IN :ids` array params. Rerank guard/blend + RRF stay as-is.
> - Migrate `test_fused_memory_search` (3) + memory-search/context filter tests + search spans. NO skip-as-green.

<objective>
Port fused memory search and entity/fact/community evidence traversal to ArcadeDB (ARC-04): re-express each seed channel (episode/fact/entity dense on native HNSW; lexical/bm25 on native Lucene, retiring the FTS5 outbox read — ARC-06) and the 2-hop entity to fact to memory traversal on the D-05-chosen graph surface, deleting the vector_id int-join (ARC-05) and replacing string-built OR-lists with bound IN :ids array params. The Python weighted RRF (retrieval_fusion.py) and the rerank guard/blend stay exactly as-is — only the upstream candidate fetch changes.

Purpose: This is the retrieval core the Phase-6 parity gate measures; keeping fusion/rerank untouched preserves comparability while the candidate source moves to ArcadeDB.
Output: Ported, tested fused search + evidence traversal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-arcadedb-direct-port/04-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-PATTERNS.md
@.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
</context>

<artifacts_this_phase_produces>
New module `src/turing_agentmemory_mcp/store_retrieval_queries.py` (excluded from drift): extracted ArcadeDB seed-channel + traversal query builders. Ports existing symbols: `_search_memory_fused` seed channels, `_episode_dense_evidence`/`_fact_dense_evidence`/`_entity_dense_evidence`/`_community_dense_evidence`, `_expand_entity_evidence`, `_fact_sources_by_ids`/`_community_sources_by_ids`/`_memory_rows_for_ids`/`_existing_entity_ids`. retrieval_fusion.py and rerank.py are consumed unchanged.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Port fused-search seed channels (native HNSW dense + native Lucene lexical), RRF unchanged, FTS5 retired</name>
  <files>src/turing_agentmemory_mcp/store_search.py, src/turing_agentmemory_mcp/store_retrieval_queries.py, tests/test_store_arcadedb_retrieval.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_search.py (lines 60-120 the seed-channel fetch incl. VECTOR SEARCH IN memory_index + m.vector_id join + the bm25 channel; 187-260 _search_memory_fused RRF glue)
    - src/turing_agentmemory_mcp/retrieval_fusion.py (weighted RRF — READ to confirm it is untouched)
    - src/turing_agentmemory_mcp/rerank.py (apply_rerank_guard/blend_rerank_orders — untouched)
    - src/turing_agentmemory_mcp/sparse_index.py (the FTS5 lexical channel being replaced — read for the channel-into-RRF shape only)
    - .planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md (resolved vector function; D-04 chosen lexical channel + analyzer; full-text score exposure)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (store_search section — leave RRF alone, only seed fetch changes; D-03 over-fetch)
  </read_first>
  <behavior>
    - Test 1: _search_memory_fused composes the same channel set (episode_dense/fact_dense/entity_dense/bm25/graph/community) with candidates fetched from ArcadeDB, and passes them to the existing RRF (assert RRF is called with per-channel candidate lists)
    - Test 2: the dense channels order by native HNSW score with no m.vector_id join
    - Test 3: the lexical/bm25 channel reads native ArcadeDB full-text (Lucene) — a fake sparse_index is NOT consulted
    - Test 4: the adaptive over-fetch multiplier is applied for the status/expires_at filter (D-03)
    - Test 5: a tenant-A search returns no tenant-B candidates on any channel
  </behavior>
  <action>
    Rewrite the seed-candidate fetch for each channel into store_retrieval_queries.py: episode/fact/entity dense use the resolved native HNSW function (record+score, no m.vector_id = ids join); the lexical/bm25 channel uses the D-04-chosen native full-text query (Lucene analyzer from 04-01), replacing the SQLite FTS5 read entirely. Keep the max(limit*4, limit) adaptive over-fetch-then-filter (D-03) and every user_identifier binding. Pass the resulting per-channel candidate lists to retrieval_fusion.py's weighted RRF and the rerank.py guard UNCHANGED — do not touch fusion weights or rerank math. Keep the fusion_enabled gate semantics.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/store_search.py src/turing_agentmemory_mcp/store_retrieval_queries.py tests/test_store_arcadedb_retrieval.py && python -m pytest tests/test_store_arcadedb_retrieval.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/store_search.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_store_arcadedb_retrieval.py` exits 0
    - `grep -cE "vector_id|VECTOR SEARCH IN" src/turing_agentmemory_mcp/store_search.py` is 0
    - `grep -c "sparse_index" src/turing_agentmemory_mcp/store_search.py` is 0 (FTS5 lexical read retired)
    - `git diff --stat src/turing_agentmemory_mcp/retrieval_fusion.py` shows no change (fusion untouched)
    - store_search.py under 600 LOC
  </acceptance_criteria>
  <done>Fused search fetches every seed channel from ArcadeDB (HNSW dense + Lucene lexical) into the unchanged RRF, no vector_id, no FTS5, tenant-scoped.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Port entity/fact/community evidence traversal (D-05 surface, bound IN arrays, no vector_id)</name>
  <files>src/turing_agentmemory_mcp/store_evidence.py, src/turing_agentmemory_mcp/store_retrieval_queries.py, tests/test_store_arcadedb_retrieval.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_evidence.py (lines 100-170 the four dense-evidence VECTOR SEARCH + vector_id joins; 289-360 _expand_entity_evidence 2-hop traversal + string-built OR-lists; the sources_by_ids/existing_entity_ids OR-list builders)
    - src/turing_agentmemory_mcp/store_retrieval_queries.py (builders from Task 1)
    - .planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md (D-05 chosen graph surface — SQL MATCH vs Cypher; whether a JSON array binds to IN :ids)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (store_evidence section — Pattern 4 bound IN :ids array param)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Pattern 4; Assumptions A7 array-param fallback)
  </read_first>
  <behavior>
    - Test 1: _expand_entity_evidence runs the 2-hop entity to fact to memory traversal on the D-05 surface and returns the same evidence shape as before (memory/fact/entity ids + confidence)
    - Test 2: multi-id lookups (fact_sources_by_ids etc.) bind a JSON array to IN :ids (or the spike-proven fallback) — a value with a single quote does not corrupt the query
    - Test 3: the four dense-evidence channels order by native HNSW score with no vector_id join
    - Test 4: every traversal/lookup is user_identifier-bound; tenant-A never sees tenant-B evidence
  </behavior>
  <action>
    Rewrite _expand_entity_evidence and the dense-evidence helpers to the D-05-chosen graph surface (SQL MATCH/TRAVERSE or openCypher per 04-SPIKE-FINDINGS.md) with user_identifier bound, and replace every string-built OR-list (fact_sources_by_ids, community_sources_by_ids, memory_rows_for_ids, existing_entity_ids) with a bound IN :ids JSON-array param (or the spike-proven per-value bound fallback if arrays are unsupported — A7). Delete the m/f/e/c.vector_id = ids joins; dense evidence uses the native HNSW record+score directly. Keep over-fetch (D-03). Put builders in store_retrieval_queries.py.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/store_evidence.py src/turing_agentmemory_mcp/store_retrieval_queries.py && python -m pytest tests/test_store_arcadedb_retrieval.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/store_evidence.py</automated>
  </verify>
  <acceptance_criteria>
    - Test 1-4 pass
    - `grep -cE "vector_id|VECTOR SEARCH IN" src/turing_agentmemory_mcp/store_evidence.py` is 0
    - the multi-id lookups bind an array param — verify `grep -cE "OR .*= \"" src/turing_agentmemory_mcp/store_evidence.py` is 0 (no string-built OR-list remains)
    - store_evidence.py under 600 LOC
  </acceptance_criteria>
  <done>Evidence traversal + dense channels run on the D-05 graph surface with bound array params, no vector_id, tenant-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP search tool → fused search + evidence | untrusted query text + entity names + user_identifier reach the retrieval channels |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure / Elevation | tenant scope across all seed channels + traversals | high | mitigate | user_identifier bound on every channel/traversal; cross-tenant tests (T1.5, T2.4) |
| T-04-07-02 | Tampering | injection via adversarial entity names / OR-list building | high | mitigate | Bound IN :ids array params replace string-built OR-lists (Pattern 4); single-quote test (T2.2) |
| T-04-07-03 | Tampering (data integrity) | dense candidate/record drift after removing vector_id | high | mitigate | Native HNSW returns record+score keyed on stable_id; no int-join (T1.2, T2.3) |
| T-04-07-04 | Information Disclosure | Lucene analyzer ranking drift vs FTS5 baseline | low | accept | Analyzer matched in 04-01; parity captured in 04-09 for the Phase 6 gate |
</threat_model>

<verification>
- `python -m pytest tests/test_store_arcadedb_retrieval.py -q` green
- `python -m ruff check src tests` clean
- `git diff --stat src/turing_agentmemory_mcp/retrieval_fusion.py` shows no change
- `grep -rcE "vector_id|VECTOR SEARCH IN" src/turing_agentmemory_mcp/store_search.py src/turing_agentmemory_mcp/store_evidence.py` is 0
- store_search.py and store_evidence.py under 600 LOC
</verification>

<success_criteria>
Fused memory search and evidence traversal run on ArcadeDB (native HNSW dense + native Lucene lexical + D-05 graph surface) feeding the unchanged Python RRF/rerank, with bound array params, no vector_id, no FTS5, tenant-scoped and injection-safe — under the LOC cap.
</success_criteria>

<output>
Create `.planning/phases/04-arcadedb-direct-port/04-07-SUMMARY.md` when done.
</output>
