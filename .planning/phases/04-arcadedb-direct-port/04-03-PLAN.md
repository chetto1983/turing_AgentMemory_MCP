---
phase: 04-arcadedb-direct-port
plan: 03
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/turing_agentmemory_mcp/arcadedb_schema.py
  - tests/test_arcadedb_schema.py
autonomous: true
requirements: [ARC-08, ARC-05, ARC-06, INFRA-03]
must_haves:
  truths:
    - "an idempotent bootstrap creates the vertex/edge types, a full-precision COSINE LSM_VECTOR index (dims from EMBED_DIMENSIONS), a native Lucene full-text index, and a UNIQUE stable_id index — running it twice is a no-op"
    - "vector-index names are versioned/namespaced (tenant blake2b digest + version suffix), the foundation D-07 atomic swap builds on"
    - "a dimension mismatch against an existing vector index raises ValueError (mirrors the current fail-fast), not a silent truncate/pad"
    - "stable_id() is the UNIQUE indexed property; ArcadeDB's native RID is never the schema identity"
  artifacts:
    - "src/turing_agentmemory_mcp/arcadedb_schema.py — bootstrap(client, ...), SchemaBootstrapConfig, versioned index-name helpers"
    - "tests/test_arcadedb_schema.py — idempotency + dimension-mismatch + versioned-naming + UNIQUE-stable_id tests"
  key_links:
    - "the vector DDL form + introspection query come from 04-SPIKE-FINDINGS.md (D-01 full-precision COSINE), not a doc guess"
    - "the versioned/namespaced naming helper is the exact seam store_rebuild (04-08) swaps atomically"
  prohibitions:
    - "no INT8/BINARY quantization — full-precision only (D-01)"
    - "ArcadeDB native RID (#12:34) is never used as the schema identity or UNIQUE key"
    - "the dimension-mismatch path raises ValueError (not RuntimeError, not a swallow)"
---

<objective>
Create `arcadedb_schema.py` — the idempotent startup schema-init routine (D-09): CREATE vertex/edge types + full-precision COSINE `LSM_VECTOR` index (dims from `EMBED_DIMENSIONS`) + native Lucene full-text index + UNIQUE `stable_id` index, all under versioned/namespaced names (D-07 foundation, INFRA-03). Raises `ValueError` on dimension mismatch, mirroring today's fail-fast.

Purpose: This is the schema contract every ported store path writes into and the versioned-index foundation the rebuild atomic-swap (04-08) depends on. It anchors ARC-08 (stable_id as the UNIQUE indexed identity), ARC-05 (vector index DDL), and ARC-06 (full-text index DDL).
Output: A tested, idempotent bootstrap module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-arcadedb-direct-port/04-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-PATTERNS.md
@.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
</context>

<artifacts_this_phase_produces>
Net-new module `src/turing_agentmemory_mcp/arcadedb_schema.py` (excluded from drift verification): `bootstrap(client, *, dimensions, indexes, version)`, `SchemaBootstrapConfig`, `versioned_vector_index(base_name, user_identifier, version)` (extends the current `_tenant_vector_index` blake2b scheme with a `_v{version}` suffix), `introspect_vector_dimension(client, name)`. Vertex types: User, Memory, Document, Chunk, Entity, Fact, Community. Edge types: HAS_MEMORY, HAS_DOCUMENT, HAS_CHUNK, NEXT_CHUNK, SUBJECT_OF, OBJECT_OF, SUPPORTED_BY, MENTIONS, IN_COMMUNITY.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Idempotent type + vector + full-text + UNIQUE stable_id bootstrap with versioned naming</name>
  <files>src/turing_agentmemory_mcp/arcadedb_schema.py, tests/test_arcadedb_schema.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store_core.py (lines 194-206 bootstrap; 241-282 _ensure_vector_index + _tenant_vector_index — the idempotent-create-then-verify + blake2b tenant-namespacing pattern to port, not copy verbatim)
    - src/turing_agentmemory_mcp/ids.py (stable_id, cypher_var — both stay canonical; the schema stores stable_id as the UNIQUE property)
    - src/turing_agentmemory_mcp/arcadedb_client.py (query/command surface the bootstrap issues DDL through)
    - .planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md (resolved: LSM_VECTOR DDL form + dims + COSINE; full-text/Lucene index DDL + chosen analyzer; schema introspection query for dimension verification)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (arcadedb_schema.py section — dimension-mismatch fail-fast; D-07 version-suffix extension)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Pattern 2 vector DDL versioned naming; D-01 full-precision; Assumptions A4)
  </read_first>
  <behavior>
    - Test 1: `bootstrap()` against a fake client issues CREATE for all 7 vertex types + 9 edge types + one LSM_VECTOR index per record type + a full-text index + a UNIQUE stable_id index; running it a second time issues no failing/duplicate DDL (idempotent — assert benign on re-run)
    - Test 2: the LSM_VECTOR DDL carries `dimensions` from the config (sourced from EMBED_DIMENSIONS) and COSINE metric, full-precision (no quantization keyword in the emitted DDL)
    - Test 3: when introspection reports an existing vector index with a different dimension, `bootstrap()` raises ValueError with an "expected N, found M" message
    - Test 4: `versioned_vector_index(base, user, version)` returns a name containing the blake2b tenant digest AND a `_v{version}` suffix; two different versions produce different names, same inputs are deterministic
    - Test 5: the UNIQUE index is on the stable_id property; the schema never references a native RID
  </behavior>
  <action>
    Create `arcadedb_schema.py` with `SchemaBootstrapConfig` (`@dataclass(frozen=True)`, validating positive dimensions in `__post_init__` per the embeddings.py convention) and `bootstrap(client, *, dimensions, version)` that idempotently CREATEs the 7 vertex + 9 edge types, one full-precision COSINE `LSM_VECTOR` index per record type (dims from config), a native Lucene full-text index (analyzer chosen in the spike to approximate FTS5 `unicode61`), and a UNIQUE `stable_id`-property index — using the resolved DDL forms from 04-SPIKE-FINDINGS.md and `IF NOT EXISTS`/benign-catch for idempotency. Add `versioned_vector_index(base_name, user_identifier, version)` extending the current `_tenant_vector_index` blake2b-digest scheme with a `_v{version}` suffix, and `introspect_vector_dimension(client, name)`. On a dimension mismatch vs an existing index, raise `ValueError` (CONTEXT.md D-09 is explicit: ValueError, not RuntimeError). Emit no quantization keyword (D-01).
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/arcadedb_schema.py tests/test_arcadedb_schema.py && python -m pytest tests/test_arcadedb_schema.py -q && bash scripts/check-file-size.sh src/turing_agentmemory_mcp/arcadedb_schema.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_arcadedb_schema.py` exits 0
    - the dimension-mismatch test asserts a `ValueError` (source: `grep -c "raise ValueError" src/turing_agentmemory_mcp/arcadedb_schema.py` >= 1)
    - `versioned_vector_index` output contains both the tenant digest and a `_v` version suffix (asserted in Test 4)
    - no quantization keyword in the module — verify: `grep -ciE "int8|binary_quant|quantiz" src/turing_agentmemory_mcp/arcadedb_schema.py` is 0
    - `grep -c "getIdentity\|@rid" src/turing_agentmemory_mcp/arcadedb_schema.py` is 0
    - arcadedb_schema.py is under 600 LOC (check-file-size.sh exits 0)
  </acceptance_criteria>
  <done>Bootstrap is idempotent, creates versioned full-precision COSINE vector + Lucene full-text + UNIQUE stable_id schema, and fails fast with ValueError on dimension drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store init → ArcadeDB schema | DDL that defines the ONLY tenant-isolation-supporting identity (stable_id) and vector-index dimensions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-01 | Tampering (data integrity) | stable_id vs RID identity drift | high | mitigate | UNIQUE index on stable_id property; RID never referenced (Test 5 + grep gate); ARC-08 |
| T-04-03-02 | Tampering | dimension mismatch silently padding/truncating vectors | high | mitigate | Introspect-and-raise-ValueError on mismatch (Test 3); no silent create over a differing index |
| T-04-03-03 | Information Disclosure | versioned index naming collision across tenants | medium | accept | blake2b tenant digest namespacing makes cross-tenant index-name collision cryptographically unlikely; full DB-per-tenant isolation is Phase 5 |
</threat_model>

<verification>
- `python -m pytest tests/test_arcadedb_schema.py -q` green
- `python -m ruff check src tests` clean
- `bash scripts/check-file-size.sh src/turing_agentmemory_mcp/arcadedb_schema.py` passes (<600 LOC)
</verification>

<success_criteria>
An idempotent, dimension-validating, versioned-naming schema bootstrap exists with stable_id as the UNIQUE identity — the schema contract for every ported write and the atomic-swap foundation for D-07.
</success_criteria>

<output>
Create `.planning/phases/04-arcadedb-direct-port/04-03-SUMMARY.md` when done.
</output>
