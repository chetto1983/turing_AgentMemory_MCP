---
phase: 04-arcadedb-direct-port
plan: 09
type: execute
wave: 5
depends_on: ["04-05", "04-06", "04-07", "04-08"]
files_modified:
  - src/turing_agentmemory_mcp/ids.py
  - src/turing_agentmemory_mcp/store_utils.py
  - tests/test_vector_id_absent.py
  - tests/test_stable_id_survives_rebuild.py
  - tests/test_arcadedb_tenant_isolation.py
  - tests/test_arcadedb_chaos_restart.py
  - scripts/e2e_score.py
autonomous: true
requirements: [ARC-05, ARC-08]
must_haves:
  truths:
    - "ids.vector_id() and the five store_utils _*_vector_id() staticmethods are deleted; no vector_id token remains in any store read/write path"
    - "stable_id() is unchanged and a computed stable_id for a fixed input is the same DB id before and after a rebuild_vector_projection (no ID drift — ARC-08)"
    - "a concurrent multi-tenant isolation test passes against the rewritten ArcadeDB queries — tenant A never sees tenant B under concurrency"
    - "a chaos-restart test proves the store reconnects after an ArcadeDB restart, /health recovers, and a search returns correct (not empty/stale) results (D-10)"
    - "the ArcadeDB-backed e2e_score / real_document_benchmark output is captured in a shape directly diffable against baseline/03-turingdb (parity-comparable for the Phase 6 gate)"
  artifacts:
    - "src/turing_agentmemory_mcp/ids.py — vector_id() removed (stable_id/cypher_var kept)"
    - "src/turing_agentmemory_mcp/store_utils.py — five _*_vector_id() staticmethods + vector_id import removed"
    - "tests/test_vector_id_absent.py, tests/test_stable_id_survives_rebuild.py, tests/test_arcadedb_tenant_isolation.py, tests/test_arcadedb_chaos_restart.py"
    - "an ArcadeDB-backed e2e/benchmark result captured in the baseline/03-turingdb artifact shape"
  key_links:
    - "quote() is deleted only after every call site has migrated to bound params (04-04..08); the guard test proves no interpolated data literal remains"
    - "the parity capture reuses the Phase-3 frozen-questions + scoring so Phase 6 can diff directly"
  prohibitions:
    - "stable_id() and cypher_var() are NOT changed"
    - "no vector_id / _*_vector_id token remains anywhere under src/turing_agentmemory_mcp/ (excluding historical .planning docs)"
    - "the turingdb service/dependency are NOT removed (Phase 7/ARC-10)"
    - "no ArcadeDB native RID used as an identifier anywhere"
---

<objective>
Close the port: delete the now-dead `vector_id` machinery (ARC-05 "delete, don't port"), keep `stable_id()` canonical and prove it survives a rebuild (ARC-08), add the concurrent tenant-isolation and D-10 chaos-restart regression guards RESEARCH flags as Wave-0 gaps, and capture an ArcadeDB-backed e2e/benchmark result in the Phase-3 baseline shape so the Phase-6 parity gate can diff directly.

Purpose: This is the Definition-of-Done surface — validated end-to-end on a real scenario (chaos-restart + parity capture), not just green unit tests — and the regression net that locks in the port's invariants.
Output: A clean tree with no vector_id, ID-drift/tenant/chaos guards green, and a parity-comparable capture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-arcadedb-direct-port/04-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-PATTERNS.md
@.planning/phases/04-arcadedb-direct-port/04-SPIKE-FINDINGS.md
</context>

<artifacts_this_phase_produces>
Deletes `ids.vector_id` and `store_utils._memory_vector_id/_entity_vector_id/_fact_vector_id/_community_vector_id/_document_vector_id` (+ the `vector_id` import). New test files: `tests/test_vector_id_absent.py`, `tests/test_stable_id_survives_rebuild.py`, `tests/test_arcadedb_tenant_isolation.py`, `tests/test_arcadedb_chaos_restart.py`. A committed ArcadeDB-backed e2e/benchmark result in the baseline/03-turingdb artifact shape (path recorded in the SUMMARY, e.g. under .benchmarks/ or a phase-4 comparison dir).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete vector_id/quote dead code + no-vector_id and no-RID regression guard</name>
  <files>src/turing_agentmemory_mcp/ids.py, src/turing_agentmemory_mcp/store_utils.py, tests/test_vector_id_absent.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/ids.py (lines 15-18 vector_id; 29-36 quote — retire; keep stable_id 9-12 and cypher_var 21-26)
    - src/turing_agentmemory_mcp/store_utils.py (line 15 the vector_id import; lines 198-216 the five _*_vector_id staticmethods)
    - the ported store_*.py files from 04-04..08 (confirm every vector_id/_*_vector_id/quote call site has already migrated)
    - .planning/phases/04-arcadedb-direct-port/04-PATTERNS.md (store_utils/ids deletions section — delete only after all callers migrated)
  </read_first>
  <behavior>
    - Test 1: importing the store package succeeds after the deletions (no dangling reference)
    - Test 2: a repo-wide guard asserts no `vector_id` token and no `_*_vector_id` symbol remains in any src/turing_agentmemory_mcp store read/write path
    - Test 3: a guard asserts ArcadeDB native RID form is never used as an identifier in the store paths
  </behavior>
  <action>
    Delete `vector_id()` from ids.py and the five `_memory_vector_id`/`_entity_vector_id`/`_fact_vector_id`/`_community_vector_id`/`_document_vector_id` staticmethods (plus the `vector_id` import) from store_utils.py. Delete `quote()` from ids.py ONLY if every call site has migrated to bound params (04-04..08); if any remains, fix that call site here on touch (NEVER-SUPPOSE: grep first). Keep `stable_id()` and `cypher_var()` unchanged. Write `tests/test_vector_id_absent.py` as a repo-scoped regression guard: scan the store modules and assert zero occurrences of the `vector_id` token, the `_*_vector_id` symbols, and any ArcadeDB RID literal used as an identifier. Use `grep -v '^#'` style filtering / concept-based checks so the guard's own reference strings do not self-invalidate.
  </action>
  <verify>
    <automated>python -m ruff check src/turing_agentmemory_mcp/ids.py src/turing_agentmemory_mcp/store_utils.py tests/test_vector_id_absent.py && python -m pytest tests/test_vector_id_absent.py -q && python -c "import turing_agentmemory_mcp.store"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import turing_agentmemory_mcp.store"` exits 0
    - `grep -rlE "\bvector_id\b|_memory_vector_id|_entity_vector_id|_fact_vector_id|_community_vector_id|_document_vector_id" src/turing_agentmemory_mcp/*.py` returns nothing
    - `grep -c "def vector_id" src/turing_agentmemory_mcp/ids.py` is 0; `grep -c "def stable_id" src/turing_agentmemory_mcp/ids.py` is 1
    - `tests/test_vector_id_absent.py` exits 0
  </acceptance_criteria>
  <done>The vector_id int-join machinery is fully deleted, stable_id/cypher_var preserved, and a guard prevents its return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ARC-08 ID-drift-across-rebuild + concurrent tenant-isolation regression guards</name>
  <files>tests/test_stable_id_survives_rebuild.py, tests/test_arcadedb_tenant_isolation.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/ids.py (stable_id — the fixed-input determinism)
    - src/turing_agentmemory_mcp/store_rebuild.py (rebuild_vector_projection — 04-08; the id must be identical pre/post)
    - tests/test_fused_memory_search.py, tests/test_hybrid_search.py (existing store-test construction pattern to mirror)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Validation Architecture item 3 tenant-isolation-through-the-port; Wave 0 gaps; Security Domain V4)
  </read_first>
  <behavior>
    - Test 1: compute stable_id for a fixed input, ingest, run rebuild_vector_projection, and assert the record's DB id is byte-identical before and after (no vector-ID drift — ARC-08 / Pitfall 6)
    - Test 2: interleave writes+reads for tenant A and tenant B concurrently against the shared ArcadeDB DB via the rewritten queries; assert no tenant-A read ever returns a tenant-B record and vice versa
    - Test 3: an empty user_identifier fails closed on the concurrent path too
  </behavior>
  <action>
    Write `tests/test_stable_id_survives_rebuild.py` asserting `stable_id()` for a fixed input equals the ArcadeDB-stored/queried `id` property both before and after a `rebuild_vector_projection` call. Write `tests/test_arcadedb_tenant_isolation.py` exercising concurrent multi-tenant writes/reads specifically through the NEW ArcadeDB query forms (bound params, sqlscript batches), asserting zero cross-tenant leakage and fail-closed on empty identifier — the copy-paste-missed-scope failure mode Pitfall 5 warns about, which has no DB-level defense-in-depth this phase.
  </action>
  <verify>
    <automated>python -m ruff check tests/test_stable_id_survives_rebuild.py tests/test_arcadedb_tenant_isolation.py && python -m pytest tests/test_stable_id_survives_rebuild.py tests/test_arcadedb_tenant_isolation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - both test files exit 0
    - Test 1 asserts byte-identical id pre/post rebuild
    - Test 2 asserts no cross-tenant leakage under concurrency (both directions)
  </acceptance_criteria>
  <done>Regression guards lock in stable_id continuity across rebuild and tenant isolation through the rewritten ArcadeDB queries.</done>
</task>

<task type="auto">
  <name>Task 3: D-10 chaos-restart integration test + parity-comparable ArcadeDB capture</name>
  <files>tests/test_arcadedb_chaos_restart.py, scripts/e2e_score.py</files>
  <read_first>
    - baseline/03-turingdb/ (the artifact shape: BASELINE.md, frozen-questions.json, corpus-manifest.json, e2e-results.json, real-document-benchmark.json — the diff target for Phase 6)
    - scripts/e2e_score.py (the deterministic gate harness to point at ArcadeDB; it currently spins a temp backend)
    - src/turing_agentmemory_mcp/arcadedb_client.py (is_ready/probe — 04-02) and store_core.py readiness (04-04)
    - .planning/phases/04-arcadedb-direct-port/04-RESEARCH.md (Validation Architecture items 2 chaos-restart + 4 parity-comparability; D-10)
    - .planning/phases/04-arcadedb-direct-port/04-CONTEXT.md (D-10 chaos-restart required in Phase 4; Definition of Done)
  </read_first>
  <action>
    Write `tests/test_arcadedb_chaos_restart.py` (the D-10 Pitfall-1 analog): bring up the compose stack, confirm `/health` reports the ArcadeDB stage ready, force-restart the arcadedb container (`docker compose restart arcadedb` / `docker kill`), then assert (a) `/health` goes degraded/503 while down, (b) the store reconnects without a manual runbook step once ArcadeDB is back, (c) `/health` returns healthy, (d) a `search_memory`/`search_documents` call immediately after recovery returns correct (not empty/stale) results. Skip cleanly with a visible reason (never silent green) when Docker is unavailable. Point `scripts/e2e_score.py` (or a thin wrapper) at the ArcadeDB-backed stack and capture its output PLUS a `real_document_benchmark` run in the exact `baseline/03-turingdb/` artifact shape (reuse the Phase-3 frozen-questions + scoring), writing the result to a phase-4 comparison location recorded in the SUMMARY — this produces the comparable numbers Phase 6 diffs (Phase 4 produces comparability, Phase 6 owns the pass/fail threshold). Do NOT change fusion weights, the corpus, or the frozen questions.
  </action>
  <verify>
    <automated>python -m ruff check tests/test_arcadedb_chaos_restart.py scripts/e2e_score.py && python -m pytest tests/test_arcadedb_chaos_restart.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_arcadedb_chaos_restart.py` runs (green against Docker, or a visibly-reasoned skip without Docker — not silent green)
    - the chaos test asserts all four transitions (degraded-while-down, reconnect, healthy, correct search after recovery)
    - an ArcadeDB-backed e2e/benchmark artifact exists in the baseline/03-turingdb field shape (same keys as e2e-results.json / real-document-benchmark.json)
    - `git diff --stat src/turing_agentmemory_mcp/retrieval_fusion.py baseline/03-turingdb/frozen-questions.json` shows no change (parity inputs untouched)
  </acceptance_criteria>
  <done>The store survives an ArcadeDB restart end-to-end and a parity-comparable capture exists for the Phase-6 gate — Definition of Done met on a real scenario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent multi-tenant traffic → shared ArcadeDB DB | app-layer user_identifier scoping is the ONLY isolation this phase (no DB-per-tenant yet) |
| ArcadeDB restart → store reconnect | availability transition where stale/empty results could leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure / Elevation | copy-paste-missed user_identifier scope in a rewritten query | high | mitigate | Concurrent multi-tenant isolation guard (Task 2 Test 2) exercises the new query forms directly; no DB-level defense this phase |
| T-04-09-02 | Tampering (data integrity) | vector-ID / stable-ID drift after rebuild enabling stale/cross-record correlation | high | mitigate | ID-survives-rebuild guard (Task 2 Test 1); vector_id fully deleted (Task 1) |
| T-04-09-03 | Denial of Service | stale/empty results served after an ArcadeDB restart | medium | mitigate | Chaos-restart test asserts degraded-while-down + correct-after-recovery (Task 3) |
| T-04-09-04 | Tampering | RID captured as identifier somewhere in the port | high | mitigate | No-RID guard (Task 1 Test 3) across store paths |
</threat_model>

<verification>
- `python -m pytest tests/test_vector_id_absent.py tests/test_stable_id_survives_rebuild.py tests/test_arcadedb_tenant_isolation.py tests/test_arcadedb_chaos_restart.py -q` green (chaos test may visibly skip without Docker)
- `python -m pytest -q` full suite green; `python -m ruff check src tests scripts` clean; `docker compose config --quiet` passes
- `grep -rlE "\bvector_id\b|_.*_vector_id" src/turing_agentmemory_mcp/*.py` returns nothing
- an ArcadeDB-backed e2e/benchmark capture exists in the baseline/03-turingdb artifact shape
</verification>

<success_criteria>
The port is complete and validated on a real scenario: no vector_id anywhere, stable_id continuity and tenant isolation guarded, the store survives an ArcadeDB restart end-to-end, and a parity-comparable capture is ready for the Phase-6 meet-or-exceed gate — with turingdb retained for Phase 6 coexistence.
</success_criteria>

<output>
Create `.planning/phases/04-arcadedb-direct-port/04-09-SUMMARY.md` when done, recording the path of the ArcadeDB-backed parity capture.
</output>
