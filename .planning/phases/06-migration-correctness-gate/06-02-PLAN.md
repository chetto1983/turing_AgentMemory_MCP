---
phase: 06-migration-correctness-gate
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/gate_guard.py
  - tests/test_phase7_gate_guard.py
autonomous: true
requirements: [ARC-09]
must_haves:
  truths:
    - "gate_guard.validate_gate_result_schema(obj) asserts every D-09-mandated field is present (verdict, tolerance, provider_config, corpus_verification, metrics_diff, baseline_bar, e2e_diff, latency, runs) and that verdict is one of {GO, NO_GO}, raising loudly on any missing field or unknown verdict (per D-09)"
    - "gate_guard.assert_gate_go(path) reads the gate-result.json fresh on every call (no caching), validates schema first, and refuses (raises AssertionError) unless verdict == 'GO' (per D-10)"
    - "assert_gate_go fails closed: a missing file, an unparseable/malformed file, or verdict == 'NO_GO' each raise — never a silent pass and never pytest.skip (per D-10, no-skip-as-green CI-07)"
    - "tests/test_phase7_gate_guard.py proves the guard fires using tmp_path fixtures (missing -> raise, malformed -> raise, NO_GO -> raise, GO -> pass) following the tests/test_no_skip_as_green_guard.py template, with no pytest.skip escape hatch anywhere"
  artifacts:
    - src/turing_agentmemory_mcp/gate_guard.py
    - tests/test_phase7_gate_guard.py
  key_links:
    - "baseline/06-gate/gate-result.json verdict -> assert_gate_go -> Phase-7 entry hard-block (cross-phase contract)"
    - "tests/conftest.py no-skip-as-green hookwrapper -> guard must hard-fail, never skip"
  prohibitions:
    - "MUST NOT allow Phase-7 removal without a committed gate-result.json whose verdict == GO"
    - "MUST NOT use pytest.skip() as an escape hatch when gate-result.json is absent or malformed"
    - "MUST NOT cache the gate-result.json contents across calls (re-read fresh to defeat tampering-after-read)"
---

<objective>
Build the fail-closed Phase-7 entry guard (`src/turing_agentmemory_mcp/gate_guard.py`) that
Phase 7 will invoke to hard-block the irreversible TuringDB removal unless the committed
`baseline/06-gate/gate-result.json` says `verdict == "GO"`. The guard validates the D-09
schema shape before trusting the verdict, reads the file fresh every call (Tampering
mitigation), and fails closed on missing/malformed/NO_GO — never skips. Proven by tmp_path
self-tests modeled on the repo's only existing fail-closed guard.

Purpose: This is the mechanism that makes ARC-09 a real gate with teeth (D-10), not a report.
It is the concrete cross-phase contract Phase 7 depends on.
Output: `src/turing_agentmemory_mcp/gate_guard.py` + `tests/test_phase7_gate_guard.py`, green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-migration-correctness-gate/06-CONTEXT.md
@.planning/phases/06-migration-correctness-gate/06-RESEARCH.md
@.planning/phases/06-migration-correctness-gate/06-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

New symbols created by this plan (excluded from source-grounding drift checks downstream):
- `src/turing_agentmemory_mcp/gate_guard.py` — new module. Functions:
  `validate_gate_result_schema(obj)` (raises on missing D-09 field / unknown verdict),
  `load_verdict(path)` (fresh read + schema validate), `assert_gate_go(path)` (fail-closed,
  raises unless verdict == "GO"). No caching, no skip.
- `tests/test_phase7_gate_guard.py` — new test module (tmp_path self-tests).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: D-09 schema validator (fail-loud on malformed gate-result.json)</name>
  <files>src/turing_agentmemory_mcp/gate_guard.py, tests/test_phase7_gate_guard.py</files>
  <read_first>
    - tests/test_no_skip_as_green_guard.py (full 42-line file — the repo's ONLY fail-closed guard template; copy its structure and no-skip discipline)
    - src/turing_agentmemory_mcp/tenant_registry.py (READ the "any pre-existing file without the exact schema is corrupt, never auto-repaired" pattern — the established fail-closed-on-malformed posture to mirror; grep for the schema-corruption raise)
    - .planning/phases/06-migration-correctness-gate/06-RESEARCH.md (Area 5 — Phase-7 guard conventions; the D-09 field list in the CONTEXT.md/RESEARCH summary)
    - baseline/03-turingdb/e2e-results.json (reference for the flat sort_keys JSON convention the artifact follows)
  </read_first>
  <behavior>
    - Test: validate_gate_result_schema() accepts a dict carrying all D-09 fields (verdict, tolerance, provider_config, corpus_verification, metrics_diff, baseline_bar, e2e_diff, latency, runs) with verdict in {GO, NO_GO}
    - Test: validate_gate_result_schema() raises (ValueError/AssertionError) when any single mandated field is absent
    - Test: validate_gate_result_schema() raises when verdict is a string not in {GO, NO_GO} (e.g. "MAYBE", "", "go")
    - Test: validate_gate_result_schema() raises when the top-level value is not a dict
  </behavior>
  <action>
    RED first: add tests/test_phase7_gate_guard.py cases for validate_gate_result_schema using
    synthetic in-memory dicts (well-formed GO, well-formed NO_GO, each with exactly one field
    removed, bad verdict string, non-dict). Then GREEN: implement
    `validate_gate_result_schema(obj)` in src/turing_agentmemory_mcp/gate_guard.py that checks
    `isinstance(obj, dict)`, asserts the required D-09 key set is a subset of obj.keys(), and asserts
    `obj["verdict"] in {"GO", "NO_GO"}`, raising a clear message naming the missing field or bad
    verdict. Mirror tenant_registry.py's "corrupt schema fails closed, never auto-repaired" posture.
    Keep the module tiny (well under the 600-LOC cap; comparable to the 41-42 line existing guards).
    Implements the D-09 field-completeness contract that D-10's guard trusts.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase7_gate_guard.py -q</automated>
  </verify>
  <done>validate_gate_result_schema accepts a complete D-09 dict and raises on any missing field, bad verdict, or non-dict; tests green; ruff clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: assert_gate_go fail-closed guard (D-10) — read fresh, refuse unless GO</name>
  <files>src/turing_agentmemory_mcp/gate_guard.py, tests/test_phase7_gate_guard.py</files>
  <read_first>
    - tests/test_no_skip_as_green_guard.py (again — replicate the no-skip discipline; pytest.raises usage)
    - src/turing_agentmemory_mcp/gate_guard.py (the validator from Task 1 — assert_gate_go calls it before trusting verdict)
    - .planning/phases/06-migration-correctness-gate/06-PATTERNS.md:186-224 (the exact fail-closed guard test shape: missing -> raise, NO_GO -> raise, GO -> pass; read JSON fresh, no caching)
  </read_first>
  <behavior>
    - Test: assert_gate_go(tmp_path/'gate-result.json') where the file is absent raises AssertionError (fail-closed on missing — NOT skip)
    - Test: assert_gate_go on a file containing invalid JSON raises (parse error -> fail-closed)
    - Test: assert_gate_go on a well-formed dict with verdict 'NO_GO' raises AssertionError
    - Test: assert_gate_go on a well-formed dict with verdict 'GO' returns without raising
    - Test: assert_gate_go re-reads the file on each call (writing GO then rewriting NO_GO between two calls makes the second call raise) — proves no caching
    - Test: no pytest.skip appears anywhere in tests/test_phase7_gate_guard.py (grep-style assertion or reviewer note); a missing artifact is a hard failure, matching conftest.py no-skip-as-green
  </behavior>
  <action>
    RED first: add tests for the missing/malformed/NO_GO/GO/no-cache behaviors using tmp_path
    fixtures (write JSON with json.dumps, no external artifact needed). Then GREEN: implement
    `load_verdict(path)` (open the path fresh, json.loads, call validate_gate_result_schema, return
    obj["verdict"]) and `assert_gate_go(path)` (call load_verdict; `assert verdict == "GO"` with a
    message that names the offending verdict or the missing/unparseable file). On FileNotFoundError or
    json.JSONDecodeError, raise AssertionError (do not swallow, do not skip). Never store the parsed
    result on the module — read fresh every call (Tampering mitigation). Implements D-10.
    REFACTOR if needed to keep both functions cohesive and under the LOC cap.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase7_gate_guard.py -q</automated>
  </verify>
  <done>assert_gate_go raises on missing/malformed/NO_GO and passes only on GO; the no-cache test proves a fresh read per call; no pytest.skip anywhere in the guard test; `python -m pytest -q` full suite still green; ruff format --check, ruff check, and check-file-size.sh all clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed gate-result.json -> assert_gate_go | The verdict file is the sole authority gating irreversible Phase-7 TuringDB removal |
| Phase-7 tooling -> gate_guard | Phase 7 invokes the guard; a fail-open bug here would authorize an irreversible destructive action |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Tampering | assert_gate_go (corrupted/tampered gate-result.json authorizing removal) | high | mitigate | Read fresh each call (no cache), validate D-09 schema shape AND verdict=='GO' explicitly, fail closed on any parse error — mirrors tenant_registry.py corrupt-schema-never-auto-repaired |
| T-06-02-02 | Elevation of Privilege | assert_gate_go (missing artifact silently passing) | high | mitigate | Missing file raises AssertionError, never pytest.skip — enforced by tmp_path self-test and conftest.py no-skip-as-green (CI-07) |
| T-06-02-03 | Repudiation | validate_gate_result_schema (partial/NO_GO artifact trusted) | medium | mitigate | Full D-09 field-completeness check before verdict is read; verdict constrained to {GO, NO_GO} |
| T-06-02-SC | Tampering | package installs | low | accept | No new dependencies — stdlib only (json, pathlib); no supply-chain surface added |
</threat_model>

<verification>
- `python -m pytest tests/test_phase7_gate_guard.py -q` green (missing/malformed/NO_GO/GO/no-cache all covered)
- `python -m pytest -q` full suite green (new guard tests do not break existing 825-test suite)
- `python -m ruff format --check src tests scripts` and `python -m ruff check src tests scripts` clean
- `bash scripts/check-file-size.sh` clean (gate_guard.py tiny, well under 600 LOC)
- No `pytest.skip` token anywhere in tests/test_phase7_gate_guard.py
</verification>

<success_criteria>
A fail-closed guard exists that Phase 7 can invoke: it reads the committed verdict fresh,
validates the D-09 schema, and refuses (raises) unless verdict == "GO" — proven green by
tmp_path self-tests that a missing, malformed, or NO_GO artifact hard-fails and only a GO passes.
</success_criteria>

<output>
Create `.planning/phases/06-migration-correctness-gate/06-02-SUMMARY.md` when done
</output>
