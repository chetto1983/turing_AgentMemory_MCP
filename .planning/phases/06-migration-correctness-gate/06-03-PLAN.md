---
phase: 06-migration-correctness-gate
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - baseline/06-gate/e2e-results.json
  - baseline/06-gate/real-document-benchmark-run1.json
  - baseline/06-gate/real-document-benchmark-run2.json
  - baseline/06-gate/real-document-benchmark-run3.json
  - baseline/06-gate/capture-provider-env.txt
autonomous: false
requirements: [ARC-09]
user_setup:
  - service: docker-compose-gpu-stack
    why: "The quality-parity gate REQUIRES the real GPU providers + the exact baseline corpus; CI's GPU-less runners degrade to a stub floor that is NOT a valid quality verdict (D-07/D-10/D-11). This capture must run on a host with an NVIDIA GPU visible to Docker."
    env_vars:
      - name: E2E_USE_EXTERNAL_EMBED
        source: "set to 1 for the e2e_score.py leg so the in-process stub embedder does NOT start"
      - name: E2E_USE_EXTERNAL_RERANK
        source: "set to 1 for the e2e_score.py leg so the in-process stub reranker does NOT start"
    dashboard_config:
      - task: "Bring up the GPU stack: docker compose up -d --wait arcadedb agentmemory-model-init agentmemory-embed agentmemory-rerank turing-agentmemory-mcp"
        location: "compose.yaml (arcadedb, agentmemory-model-init downloads+verifies granite + BGE GGUFs, agentmemory-embed, agentmemory-rerank, turing-agentmemory-mcp published on 127.0.0.1:8095)"
      - task: "Confirm the exact baseline corpus is present at D:/tmp/baseline-corpus (all 12 files)"
        location: "local host filesystem"
must_haves:
  truths:
    - { statement: "The gate run uses the SAME real GPU providers as the baseline: embedder mykor/granite-embedding-311m-multilingual-r2-GGUF:Q4_K_M (768d) and reranker bge-reranker-v2-m3-Q8_0.gguf, with E2E_USE_EXTERNAL_EMBED=1 E2E_USE_EXTERNAL_RERANK=1, not a stub (per D-07)", verification: backstop }
    - "Before any capture, gate_diff.py verify_corpus fails closed unless every file under D:/tmp/baseline-corpus sha256-matches baseline/03-turingdb/corpus-manifest.json (per D-11)"
    - { statement: "The captured baseline/06-gate/e2e-results.json records check #1 detail with sidecar provider hostnames (agentmemory-embed:8080 / agentmemory-rerank:8080), NOT 127.0.0.1 stub URLs, so is_stub_provider is False (per D-07)", verification: backstop }
    - { statement: "e2e check #13 (document_search_retrieves_exact_top1_with_citation_and_neighbor_context) passes under real providers on ArcadeDB — direct evidence the document_id-length bug is fixed on the port (per D-03)", verification: backstop }
    - { statement: "The N=3 real-document-benchmark runs replay the exact 60 frozen questions via --frozen-questions baseline/03-turingdb/frozen-questions.json (no regeneration), and the 5 normattiva docs retrieve with non-zero MRR/recall (per D-03/D-08)", verification: backstop }
    - { statement: "Per-query retrieval latency is captured on the ArcadeDB run for the D-06 no-regression comparison against the clean-DB baseline (5.4s mean), with the GLiNER-scope choice recorded (per D-06)", verification: backstop }
  artifacts:
    - baseline/06-gate/e2e-results.json
    - baseline/06-gate/real-document-benchmark-run1.json
    - baseline/06-gate/real-document-benchmark-run2.json
    - baseline/06-gate/real-document-benchmark-run3.json
    - baseline/06-gate/capture-provider-env.txt
  key_links:
    - "frozen-questions.json -> real_document_benchmark.py --frozen-questions (exact 60-question replay)"
    - "corpus-manifest.json -> gate_diff.py verify_corpus pre-flight (fail-closed)"
    - "compose.yaml agentmemory-embed/agentmemory-rerank sidecars -> real provider parity"
  prohibitions:
    - "MUST NOT capture with stub embed/rerank providers or a corpus that does not sha256-match the manifest"
    - "MUST NOT regenerate the frozen questions — replay the exact committed 60"
    - "MUST NOT substitute a different corpus if D:/tmp/baseline-corpus is unavailable — reproduce the exact corpus (D-11 fallback is a full re-baseline, a last resort)"
---

<objective>
Capture the GPU-backed, quality-comparable ArcadeDB-side measurements under conditions
matched to the Phase 3 baseline: same real granite+BGE providers (D-07), same sha256-verified
corpus (D-11), same 60 frozen questions (D-08). Produce the raw captures the gate diff consumes:
one real-provider e2e-results.json (expecting check #13 to now pass — D-03) and N=3
real-document-benchmark runs with per-query latency recorded (D-06). No new code — this plan
runs existing scripts (`e2e_score.py`, `real_document_benchmark.py`) plus the 06-01
`gate_diff.py --verify-corpus` pre-flight.

Purpose: These captures are the reference-oracle data the ARC-09 verdict is computed from.
They are inherently a GPU/Docker manual measurement (VALIDATION.md Manual-Only), not a
unit-testable behavior.
Output: `baseline/06-gate/e2e-results.json`, three `real-document-benchmark-run{1,2,3}.json`,
and a `capture-provider-env.txt` recording the exact provider/env conditions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-migration-correctness-gate/06-CONTEXT.md
@.planning/phases/06-migration-correctness-gate/06-RESEARCH.md
@baseline/03-turingdb/BASELINE.md
</context>

## Artifacts this phase produces (this plan's share)

- `baseline/06-gate/e2e-results.json` — real-provider ArcadeDB e2e capture (field-shape mirrors baseline/04-arcadedb/e2e-results.json: adds backend, arcadedb_image).
- `baseline/06-gate/real-document-benchmark-run{1,2,3}.json` — N=3 frozen-question replay captures.
- `baseline/06-gate/capture-provider-env.txt` — recorded provider model IDs, endpoints, env flags, GLiNER-scope choice.
- (No new code symbols — runs existing scripts + the 06-01 gate_diff CLI.)

<tasks>

<task type="execute">
  <name>Task 1: Pre-flight corpus verify (D-11) + real-provider ArcadeDB e2e capture (D-07/D-03)</name>
  <files>baseline/06-gate/e2e-results.json, baseline/06-gate/capture-provider-env.txt</files>
  <read_first>
    - scripts/gate_diff.py (the 06-01 --verify-corpus / --corpus-root / --manifest CLI to run as pre-flight)
    - src/turing_agentmemory_mcp/e2e_score.py:54-193 (run_e2e always drives ArcadeE2EBackend; E2E_USE_EXTERNAL_EMBED/RERANK gate whether the in-process stubs start; --out flag)
    - baseline/03-turingdb/BASELINE.md (the exact provider config + reproduction commands to match)
    - baseline/03-turingdb/e2e-results.json (baseline check #1 detail shows agentmemory-embed:8080 / agentmemory-rerank:8080 — the non-stub target shape; #13 fails there with IndexError)
    - baseline/04-arcadedb/e2e-results.json (the ArcadeDB e2e field shape to reproduce: backend, arcadedb_image, 19 checks)
    - compose.yaml (arcadedb, agentmemory-model-init, agentmemory-embed, agentmemory-rerank services; sidecar internal :8080 endpoints and pinned granite/BGE GGUF model IDs)
  </read_first>
  <action>
    Bring up the GPU stack per user_setup. First run the D-11 pre-flight:
    `python scripts/gate_diff.py --verify-corpus --corpus-root D:/tmp/baseline-corpus --manifest baseline/03-turingdb/corpus-manifest.json`
    — it MUST report ok with zero mismatches (fail closed and STOP if any drift). Then capture the
    real-provider e2e the same way the baseline was captured (inside the compose network so provider
    URLs resolve to the sidecar hostnames), with GLiNER off for the e2e leg (e2e_score.py builds its
    own store and never wires GLiNER — confirmed): run e2e_score.py with
    E2E_USE_EXTERNAL_EMBED=1 E2E_USE_EXTERNAL_RERANK=1 and EMBED_BASE_URL/RERANK_BASE_URL pointed at
    agentmemory-embed:8080 / agentmemory-rerank:8080, writing --out to baseline/06-gate/e2e-results.json.
    Confirm the capture's check #1 detail shows the sidecar hostnames (NOT 127.0.0.1) and that check
    #13 (document_search_retrieves_exact_top1_with_citation_and_neighbor_context) reports ok true
    (D-03 evidence the document_id-length bug is fixed on ArcadeDB). If #13 fails, that is a Phase-4
    defect surfaced by the gate (fix-on-touch), not new Phase-6 scope — record it and stop for
    triage. Write baseline/06-gate/capture-provider-env.txt recording embedder/reranker model IDs,
    endpoints, the E2E_USE_EXTERNAL_* flags, and GLiNER=off-for-this-leg. Force-add is handled in 06-04.
  </action>
  <verify>
    <automated>python -c "import json,sys; d=json.load(open('baseline/06-gate/e2e-results.json',encoding='utf-8')); c1=d['checks'][0]['detail']; c13=[c for c in d['checks'] if 'document_search_retrieves_exact_top1' in c['name']][0]; ok = d.get('check_count')==19 and '127.0.0.1' not in (c1.get('embedding_base_url','')+c1.get('rerank_base_url','')) and bool(c13.get('ok')); sys.exit(0 if ok else 1)"</automated>
    <human-check>Confirm on the GPU host: the e2e capture ran against the real granite+BGE sidecars (check #1 detail hostnames are agentmemory-embed/agentmemory-rerank, not 127.0.0.1), all 19 checks executed, and check #13 passes (document retrieval works on ArcadeDB under real providers). Spot-check the score is not a stub artifact.</human-check>
  </verify>
  <done>baseline/06-gate/e2e-results.json exists with 19 checks, non-stub sidecar provider URLs, and check #13 ok; capture-provider-env.txt records the exact provider/env conditions; corpus pre-flight passed with zero drift.</done>
</task>

<task type="execute">
  <name>Task 2: N=3 frozen-question real-document-benchmark capture with latency (D-08/D-03/D-06)</name>
  <files>baseline/06-gate/real-document-benchmark-run1.json, baseline/06-gate/real-document-benchmark-run2.json, baseline/06-gate/real-document-benchmark-run3.json</files>
  <read_first>
    - scripts/real_document_benchmark.py:77-100 (parse_args: --root, --mcp-url, --output [NOT --out], --frozen-questions, --top-k, --search-concurrency, --chunk-bytes, --poll-seconds; run() validation bounds)
    - scripts/real_document_benchmark_scoring.py:152-165 (load_frozen_questions schema contract), :288-324 (summarize_results shape carrying summary.mrr_at_20 / recall_at_k / latency_ms / documents)
    - baseline/03-turingdb/real-document-benchmark.json (baseline output shape + summary; baseline used --search-concurrency 3, --top-k 20, --chunk-bytes 524288, --poll-seconds 10)
    - baseline/03-turingdb/BASELINE.md:81-90 (exact run parameters used for the baseline capture)
    - .planning/phases/06-migration-correctness-gate/06-RESEARCH.md (Area 4 GLiNER-scope nuance + Pitfall 4: compose hardcodes GLINER_ENABLED=1 for turing-agentmemory-mcp with no env toggle)
  </read_first>
  <action>
    Point --mcp-url at the running MCP (http://127.0.0.1:8095/mcp/). Run the benchmark THREE times
    (D-04 N=3 floor to smooth rerank nondeterminism), each replaying the exact 60 frozen questions:
    `python scripts/real_document_benchmark.py --frozen-questions baseline/03-turingdb/frozen-questions.json --root D:/tmp/baseline-corpus --top-k 20 --search-concurrency 3 --chunk-bytes 524288 --poll-seconds 10 --output baseline/06-gate/real-document-benchmark-run<N>.json`
    for N in 1,2,3. Use --search-concurrency 3 to match the captured baseline conditions exactly
    (concurrency affects only wall-clock, not scoring). Decide GLiNER handling explicitly per D-06:
    for a literal apples-to-apples latency comparison, override GLINER_ENABLED=0 on the
    turing-agentmemory-mcp compose service via a compose override before this capture; otherwise
    accept the compose default (GLINER_ENABLED=1) as a documented low-risk deviation (document_search
    consumes no entity channel, so scores are unaffected — only ingestion-time latency). Record the
    choice in capture-provider-env.txt. Confirm each run's summary carries mrr_at_20, recall_at_k['1'],
    recall_at_k['20'], and latency_ms{mean,max}, and that the 5 normattiva_* documents show non-zero
    per-document MRR/recall (D-03 positive evidence). Do NOT regenerate questions; do NOT change --top-k
    away from 20 (MRR@20 is hardcoded at cutoff 20 regardless). Force-add is handled in 06-04.
  </action>
  <verify>
    <automated>python -c "import json,sys; runs=[json.load(open(f'baseline/06-gate/real-document-benchmark-run{n}.json',encoding='utf-8')) for n in (1,2,3)]; ok=all(r['summary'].get('question_count')==60 and 'mrr_at_20' in r['summary'] and 'latency_ms' in r['summary'] and len(r['summary']['documents'])==12 for r in runs); sys.exit(0 if ok else 1)"</automated>
    <human-check>Confirm the three runs replayed the exact 60 frozen questions (no regeneration), the 5 normattiva PDFs now retrieve with non-zero MRR/recall (the port fixes the baseline's flat-zero), and per-query latency is recorded for the D-06 comparison. Note the GLiNER-scope choice made.</human-check>
  </verify>
  <done>Three real-document-benchmark-run{1,2,3}.json exist, each with 60 questions across 12 documents, mrr_at_20/recall_at_k/latency_ms in summary, and non-zero metrics on the 5 normattiva docs; GLiNER-scope choice recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external corpus -> capture run | The corpus under --root must be the exact sha256-verified baseline corpus before any capture is trusted |
| GPU sidecars -> capture | The provider identity (real granite+BGE vs stub) determines whether the capture is a valid quality verdict input |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Spoofing/Tampering | corpus under --root (substitution) | high | mitigate | gate_diff --verify-corpus pre-flight fails closed on any sha256 drift before capture proceeds (D-11) |
| T-06-03-02 | Tampering | provider identity (stub masquerading as GPU) | high | mitigate | E2E_USE_EXTERNAL_EMBED/RERANK=1 + sidecar-hostname endpoints; the automated verify asserts check #1 detail is non-127.0.0.1; 06-04 gate_diff re-checks is_stub_provider (D-07) |
| T-06-03-03 | Tampering | question drift (regeneration instead of replay) | medium | mitigate | --frozen-questions replays the exact committed 60; load_frozen_questions fails loudly on schema drift (D-08) |
| T-06-03-04 | Repudiation | GLiNER-on-one-side latency confound | low | accept | Choice recorded in capture-provider-env.txt + GATE.md deviation log; document_search consumes no entity channel so scores are unaffected (D-06, RESEARCH Pitfall 4) |
| T-06-03-SC | Tampering | package installs | low | accept | No new dependencies — runs existing scripts against the pinned compose stack |
</threat_model>

<verification>
- gate_diff --verify-corpus pre-flight reports zero drift before capture
- baseline/06-gate/e2e-results.json: 19 checks, non-stub sidecar provider URLs, check #13 ok (D-03)
- three real-document-benchmark-run{1,2,3}.json: 60 questions, 12 docs, latency recorded, non-zero normattiva metrics
- capture-provider-env.txt records provider model IDs, endpoints, env flags, GLiNER-scope choice
- SC#2 quality judgment deferred to end-of-phase human-verify (Manual-Only, VALIDATION.md)
</verification>

<success_criteria>
Real GPU-backed ArcadeDB captures exist under baseline-matched conditions (sha-verified corpus,
real granite+BGE providers, exact 60 frozen questions, N=3 runs), with check #13 passing and the
5 normattiva docs retrieving non-zero — the reference-oracle data the ARC-09 verdict is computed from.
</success_criteria>

<output>
Create `.planning/phases/06-migration-correctness-gate/06-03-SUMMARY.md` when done
</output>
