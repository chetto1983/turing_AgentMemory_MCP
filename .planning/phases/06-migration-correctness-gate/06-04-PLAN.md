---
phase: 06-migration-correctness-gate
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - baseline/06-gate/gate-result.json
  - baseline/06-gate/e2e-baseline-corrected.json
  - baseline/06-gate/GATE.md
  - tests/test_gate_artifact_schema.py
autonomous: true
requirements: [ARC-09]
must_haves:
  truths:
    - "gate_diff.py runs over the N=3 port captures + the port e2e capture + the committed baseline, writing baseline/06-gate/gate-result.json with a top-level verdict in {GO, NO_GO} and every D-09 field: per-metric diff, per-check diff, per-document diff, latency, tolerance params, run count, provider config, corpus sha-verification result (per D-09, SC#3)"
    - "The corrected TuringDB baseline is materialized to baseline/06-gate/e2e-baseline-corrected.json via gate_diff --derive-corrected-baseline (ok=bool(detail) over the committed inflated capture), and the original baseline/03-turingdb/e2e-results.json is left intact for provenance (per D-05)"
    - "baseline/06-gate/GATE.md records the GO/NO-GO verdict + rationale, per-metric/per-check/per-document diff, the GLiNER-scope + latency-confound deviations, and exact reproduction commands (using --output for real_document_benchmark.py, --out for e2e_score.py), mirroring baseline/03-turingdb/BASELINE.md (per D-09)"
    - "The raw ArcadeDB-side captures (e2e-results.json, the three real-document-benchmark-run{1,2,3}.json) and the corrected baseline are force-added under baseline/06-gate/ because .benchmarks/ and e2e-results.json are gitignored (per D-09)"
    - "tests/test_gate_artifact_schema.py asserts the committed baseline/06-gate/gate-result.json is well-formed (all D-09 fields present, verdict in {GO, NO_GO}) — always green regardless of the verdict value (SC#3)"
    - { statement: "tests/test_gate_artifact_schema.py asserts the committed verdict is GO via gate_guard.assert_gate_go — green only after a GO capture; a NO_GO capture legitimately blocks the phase and Phase-7 cutover (per D-01/D-10)", verification: backstop }
    - { statement: "The port's full-corpus retrieval meets-or-exceeds the bug-corrected 7-doc bar within the epsilon band on the N=3 mean, and per-query latency does not regress vs the clean-DB baseline (SC#2/D-01/D-04/D-06)", verification: backstop }
  artifacts:
    - baseline/06-gate/gate-result.json
    - baseline/06-gate/e2e-baseline-corrected.json
    - baseline/06-gate/GATE.md
    - tests/test_gate_artifact_schema.py
  key_links:
    - "gate_diff.py build_gate_result -> baseline/06-gate/gate-result.json -> Phase-7 assert_gate_go guard"
    - "06-03 raw captures -> gate_diff --port-runs / --e2e-port inputs"
    - "gate-result.json verdict -> GATE.md ## Verdict cross-reference"
  prohibitions:
    - "MUST NOT emit a GO verdict from a stub/degraded provider or a corpus that did not sha256-match the manifest"
    - "MUST NOT grade the port against the deflated full-corpus baseline instead of the bug-corrected 7-doc bar"
    - "MUST NOT let an unchanged aggregate hide a per-document regression in GATE.md"
    - "MUST NOT overwrite or discard the original baseline/03-turingdb/e2e-results.json (keep the inflated capture intact for provenance)"
    - "MUST NOT allow Phase-7 removal to be authorized by anything other than a committed verdict == GO"
---

<objective>
Compute the ARC-09 verdict from the 06-03 captures and record the committed gate artifact:
run `gate_diff.py` over the N=3 port runs + port e2e + committed baseline to write
`baseline/06-gate/gate-result.json` (D-09 machine artifact); materialize the corrected
TuringDB baseline (D-05); author `baseline/06-gate/GATE.md` (human artifact mirroring
BASELINE.md); force-add the raw captures; and add the committed-artifact verification tests
(well-formedness + the GO assertion via the 06-02 guard). This is SC#3 — the recorded gate
that authorizes or blocks Phase-7 cutover.

Purpose: Turn the deterministic engine (06-01) + the guard (06-02) + the captures (06-03)
into the single committed, human+machine-readable GO/NO-GO artifact with teeth.
Output: `baseline/06-gate/gate-result.json`, `baseline/06-gate/e2e-baseline-corrected.json`,
`baseline/06-gate/GATE.md`, `tests/test_gate_artifact_schema.py`, and the force-added raw captures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-migration-correctness-gate/06-CONTEXT.md
@.planning/phases/06-migration-correctness-gate/06-RESEARCH.md
@baseline/03-turingdb/BASELINE.md
</context>

## Artifacts this phase produces (this plan's share)

- `baseline/06-gate/gate-result.json` — the D-09 machine verdict (verdict, tolerance, provider_config, corpus_verification, metrics_diff{aggregate,per_document}, baseline_bar, e2e_diff, latency, runs, normattiva_evidence).
- `baseline/06-gate/e2e-baseline-corrected.json` — the D-05 derived corrected TuringDB baseline.
- `baseline/06-gate/GATE.md` — the D-09 human artifact (What This Is / Provider Config / Metrics Diff / Deviations / Reproduction Commands / Verdict).
- `tests/test_gate_artifact_schema.py` — new test module (well-formedness always-green + committed-verdict-GO assertion via gate_guard.assert_gate_go).

<tasks>

<task type="execute">
  <name>Task 1: Derive corrected baseline (D-05) + compute gate-result.json (D-09) + force-add raw captures</name>
  <files>baseline/06-gate/gate-result.json, baseline/06-gate/e2e-baseline-corrected.json</files>
  <read_first>
    - scripts/gate_diff.py (the 06-01 CLI: --baseline-benchmark, --port-runs repeatable, --e2e-baseline, --e2e-port, --corpus-root, --manifest, --frozen-questions, --epsilon, --derive-corrected-baseline, --out; build_gate_result field set)
    - baseline/06-gate/e2e-results.json + real-document-benchmark-run{1,2,3}.json (the 06-03 captures — the --e2e-port and --port-runs inputs)
    - baseline/03-turingdb/e2e-results.json (the --e2e-baseline + --derive-corrected-baseline source; original stays intact)
    - baseline/03-turingdb/real-document-benchmark.json (the --baseline-benchmark source for the 7-doc bar)
    - baseline/03-turingdb/corpus-manifest.json (the --manifest for the recorded corpus_verification result)
    - .planning/phases/06-migration-correctness-gate/06-PATTERNS.md:260-262 (force-add convention for gitignored baseline artifacts)
  </read_first>
  <action>
    Materialize the corrected TuringDB baseline:
    `python scripts/gate_diff.py --derive-corrected-baseline baseline/03-turingdb/e2e-results.json --out baseline/06-gate/e2e-baseline-corrected.json`
    (leaves baseline/03-turingdb/e2e-results.json untouched — D-05 provenance). Then compute the verdict:
    run gate_diff with --baseline-benchmark baseline/03-turingdb/real-document-benchmark.json,
    --port-runs the three baseline/06-gate/real-document-benchmark-run{1,2,3}.json,
    --e2e-baseline baseline/03-turingdb/e2e-results.json, --e2e-port baseline/06-gate/e2e-results.json,
    --corpus-root D:/tmp/baseline-corpus, --manifest baseline/03-turingdb/corpus-manifest.json,
    --frozen-questions baseline/03-turingdb/frozen-questions.json, --epsilon 0.03, and
    --out baseline/06-gate/gate-result.json. Confirm the emitted verdict is NO_GO if the corpus
    verification failed, a stub provider was detected, or any locked metric fell below the band — and
    GO only when all pass. Then force-add the gitignored artifacts:
    `git add -f baseline/06-gate/e2e-results.json baseline/06-gate/real-document-benchmark-run1.json baseline/06-gate/real-document-benchmark-run2.json baseline/06-gate/real-document-benchmark-run3.json baseline/06-gate/e2e-baseline-corrected.json baseline/06-gate/gate-result.json baseline/06-gate/capture-provider-env.txt`.
    Annotate any e2e check flip among #12/#15/#16 as chunk-count-driven (expected, non-regression) by
    reading the detail/error payload, not just ok (RESEARCH Pitfall 3). Implements D-05 + D-09.
  </action>
  <verify>
    <automated>python -c "import json,sys; d=json.load(open('baseline/06-gate/gate-result.json',encoding='utf-8')); req={'verdict','tolerance','provider_config','corpus_verification','metrics_diff','baseline_bar','e2e_diff','latency','runs'}; ok=req<=set(d) and d['verdict'] in {'GO','NO_GO'} and 'aggregate' in d['metrics_diff'] and 'per_document' in d['metrics_diff']; sys.exit(0 if ok else 1)"</automated>
  </verify>
  <done>baseline/06-gate/gate-result.json exists with a verdict in {GO,NO_GO} and all D-09 fields (aggregate + per_document metrics_diff, per-check e2e_diff, latency, tolerance, provider_config, corpus_verification, runs); e2e-baseline-corrected.json materialized; original baseline/03-turingdb/e2e-results.json unchanged; all raw captures force-added.</done>
</task>

<task type="execute">
  <name>Task 2: Author GATE.md (D-09 human artifact, mirrors BASELINE.md)</name>
  <files>baseline/06-gate/GATE.md</files>
  <read_first>
    - baseline/03-turingdb/BASELINE.md (the section structure to mirror: What This Is, Provider Configuration, Corpus, Frozen Questions, Run Parameters, deviation callouts in bold)
    - baseline/06-gate/gate-result.json (the machine verdict to narrate — cross-reference exact numbers, do not restate different figures)
    - baseline/06-gate/capture-provider-env.txt (the recorded provider/env conditions + GLiNER-scope choice)
    - .planning/phases/06-migration-correctness-gate/06-PATTERNS.md:228-236 (the required GATE.md section list)
  </read_first>
  <action>
    Write baseline/06-gate/GATE.md with sections: `## What This Is` (the GO/NO-GO verdict + one-paragraph
    rationale, cross-referenced to gate-result.json); `## Provider Configuration` (Role | Model | Dimensions
    | Endpoint table — granite 768d + BGE, matching the baseline); `## Corpus & sha256 Verification` (D-11
    result: root, manifest, zero-drift confirmation); `## Metrics Diff` (per-metric aggregate + per-document
    for MRR@20/recall@1/recall@20 vs the bug-corrected 7-doc bar 0.5979/0.5143/0.7714, epsilon=0.03, N=3
    mean + stddev — D-01/D-02/D-04); `## E2E Per-Check Diff` (the corrected TuringDB baseline vs the ArcadeDB
    port per-check, flagging #12/#15/#16 as chunk-count-sensitive and #13 as the normattiva/document_id-bug
    sentinel now passing — D-03/D-05); `## Latency` (per-query mean/max, compared to the clean-DB baseline
    5.4s, with the volume-bloat confound documented — D-06); `## Deviations / Confounds` (bold callouts: the
    GLiNER-scope choice, the latency confound, the D-05 CRITICAL-FINDING note that check() was already honest
    at HEAD via 8120efd so D-05 was verify+derive not an edit); `## Reproduction Commands` (exact CLI: e2e_score.py
    with --out and E2E_USE_EXTERNAL_*, real_document_benchmark.py with --output and --frozen-questions —
    do NOT conflate the two flag names, RESEARCH discrepancy #7); `## Verdict` (GO | NO_GO, one line,
    machine-cross-referenced to gate-result.json). Every figure must match gate-result.json exactly.
    Implements D-09; honors CLAUDE.md "never claim a benchmark win from one corpus/one run/mismatched configs".
  </action>
  <verify>
    <automated>python -c "import sys,pathlib,re; t=pathlib.Path('baseline/06-gate/GATE.md').read_text(encoding='utf-8'); need=['## What This Is','## Provider Configuration','## Metrics Diff','## Reproduction Commands','## Verdict']; sys.exit(0 if all(s in t for s in need) and re.search(r'\\b(GO|NO_GO)\\b', t) else 1)"</automated>
    <human-check>Read GATE.md end-to-end: the verdict + rationale are honest (port graded against the bug-corrected 7-doc bar, not the deflated aggregate; not flattered against a broken baseline), the per-document diff is present, the deviations (GLiNER, latency confound, D-05 already-fixed) are disclosed, and the reproduction commands use the correct per-script flag names.</human-check>
  </verify>
  <done>baseline/06-gate/GATE.md exists with all required sections, a GO|NO_GO verdict matching gate-result.json, per-metric/per-check/per-document diff, disclosed deviations, and exact reproduction commands.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 3: Committed-artifact verification tests (well-formedness + GO assertion via guard)</name>
  <files>tests/test_gate_artifact_schema.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/gate_guard.py (the 06-02 validate_gate_result_schema + assert_gate_go to import)
    - baseline/06-gate/gate-result.json (the committed artifact under test)
    - tests/test_no_skip_as_green_guard.py (no-skip discipline — a missing/NO_GO artifact must hard-fail, never skip)
  </read_first>
  <behavior>
    - Test: the committed baseline/06-gate/gate-result.json loads and passes validate_gate_result_schema (all D-09 fields present, verdict in {GO, NO_GO}) — always green regardless of verdict value
    - Test: metrics_diff carries both an aggregate and a per_document block (D-02 granularity is recorded)
    - Test: assert_gate_go('baseline/06-gate/gate-result.json') passes — green only when the committed verdict is GO; a NO_GO capture makes this test fail, which correctly blocks the phase and Phase-7 cutover (no pytest.skip escape hatch)
  </behavior>
  <action>
    Write tests/test_gate_artifact_schema.py importing validate_gate_result_schema and assert_gate_go
    from turing_agentmemory_mcp.gate_guard. Add a well-formedness test that reads the committed
    baseline/06-gate/gate-result.json and asserts it passes validate_gate_result_schema and carries
    metrics_diff.aggregate + metrics_diff.per_document (always green). Add the committed-verdict test that
    calls assert_gate_go on the real path (green iff verdict == GO). Use no pytest.skip anywhere — a missing
    or NO_GO artifact is a hard failure per the repo's no-skip-as-green discipline. Run the full suite to
    confirm the new tests plus the 06-01/06-02 tests all pass together. Implements SC#3 + D-10 enforcement
    over the real committed artifact.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gate_artifact_schema.py -q</automated>
  </verify>
  <done>tests/test_gate_artifact_schema.py green: the committed gate-result.json is well-formed with aggregate + per_document diff, and assert_gate_go passes on the committed GO verdict; full `python -m pytest -q` suite green; ruff format --check, ruff check, and check-file-size.sh all clean; `docker compose config --quiet` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 06-03 captures -> gate_diff | Untrusted-until-verified capture JSON crosses into the committed verdict |
| gate-result.json -> committed artifact / Phase-7 guard | The written verdict becomes the durable authority for irreversible Phase-7 removal |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Tampering | gate-result.json (a stub/mismatch capture emitting GO) | high | mitigate | gate_diff re-runs verify_corpus + is_stub_provider on the captures; verdict forced NO_GO on either failure before write (D-07/D-11) |
| T-06-04-02 | Repudiation | GATE.md (grading against deflated aggregate / flattered bar) | high | mitigate | GATE.md figures must equal gate-result.json exactly; the bar is the bug-corrected 7-doc subset; per-document diff mandatory (D-01/D-02); human-check reviews honesty |
| T-06-04-03 | Tampering | provenance loss (overwriting the inflated baseline) | medium | mitigate | Corrected baseline written to a NEW file (e2e-baseline-corrected.json); original 03-turingdb capture left intact (D-05) |
| T-06-04-04 | Elevation of Privilege | committed-verdict test skipping instead of failing | high | mitigate | assert_gate_go-based test has no pytest.skip; a NO_GO/missing artifact hard-fails (D-10, CI-07) |
| T-06-04-SC | Tampering | package installs | low | accept | No new dependencies — stdlib + existing scripts only |
</threat_model>

<verification>
- `python -m pytest tests/test_gate_artifact_schema.py -q` green
- `python -m pytest -q` full suite green (06-01/06-02/06-04 tests coexist)
- gate-result.json: verdict in {GO,NO_GO}, all D-09 fields, aggregate + per_document metrics_diff
- GATE.md: all required sections, figures matching gate-result.json, deviations disclosed
- e2e-baseline-corrected.json materialized; baseline/03-turingdb/e2e-results.json unchanged
- raw captures force-added under baseline/06-gate/
- `python -m ruff format --check src tests scripts`, `python -m ruff check src tests scripts`, `bash scripts/check-file-size.sh`, `docker compose config --quiet` all clean
- End-of-phase human-verify confirms SC#2 (retrieval meets-or-exceeds; latency no-regression)
</verification>

<success_criteria>
The committed `baseline/06-gate/` gate artifact exists (gate-result.json machine verdict +
GATE.md human record + corrected baseline + raw captures), the verdict is computed honestly
against the bug-corrected bar with per-document granularity and fail-closed provider/corpus
checks, and the committed artifact is verified green by tests — the recorded SC#3 gate that
the Phase-7 guard reads to authorize or block cutover.
</success_criteria>

<output>
Create `.planning/phases/06-migration-correctness-gate/06-04-SUMMARY.md` when done
</output>
