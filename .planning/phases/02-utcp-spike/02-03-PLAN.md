---
phase: 02-utcp-spike
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - .planning/phases/02-utcp-spike/02-FINDINGS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [UTCP-01]

must_haves:
  truths:
    - "D-09: `.planning/phases/02-utcp-spike/02-FINDINGS.md` exists and documents (a) the SC#1 gaps — the three source-verified conformance gaps plus whatever the live round-trip observed, (b) the SC#2 verdict under a `## Verdict` heading naming exactly one of build / stay-manual / defer with rationale, and (c) explicit trigger conditions for revisiting if the verdict is 'defer'."
    - "D-04: the verdict is grounded in the empirical answer to 'does mcp-via-UTCP already satisfy the real consumer utcp-agent?' — weighing 'mcp-via-UTCP already works, minus fixable emission bugs' against 'native http serving buys UTCP's stated no-wrapper benefit at nonzero build cost'. The harness evidence decides it; the plan does NOT pre-decide it."
    - "D-05: FINDINGS records the registry-drift dimension — that mcp-path discovery is live via `session.list_tools()`, so `AGENTMEMORY_TOOL_SPECS` is largely moot for that path, and notes auto-generating-the-manual-from-registry only as a candidate for gated follow-on (not acted on here)."
    - "SC#3 hard gate: `grep -rl \"HttpCallTemplate\\|utcp_http\\|http_call_template\" src/` is EMPTY, `git diff` shows no UTCP native-serving additions under src/, and compose.yaml gained no UTCP service — proven by an explicit guard, not assumed."
    - "D-10: the verdict is recorded as a PROJECT.md Key Decisions row; IF (and only if) the verdict is 'build', a gated backlog phase entry is added to ROADMAP.md whose precondition cites this FINDINGS verdict. Follow-on UTCP build work is gated on the verdict, never assumed."
  artifacts:
    - .planning/phases/02-utcp-spike/02-FINDINGS.md
    - "PROJECT.md Key Decisions row recording the UTCP verdict"
    - "ROADMAP.md gated backlog entry (CONDITIONAL — only if verdict=build)"
  key_links:
    - "FINDINGS.md ← evidence from 02-01 (static conformance pytest) + 02-02 (live mcp round-trip, native-http prototype, optional agent chat)."
    - "ROADMAP.md gated entry precondition ← cites the FINDINGS.md verdict (D-10)."
  prohibitions:
    - "MUST NOT commit any UTCP native-serving code under src/, and MUST NOT wire the D-06 prototype or a llama.cpp chat sidecar into compose.yaml (SC#3) — the guard task proves this."
    - "MUST NOT pre-decide the verdict — the recommendation must follow from the captured 02-02 evidence; if the live round-trip was not run, say so and mark the verdict provisional rather than inventing evidence."
    - "MUST NOT add a gated ROADMAP entry unless the verdict is 'build'; a 'stay-manual' or 'defer' verdict adds only the PROJECT.md decision row (+ trigger conditions for defer)."
---

<objective>
Turn the static (02-01) and live (02-02) evidence into the phase deliverable: write
`02-FINDINGS.md` with the documented SC#1 gaps and the SC#2 verdict (build / stay-manual / defer)
plus rationale and — if deferring — trigger conditions; PROVE the SC#3 hard gate that no UTCP
build work leaked into src/ or compose.yaml; and apply the D-10 gating (record the verdict in
PROJECT.md, and only if the verdict is 'build' add a gated backlog phase to ROADMAP.md).

Purpose: SC#2 requires a written, rationale-backed verdict; SC#3 requires proof that this phase
committed zero UTCP build work and that any follow-on is explicitly gated on the verdict. This plan
closes the findings-only spike.
Output: `02-FINDINGS.md`, a PROJECT.md Key Decisions row, and (conditionally) a gated ROADMAP entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-utcp-spike/02-CONTEXT.md
@.planning/phases/02-utcp-spike/02-RESEARCH.md
@.planning/phases/02-utcp-spike/02-01-PLAN.md
@.planning/phases/02-utcp-spike/02-02-PLAN.md
</context>

## Artifacts this plan produces

- `.planning/phases/02-utcp-spike/02-FINDINGS.md` — D-09 verdict deliverable (SC#1 gaps + SC#2 verdict + trigger conditions)
- `.planning/PROJECT.md` — Key Decisions row recording the UTCP verdict (D-10)
- `.planning/ROADMAP.md` — CONDITIONAL gated backlog entry (only if verdict=build, D-10)

(Whole-phase artifact list is enumerated in 02-01-PLAN.md.)

<tasks>

<task type="auto">
  <name>Task 1: Write 02-FINDINGS.md — SC#1 gaps + SC#2 verdict + trigger conditions (D-09/D-04/D-05)</name>
  <files>.planning/phases/02-utcp-spike/02-FINDINGS.md</files>
  <read_first>
    - .planning/phases/02-utcp-spike/02-RESEARCH.md §Summary (the three source-verified gaps #1/#2/#3 + the structural list_tools finding + the utcp.io "no wrapper servers" framing), §State of the Art (README file_path deprecation, hardcoded utcp_version drift), §Common Pitfalls (Pitfall 4 version-drift is informational, not a bug)
    - .planning/phases/02-utcp-spike/02-CONTEXT.md D-04/D-05/D-09 and §Deferred Ideas (native serving + manual-from-registry auto-gen are candidates, not acted on)
    - The captured evidence from 02-01 (conformance pytest results) and 02-02 (live round-trip / native-http / optional agent) — read the 02-01/02-02 SUMMARY files and any captured run output before writing the verdict
  </read_first>
  <action>
    Create `.planning/phases/02-utcp-spike/02-FINDINGS.md`. Structure it with clear headings:
    a `## SC#1 — Documented Gaps` section listing each gap with its source evidence — gap #1 (api_key
    auth fails `McpCallTemplate.auth: Optional[OAuth2Auth]` — reproduced by the 02-01 conformance test),
    gap #2 (mcpServers command/args shape: `utcp.py` emits the full argv as one `command` array + an
    extra `transport` key vs the real string-command + separate args list — observed via the 02-02
    round-trip), gap #3 (README `UTCP_CONFIG_FILE` `text`/`file_path` example rejected by the current
    `TextCallTemplateSerializer` — reproduced by the 02-01 conformance test), plus the low-severity
    informational drift (hardcoded `utcp_version="1.0.2"`); a `## D-05 — Manual vs Registry Drift`
    section noting mcp-path discovery is live via `session.list_tools()` (so `AGENTMEMORY_TOOL_SPECS` is
    largely dead weight for that path) and flagging auto-generating-the-manual-from-registry as a
    candidate for gated follow-on only; a `## D-04 — Does mcp-via-UTCP already satisfy utcp-agent?`
    section stating the EMPIRICAL answer from the 02-02 evidence; and a `## Verdict` heading naming
    EXACTLY ONE of build / stay-manual / defer, with rationale that weighs "mcp-via-UTCP already works
    for the real consumer, minus the fixable emission bugs above" against "native http serving buys
    UTCP's stated no-wrapper benefit at nonzero build cost" (the 02-02 native-http prototype's effort
    surface informs the cost side). If the verdict is 'defer', include a `## Trigger Conditions`
    section with explicit conditions for revisiting. Let the evidence decide — do NOT pre-select a
    verdict; if the live round-trip (02-02) was not executed (e.g. Docker/GPU unavailable), say so
    plainly and mark the verdict provisional on the static evidence rather than fabricating live
    results. Keep the document self-contained and cite the RESEARCH source lines already gathered.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-utcp-spike/02-FINDINGS.md && grep -qi "## Verdict" .planning/phases/02-utcp-spike/02-FINDINGS.md && grep -Eqi "build|stay-manual|defer" .planning/phases/02-utcp-spike/02-FINDINGS.md && echo FINDINGS_OK</automated>
    <human-check>Read 02-FINDINGS.md: confirm SC#1 gaps are documented with their evidence source, the `## Verdict` names exactly one of build/stay-manual/defer with rationale grounded in the captured 02-02 evidence, and (if defer) trigger conditions are present. Confirm the verdict was not pre-decided — it follows from the evidence.</human-check>
  </verify>
  <acceptance_criteria>
    - `02-FINDINGS.md` exists with a `## SC#1 — Documented Gaps` section covering gaps #1/#2/#3, a D-05 drift section, a D-04 empirical-answer section, and a `## Verdict` heading naming exactly one of build/stay-manual/defer with rationale.
    - If the verdict is 'defer', a `## Trigger Conditions` section is present.
    - The automated check prints `FINDINGS_OK`.
  </acceptance_criteria>
  <done>The D-09 deliverable documents the SC#1 gaps and states a rationale-backed SC#2 verdict grounded in the real-client evidence.</done>
</task>

<task type="auto">
  <name>Task 2: SC#3 hard-gate guard — prove zero UTCP build work under src/ or compose.yaml</name>
  <files>.planning/phases/02-utcp-spike/02-FINDINGS.md</files>
  <read_first>
    - .planning/phases/02-utcp-spike/02-VALIDATION.md §Per-Task Verification Map (the SC#3 guard row: grep src/ must be empty)
    - .planning/phases/02-utcp-spike/02-CONTEXT.md §domain (SC#3 non-negotiable) and §Deferred Ideas (native serving deferred, zero serving code in src/)
  </read_first>
  <action>
    Run the SC#3 guard and record its result in a `## SC#3 — No Build Work Committed` section of
    `02-FINDINGS.md`. The guard is three checks: (1) the negative grep over `src/` for native-http
    serving symbols is EMPTY; (2) `git diff` for this phase's commits shows no UTCP native-serving
    additions under `src/` (the pre-existing `src/turing_agentmemory_mcp/utcp.py` manual EXPORT is not
    "native serving" and is unchanged by this phase — confirm it is untouched); (3) `compose.yaml`
    gained no UTCP service and no llama.cpp chat sidecar. Paste the exact commands and their outputs
    into the FINDINGS section as the audit trail. If ANY check fails, STOP and report — do not
    rationalize it away (SC#3 is the phase's hard gate). This task WRITES only to FINDINGS.md (it audits
    src/ and compose.yaml, it does not modify them).
  </action>
  <verify>
    <automated>test -z "$(grep -rl 'HttpCallTemplate\|utcp_http\|http_call_template' src/ 2>/dev/null)" && echo SRC_CLEAN</automated>
    <automated>git diff --name-only -- src/ | grep -q . && echo "SRC_MODIFIED" || echo "SRC_UNMODIFIED"</automated>
    <automated>grep -Eqi "utcp.*serve|native.*utcp|llama.*chat|full_agent" compose.yaml && echo "COMPOSE_HAS_UTCP" || echo "COMPOSE_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - First automated check prints `SRC_CLEAN` (negative grep over src/ is empty).
    - Second check prints `SRC_UNMODIFIED` for this phase's work (no src/ files changed by phase 2).
    - Third check prints `COMPOSE_CLEAN` (no UTCP-serving or chat-sidecar service added to compose.yaml).
    - `02-FINDINGS.md` contains a `## SC#3 — No Build Work Committed` section with the commands + outputs as the audit trail.
  </acceptance_criteria>
  <done>The SC#3 hard gate is proven: no UTCP native-serving code in src/, no src/ modification by this phase, and no UTCP/chat service in compose.yaml — recorded in FINDINGS.md.</done>
</task>

<task type="auto">
  <name>Task 3: D-10 gating — record the verdict in PROJECT.md; add a gated ROADMAP entry only if verdict=build</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/02-utcp-spike/02-CONTEXT.md D-10 (record verdict as a PROJECT.md Key Decisions row; if build, add a gated ROADMAP backlog entry whose precondition cites this verdict)
    - .planning/PROJECT.md §Key Decisions (table format: `| Decision | Rationale | Outcome |`)
    - .planning/ROADMAP.md §Phases + §Phase Details (phase-entry format; note decimal-phase / backlog conventions)
  </read_first>
  <action>
    Read the `## Verdict` from `02-FINDINGS.md` (Task 1) and apply D-10 gating with `Edit`
    (scoped edits only — never a whole-file Write on PROJECT.md or ROADMAP.md):
    (a) ALWAYS append one row to the PROJECT.md `## Key Decisions` table: Decision = "UTCP: <verdict>
    (native serving / stay on manual export / defer)", Rationale = a one-line summary citing the
    02-FINDINGS verdict + the D-04 evidence, Outcome = the gate status (e.g. "Verdict recorded;
    follow-on gated on FINDINGS").
    (b) CONDITIONAL — ONLY if the verdict is 'build': add a gated backlog phase entry to ROADMAP.md
    (in the phases list and/or a backlog/Phase Details entry consistent with the file's existing
    conventions) whose **Depends on / precondition** explicitly cites the Phase 2 FINDINGS verdict as
    the authorizing gate (e.g. "Gated on the Phase 2 UTCP Spike verdict = build"). Do NOT add any
    ROADMAP entry for a 'stay-manual' or 'defer' verdict — for 'defer', the trigger conditions already
    live in FINDINGS.md (Task 1). Do NOT touch the Phase 2 status/plan-list lines the plan-phase
    orchestrator manages; confine edits to the Key Decisions row and (if build) the new gated entry.
    Keep edits minimal and scoped.
  </action>
  <verify>
    <automated>grep -qi "UTCP" .planning/PROJECT.md && grep -qi "verdict\|native serving\|manual export\|defer" .planning/PROJECT.md && echo PROJECT_DECISION_OK</automated>
    <human-check>Confirm: PROJECT.md has a new Key Decisions row recording the UTCP verdict. If (and only if) the verdict is 'build', ROADMAP.md has a new gated backlog entry whose precondition cites the Phase 2 FINDINGS verdict; if the verdict is stay-manual/defer, ROADMAP.md has NO new UTCP build phase (defer trigger conditions live in FINDINGS.md instead).</human-check>
  </verify>
  <acceptance_criteria>
    - PROJECT.md `## Key Decisions` has a new row recording the UTCP verdict (automated check prints `PROJECT_DECISION_OK`).
    - ROADMAP.md gains a gated backlog entry citing the FINDINGS verdict IF AND ONLY IF the verdict is 'build'; otherwise ROADMAP.md gains no new UTCP build phase.
    - Edits are scoped (no whole-file rewrite; Phase 2 orchestrator-managed lines untouched).
  </acceptance_criteria>
  <done>Any follow-on UTCP build work is explicitly gated on the recorded verdict (D-10): the decision is logged in PROJECT.md, and a ROADMAP gate exists only when the verdict authorizes building.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike phase → src/ + compose.yaml | The SC#3 gate is the boundary that prevents throwaway spike work from leaking into shipped code or the production stack |
| verdict → future build authorization | The D-10 gate ensures follow-on UTCP build work cannot start without citing this verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering / Elevation of Privilege | UTCP build work leaking into src/ or compose.yaml under cover of a "spike" | low | mitigate | Task 2 SC#3 guard: negative grep over src/ must be empty, `git diff -- src/` shows no phase-2 modification, compose.yaml has no UTCP/chat service — audit trail recorded in FINDINGS.md; any failure STOPS the phase. Not blocking (guard is the mitigation). |
| T-02-04 | Repudiation | follow-on build work started without an authorizing verdict | low | mitigate | Task 3 D-10 gating: verdict recorded in PROJECT.md Key Decisions; a ROADMAP build phase exists only when the verdict = build and its precondition cites the FINDINGS verdict. Not blocking. |
</threat_model>

<verification>
- `02-FINDINGS.md` exists with `## SC#1 — Documented Gaps`, `## D-04 ...`, `## D-05 ...`, `## Verdict` (one of build/stay-manual/defer), and `## SC#3 — No Build Work Committed`; `## Trigger Conditions` present iff verdict=defer.
- `grep -rl "HttpCallTemplate\|utcp_http\|http_call_template" src/` is EMPTY; `git diff --name-only -- src/` shows no phase-2 changes; compose.yaml has no UTCP/chat service.
- PROJECT.md has a new Key Decisions row for the UTCP verdict; ROADMAP.md has a gated entry iff verdict=build.
</verification>

<success_criteria>
A written, rationale-backed verdict (build / stay-manual / defer) exists in `02-FINDINGS.md` grounded
in real-client evidence (SC#2); it is proven that zero UTCP build work was committed to src/ or
compose.yaml (SC#3); and any follow-on build work is explicitly gated on the verdict (D-10) — the
findings-only spike is complete.
</success_criteria>

<output>
Create `.planning/phases/02-utcp-spike/02-03-SUMMARY.md` when done.
</output>
