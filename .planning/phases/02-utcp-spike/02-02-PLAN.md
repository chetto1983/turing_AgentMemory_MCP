---
phase: 02-utcp-spike
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/spike/utcp_roundtrip.py
  - scripts/spike/native_http_prototype.py
  - scripts/spike/full_agent_chat.py
autonomous: true
requirements: [UTCP-01]

must_haves:
  truths:
    - "D-01/D-02/D-07/D-08: `scripts/spike/utcp_roundtrip.py` uses the REAL `UtcpClient` to register a hand-built mcp call-template (the status-quo path) pointed at the Dockerized MCP server via `docker compose run --rm -T ... serve --transport stdio`, then calls `memory_store_message` then `memory_search` — proving the mcp-via-UTCP path end-to-end WITHOUT an LLM. A register/call failure is itself valid SC#1 evidence, not an inconclusive result."
    - "The round-trip empirically records whether mcp-via-UTCP already discovers our tools with accurate live schemas (RESEARCH: `register_manual` discovers via `session.list_tools()`, so `AGENTMEMORY_TOOL_SPECS` is not consulted for this path) — the key input to the D-04 verdict."
    - "D-06: `scripts/spike/native_http_prototype.py` stands up a THROWAWAY one-tool http endpoint bound to 127.0.0.1 only, registers it via the http call-template, calls it, tears it down — yielding native-serving effort/behavior evidence. It is never merged to src/ and never wired into compose.yaml."
    - "D-03/D-08a: `scripts/spike/full_agent_chat.py` optionally drives the REAL `utcp-agent` consumer via a local llama.cpp Gemma endpoint; when no GPU/endpoint is available it RECORDS 'full-agent chat not exercised (no GPU)' rather than silently skipping."
    - "All three scripts print structured evidence for the D-09 FINDINGS.md and never leak a real bearer token (dummy-only)."
  artifacts:
    - "scripts/spike/utcp_roundtrip.py (main() -> int)"
    - "scripts/spike/native_http_prototype.py (main() with --self-test)"
    - "scripts/spike/full_agent_chat.py (main() with GPU/endpoint probe + --check)"
  key_links:
    - "utcp_roundtrip.py → docker compose run --rm -T turing-agentmemory-mcp serve --transport stdio (compose.yaml path derived from Path(__file__).resolve().parents[2]); command is a STRING 'docker.exe' plus a separate args list (NOT utcp.py's single combined array)."
    - "native_http_prototype.py http server binds 127.0.0.1 only; UtcpClient registers an http call-template against it."
    - "full_agent_chat.py → ChatOpenAI(base_url=local llama.cpp) → UtcpAgent.create(utcp_config with mcpServers dict)."
  prohibitions:
    - "MUST NOT write any of these scripts, or any UTCP native-serving code, under src/ — they live only in scripts/spike/ (SC#3)."
    - "MUST NOT add the D-06 http prototype (or a llama.cpp chat sidecar) as a service in compose.yaml."
    - "MUST NOT bind the native-http prototype to 0.0.0.0 — 127.0.0.1 only; tear down immediately after evidence is gathered."
    - "MUST NOT silently skip the D-08a full-agent chat when no GPU is present — record the non-exercise explicitly."
    - "MUST NOT print or persist a real AGENTMEMORY_AUTH_TOKEN — dummy-only, with a no-leak assertion."
---

<objective>
Gather the LIVE half of SC#1 evidence and the D-04 verdict input by exercising the current UTCP
export against the REAL python-utcp client and the REAL `utcp-agent` consumer:
1. `scripts/spike/utcp_roundtrip.py` — the deterministic, LLM-free mcp call-template round-trip
   against the Dockerized MCP server (D-01/D-02/D-07/D-08).
2. `scripts/spike/native_http_prototype.py` — a throwaway 127.0.0.1 http endpoint for native-serving
   effort/behavior comparison (D-06).
3. `scripts/spike/full_agent_chat.py` — optional color: the real `utcp-agent` over a local llama.cpp
   Gemma endpoint, with an explicit GPU-fallback record (D-03/D-08a).

Purpose: SC#1 demands gaps surface as OBSERVED failures against a real client, and D-04 requires an
empirical answer to "does mcp-via-UTCP already satisfy the real consumer?" — both feed the D-09
verdict. All harness code is throwaway and stays out of src/ (SC#3).
Output: three spike scripts that print structured, no-leak evidence for FINDINGS.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-utcp-spike/02-CONTEXT.md
@.planning/phases/02-utcp-spike/02-RESEARCH.md
@.planning/phases/02-utcp-spike/02-PATTERNS.md
@src/turing_agentmemory_mcp/utcp.py
@src/turing_agentmemory_mcp/e2e_score_stubs.py
@tests/test_utcp_manual.py
</context>

## Artifacts this plan produces

- `scripts/spike/utcp_roundtrip.py` — `main() -> int` live mcp round-trip harness (D-01/02/07/08)
- `scripts/spike/native_http_prototype.py` — throwaway http prototype with `--self-test` (D-06)
- `scripts/spike/full_agent_chat.py` — optional utcp-agent + llama.cpp Gemma chat with GPU probe (D-03/08a)

(Whole-phase artifact list is enumerated in 02-01-PLAN.md.)

<tasks>

<task type="auto">
  <name>Task 1: Live mcp call-template round-trip harness (D-01/D-02/D-07/D-08)</name>
  <files>scripts/spike/utcp_roundtrip.py</files>
  <read_first>
    - .planning/phases/02-utcp-spike/02-RESEARCH.md §Architecture Patterns Pattern 1 (verbatim UtcpClient.create/register_manual/call_tool + hand-built McpCallTemplate/McpConfig), §Summary structural finding (list_tools live discovery), Common Pitfalls 2 & 3 (auth-enabled path; dependent GPU sidecars + cold-start timing), Anti-Patterns (a failed register_manual IS evidence)
    - .planning/phases/02-utcp-spike/02-PATTERNS.md §`scripts/spike/utcp_roundtrip.py` (docker argv shape from tests/test_utcp_manual.py lines 73-85; thin main() wrapper from scripts/e2e_score.py; no-leak discipline; command STRING + args LIST correction)
    - tests/test_utcp_manual.py (the exact docker.exe compose run --rm -T argv the mcpServers entry expects; no-leak assertion)
    - src/turing_agentmemory_mcp/utcp.py (what the current export emits — do NOT reuse its single-array command shape; build the corrected template by hand and document the gap)
  </read_first>
  <action>
    Create `scripts/spike/utcp_roundtrip.py` with `from __future__ import annotations`, a thin
    `if __name__ == "__main__": raise SystemExit(main())` wrapper over an async `main() -> int`
    (mirror scripts/e2e_score.py's shape). Derive the repo root and compose path from
    `Path(__file__).resolve().parents[2] / "compose.yaml"` — do NOT hardcode an absolute path.
    Build an `McpCallTemplate(name="turing-agentmemory-mcp", call_template_type="mcp", config=McpConfig(mcpServers={...}))`
    BY HAND per RESEARCH Pattern 1: the `mcpServers["turing-agentmemory-mcp"]` entry uses
    `"command": "docker.exe"` (a STRING) plus a SEPARATE `"args": ["compose","-f",<compose_path>,"run","--rm","-T","turing-agentmemory-mcp","serve","--transport","stdio"]` LIST. Explicitly note in a
    docstring/comment that this hand-corrected shape diverges from `utcp.py`'s single combined
    `command` array — that divergence IS documented SC#1 gap #2 (do not silently paper over it).
    Then: `client = await UtcpClient.create()`; `result = await client.register_manual(mcp_template)`;
    print `result.success` and, on success, `len(result.manual.tools)` (the LIVE FastMCP-discovered
    tool count — evidence for D-04/D-05 that discovery is live via `session.list_tools()`, not from our
    manual). Call `client.call_tool("turing-agentmemory-mcp.memory_store_message", {...})` then
    `client.call_tool("turing-agentmemory-mcp.memory_search", {"query": ...})` (Claude's Discretion:
    memory write→search is the representative, deterministic loop per RESEARCH Open Question #2). Treat a
    failed `register_manual`/`call_tool` as a first-class OBSERVED result: catch, print the structured
    error, and return a non-zero exit code — never swallow it. Support a `--dry-run` flag that builds and
    prints the McpCallTemplate JSON and exits 0 WITHOUT invoking Docker (the fast automated gate). If a
    dummy `AGENTMEMORY_AUTH_TOKEN` is ever set for the run, assert it is absent from any printed JSON
    (reuse the no-leak discipline from tests/test_utcp_manual.py). Handle the GPU-sidecar cold-start
    caveat (RESEARCH Pitfall 3) by documenting in the script header that dependent services must be
    healthy first (`docker compose up -d` deps, or stub EMBED/RERANK URLs) and by using a generous
    subprocess timeout so a slow-but-valid boot is not reported as a false negative. Keep < 600 LOC,
    ruff-clean.
  </action>
  <verify>
    <automated>python scripts/spike/utcp_roundtrip.py --dry-run</automated>
    <automated>python -m ruff check scripts/spike/utcp_roundtrip.py</automated>
    <human-check>Bring up compose dependencies (`docker compose up -d turingdb agentmemory-embed agentmemory-rerank agentmemory-gliner`, wait for health — up to ~20 min cold start), then run `python scripts/spike/utcp_roundtrip.py` and capture the structured output (register success + live tool count + both call_tool results, OR the observed failure). This live run is the SC#1 evidence for FINDINGS.md; ~10-20 min, Docker + GPU, not a per-commit gate (VALIDATION.md classifies it manual/scripted).</human-check>
  </verify>
  <acceptance_criteria>
    - `python scripts/spike/utcp_roundtrip.py --dry-run` exits 0 and prints an McpCallTemplate whose mcpServers entry has `command` == "docker.exe" (string) and a separate `args` list — proving the corrected shape and imports load without Docker.
    - Ruff clean; file < 600 LOC; compose path derived from `parents[2]`, not hardcoded.
    - The live run (human-check) produces structured evidence: register outcome, live-discovered tool count, and the memory write→search results OR a captured, non-swallowed failure with a non-zero exit code.
    - No real bearer token appears in any printed output.
  </acceptance_criteria>
  <done>The deterministic, LLM-free mcp round-trip harness exists, dry-run-verifiable offline, and produces real register/call evidence (success or observed failure) against the Dockerized server for the D-04 verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Throwaway native-http prototype for native-serving effort evidence (D-06)</name>
  <files>scripts/spike/native_http_prototype.py</files>
  <read_first>
    - .planning/phases/02-utcp-spike/02-RESEARCH.md §Architecture Patterns Pattern 3 (HttpCallTemplate url/http_method/body_field), Open Question #3 (wire to a real-ish call so the effort estimate is honest), §Security Domain (bind 127.0.0.1; tear down; never in compose.yaml)
    - .planning/phases/02-utcp-spike/02-PATTERNS.md §`scripts/spike/native_http_prototype.py` (stdlib ThreadingHTTPServer + BaseHTTPRequestHandler idiom, 127.0.0.1 bind, silent log_message, daemon thread)
    - src/turing_agentmemory_mcp/e2e_score_stubs.py (LocalEmbedServer/free_port shapes to mirror; note: real TuringDaemon needs turingdb, which has NO Windows wheel — see action for the honest stand-in)
  </read_first>
  <action>
    Create `scripts/spike/native_http_prototype.py` (`from __future__ import annotations`, thin
    `main()` wrapper). Stand up a stdlib `ThreadingHTTPServer` bound to `("127.0.0.1", <free_port>)`
    (reuse the `free_port()` + handler idiom from e2e_score_stubs.py) exposing ONE endpoint
    `POST /tools/memory_search`; the handler reads the JSON body (args wrapped under `{"body": {...}}`),
    reads `user_identifier` for tenant scoping, and returns a realistic `memory_search`-shaped JSON
    response. Per RESEARCH Open Question #3, make the effort estimate HONEST by exercising the real
    integration surface (endpoint routing, body unwrapping, tenant-scope read, output-schema mapping,
    an auth-header check) rather than a bare canned blob; because `turingdb` has no Windows wheel and
    this harness runs on the host, use an in-memory stand-in for the actual store lookup (do NOT import
    real turingdb on Windows — CLAUDE.md NEVER SUPPOSE / respect the platform constraint) and record in
    the script's printed output which backing mode was used. Then, in the same process, use the REAL
    client: `UtcpClient.create()`, register an `HttpCallTemplate(name=..., call_template_type="http",
    url="http://127.0.0.1:<port>/tools/memory_search", http_method="POST", content_type="application/json",
    body_field="body")`, call the tool once, print the result, and `server.shutdown()` in a `finally`
    block (tear-down guaranteed). Provide a `--self-test` flag (default behavior) that runs the full
    start→register→call→teardown cycle in-process and exits non-zero on any failure — this is fully
    automatable on the host (no Docker, no GPU). Print a short enumeration of the integration surface the
    prototype had to implement, as native-serving effort evidence for FINDINGS.md. Bind 127.0.0.1 ONLY;
    never 0.0.0.0. Keep < 600 LOC, ruff-clean.
  </action>
  <verify>
    <automated>python scripts/spike/native_http_prototype.py --self-test</automated>
    <automated>python -m ruff check scripts/spike/native_http_prototype.py</automated>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('scripts/spike/native_http_prototype.py').read_text(); sys.exit(1 if '0.0.0.0' in t else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/spike/native_http_prototype.py --self-test` starts the 127.0.0.1 server, registers an http call-template with the real UtcpClient, calls the tool, prints a result + the integration-surface enumeration, tears down, and exits 0.
    - The source binds 127.0.0.1 only (grep finds no `0.0.0.0`); ruff clean; < 600 LOC.
    - Printed output states the backing mode used (in-memory stand-in on host / real store if run where turingdb is importable) so the effort evidence is honest.
  </acceptance_criteria>
  <done>A throwaway, self-testing native-http prototype yields honest native-serving effort/behavior evidence, bound to localhost, never merged to src/ or compose.yaml.</done>
</task>

<task type="auto">
  <name>Task 3: Optional full utcp-agent chat over local llama.cpp Gemma with GPU-fallback record (D-03/D-08a)</name>
  <files>scripts/spike/full_agent_chat.py</files>
  <read_first>
    - .planning/phases/02-utcp-spike/02-CONTEXT.md D-08 & D-08a (LLM-free round-trip is the core evidence; full-agent chat is OPTIONAL color; GPU-mandatory with an explicit non-exercise record)
    - .planning/phases/02-utcp-spike/02-RESEARCH.md §Code Examples (utcp-agent `manual_call_templates` config dict with mcpServers), §Environment Availability (GPU present per nvidia-smi but the harness must still check+report), Anti-Patterns (never skip the GPU fallback silently)
    - .planning/phases/02-utcp-spike/02-PATTERNS.md §`scripts/spike/full_agent_chat.py` (ChatOpenAI(base_url=local llama.cpp) + UtcpAgent.create; docker/llama-provider.Dockerfile sidecar pattern, GGUF via LLAMA_CACHE, non-root)
    - docker/llama-provider.Dockerfile (the ghcr.io/ggml-org/llama.cpp:server-cuda pattern to reuse for a one-off run — NOT a compose.yaml service)
  </read_first>
  <action>
    Create `scripts/spike/full_agent_chat.py` (`from __future__ import annotations`, thin `main()`).
    First perform an explicit probe: check for GPU/endpoint availability (e.g. probe the local
    llama.cpp `/v1/chat/completions` base_url reachability and/or `nvidia-smi`). If unavailable, print
    and return a clear "full-agent chat NOT exercised (no GPU / endpoint unavailable)" line and exit 0 —
    RECORD the non-exercise, never silently skip (D-08a). Provide a `--check` flag that runs ONLY this
    probe (the automated gate) and exits 0 in both branches. When available: construct
    `ChatOpenAI(model="local-gemma", base_url=<llama.cpp>/v1, api_key="not-needed", temperature=0.1)`
    and `UtcpAgent.create(llm=llm, utcp_config={"manual_call_templates": [{"name":"turing-agentmemory-mcp",
    "call_template_type":"mcp","config":{"mcpServers":{"turing-agentmemory-mcp":{"command":"docker.exe",
    "args":[...same docker compose run --rm -T stdio argv, compose path via parents[2]...]}}}}]})`,
    then `await agent.chat("Store the note '...' and then search for it.")` and print the transcript as
    optional color evidence for FINDINGS.md. Document in the header that the Gemma GGUF
    (`unsloth/gemma-4-12B-it-qat-GGUF`, file `gemma-4-12B-it-qat-UD-Q4_K_XL.gguf`) is served via the
    repo's existing llama.cpp sidecar pattern as a THROWAWAY one-off (never added to compose.yaml, SC#3).
    Reuse the docker argv construction from Task 1 conceptually (the mcpServers dict is a plain literal
    here — no cross-plan import needed). Keep < 600 LOC, ruff-clean, no leaked secrets.
  </action>
  <verify>
    <automated>python scripts/spike/full_agent_chat.py --check</automated>
    <automated>python -m ruff check scripts/spike/full_agent_chat.py</automated>
    <human-check>If a GPU + local llama.cpp Gemma endpoint is available, run `python scripts/spike/full_agent_chat.py` and capture the chat transcript as optional color evidence. If not available, confirm the script printed the explicit "not exercised (no GPU)" record — this recorded non-exercise is the required behavior, not a skip.</human-check>
  </verify>
  <acceptance_criteria>
    - `python scripts/spike/full_agent_chat.py --check` exits 0 and prints either "GPU/endpoint available" or an explicit "full-agent chat NOT exercised (no GPU / endpoint unavailable)" record — never a silent skip.
    - Ruff clean; < 600 LOC; the mcpServers argv derives the compose path from `parents[2]`.
    - No real API key or bearer token is printed (dummy `api_key="not-needed"` only).
  </acceptance_criteria>
  <done>The optional D-08a full-agent path exists with a mandatory GPU-fallback record, driving the real utcp-agent consumer when a GPU is present and honestly recording non-exercise otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host client → Dockerized MCP (stdio) | UtcpClient on the host spawns `docker compose run --rm -T` and speaks MCP JSON-RPC over the container's stdio |
| host loopback → native-http prototype | The D-06 throwaway server accepts POSTs on 127.0.0.1 only |
| retrieved MCP content → printed evidence | Round-trip results are untrusted evidence; they must not carry a real bearer token into printed/committed output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | round-trip/full-agent scripts printing manual/auth JSON | low | mitigate | Only ever set a DUMMY AGENTMEMORY_AUTH_TOKEN; assert the token is absent from any printed JSON (mirror tests/test_utcp_manual.py). Not blocking. |
| T-02-02 | Elevation of Privilege / Information Disclosure | D-06 native-http prototype exposed on a host port | low | mitigate | Bind `127.0.0.1` only (never 0.0.0.0); tear down in a `finally` block immediately after evidence is gathered; never add to compose.yaml. Grep asserts no `0.0.0.0`. Not blocking. |
| T-02-SC | Tampering | one-off llama.cpp Gemma sidecar / GGUF download (D-08a) | low | mitigate | Reuse the repo's pinned `ghcr.io/ggml-org/llama.cpp:server-cuda` image + non-root pattern; GGUF pulled via LLAMA_CACHE; throwaway one-off, `.gitignore`d (`scripts/spike/*.gguf`), never a compose.yaml service. Not blocking. |
</threat_model>

<verification>
- `python scripts/spike/utcp_roundtrip.py --dry-run` exits 0 with the corrected McpCallTemplate shape.
- `python scripts/spike/native_http_prototype.py --self-test` exits 0 (full start→register→call→teardown).
- `python scripts/spike/full_agent_chat.py --check` exits 0 with an explicit availability/non-exercise record.
- `python -m ruff check scripts/spike` clean; every script < 600 LOC.
- `grep -rl "HttpCallTemplate\|utcp_http\|http_call_template" src/` is EMPTY (all http-template code lives only in scripts/spike/).
- Live evidence (human-check) captured for FINDINGS.md: mcp round-trip result + native-http effort surface (+ optional agent transcript or non-exercise record).
</verification>

<success_criteria>
The current UTCP export is exercised against the real python-utcp client (mcp path) and the real
`utcp-agent` consumer, a throwaway native-http prototype yields honest native-serving effort
evidence, and every observation (success or failure) is captured with no-leak discipline — all
throwaway, zero src/ build work, feeding the D-04/D-09 verdict.
</success_criteria>

<output>
Create `.planning/phases/02-utcp-spike/02-02-SUMMARY.md` when done.
</output>
