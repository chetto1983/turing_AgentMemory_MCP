---
phase: 02-utcp-spike
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/spike/requirements.txt
  - tests/test_utcp_conformance.py
  - .gitignore
autonomous: true
requirements: [UTCP-01]

must_haves:
  truths:
    - "Running `python -m pytest tests/test_utcp_conformance.py` with spike deps installed reproduces SC#1 gap #1: `utcp.py`'s auth-enabled manual raises a python-utcp validation error (api_key auth does not satisfy McpCallTemplate's Optional[OAuth2Auth]) — an OBSERVED failure, not a reasoned one."
    - "The same test reproduces SC#1 gap #3: README's `UTCP_CONFIG_FILE` `text`/`file_path` example is rejected by the current `TextCallTemplateSerializer`."
    - "Spike deps (utcp, utcp-mcp, utcp-http, utcp-text, utcp-agent) install from `scripts/spike/requirements.txt` and are NOT added to `pyproject.toml` — spike-only footprint that a later verdict (D-09) may gate, never presupposed here."
    - "The conformance test is committed UNMARKED and skips cleanly under CI (spike deps absent) — it never trips the Phase-1 no-skip-as-green guard, which enforces only `integration`/`gpu` markers (verified in tests/conftest.py)."
    - "This static evidence is one input to the D-09 FINDINGS verdict; it does not itself decide build/stay-manual/defer."
  artifacts:
    - scripts/spike/requirements.txt
    - "tests/test_utcp_conformance.py::test_manual_with_auth_fails_current_utcp_pydantic_validation"
    - "tests/test_utcp_conformance.py::test_readme_utcp_config_example_is_stale"
    - ".gitignore entries covering scripts/spike secrets + *.gguf"
  key_links:
    - "tests/test_utcp_conformance.py imports build_utcp_manual/utcp_manual_from_env from turing_agentmemory_mcp.utcp (behind the load-bearing turingdb sys.modules stub) and validates against the real utcp/utcp_text serializers."
  prohibitions:
    - "MUST NOT add utcp/utcp-mcp/utcp-http/utcp-text/utcp-agent to pyproject.toml dependencies or any extra — spike deps stay out-of-tree in scripts/spike/requirements.txt (SC#3)."
    - "MUST NOT commit any UTCP native-serving code under src/; this plan touches only tests/, scripts/spike/, and .gitignore."
    - "MUST NOT mark the conformance test `integration` or `gpu` — under CI (deps absent) that skip would FAIL the Phase-1 gate; keep it unmarked with pytest.importorskip."
    - "MUST NOT print or commit a real AGENTMEMORY_AUTH_TOKEN — dummy token only, and assert non-leakage."
---

<objective>
Produce the fast, Docker-free SC#1 static-conformance evidence: a committed pytest that runs the
current `utcp.py` manual through the REAL python-utcp pydantic serializers and reproduces two
source-verified gaps — (1) api_key auth on the mcp call-template fails validation against
`McpCallTemplate.auth: Optional[OAuth2Auth]`, and (3) the README `UTCP_CONFIG_FILE` `text`/`file_path`
example is rejected by the current `TextCallTemplateSerializer`. Also pin the spike-only UTCP
dependencies out-of-tree so later live-evidence plans (02-02) can install them.

Purpose: SC#1 requires the current export's gaps to be DOCUMENTED against a real UTCP client. This
plan gives the deterministic, seconds-fast half of that evidence (the live half is 02-02), feeding
the D-09 FINDINGS verdict without any src/ build work (SC#3).
Output: `scripts/spike/requirements.txt`, `tests/test_utcp_conformance.py` (two conformance tests),
and `.gitignore` hardening for spike secrets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-utcp-spike/02-CONTEXT.md
@.planning/phases/02-utcp-spike/02-RESEARCH.md
@.planning/phases/02-utcp-spike/02-PATTERNS.md
@src/turing_agentmemory_mcp/utcp.py
@tests/test_utcp_manual.py
@tests/conftest.py
</context>

## Artifacts this phase produces (whole phase, for reference)

- `scripts/spike/requirements.txt` — pinned spike-only UTCP deps (THIS plan)
- `tests/test_utcp_conformance.py` — static SC#1 conformance tests (THIS plan)
- `.gitignore` — spike secret/GGUF ignores (THIS plan)
- `scripts/spike/utcp_roundtrip.py` — live mcp round-trip harness `main() -> int` (plan 02-02)
- `scripts/spike/native_http_prototype.py` — D-06 throwaway http prototype `--self-test` (plan 02-02)
- `scripts/spike/full_agent_chat.py` — optional D-08a utcp-agent + llama.cpp chat (plan 02-02)
- `.planning/phases/02-utcp-spike/02-FINDINGS.md` — D-09 verdict deliverable (plan 02-03)
- Conditional (D-10, if verdict=build): a gated ROADMAP backlog entry + a PROJECT.md Key Decisions row (plan 02-03)

<tasks>

<task type="auto">
  <name>Task 1: Pin spike-only UTCP dependencies out-of-tree + harden .gitignore</name>
  <files>scripts/spike/requirements.txt, .gitignore</files>
  <read_first>
    - .planning/phases/02-utcp-spike/02-RESEARCH.md §Standard Stack (exact pinned versions), §Package Legitimacy Audit (all approved, spike-only), Open Question #1 (why these stay OUT of pyproject)
    - .planning/phases/02-utcp-spike/02-PATTERNS.md §`scripts/spike/requirements.txt` (flat pkg==version list, no analog)
    - .planning/phases/02-utcp-spike/02-RESEARCH.md §Security Domain (V14: no real secrets / no GGUF binaries committed under scripts/spike/)
  </read_first>
  <action>
    Create `scripts/spike/requirements.txt` as a flat, one-per-line pinned list EXACTLY:
    `utcp==1.1.3`, `utcp-mcp==1.1.2`, `utcp-http==1.1.11`, `utcp-text==1.1.0`, `utcp-agent==1.0.3`.
    Add a single trailing optional line for the D-08a color path: `langchain-openai>=1.0.0` and
    `python-dotenv` under a comment header line (`# optional D-08a full-agent chat only`). These are
    spike-only and MUST NOT be added to `pyproject.toml` `[project.dependencies]` or any extra
    (RESEARCH Open Question #1; SC#3 forbids presupposing UTCP build infrastructure). Before finalizing,
    re-verify each pinned version is still current with `python -m pip index versions <pkg>` (RESEARCH
    is fresh but marked "re-verify if >2 weeks"); if a version moved, pin the current one and note it in
    the SUMMARY — do NOT guess.
    Append to `.gitignore` (create the entries if absent) a `# Phase 2 UTCP spike (throwaway)` block
    ignoring `scripts/spike/*.gguf`, `scripts/spike/.env`, and `scripts/spike/*.local` so no downloaded
    Gemma GGUF binary and no real `AGENTMEMORY_AUTH_TOKEN`/API key can be committed (RESEARCH §Security
    Domain V14). Keep the requirements file and .gitignore ruff-irrelevant (non-Python) — no code.
  </action>
  <verify>
    <automated>python -m pip install -r scripts/spike/requirements.txt && python -c "import utcp, utcp_mcp, utcp_http, utcp_text, utcp_agent; print('spike deps import OK')"</automated>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('pyproject.toml').read_text(); sys.exit(1 if any(p in t for p in ['utcp==','utcp-mcp','utcp-http','utcp-text','utcp-agent']) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/requirements.txt` exists with the five pinned UTCP packages (>=5 `==` pins).
    - `python -m pip install -r scripts/spike/requirements.txt` succeeds and all five packages import.
    - No UTCP PyPI package appears in `pyproject.toml` (second automated check exits 0).
    - `.gitignore` ignores `scripts/spike/*.gguf`, `scripts/spike/.env`, `scripts/spike/*.local`.
  </acceptance_criteria>
  <done>Spike deps are installable from the out-of-tree requirements file, importable, absent from pyproject, and secret/GGUF ignores are in place.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Committed static-conformance pytest reproducing SC#1 gaps #1 and #3</name>
  <files>tests/test_utcp_conformance.py</files>
  <read_first>
    - .planning/phases/02-utcp-spike/02-RESEARCH.md §Summary findings #1 and #3 (exact source lines), §Architecture Patterns Pattern 2 (static conformance example), Common Pitfalls 1 & 2 (auth-enabled path is the one that raises; auth-free would look falsely green)
    - .planning/phases/02-utcp-spike/02-PATTERNS.md §`tests/test_utcp_conformance.py` (verbatim turingdb sys.modules stub, auth-enabled construction, no-leak assertion, README-stale pattern)
    - tests/test_utcp_manual.py (existing subject-module tests + the load-bearing `sys.modules["turingdb"]` shim at lines 10-11 + no-leak assertion at line 92)
    - tests/conftest.py (no-skip-as-green guard enforces ONLY `integration`/`gpu` markers — the reason this test stays UNMARKED)
    - src/turing_agentmemory_mcp/utcp.py (`build_utcp_manual`, `utcp_manual_from_env`, `_auth_env_placeholder` — the api_key auth block emitted when AGENTMEMORY_AUTH_TOKEN is set)
    - README.md lines 320-334 (the `call_template_type: "text"` + `file_path` example under test)
  </read_first>
  <behavior>
    - Test A `test_manual_with_auth_fails_current_utcp_pydantic_validation`: with a DUMMY `AGENTMEMORY_AUTH_TOKEN` set, `utcp_manual_from_env()` output is fed to `UtcpManualSerializer().validate_dict(...)` and MUST raise the python-utcp validation error (api_key auth ≠ Optional[OAuth2Auth]); the raw dummy token MUST NOT appear in `json.dumps(manual)`.
    - Test B `test_readme_utcp_config_example_is_stale`: README.md contains both `call_template_type` and `file_path` (documents the stale shape today) AND `TextCallTemplateSerializer().validate_dict({"call_template_type":"text","file_path":"..."})` MUST raise (current serializer rejects `file_path`).
    - RED→GREEN semantics (honest): the assertions describe the KNOWN gaps; GREEN = they hold because the real python-utcp models genuinely reject our output. This is a characterization/conformance test — it "passes" by proving the gaps exist, not by exercising a feature.
    - Under CI (spike deps absent) the test module skips cleanly via `pytest.importorskip` and is UNMARKED, so the Phase-1 no-skip-as-green guard (integration/gpu only) does not fail it.
  </behavior>
  <action>
    Create `tests/test_utcp_conformance.py` starting with `from __future__ import annotations`, then
    `import json, sys, types`, `from pathlib import Path`, `import pytest`. Call
    `pytest.importorskip("utcp")` and `pytest.importorskip("utcp_text")` at module top so the file
    skips cleanly when spike deps are not installed (CI). Immediately after, install the load-bearing
    turingdb shim VERBATIM from tests/test_utcp_manual.py
    (`if "turingdb" not in sys.modules: sys.modules["turingdb"] = types.SimpleNamespace(TuringDB=object, __version__="test")`)
    BEFORE importing `from turing_agentmemory_mcp.utcp import build_utcp_manual, utcp_manual_from_env`.
    Do NOT mark the module `integration`/`gpu`/`slow` — leave it unmarked (surfaced decision per RESEARCH
    Open Question #1: unmarked + importorskip is the only shape that keeps zero pyproject footprint AND
    passes CI, since the guard enforces only integration/gpu).
    Implement Test A per the behavior block: set `AGENTMEMORY_UTCP_SERVER_NAME` and a DUMMY
    `AGENTMEMORY_AUTH_TOKEN` (e.g. "throwaway-dummy-token") via monkeypatch, build the manual with
    `utcp_manual_from_env()`, import the serializer + validation-error type from the INSTALLED utcp
    package (RESEARCH cites `utcp.data.utcp_manual.UtcpManualSerializer` and
    `utcp.exceptions.UtcpSerializerValidationError`; if either import path differs in the installed
    version, confirm the real symbol against the installed package rather than guessing — CLAUDE.md
    NEVER SUPPOSE), assert the validation raises, and assert the dummy token is absent from
    `json.dumps(manual)`.
    Implement Test B per the behavior block: read README.md via `Path(__file__).resolve().parents[1]`,
    assert it contains `call_template_type` and `file_path`, then import
    `TextCallTemplateSerializer` from `utcp_text.text_call_template` and assert `validate_dict` on a
    minimal `{"call_template_type":"text","file_path":...}` raises the serializer validation error.
    Keep the file well under 600 LOC; ruff-clean (E,F,I,B,UP); no comments except where a "why" is
    non-obvious (e.g. one line explaining the unmarked+importorskip choice).
  </action>
  <verify>
    <automated>python -m pytest tests/test_utcp_conformance.py -q</automated>
    <automated>python -m ruff check tests/test_utcp_conformance.py</automated>
    <automated>bash scripts/check-file-size.sh tests/test_utcp_conformance.py 2>/dev/null || python -c "import pathlib,sys; n=len(pathlib.Path('tests/test_utcp_conformance.py').read_text().splitlines()); print('LOC',n); sys.exit(1 if n>600 else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_utcp_conformance.py -q` reports 2 passed (spike deps installed by Task 1) in seconds, with no Docker.
    - Test A raises the real python-utcp validation error with a dummy token, and the token never appears in the dumped manual JSON.
    - Test B asserts README staleness AND that the current `TextCallTemplateSerializer` rejects `file_path`.
    - Ruff clean; file < 600 LOC; module is UNMARKED and skips (not errors) when utcp is absent.
  </acceptance_criteria>
  <done>Both SC#1 static gaps are reproduced as observed pydantic-validation failures by a committed, ruff-clean, CI-safe pytest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike test env → committed repo | A dummy auth token set for the conformance test must never be persisted or printed into committed artifacts |
| PyPI → local venv | Spike-only UTCP packages are installed into the venv for evidence; they are audited (RESEARCH §Package Legitimacy Audit) but not project deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | conformance test setting AGENTMEMORY_AUTH_TOKEN | low | mitigate | Only ever set a DUMMY token in the test env; assert the token string is absent from `json.dumps(manual)` (mirrors tests/test_utcp_manual.py line 92). Not blocking (dummy-only). |
| T-02-SC | Tampering | pip install of utcp/utcp-mcp/utcp-http/utcp-text/utcp-agent | low | mitigate | Pinned exact versions from the org-verified RESEARCH §Package Legitimacy Audit (all `OK`/approved, org-backed); spike-scoped in scripts/spike/requirements.txt, never added to pyproject. `mcp-use` arrives only transitively (SUS-informational, not a direct install target) — no human checkpoint required. Not blocking. |
</threat_model>

<verification>
- `python -m pip install -r scripts/spike/requirements.txt` then `python -m pytest tests/test_utcp_conformance.py -q` → 2 passed.
- `python -m ruff check tests scripts` clean for the new files.
- `grep -rl "HttpCallTemplate\|utcp_http\|http_call_template" src/` is EMPTY (no src/ touched this plan).
- No UTCP PyPI package present in `pyproject.toml`.
</verification>

<success_criteria>
The two SC#1 static gaps (auth-type mismatch; stale README `file_path` example) are reproduced as
real python-utcp validation failures by a committed, CI-safe, ruff-clean pytest, and the spike-only
UTCP deps are pinned out-of-tree with secret/GGUF ignores — zero src/ build work.
</success_criteria>

<output>
Create `.planning/phases/02-utcp-spike/02-01-SUMMARY.md` when done.
</output>
