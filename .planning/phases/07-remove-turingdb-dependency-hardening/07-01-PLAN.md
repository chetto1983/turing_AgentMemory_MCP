---
phase: 07-remove-turingdb-dependency-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/benchmark.py
  - src/turing_agentmemory_mcp/benchmark_stages.py
  - src/turing_agentmemory_mcp/benchmark_memoryarena.py
  - src/turing_agentmemory_mcp/benchmark_schema.py
  - src/turing_agentmemory_mcp/agent_quality_eval.py
  - src/turing_agentmemory_mcp/admin_repair.py
  - src/turing_agentmemory_mcp/cli.py
  - scripts/benchmark.py
  - scripts/agent_quality_eval.py
  - tests/test_benchmark.py
  - tests/test_agent_quality_eval.py
  - tests/test_admin_repair.py
  - .gitignore
autonomous: true
requirements: [ARC-10]
must_haves:
  truths:
    - "gate_guard.assert_gate_go(baseline/06-gate/gate-result.json) succeeds (verdict == GO) before any deletion — the irreversible removal is authorized."
    - "The legacy TuringDB benchmark/eval harness cluster (benchmark.py + 3 siblings + agent_quality_eval.py + 2 scripts wrappers) no longer exists in the tree."
    - "admin_repair.py and its CLI dispatch (repair-vector-index) are gone; no ArcadeDB-meaningless CSV-vector-repair path remains."
    - "The cli.py subcommand surface still constructs and the retained subcommands (serve, file-pipe, e2e-score, utcp-manual, lab) are untouched."
  artifacts:
    - "src/turing_agentmemory_mcp/cli.py (rewritten: agent-quality-eval + repair-vector-index subparsers and dispatch removed)"
  key_links:
    - "cli.py argparse subparser wiring — removing the two dead subcommands must not break the five retained ones."
  prohibitions:
    - "MUST NOT bypass, weaken, or skip gate_guard.assert_gate_go — a missing/malformed/NO_GO artifact fails closed and blocks the phase."
    - "MUST NOT delete baseline/03-turingdb/ or baseline/06-gate/ provenance artifacts."
    - "MUST NOT delete a still-CLI-wired path — confirm supersession/non-use before each delete (fix-on-touch)."
---

<!-- planner-discipline-allow: import turingdb -->
<!-- planner-discipline-allow: from turingdb -->

<objective>
Assert the Phase-6 GO gate (the precondition that authorizes this irreversible removal), then delete the confirmed-dead TuringDB-only legacy harness cluster and the ArcadeDB-meaningless `admin_repair.py` repair tooling, pruning their CLI dispatch. This is D-01's "full purge" of the code ArcadeDB obsoletes.

Purpose: Remove the largest block of dead TuringDB-coupled code first so that the surviving `from turingdb import ...` sites (stripped in Plan 02) and the 31-file test stub (swept in Plan 04) become removable without breaking collection.
Output: A smaller `src/` with the benchmark/eval/repair cluster gone and a clean `cli.py` exposing only live subcommands.

## Artifacts This Phase Produces (this plan)
- None created. This plan only deletes files and edits `cli.py`/`.gitignore`. Recorded discretion decision: **delete the whole of `admin_repair.py` (all three functions), not just `repair_vector_index`** — see Task 3 rationale.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the Phase-6 GO gate (irreversible-removal precondition)</name>
  <files>(no files modified — reads gate_guard.py + baseline/06-gate/gate-result.json as a precondition assertion)</files>
  <read_first>
    - src/turing_agentmemory_mcp/gate_guard.py (assert_gate_go — already built in Phase 6; consume, do NOT rebuild)
    - baseline/06-gate/GATE.md and baseline/06-gate/gate-result.json (the committed GO verdict — verdict: "GO")
    - .planning/phases/06-migration-correctness-gate/06-CONTEXT.md (D-10 guard contract)
  </read_first>
  <action>
    Run the existing `turing_agentmemory_mcp.gate_guard.assert_gate_go(Path("baseline/06-gate/gate-result.json"))` as the phase-entry precondition. Do NOT author any new guard code — `gate_guard.py` is fully built and tested in Phase 6 (tests/test_gate_artifact_schema.py, tests/test_phase7_gate_guard.py). It reads the artifact fresh (no caching), validates the D-09 schema, and raises AssertionError unless `verdict == "GO"`. If it raises, STOP the entire phase — the migration-correctness gate has not authorized removal. Do NOT delete or modify baseline/06-gate/ or baseline/03-turingdb/ (yardstick/gate provenance).
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; from turing_agentmemory_mcp.gate_guard import assert_gate_go; assert_gate_go(Path('baseline/06-gate/gate-result.json')); print('GO')"</automated>
  </verify>
  <done>The command prints GO and exits 0; the committed baseline/06-gate/ and baseline/03-turingdb/ trees are unmodified.</done>
  <acceptance_criteria>
    - `python -c "... assert_gate_go(Path('baseline/06-gate/gate-result.json'))"` exits 0 and prints GO.
    - `git status --porcelain baseline/` shows no changes to baseline/03-turingdb/ or baseline/06-gate/.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete the legacy TuringDB benchmark/eval harness cluster</name>
  <files>src/turing_agentmemory_mcp/benchmark.py, benchmark_stages.py, benchmark_memoryarena.py, benchmark_schema.py, agent_quality_eval.py (delete); scripts/benchmark.py, scripts/agent_quality_eval.py (delete); tests/test_benchmark.py, tests/test_agent_quality_eval.py (delete); .gitignore (edit)</files>
  <read_first>
    - src/turing_agentmemory_mcp/e2e_score.py (its docstring confirms benchmark.py is "legacy, still-TuringDB-backed" and that e2e_score.py + real_document_benchmark.py supersede it)
    - src/turing_agentmemory_mcp/benchmark_schema.py (holds the dead `from turingdb import __version__ as turingdb_version`)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md ("Legacy harness cluster — DELETE" table, with per-file supersession evidence)
  </read_first>
  <action>
    `git rm` the confirmed-dead legacy harness cluster (all deletions, near-zero context each): src/turing_agentmemory_mcp/benchmark.py, benchmark_stages.py, benchmark_memoryarena.py, benchmark_schema.py, agent_quality_eval.py; their two thin CLI wrappers scripts/benchmark.py and scripts/agent_quality_eval.py; and their direct tests tests/test_benchmark.py and tests/test_agent_quality_eval.py. Fix-on-touch guard: before each delete confirm no LIVE path still imports it — RESEARCH verified the only importers are inside this same doomed cluster. Do NOT touch src/turing_agentmemory_mcp/memoryarena.py (KEEP — imported by the live e2e_score_scenarios.py; it is a different file from the deleted benchmark_memoryarena.py despite the similar name) and do NOT touch scripts/real_document_benchmark.py or tests/test_real_document_benchmark.py (the KEPT ArcadeDB-era gate script). Also remove the now-dead `.turingdb/` entry from `.gitignore` (its sole creator was agent_quality_eval.py's TURINGDB_AGENT_QUALITY_HOME).
  </action>
  <verify>
    <automated>python -m pytest tests/ --collect-only -q 2>&1 | grep -viE "warning" | grep -iE "error|no module named 'turing_agentmemory_mcp.benchmark'|no module named 'turing_agentmemory_mcp.agent_quality_eval'" ; test $? -ne 0</automated>
  </verify>
  <done>The 9 cluster files are gone; memoryarena.py and real_document_benchmark.py remain; test collection does not error on any deleted module; `.gitignore` no longer lists `.turingdb/`.</done>
  <acceptance_criteria>
    - `test ! -f src/turing_agentmemory_mcp/benchmark.py -a ! -f src/turing_agentmemory_mcp/benchmark_stages.py -a ! -f src/turing_agentmemory_mcp/benchmark_memoryarena.py -a ! -f src/turing_agentmemory_mcp/benchmark_schema.py -a ! -f src/turing_agentmemory_mcp/agent_quality_eval.py` exits 0.
    - `test -f src/turing_agentmemory_mcp/memoryarena.py -a -f scripts/real_document_benchmark.py` exits 0 (KEEP files intact).
    - `grep -c "\.turingdb/" .gitignore` returns 0.
    - `python -m pytest tests/ --collect-only -q` reports no collection error referencing a deleted module.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete admin_repair.py and prune both dead cli.py subcommands</name>
  <files>src/turing_agentmemory_mcp/admin_repair.py (delete), tests/test_admin_repair.py (delete), src/turing_agentmemory_mcp/cli.py (edit)</files>
  <read_first>
    - src/turing_agentmemory_mcp/admin_repair.py (three functions: repair_vector_index [CLI-wired, TuringDB-CSV-specific], repair_sparse_projection + repair_community_projection [never CLI-wired])
    - src/turing_agentmemory_mcp/cli.py (the argparse subparsers + dispatch for agent-quality-eval [lines ~27-34, ~74-88] and repair-vector-index [lines ~45-51, ~108-117])
    - src/turing_agentmemory_mcp/store_evidence.py (lines ~116-119: SparseIndex/SQLite-FTS5 is already dead for reads — evidence repair_sparse_projection targets a dead path)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md ("admin_repair.py — split verdict" table + Pitfall 3)
  </read_first>
  <action>
    **Discretion decision (LOCKED here, documented per RESEARCH Pitfall 3):** delete the ENTIRE `admin_repair.py` file — all three functions — plus tests/test_admin_repair.py. Rationale: `repair_vector_index` quarantines a TuringDB CSV `vector/` directory that does not exist under ArcadeDB native LSM_VECTOR/HNSW (meaningless on the new backend); `repair_sparse_projection` rebuilds the SQLite-FTS5 SparseIndex that store_evidence.py:116-119 proves is never consulted for reads anymore; `repair_community_projection` is never wired to any CLI command. All three are dead/unreachable in the ArcadeDB reality — the aggressive debt-clearing path the user chose (D-01). `git rm src/turing_agentmemory_mcp/admin_repair.py tests/test_admin_repair.py`. Then edit `cli.py` to remove BOTH the `agent-quality-eval` subparser+dispatch (dead after Task 2) AND the `repair-vector-index` subparser+dispatch (its `from turing_agentmemory_mcp.admin_repair import repair_vector_index`). Leave the `serve`, `file-pipe`, `e2e-score`, `utcp-manual`, and `lab` subcommands and their dispatch byte-for-byte unchanged. Run `ruff format` + `ruff check` on cli.py after the edit.
  </action>
  <verify>
    <automated>python -c "import turing_agentmemory_mcp.cli" && python -m turing_agentmemory_mcp.cli --help 2>&1 | grep -viE "agent-quality-eval|repair-vector-index" | grep -qE "serve" && echo OK</automated>
  </verify>
  <done>admin_repair.py + its test are gone; cli.py imports cleanly, its --help lists serve/file-pipe/e2e-score/utcp-manual/lab and neither agent-quality-eval nor repair-vector-index; ruff format --check and ruff check pass on cli.py.</done>
  <acceptance_criteria>
    - `test ! -f src/turing_agentmemory_mcp/admin_repair.py -a ! -f tests/test_admin_repair.py` exits 0.
    - `grep -rn "admin_repair" src/ tests/` returns 0 matches.
    - `python -m turing_agentmemory_mcp.cli --help` output contains `serve`, `file-pipe`, `e2e-score`, `utcp-manual`, `lab` and does NOT contain `agent-quality-eval` or `repair-vector-index`.
    - `python -m ruff format --check src/turing_agentmemory_mcp/cli.py` and `python -m ruff check src/turing_agentmemory_mcp/cli.py` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase-entry → irreversible removal | The Phase-6 GO verdict is the only authorization for deleting TuringDB code; deletion is not reversible without git history. |
| operator CLI → subcommand dispatch | `cli.py` argparse routing; removing dead subcommands must not alter the live dispatch surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | gate_guard entry precondition | high | mitigate | Task 1 calls the existing fail-closed `assert_gate_go`; a missing/malformed/NO_GO artifact raises and stops the phase. No new guard code, no skip path. |
| T-07-06 | Tampering | fix-on-touch deletion of a still-wired path | medium | mitigate | Each delete is gated on RESEARCH's verified supersession evidence; the admin_repair whole-file delete is an explicit documented discretion decision, not a silent side effect. |
| T-07-SC | Tampering | supply-chain (package installs) | low | accept | No package installs this phase (removal-only); RESEARCH Package Legitimacy Gate confirmed N/A. |
</threat_model>

<verification>
- `python -c "... assert_gate_go(...)"` prints GO.
- Deleted cluster + admin_repair absent; memoryarena.py + real_document_benchmark.py intact.
- `python -m pytest tests/ --collect-only -q` clean of deleted-module errors.
- `python -m turing_agentmemory_mcp.cli --help` shows only the five retained subcommands.
- `python -m ruff format --check src tests scripts` and `python -m ruff check src tests scripts` pass.
</verification>

<success_criteria>
Phase-6 GO gate asserted; the legacy benchmark/eval harness cluster and admin_repair.py are deleted; cli.py is pruned to the five live subcommands and imports cleanly; baseline/ provenance untouched; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/07-remove-turingdb-dependency-hardening/07-01-SUMMARY.md` when done.
</output>
