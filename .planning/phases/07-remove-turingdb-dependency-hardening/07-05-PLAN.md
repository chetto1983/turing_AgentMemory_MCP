---
phase: 07-remove-turingdb-dependency-hardening
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - tests/test_no_turingdb_imports.py
  - tests/test_graspologic_compat.py
  - tests/test_warning_filters.py
autonomous: true
requirements: [ARC-10, DEP-01, DEP-02]
must_haves:
  truths:
    - "A new grep-gate test asserts zero occurrences of the two turingdb import forms across all of src/turing_agentmemory_mcp/*.py (removal completeness, ARC-10)."
    - "graspologic-native installed version equals the exact pin 1.3.1, and a live hierarchical_leiden smoke over a tiny graph returns final-cluster records covering all nodes (DEP-01)."
    - "fastmcp installed version satisfies >=3.4,<4 at the range endpoints (major==3 and minor>=4, or checked as major<4), and create_mcp_app(store=object()).list_tools() returns a deterministic tool set of at least 20 tools (DEP-02)."
    - "None of the DEP-01/DEP-02 compat tests contain a pytest.skip path — a skip would fail, not pass green (no-skip-as-green by construction)."
  artifacts:
    - "tests/test_no_turingdb_imports.py (new grep-gate)"
    - "tests/test_graspologic_compat.py (new DEP-01 compat-smoke)"
    - "tests/test_warning_filters.py (extended with DEP-02 fastmcp compat-smoke)"
  key_links:
    - "The grep-gate scans src/ only, so it goes green once Plans 01+02 land; the DEP tests exercise community_detection.py's real Leiden signature and server.py's real create_mcp_app."
  prohibitions:
    - "MUST NOT add a pytest.skip to any compat test — both deps are unconditional pyproject dependencies, so there is no legitimate skip condition."
    - "MUST NOT duplicate the existing fastmcp pin-string assertion — extend tests/test_warning_filters.py, do not shadow it with a second overlapping file."
---

<!-- planner-discipline-allow: import turingdb -->
<!-- planner-discipline-allow: from turingdb -->

<objective>
Add the phase's own verification oracles: a src-wide no-turingdb-import grep-gate (D-04 cut proof / ARC-10), a graspologic-native version-pin + live Leiden smoke (DEP-01), and a fastmcp version-range + app-construction smoke extending the existing test_warning_filters.py (DEP-02). These are the tests themselves — their RED is a real failing assertion on a violation, so each is a first-class artifact, not production logic needing its own separate test.

Purpose: A future graspologic-native/fastmcp version bump that changes behavior must fail loudly on CI before adoption; and any residual turingdb import in src must fail the grep-gate.
Output: Three test files (two new, one extended) that pass on today's pinned/installed environment and fail only on drift.

## Artifacts This Phase Produces (this plan)
- tests/test_no_turingdb_imports.py — new; function test_no_turingdb_import_anywhere_in_src scanning src/turing_agentmemory_mcp/*.py.
- tests/test_graspologic_compat.py — new; functions test_graspologic_compat_version_is_pinned and test_graspologic_compat_hierarchical_leiden_smoke (matches the VALIDATION oracle -k graspologic_compat).
- tests/test_warning_filters.py — extended with test_fastmcp_compat_installed_version_satisfies_pin and test_fastmcp_compat_create_mcp_app_registers_tools (matches -k fastmcp_compat).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-remove-turingdb-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the src-wide no-turingdb-import grep-gate</name>
  <files>tests/test_no_turingdb_imports.py (new)</files>
  <read_first>
    - tests/test_store_arcadedb_core.py (line 163: test_seam_contains_no_turingdb_write_primitives_or_csv_vector_load — the existing single-file forbidden-substring model to generalize; keep it, do not fold into it)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (No-import Grep-Gate Guard section — the exact rglob loop shape)
  </read_first>
  <action>
    Create tests/test_no_turingdb_imports.py with one function test_no_turingdb_import_anywhere_in_src that resolves the src package root (Path(__file__).resolve().parents[1] / "src" / "turing_agentmemory_mcp"), rglobs every *.py, reads each file's text, and collects any file whose text contains the substring for either turingdb import form; assert the offender list is empty with a message naming the offenders. Generalize the existing single-file pattern from test_store_arcadedb_core.py to the whole tree; do NOT modify or remove that existing narrower guard (it also forbids store_core.py-specific TuringDB write primitives). No pytest.skip. This new test must be green because Plans 01+02 already removed every src-side turingdb import — if it fails, it correctly flags an unfinished src removal.
  </action>
  <verify>
    <automated>python -m pytest tests/test_no_turingdb_imports.py -q</automated>
  </verify>
  <done>tests/test_no_turingdb_imports.py exists and passes; it scans all of src/turing_agentmemory_mcp and asserts zero turingdb imports; the existing store_core seam guard is untouched.</done>
  <acceptance_criteria>
    - python -m pytest tests/test_no_turingdb_imports.py -q exits 0.
    - The test reads from a src root path built with rglob("*.py") (not a hardcoded file list).
    - grep -c "pytest.skip" tests/test_no_turingdb_imports.py returns 0.
    - python -m pytest tests/test_store_arcadedb_core.py -q still exits 0 (existing guard preserved).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the DEP-01 graspologic-native compat-smoke</name>
  <files>tests/test_graspologic_compat.py (new)</files>
  <read_first>
    - src/turing_agentmemory_mcp/community_detection.py (lines ~315-322 and ~153-161: the live hierarchical_leiden call signature — edges as list of (source_id, target_id, weight); resolution=1.0, randomness=0.001, iterations=2, max_cluster_size=100, seed=42; records expose is_final_cluster/node/level/cluster)
    - pyproject.toml (the graspologic-native==1.3.1 exact pin)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (DEP-01 Compat-Smoke Test Shape section)
  </read_first>
  <action>
    Create tests/test_graspologic_compat.py with two functions. test_graspologic_compat_version_is_pinned asserts importlib.metadata.version("graspologic-native") == "1.3.1" (matches the pyproject exact pin). test_graspologic_compat_hierarchical_leiden_smoke imports hierarchical_leiden from graspologic_native, builds a tiny connected graph (edges [("a","b",1.0),("b","c",1.0)]), calls it with the exact verified signature (resolution=1.0, randomness=0.001, iterations=2, max_cluster_size=100, seed=42), filters records where bool(r.is_final_cluster) is true, asserts the final list is non-empty and the set of assigned str(r.node) equals {"a","b","c"}. No pytest.skip — graspologic-native is an unconditional dependency, so a missing package is a hard error, not a skip.
  </action>
  <verify>
    <automated>python -m pytest tests/test_graspologic_compat.py -q</automated>
  </verify>
  <done>tests/test_graspologic_compat.py passes on the installed 1.3.1; the Leiden smoke exercises the real package (not a fake backend) with the exact live call signature; no skip path.</done>
  <acceptance_criteria>
    - python -m pytest -q -k graspologic_compat exits 0 (matches the VALIDATION oracle).
    - The version assertion uses importlib.metadata.version and the exact string "1.3.1".
    - grep -c "pytest.skip" tests/test_graspologic_compat.py returns 0.
    - The smoke calls hierarchical_leiden with seed=42, iterations=2, max_cluster_size=100 (grep confirms the signature).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend test_warning_filters.py with the DEP-02 fastmcp compat-smoke</name>
  <files>tests/test_warning_filters.py (extend)</files>
  <read_first>
    - tests/test_warning_filters.py (the existing test_project_requires_fastmcp_v3 pin-string assertion and test_fastmcp_import_is_deprecation_clean subprocess version check — extend, do not shadow)
    - tests/test_auth.py (line ~48: the create_mcp_app(store=object()) pattern that avoids a live ArcadeDB connection)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (DEP-02 Compat-Smoke Test Shape section — 26 tools registered today)
  </read_first>
  <action>
    Add two functions to tests/test_warning_filters.py (do not create a new overlapping file). test_fastmcp_compat_installed_version_satisfies_pin parses importlib.metadata.version("fastmcp") into (major, minor) and asserts (major, minor) >= (3, 4) and major < 4 — the >=3.4,<4 range endpoints. test_fastmcp_compat_create_mcp_app_registers_tools imports create_mcp_app from turing_agentmemory_mcp.server, constructs app = create_mcp_app(store=object()) (dummy store, no live ArcadeDB, same pattern as test_auth.py:48), runs asyncio.run(app.list_tools()), and asserts len(tools) >= 20 (a loose deterministic floor; 26 register today; a breaking bump that silently drops registration must fail this). No pytest.skip — fastmcp is an unconditional dependency.
  </action>
  <verify>
    <automated>python -m pytest tests/test_warning_filters.py -q</automated>
  </verify>
  <done>test_warning_filters.py now also range-checks fastmcp and smoke-constructs the app with a >=20 tool floor; the two pre-existing tests still pass; no skip path.</done>
  <acceptance_criteria>
    - python -m pytest -q -k fastmcp_compat exits 0 (matches the VALIDATION oracle) and covers both new functions.
    - python -m pytest tests/test_warning_filters.py -q exits 0 (existing tests preserved).
    - grep -c "pytest.skip" tests/test_warning_filters.py returns 0.
    - The app smoke uses create_mcp_app(store=object()) and asserts a tool-count floor of at least 20.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned dependency contract → future upgrade | A graspologic-native/fastmcp bump can change API/return shape; the compat tests are the drift tripwire run on every CI push. |
| src tree → removal-completeness oracle | The grep-gate is an independent, non-stub oracle proving no turingdb import remains. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | graspologic-native / fastmcp version drift | high | mitigate | DEP-01/DEP-02 compat-smoke assert the exact/ranged installed version AND exercise the real API (Leiden clustering, app construction); a breaking bump fails CI before adoption. No skip path (no-skip-as-green by construction). |
| T-07-05 | Tampering | residual turingdb import in src | high | mitigate | The grep-gate scans all of src/ independently of the removed test stub — a missed import fails loudly. |
| T-07-SC | Tampering | supply-chain | low | accept | No new packages installed; tests run against already-pinned deps via stdlib importlib.metadata. |
</threat_model>

<verification>
- python -m pytest tests/test_no_turingdb_imports.py tests/test_graspologic_compat.py tests/test_warning_filters.py -q passes.
- python -m pytest -q -k graspologic_compat and -k fastmcp_compat both pass.
- grep -rc "pytest.skip" across the three files returns 0.
- python -m ruff format --check tests and python -m ruff check tests pass.
</verification>

<success_criteria>
The three compat/grep-gate oracles exist, pass on today's pinned environment, contain no skip path, and will fail on turingdb-import drift, a graspologic-native version change, or a fastmcp range/registration break.
</success_criteria>

<output>
Create `.planning/phases/07-remove-turingdb-dependency-hardening/07-05-SUMMARY.md` when done.
</output>
