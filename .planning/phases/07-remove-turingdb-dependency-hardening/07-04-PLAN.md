---
phase: 07-remove-turingdb-dependency-hardening
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - tests/_arcadedb_physical_isolation_support.py
  - tests/_store_arcadedb_core_shared.py
  - tests/_retrieval_arcadedb_shared.py
  - tests/_documents_arcadedb_shared.py
  - tests/_batch_memory_shared.py
  - tests/test_tenant_binding_enforcement.py
  - tests/test_tenant_telemetry_pseudonymity.py
  - tests/test_document_file_pipe.py
  - tests/test_document_ingest_file.py
  - tests/test_store_entity_processing.py
  - tests/test_store_arcadedb_retrieval.py
  - tests/test_store_arcadedb_rebuild.py
  - tests/test_store_arcadedb_memory.py
  - tests/test_store_arcadedb_documents.py
  - tests/test_runtime_pipeline.py
  - tests/test_stable_id_survives_rebuild.py
  - tests/test_retrieval_filters.py
  - tests/test_observability.py
  - tests/test_fused_memory_search.py
  - tests/test_community_detection.py
  - tests/test_batch_memory_write.py
  - tests/test_batch_memory_dedup.py
  - tests/test_batch_memory.py
  - tests/test_arcadedb_tenant_isolation.py
  - tests/test_utcp_conformance.py
  - tests/test_rerank.py
  - tests/test_backboard_locomo_runner.py
  - tests/test_utcp_manual.py
  - tests/test_server_batch_tool.py
  - tests/test_auth.py
autonomous: true
requirements: [ARC-10]
must_haves:
  truths:
    - "The 2-line sys.modules[turingdb] stub is removed from all 31 test/shared-fixture files that carried it."
    - "No test file stubs, imports, or references turingdb after this plan — the real package being gone is no longer masked."
    - "The full test suite still collects and passes with turingdb neither installed nor stubbed (proving Plans 01/02 removed every src-side import)."
  artifacts: []
  key_links:
    - "This plan is strictly downstream of Plans 01+02 — removing the stub before the src-side imports are gone would fail collection on all 31 files (RESEARCH Pitfall 1)."
  prohibitions:
    - "MUST NOT re-add the stub or a pytest.skip to paper over a collection failure — a failure here means a src-side turingdb import was missed and must be removed at its source."
---

<objective>
Remove the copy-pasted `sys.modules["turingdb"]` Windows import stub from all 31 test/shared-fixture files now that Plans 01+02 have removed every src-side `from turingdb import ...`. This is the final step of D-01's purge on the test surface.

Purpose: The stub existed only because src transitively imported turingdb (a package with no Windows wheel). With those imports gone, the stub is unnecessary and actively misleading — leaving it would mask that the real package was removed. Removing it makes the whole suite collect against a turingdb-free import graph.
Output: A test tree with zero turingdb stubs/imports, collecting green without turingdb installed.

## Artifacts This Phase Produces (this plan)
- None created. Stub removal only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-remove-turingdb-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the sys.modules[turingdb] stub from all 31 files</name>
  <files>the 29 tests/*.py + tests/_*_shared.py files listed in this plan's files_modified (the 31-file stub surface)</files>
  <read_first>
    - tests/_store_arcadedb_core_shared.py (lines 19-20: the exact 2-line stub pattern to remove; confirm the imports `sys`/`types` above it are used elsewhere before deciding whether they become unused)
    - tests/conftest.py (contains ONLY the no-skip-as-green guard — zero turingdb content; do NOT touch it)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (Test-stub sweep section — the full 31-file list + the exact 2-line pattern + the sequencing note)
  </read_first>
  <action>
    In each of the 31 files listed in this plan's files_modified, delete the exact 2-line stub block (the `if "turingdb" not in sys.modules:` guard and its `sys.modules["turingdb"] = types.SimpleNamespace(TuringDB=object, __version__="test")` body). After removal, if `import types` and/or `import sys` at the top of a file were there ONLY to support the stub and are now unused, remove them too (let ruff F401 confirm which are unused; do not remove sys/types if the file still uses them). Do NOT touch tests/conftest.py. Do NOT add any pytest.skip. This plan depends on Plans 01+02 having landed (Wave 1) — if any file fails collection with ModuleNotFoundError: turingdb, that means a src-side import was missed; STOP and record which src file still imports turingdb rather than re-adding the stub (RESEARCH Pitfall 1). Run ruff format + ruff check across tests.
  </action>
  <verify>
    <automated>python -m pytest tests/ --collect-only -q</automated>
  </verify>
  <done>All 31 files have the stub removed; the full suite collects with no ModuleNotFoundError: turingdb; no test file references turingdb; ruff clean.</done>
  <acceptance_criteria>
    - grep -rl "sys.modules\[.turingdb" tests/ returns 0 files.
    - grep -rnE "import turingdb|from turingdb|SimpleNamespace\(TuringDB" tests/ returns 0 matches (comment-only mentions may remain; live stubs/imports must be zero).
    - python -m pytest tests/ --collect-only -q exits 0 with no collection error.
    - tests/conftest.py is unchanged (git diff tests/conftest.py is empty).
    - python -m ruff check tests/ exits 0 (no unused sys/types imports left behind).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep the residual comment-only turingdb mentions in the test tree</name>
  <files>tests/test_gate_diff.py, tests/test_docker_hardening.py, tests/test_gliner_provider_extraction.py, tests/test_entity_extraction_http.py, tests/test_entity_extraction.py, tests/test_lab.py, tests/test_tenant_server_routing.py (prose only; NOT test_store_arcadedb_core.py's grep-gate literals)</files>
  <read_first>
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (the 10 comment-only files list: test_gate_diff.py, test_store_arcadedb_core.py, test_docker_hardening.py, test_gliner_provider_extraction.py, test_entity_extraction_http.py, test_entity_extraction.py, test_lab.py, test_tenant_server_routing.py — note test_compose_config.py is handled in Plan 03 and test_benchmark.py was deleted in Plan 01)
    - tests/test_store_arcadedb_core.py (line 165: test_seam grep-gate legitimately contains the literal "from turingdb" as a FORBIDDEN substring — do NOT remove that assertion; it is a live guard, not a stale mention)
  </read_first>
  <action>
    Update the remaining comment/docstring-only turingdb mentions in the test tree to read ArcadeDB by CONCEPT, EXCEPT the intentional forbidden-substring literals inside tests/test_store_arcadedb_core.py's test_seam_contains_no_turingdb_write_primitives_or_csv_vector_load (line 165) — those literals are the guard's assertion payload and MUST stay. Skip test_compose_config.py (Plan 03) and any file already deleted (Plan 01). This is a low-priority prose sweep; do not alter any test logic. Run ruff.
  </action>
  <verify>
    <automated>python -m pytest tests/test_store_arcadedb_core.py tests/test_lab.py -q</automated>
  </verify>
  <done>Residual comment-only turingdb mentions describe ArcadeDB; the test_seam grep-gate's forbidden-substring literals are preserved; the touched tests pass.</done>
  <acceptance_criteria>
    - python -m pytest tests/test_store_arcadedb_core.py -q exits 0 (the seam grep-gate still asserts "from turingdb" is absent from store_core.py).
    - grep -rnE "TuringDB|turingdb" tests/ returns only intentional grep-gate literals (test_store_arcadedb_core.py line ~165) and, until Plan 05 lands, the new-guard authoring — no stale prose mentions.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test import graph → collection | Removing the stub relies on Plans 01+02 having cleared every src-side turingdb import; a masked ModuleNotFoundError would give false confidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | 31-file stub giving false test confidence | high | mitigate | Stub removed strictly after src-side imports (wave ordering via depends_on 07-01/07-02); the Plan-05 grep-gate is an independent non-stub oracle; no pytest.skip or re-stub is permitted on failure. |
| T-07-SC | Tampering | supply-chain | low | accept | No installs; removal-only. |
</threat_model>

<verification>
- python -m pytest tests/ --collect-only -q collects clean with turingdb neither installed nor stubbed.
- grep -rl "sys.modules\[.turingdb" tests/ returns 0.
- tests/conftest.py unchanged; the test_seam grep-gate literals preserved.
- python -m ruff format --check tests and python -m ruff check tests pass.
</verification>

<success_criteria>
Zero turingdb stubs/live-imports remain in tests/; the suite collects and (per the phase cut-proof in Plan 08) passes green without turingdb installed or stubbed; the intentional grep-gate forbidden literals survive.
</success_criteria>

<output>
Create `.planning/phases/07-remove-turingdb-dependency-hardening/07-04-SUMMARY.md` when done.
</output>