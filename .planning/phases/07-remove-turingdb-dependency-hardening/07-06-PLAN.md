---
phase: 07-remove-turingdb-dependency-hardening
plan: 06
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - README.md
  - CHANGELOG.md
  - docs/configuration.md
  - docs/deployment.md
  - docs/operations.md
  - docs/security.md
  - docs/performance.md
  - docs/limitations.md
  - docs/publication/HACKER_NEWS.md
  - skills/turing-agentmemory/SKILL.md
  - skills/turing-agentmemory/references/architecture.md
  - skills/turing-agentmemory/references/operations.md
  - src/turing_agentmemory_mcp/lab.py
  - tests/test_lab.py
  - src/turing_agentmemory_mcp/frontend/app.js
  - src/turing_agentmemory_mcp/frontend/index.html
autonomous: true
requirements: [ARC-10]
must_haves:
  truths:
    - "User-facing docs (README, docs operational set, HACKER_NEWS, skill docs) describe ArcadeDB as the sole backend and the Bertoni-rooted app-state paths — no TuringDB-backed claims remain."
    - "CHANGELOG.md gains a Removed entry recording the TuringDB dependency/service removal and the /turing to /bertoni app-state rename."
    - "lab.py no longer requires a turingdb_version benchmark field and its architecture diagram shows an ArcadeDB store node, not a TuringDB one; tests/test_lab.py's fixture matches."
    - "The frontend example query and architecture label read ArcadeDB (SQL MATCH object-notation, not a Cypher TuringDB example)."
  artifacts:
    - "CHANGELOG.md Removed entry for the TuringDB cut + Bertoni app-state rename"
  key_links:
    - "lab.py REQUIRED_BENCHMARK_FIELDS <-> tests/test_lab.py fixture must agree after dropping/renaming the turingdb_version key."
  prohibitions:
    - "MUST NOT rewrite docs/architecture.md (already TuringDB-free — verify and skip) and MUST NOT touch historical dated design records under docs/superpowers/ (point-in-time provenance, like baseline/)."
    - "MUST NOT rename the product/skill/package names (turing-agentmemory) — only the TuringDB backend references; the full rebrand is a deferred phase."
---

<objective>
Sweep every user-facing TuringDB reference to the ArcadeDB reality and fix the Lab/frontend UI labels (D-03 docs alignment). Covers README, the operational docs set, the public HACKER_NEWS post, the skill docs, the CHANGELOG removal entry, and the Lab dashboard's required-field + architecture-diagram, plus the two frontend UI labels.

Purpose: The product's documentation and the Lab UI must not claim a TuringDB backend that no longer exists, and must reflect the Bertoni app-state naming from Plan 03.
Output: TuringDB-free docs + UI, a CHANGELOG removal entry, and a Lab that validates ArcadeDB-era benchmark rows.

## Artifacts This Phase Produces (this plan)
- CHANGELOG.md Removed entry (TuringDB cut + Bertoni app-state rename).
- lab.py REQUIRED_BENCHMARK_FIELDS updated (turingdb_version dropped/renamed to backend_version) and its architecture-diagram node id/label changed to arcadedb/ArcadeDB.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-remove-turingdb-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep user-facing docs + add the CHANGELOG removal entry</name>
  <files>README.md, CHANGELOG.md, docs/configuration.md, docs/deployment.md, docs/operations.md, docs/security.md, docs/performance.md, docs/limitations.md, docs/publication/HACKER_NEWS.md, skills/turing-agentmemory/SKILL.md, skills/turing-agentmemory/references/architecture.md, skills/turing-agentmemory/references/operations.md</files>
  <read_first>
    - README.md (the "TuringDB-backed" framing to flip to ArcadeDB)
    - docs/configuration.md, docs/deployment.md, docs/operations.md, docs/security.md, docs/performance.md, docs/limitations.md, docs/publication/HACKER_NEWS.md (grep each for turingdb/TuringDB/TURINGDB and the /turing paths)
    - skills/turing-agentmemory/SKILL.md, skills/turing-agentmemory/references/architecture.md, skills/turing-agentmemory/references/operations.md
    - CHANGELOG.md (add under a Removed heading)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (correction #4: docs/architecture.md is already clean; HACKER_NEWS.md IS in scope)
  </read_first>
  <action>
    Replace TuringDB-backend references with ArcadeDB across README, the six operational docs, HACKER_NEWS.md, and the three skill docs: "TuringDB-backed" to "ArcadeDB-backed"; describe native LSM_VECTOR HNSW + native Lucene (not TuringDB CSV vectors / SQLite-FTS5 outbox); update any TURINGDB_HOME / /turing app-state references to BERTONI_HOME / /bertoni and any TURINGDB_GRAPH mention to AGENTMEMORY_GRAPH (consistent with Plan 03). Verify docs/architecture.md is already TuringDB-free and do NOT edit it (RESEARCH correction #4). Do NOT touch docs/superpowers/ dated design records. Add a CHANGELOG.md Removed entry recording: TuringDB dependency + turingdb/turingdb-volume-init compose services + docker/turingdb.Dockerfile removed; legacy benchmark/eval/repair harnesses removed; app-state renamed /turing to /bertoni (turing-data volume to bertoni-data). Keep the product/skill name turing-agentmemory unchanged.
  </action>
  <verify>
    <automated>bash -c 'grep -rilE "turingdb" README.md docs/configuration.md docs/deployment.md docs/operations.md docs/security.md docs/performance.md docs/limitations.md docs/publication/HACKER_NEWS.md skills/turing-agentmemory/ | grep -v "docs/architecture.md" | wc -l | grep -qx 0 && echo OK'</automated>
  </verify>
  <done>No TuringDB-backend references remain in the swept docs/skill set; docs/architecture.md untouched; CHANGELOG has the Removed entry; product/skill name preserved.</done>
  <acceptance_criteria>
    - grep -rilE "turingdb" README.md docs/configuration.md docs/deployment.md docs/operations.md docs/security.md docs/performance.md docs/limitations.md docs/publication/HACKER_NEWS.md skills/turing-agentmemory/ returns 0 files.
    - git diff --name-only docs/architecture.md is empty (not edited).
    - grep -niE "Removed" CHANGELOG.md shows an entry mentioning TuringDB and /bertoni.
    - git diff docs/superpowers/ is empty (historical records untouched).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix the Lab dashboard required-field + architecture diagram</name>
  <files>src/turing_agentmemory_mcp/lab.py, tests/test_lab.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/lab.py (line 19 REQUIRED_BENCHMARK_FIELDS tuple with "turingdb_version"; the required_ok check ~line 99; line 115 diagram node {"id":"turingdb","label":"TuringDB",...}; line 129 edge {"source":"mcp","target":"turingdb",...})
    - tests/test_lab.py (line 10 fixture dict includes "turingdb_version":"test" to satisfy REQUIRED_BENCHMARK_FIELDS)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (Frontend / Lab — REWRITE table)
  </read_first>
  <action>
    In lab.py: drop "turingdb_version" from REQUIRED_BENCHMARK_FIELDS (rename to "backend_version" if a backend-version field is still meaningful, otherwise remove it entirely — the benchmark harness that produced it was deleted in Plan 01, so removing the requirement is the clean choice; document which you chose). Rename the architecture-diagram store node id from "turingdb" to "arcadedb" and its label from "TuringDB" to "ArcadeDB", and update the edge target "turingdb" to "arcadedb". In tests/test_lab.py update the fixture dict to match the new REQUIRED_BENCHMARK_FIELDS (drop or rename turingdb_version consistently). Run ruff, then the lab test.
  </action>
  <verify>
    <automated>python -m pytest tests/test_lab.py -q</automated>
  </verify>
  <done>lab.py no longer requires turingdb_version and shows an ArcadeDB node/label/edge; tests/test_lab.py passes with the updated fixture; ruff clean.</done>
  <acceptance_criteria>
    - grep -nE "turingdb|TuringDB" src/turing_agentmemory_mcp/lab.py returns 0 matches.
    - python -m pytest tests/test_lab.py -q exits 0.
    - grep -nE "arcadedb|ArcadeDB" src/turing_agentmemory_mcp/lab.py returns >0 matches (diagram node/label).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix the frontend UI labels + example query</name>
  <files>src/turing_agentmemory_mcp/frontend/app.js, src/turing_agentmemory_mcp/frontend/index.html</files>
  <read_first>
    - src/turing_agentmemory_mcp/frontend/app.js (line 19: the stale Cypher example query with a :TuringDB label)
    - src/turing_agentmemory_mcp/frontend/index.html (line 24: the static <span>TuringDB</span> architecture-panel label)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (Frontend / Lab — REWRITE table; Phase 4 D-05 settled on ArcadeDB SQL MATCH object-notation, not Cypher)
  </read_first>
  <action>
    In frontend/app.js replace the stale Cypher-shaped example query string (the (m:Memory)-[r]->(store:TuringDB) example) with an ArcadeDB-shaped example using the SQL MATCH object-notation form and an ArcadeDB label (consistent with Phase 4 D-05). In frontend/index.html replace the static TuringDB span label with ArcadeDB. Keep assets self-contained; no other UI change.
  </action>
  <verify>
    <automated>bash -c 'grep -riE "turingdb" src/turing_agentmemory_mcp/frontend/ | wc -l | grep -qx 0 && echo OK'</automated>
  </verify>
  <done>No TuringDB references remain in frontend/; the example query is ArcadeDB-shaped; the panel label reads ArcadeDB.</done>
  <acceptance_criteria>
    - grep -riE "turingdb" src/turing_agentmemory_mcp/frontend/ returns 0 matches.
    - grep -niE "arcadedb" src/turing_agentmemory_mcp/frontend/index.html returns >0 matches.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/reader docs → deployment decisions | Stale TuringDB docs could mislead a future deployment into configuring a removed backend. |
| Lab benchmark validation → dashboard trust | The required-field contract must match the ArcadeDB-era output shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Denial of Service | stale docs pointing at a removed backend/host | low | mitigate | Complete doc sweep of the user-facing set + a CHANGELOG removal entry; grep acceptance criteria assert zero TuringDB-backend references remain. |
| T-07-09 | Information Disclosure | public HACKER_NEWS.md advertising a wrong architecture | low | mitigate | HACKER_NEWS.md explicitly included in the sweep (RESEARCH correction #4). |
| T-07-SC | Tampering | supply-chain | low | accept | Docs/UI only; no code path or dependency change. |
</threat_model>

<verification>
- grep for turingdb across the swept docs/skill/lab/frontend set returns 0 (excluding docs/architecture.md which is verified already-clean and untouched, and docs/superpowers/ historical records).
- python -m pytest tests/test_lab.py -q passes.
- CHANGELOG has the Removed entry.
- python -m ruff format --check src tests and python -m ruff check src tests pass.
</verification>

<success_criteria>
User-facing docs, the Lab dashboard, and the frontend describe ArcadeDB and the Bertoni app-state layout; the CHANGELOG records the removal; the product/skill name and historical design records are preserved.
</success_criteria>

<output>
Create `.planning/phases/07-remove-turingdb-dependency-hardening/07-06-SUMMARY.md` when done.
</output>
