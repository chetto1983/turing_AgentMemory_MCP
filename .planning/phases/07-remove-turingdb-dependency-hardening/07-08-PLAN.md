---
phase: 07-remove-turingdb-dependency-hardening
plan: 08
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07"]
files_modified: []
autonomous: false
requirements: [ARC-10]
must_haves:
  truths:
    - "The full unit suite passes turingdb-free (no package installed, no stub) at the CI floor: -m 'not integration and not gpu' --cov-fail-under=78."
    - "docker compose config --quiet validates the ArcadeDB-only stack with the turingdb + turingdb-volume-init services gone."
    - "The no-import grep-gate + both DEP compat oracles are green (ARC-10/DEP-01/DEP-02)."
    - "The E2E score gate stays green on ArcadeDB alone (score >= 9.8 and check_count == 19) — the cut did not regress retrieval correctness."
    - "A human has reviewed the rewritten CLAUDE.md / .claude/CLAUDE.md invariants and confirmed the irreversible cut."
  artifacts: []
  key_links:
    - "This is the phase cut-proof gate (RESEARCH step 9 / D-04) — it depends on every prior plan and proves the ArcadeDB-alone stack is correct and tenant-isolated."
  prohibitions:
    - "MUST NOT mark the phase done on a red suite, a failed compose validation, or an E2E-gate regression — the cut is only proven when all three are green."
---

<objective>
Prove the TuringDB cut end-to-end (D-04 cut proof): the full unit suite green turingdb-free, docker compose config --quiet clean on the ArcadeDB-only stack, the phase oracles (grep-gate + DEP compat) green, and the E2E score gate still green on ArcadeDB alone — then a human review of the invariant rewrite confirms the irreversible removal.

Purpose: The Definition of Done for a removal/hardening phase is end-to-end correctness on a real scenario, not just unit-green. The system must remain correct and tenant-isolated on ArcadeDB alone.
Output: A confirmed, human-reviewed green cut.

## Artifacts This Phase Produces (this plan)
- None (verification + checkpoint only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-remove-turingdb-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated cut-proof (full suite + compose + oracles)</name>
  <files>(no files modified — runs the full CI-equivalent gate + E2E score gate as verification)</files>
  <read_first>
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (Cut-Proof Gate Wiring: 882 collected tests baseline; ci.yml unit-tests command; E2E gate asserts score>=9.8 and check_count==19)
    - .github/workflows/ci.yml (the unit-tests + compose-validation + dockerized-integration job commands to mirror)
    - CLAUDE.md (Post-edit validation + Definition of Done: ruff format --check, ruff check, check-file-size.sh, pytest, docker compose config --quiet)
  </read_first>
  <action>
    Run the pre-commit gate in CI order: python -m ruff format --check src tests scripts, then python -m ruff check src tests scripts, then bash scripts/check-file-size.sh (re-run after any format pass — reformatting changes line counts and can push a file over the 600-LOC cap). Then run the full unit suite exactly as CI does: python -m pytest -m "not integration and not gpu" --cov=src/turing_agentmemory_mcp --cov-fail-under=78 -q. Then docker compose config --quiet. Then the phase oracles: python -m pytest tests/test_no_turingdb_imports.py -q, python -m pytest -q -k graspologic_compat, python -m pytest -q -k fastmcp_compat. Confirm gate_guard.assert_gate_go(Path("baseline/06-gate/gate-result.json")) still succeeds (verdict GO, baseline untouched). Finally, if the ArcadeDB Compose service is available, run python scripts/e2e_score.py --out e2e-results.json and confirm score >= 9.8 and check_count == 19; if the ArcadeDB service / GPU providers are not available locally, record that the E2E gate must be confirmed green via CI's dockerized-integration job and hand that confirmation to Task 2. Fix any red before proceeding — do not leave a red gate.
  </action>
  <verify>
    <automated>python -m pytest -m "not integration and not gpu" --cov=src/turing_agentmemory_mcp --cov-fail-under=78 -q && docker compose config --quiet && python -m pytest tests/test_no_turingdb_imports.py -q -k "" && python -c "from pathlib import Path; from turing_agentmemory_mcp.gate_guard import assert_gate_go; assert_gate_go(Path('baseline/06-gate/gate-result.json')); print('GO')"</automated>
  </verify>
  <done>Full unit suite passes turingdb-free at the coverage floor; ruff format/check + file-size cap pass; docker compose config --quiet exits 0 with turingdb services gone; the grep-gate + both DEP oracles pass; gate_guard still reads GO; the E2E score gate is green (locally or handed to Task 2 for CI confirmation).</done>
  <acceptance_criteria>
    - python -m pytest -m "not integration and not gpu" --cov=src/turing_agentmemory_mcp --cov-fail-under=78 -q exits 0.
    - docker compose config --quiet exits 0.
    - python -m pytest tests/test_no_turingdb_imports.py -q exits 0; python -m pytest -q -k graspologic_compat exits 0; python -m pytest -q -k fastmcp_compat exits 0.
    - python -m ruff format --check src tests scripts and python -m ruff check src tests scripts exit 0; bash scripts/check-file-size.sh exits 0.
    - E2E score gate: score >= 9.8 and check_count == 19 (from a local ArcadeDB-service run or CI's dockerized-integration job).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of the invariant rewrite + irreversible-cut confirmation</name>
  <files>CLAUDE.md, .claude/CLAUDE.md</files>
  <action>Present the automated cut-proof results and the rewritten invariants for human review; pause for approval before finalizing the irreversible TuringDB removal. This is a review checkpoint — no code is written.</action>
  <what-built>
    TuringDB is fully removed and the invariants are rewritten. Automated cut-proof (Task 1) is green: full unit suite turingdb-free at the coverage floor, docker compose config --quiet clean on the ArcadeDB-only stack, the no-import grep-gate + DEP-01/DEP-02 compat oracles green, gate_guard still GO, and the E2E score gate green on ArcadeDB alone.
  </what-built>
  <how-to-verify>
    1. Read the rewritten invariants in CLAUDE.md (## Invariants) and .claude/CLAUDE.md (Constraints): confirm ArcadeDB is stated as the sole canonical backend; the MVCC HTTP-503 ConcurrentModificationException handling, native LSM_VECTOR+Lucene ACID-consistency, and one-DB-per-tenant + TenantBinding invariants are codified; and the user_identifier fail-closed scope + stable/deterministic-ID invariants are reconfirmed (not weakened). Confirm no fabricated vector-ordering claim.
    2. Confirm the E2E score gate result: if Task 1 ran it locally, review score >= 9.8 and check_count == 19; if it was deferred, confirm CI's dockerized-integration job is green on this branch.
    3. Spot-check that baseline/03-turingdb/ and baseline/06-gate/ are intact (provenance preserved) and that the product/package/skill names (turing-agentmemory) are unchanged (full rebrand deferred).
    4. Confirm docker compose config --quiet passes and no turingdb service/env/volume remains.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize the irreversible TuringDB removal, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase completion → irreversible cut | Marking the phase done authorizes downstream Phases 8-12 to build on an ArcadeDB-only stack; a hidden regression would propagate. |
| automated gate → human confirmation | The human review is the last guard on the invariant text and the correctness proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Repudiation | claiming a green cut without proof | high | mitigate | Task 1 runs the full CI-equivalent gate + E2E score gate; the blocking human checkpoint confirms the invariant text and the correctness proof before finalization. |
| T-07-02 | Elevation of Privilege | tenant isolation regressed by the cut | high | mitigate | The full unit suite includes the tenant-isolation/binding tests (now turingdb-stub-free); the human review reconfirms the tenant-scope invariants. |
| T-07-SC | Tampering | supply-chain | low | accept | No installs; verification + checkpoint only. |
</threat_model>

<verification>
- Full unit suite green turingdb-free at --cov-fail-under=78.
- docker compose config --quiet clean; turingdb services gone.
- grep-gate + DEP-01 + DEP-02 oracles green; gate_guard reads GO.
- E2E score gate green (score>=9.8, check_count==19) on ArcadeDB alone.
- Human approves the invariant rewrite + the irreversible cut.
</verification>

<success_criteria>
The ArcadeDB-alone stack is proven correct and tenant-isolated (full suite + compose + E2E gate green), the invariants are human-reviewed, provenance and product names are preserved, and the irreversible TuringDB removal is approved.
</success_criteria>

<output>
Create `.planning/phases/07-remove-turingdb-dependency-hardening/07-08-SUMMARY.md` when done.
</output>
