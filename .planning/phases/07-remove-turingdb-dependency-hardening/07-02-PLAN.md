---
phase: 07-remove-turingdb-dependency-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/e2e_score.py
  - src/turing_agentmemory_mcp/e2e_score_stubs.py
  - src/turing_agentmemory_mcp/store_core.py
  - src/turing_agentmemory_mcp/store_documents.py
  - src/turing_agentmemory_mcp/store_rebuild.py
  - src/turing_agentmemory_mcp/store.py
  - pyproject.toml
autonomous: true
requirements: [ARC-10]
must_haves:
  truths:
    - "No live `from turingdb import ...` / `import turingdb` remains in e2e_score.py or e2e_score_stubs.py."
    - "e2e_score.py no longer emits a `turingdb_version` field, and run_e2e() still returns score/check_count/verdict (the E2E gate asserts score>=9.8 and check_count==19, unaffected by the dropped field)."
    - "The dead TuringDaemon class and wait_rest() are removed from e2e_score_stubs.py, while the LIVE stub servers (LocalEmbedServer, LocalRerankServer, free_port, ArcadeE2EBackend, ARCADEDB_E2E_IMAGE) are preserved unchanged."
    - "store_core.py / store_documents.py / store_rebuild.py / store.py carry no TuringDB-worded docstrings or comments; store.py's module docstring reads ArcadeDB-backed."
    - "turingdb==1.35 is gone from pyproject.toml dependencies, the `turingdb` keyword is removed, and the description string names ArcadeDB, not TuringDB."
  artifacts:
    - "pyproject.toml (turingdb dependency + keyword removed, ArcadeDB-worded description)"
  key_links:
    - "e2e_score.py imports live names from e2e_score_stubs — removing TuringDaemon/wait_rest must not touch the live re-exports run_e2e() depends on."
  prohibitions:
    - "MUST NOT alter e2e_score.py's score/check_count/verdict computation — only remove the turingdb_version field and its import."
    - "MUST NOT delete sparse_index.py / SparseIndex — it survives as a fusion-gated channel; only TuringDaemon-era dead code is removed here."
---

<!-- planner-discipline-allow: import turingdb -->
<!-- planner-discipline-allow: from turingdb -->

<objective>
Strip the last LIVE `turingdb` imports out of the kept ArcadeDB-era E2E harness (`e2e_score.py`, `e2e_score_stubs.py`), scrub the stale TuringDB-worded comments/docstrings in the already-ported store modules, and remove `turingdb==1.35` (plus its keyword and the "TuringDB-backed" description) from `pyproject.toml`. This is D-01's import/description purge on the files that survive.

Purpose: After Plan 01 and this plan land, `src/` has zero `import turingdb` / `from turingdb` sites — the strict precondition that lets Plan 04 remove the 31-file test stub without breaking collection (RESEARCH Pitfall 1 / sequencing).
Output: A turingdb-import-free `src/` E2E harness and store layer, and a pyproject.toml with no turingdb dependency.

## Artifacts This Phase Produces (this plan)
- No new symbols. Edits/removals only. `pyproject.toml` description reworded to "ArcadeDB-backed Agent Memory MCP…" and the `turingdb` keyword dropped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune vestigial turingdb imports + dead TuringDaemon from the E2E harness</name>
  <files>src/turing_agentmemory_mcp/e2e_score.py, src/turing_agentmemory_mcp/e2e_score_stubs.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/e2e_score.py (line ~35 top-level turingdb version import; lines ~171-175 write the turingdb_version field into the result dict; run_e2e already uses ArcadeE2EBackend)
    - src/turing_agentmemory_mcp/e2e_score_stubs.py (line ~26 top-level turingdb import; TuringDaemon class ~171-221; wait_rest ~41-52; the LIVE names LocalEmbedServer/LocalRerankServer/free_port/ArcadeE2EBackend/ARCADEDB_E2E_IMAGE)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md ("e2e_score.py / e2e_score_stubs.py — REWRITE" table)
  </read_first>
  <action>
    In e2e_score.py: delete the top-level turingdb version import (the `... __version__ as turingdb_version` line) and delete the `"turingdb_version": turingdb_version` entry written into run_e2e()'s result dict (the field kept only for baseline field-shape parity, no longer needed post-Phase-6, per D-01). Do NOT change any score/points/check_count/verdict logic. In e2e_score_stubs.py: delete the top-level turingdb import (the `... TuringDB` line), the entire `TuringDaemon` class, and the `wait_rest()` helper — all three are dead (their only instantiators, agent_quality_eval.py and benchmark.py, were deleted in Plan 01; no test imports them). KEEP LocalEmbedServer, LocalRerankServer, free_port, ArcadeE2EBackend, and ARCADEDB_E2E_IMAGE exactly as-is (run_e2e imports and uses them). Run ruff format + ruff check on both files.
  </action>
  <verify>
    <automated>python -c "import ast,sys; [sys.exit(f'turingdb ref in {p}') for p in ['src/turing_agentmemory_mcp/e2e_score.py','src/turing_agentmemory_mcp/e2e_score_stubs.py'] if 'turingdb' in open(p,encoding='utf-8').read().lower().replace('turingdb_version','')] ; import turing_agentmemory_mcp.e2e_score; import turing_agentmemory_mcp.e2e_score_stubs; print('OK')"</automated>
  </verify>
  <done>Both modules import cleanly with no turingdb references; TuringDaemon and wait_rest are gone; the live stub servers remain; ruff passes.</done>
  <acceptance_criteria>
    - `grep -nE "import turingdb|from turingdb|turingdb_version|TuringDaemon|wait_rest" src/turing_agentmemory_mcp/e2e_score.py src/turing_agentmemory_mcp/e2e_score_stubs.py` returns 0 matches.
    - `grep -nE "LocalEmbedServer|LocalRerankServer|ArcadeE2EBackend|ARCADEDB_E2E_IMAGE" src/turing_agentmemory_mcp/e2e_score_stubs.py` returns >0 matches (live names preserved).
    - `python -c "import turing_agentmemory_mcp.e2e_score, turing_agentmemory_mcp.e2e_score_stubs"` exits 0.
    - `python -m ruff check src/turing_agentmemory_mcp/e2e_score.py src/turing_agentmemory_mcp/e2e_score_stubs.py` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scrub stale TuringDB comments/docstrings from the ported store modules</name>
  <files>src/turing_agentmemory_mcp/store.py, store_core.py, store_documents.py, store_rebuild.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/store.py (module docstring line 1: "Canonical TuringDB-backed memory/document store." — user-facing, must read ArcadeDB-backed)
    - src/turing_agentmemory_mcp/store_core.py (module docstring; the ~line 317 comment "D-10: reconnect is a reachability re-probe, not TuringDB's ...")
    - src/turing_agentmemory_mcp/store_documents.py (comment ~line 11 "old TuringDB-shaped byte-budget batch splitter"; ~line 373 "TuringDB's submit-before-match visibility gap")
    - src/turing_agentmemory_mcp/store_rebuild.py (comment ~line 9 "instead of the retired TuringDB CSV...")
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md ("store_core.py / store_documents.py / store_rebuild.py / store.py — REWRITE (comments only)")
  </read_first>
  <action>
    These files have NO live turingdb import (Phase 4 already ported them) — only stale prose. Rewrite each reference to describe the ArcadeDB reality by CONCEPT, do not merely delete the sentence where it explains a real behavior: store.py docstring → "Canonical ArcadeDB-backed memory/document store."; store_core.py docstring + the reconnect comment → describe reconnect as an ArcadeDB reachability re-probe (drop the TuringDB contrast); store_documents.py → the batch mechanism is the ArcadeDB single managed-transaction `_write_many` (drop the "byte-budget batch splitter" / "submit-before-match visibility gap" TuringDB framing); store_rebuild.py → the D-07 versioned-index atomic swap on ArcadeDB native LSM_VECTOR (drop "retired TuringDB CSV"). Do NOT change any code, only comment/docstring text. NOTE: store_core.py already has a live grep-gate test (test_seam_contains_no_turingdb_write_primitives_or_csv_vector_load in tests/test_store_arcadedb_core.py) that forbids the substring `from turingdb` in it — keep that green. Run ruff format + ruff check after.
  </action>
  <verify>
    <automated>python -m pytest tests/test_store_arcadedb_core.py -q && grep -riE "turingdb" src/turing_agentmemory_mcp/store.py src/turing_agentmemory_mcp/store_core.py src/turing_agentmemory_mcp/store_documents.py src/turing_agentmemory_mcp/store_rebuild.py | grep -c . | grep -qx 0 && echo OK</automated>
  </verify>
  <done>No `turingdb`/`TuringDB` text remains in the four store modules; store.py docstring reads ArcadeDB-backed; the existing store_core grep-gate test still passes; no code changed.</done>
  <acceptance_criteria>
    - `grep -riE "turingdb" src/turing_agentmemory_mcp/store.py src/turing_agentmemory_mcp/store_core.py src/turing_agentmemory_mcp/store_documents.py src/turing_agentmemory_mcp/store_rebuild.py` returns 0 matches.
    - `head -1 src/turing_agentmemory_mcp/store.py` (the module docstring line) contains `ArcadeDB` and not `TuringDB`.
    - `python -m pytest tests/test_store_arcadedb_core.py -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remove turingdb from pyproject.toml (dependency, keyword, description)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (line ~8 description "TuringDB-backed Agent Memory MCP..."; line ~13 keywords list with "turingdb"; line ~30 dependencies list with "turingdb==1.35")
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md ("Installation" — the exact three edits)
  </read_first>
  <action>
    Remove `"turingdb==1.35"` from `[project].dependencies`. Remove `"turingdb"` from `[project].keywords`. Reword the `[project].description` from "TuringDB-backed Agent Memory MCP…" to "ArcadeDB-backed Agent Memory MCP…" (keep the rest of the sentence). Do NOT touch the `name` field (turing_agentmemory_mcp), the console entrypoint (turing-agentmemory-mcp), or any other dependency — the full package/product rebrand is deferred to its own phase. Leave the `"fastmcp>=3.4,<4"` and `"graspologic-native==1.3.1"` pins exactly as-is (Plan 05 version-gates them). Run ruff (no Python change, but validate the toml parses).
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb'))['project']; assert not any('turingdb' in x for x in d['dependencies']), d['dependencies']; assert 'turingdb' not in d.get('keywords',[]); assert 'ArcadeDB' in d['description'] and 'TuringDB' not in d['description']; print('OK')"</automated>
  </verify>
  <done>pyproject.toml parses; no turingdb dependency or keyword remains; description names ArcadeDB; fastmcp and graspologic-native pins are unchanged.</done>
  <acceptance_criteria>
    - `python -c "import tomllib; ..."` (above) exits 0 and prints OK.
    - `grep -nc "turingdb" pyproject.toml` returns 0.
    - `grep -nE "fastmcp>=3.4,<4|graspologic-native==1.3.1" pyproject.toml` still returns both pins.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import graph → test collection | src-side turingdb imports must all disappear here so Plan 04's stub removal cannot surface a masked ModuleNotFoundError. |
| E2E gate output contract → CI | e2e_score.py's JSON is consumed by CI's score/check_count assertions; dropping a field must not change those keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | 31-file stub masking a real import error | high | mitigate | Removing every live src-side turingdb import here (not just deleting the stub in Plan 04) means the independent grep-gate (Plan 05) is a true oracle, not stub-masked. Sequencing enforced by waves. |
| T-07-07 | Tampering | E2E gate output contract | medium | mitigate | Only the turingdb_version field + its import are removed; score/check_count/verdict logic is untouched (RESEARCH confirms CI reads only .score/.check_count/.verdict). |
| T-07-SC | Tampering | supply-chain (dependency set) | low | mitigate | Removing turingdb==1.35 shrinks the installed-dependency attack surface; no new dependency is added. |
</threat_model>

<verification>
- `grep -rE "import turingdb|from turingdb" src/turing_agentmemory_mcp/e2e_score.py src/turing_agentmemory_mcp/e2e_score_stubs.py` returns 0.
- `grep -riE "turingdb" src/turing_agentmemory_mcp/store*.py` returns 0.
- `python -c "import tomllib; ..."` confirms pyproject is turingdb-free with ArcadeDB description.
- `python -m pytest tests/test_store_arcadedb_core.py -q` passes.
- `python -m ruff format --check src tests scripts` and `python -m ruff check src tests scripts` pass.
</verification>

<success_criteria>
Every LIVE turingdb import is gone from the kept src harness; the store modules read ArcadeDB in prose; pyproject.toml declares no turingdb dependency/keyword and an ArcadeDB description; the fastmcp/graspologic-native pins survive for Plan 05.
</success_criteria>

<output>
Create `.planning/phases/07-remove-turingdb-dependency-hardening/07-02-SUMMARY.md` when done.
</output>
