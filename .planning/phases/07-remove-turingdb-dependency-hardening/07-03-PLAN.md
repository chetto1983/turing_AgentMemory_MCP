---
phase: 07-remove-turingdb-dependency-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/server.py
  - src/turing_agentmemory_mcp/document_job_manager.py
  - src/turing_agentmemory_mcp/provider_config.py
  - compose.yaml
  - .env.example
  - docker/turingdb.Dockerfile
  - tests/test_compose_config.py
autonomous: true
requirements: [ARC-10]
user_setup:
  - service: local-app-state-volume
    why: "The durable app-state Docker volume is renamed turing-data to bertoni-data. Fresh-start milestone: no TuringDB data exists to migrate, so the rename is safe; but a dev/CI environment that already ran the old stack must recreate the volume."
    dashboard_config:
      - task: "Recreate the renamed app-state volume on any environment that already ran the old stack"
        location: "local docker host: docker compose down, then docker volume rm <project>_turing-data, then docker compose up"
must_haves:
  truths:
    - "TURINGDB_HOME is renamed to BERTONI_HOME (default /turing to /bertoni) at all three live read sites (server.py x2, document_job_manager.py x1)."
    - "TURINGDB_GRAPH is RENAMED to AGENTMEMORY_GRAPH (NOT deleted), default value agent_memory unchanged, so index_prefix defaults and self.graph telemetry labels are preserved."
    - "The dead TURINGDB_EMBED_DIMENSIONS fallback is removed from provider_config.py; EMBED_DIMENSIONS remains the sole read."
    - "TURINGDB_URL and the five TURINGDB_*_INDEX vars are deleted from compose.yaml and .env.example (they are read nowhere in src/)."
    - "The turingdb and turingdb-volume-init compose services and docker/turingdb.Dockerfile are gone; the turing-agentmemory-mcp service no longer depends_on turingdb and mounts bertoni-data:/bertoni; every /turing app-state path default is now /bertoni."
    - "tests/test_compose_config.py asserts the renamed reality (bertoni-data:/bertoni, /bertoni registry path) and no longer asserts the deleted TURINGDB_*_INDEX vars are present."
  artifacts:
    - "BERTONI_HOME env var (default /bertoni)"
    - "AGENTMEMORY_GRAPH env var (default agent_memory) — the TURINGDB_GRAPH rename target"
    - "bertoni-data Docker volume mounted at /bertoni"
  key_links:
    - "server.py registry_path / sparse_path derivation reads BERTONI_HOME — the tenant registry, sparse index, job DB, staging, and audit/span JSONL all re-root under /bertoni together."
    - "compose.yaml, .env.example, and tests/test_compose_config.py must agree byte-for-byte on the renamed paths/volume."
  prohibitions:
    - "MUST NOT weaken or alter the user_identifier tenant predicate, TenantBinding, or any tenant-scoping logic — this is a path/env rename only."
    - "MUST NOT delete TURINGDB_GRAPH's behavior — rename it and keep the agent_memory default so no deployment silently shifts index naming/telemetry (RESEARCH Pitfall 2)."
    - "MUST NOT rename the top-level compose name, the turing-agentmemory-mcp service/image names, or the console entrypoint — the full product rebrand is a deferred phase."
    - "MUST NOT introduce a data-loss path — the volume rename is safe only because the milestone is fresh-start; document the recreate step for existing environments."
---

<objective>
Adopt Bertoni-based app-state naming NOW (D-02) and delete the dead TuringDB connection vars, Compose services, and Dockerfile. Rename live TURINGDB_HOME to BERTONI_HOME (/turing to /bertoni) and TURINGDB_GRAPH to AGENTMEMORY_GRAPH, delete the six genuinely-dead TURINGDB_* vars, tear out the turingdb / turingdb-volume-init services + docker/turingdb.Dockerfile, rename the turing-data volume to bertoni-data, and flip tests/test_compose_config.py to the new reality.

Purpose: The stack runs on ArcadeDB alone with app-state paths named for the product it is becoming, with zero silent behavior shift (RESEARCH Pitfalls 2 and 4) and the tenant-isolation contract untouched.
Output: An ArcadeDB-only compose stack, a Bertoni-rooted app-state layout, and a compose-config test that matches.

## Artifacts This Phase Produces (this plan)
- BERTONI_HOME env var (default /bertoni) — replaces TURINGDB_HOME.
- AGENTMEMORY_GRAPH env var (default agent_memory) — the chosen TURINGDB_GRAPH rename target.
- bertoni-data Docker volume mounted at /bertoni — replaces turing-data:/turing.

Discretion decision (LOCKED here): TURINGDB_GRAPH becomes AGENTMEMORY_GRAPH. Rationale: it is a live logical graph/namespace label feeding index_prefix (ArcadeDB index-name defaults) and self.graph (telemetry/span labels), not an ArcadeDB connection detail (so not ARCADEDB_*) nor a durable app-state path (so not BERTONI_*); it belongs to the existing product-neutral AGENTMEMORY_* config family, which survives the deferred rebrand. Default stays agent_memory so no deployment shifts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the live app-state env reads in src (BERTONI_HOME, AGENTMEMORY_GRAPH)</name>
  <files>src/turing_agentmemory_mcp/server.py, src/turing_agentmemory_mcp/document_job_manager.py, src/turing_agentmemory_mcp/provider_config.py</files>
  <read_first>
    - src/turing_agentmemory_mcp/server.py (line 129 TURINGDB_GRAPH read feeding self.graph/index_prefix; line 130 TURINGDB_HOME in _unbootstrapped_store_from_env; line 223 TURINGDB_HOME in tenant_router_from_env; lines 160-163 stale coexistence comment; lines 224-229 registry_path default; lines 134-139 sparse_path default)
    - src/turing_agentmemory_mcp/document_job_manager.py (line 370 TURINGDB_HOME in document_ingest_manager_from_env; lines 374-381 job DB + staging defaults derived from home)
    - src/turing_agentmemory_mcp/provider_config.py (lines 52-59 store_embedding_dimensions: EMBED_DIMENSIONS primary, TURINGDB_EMBED_DIMENSIONS dead fallback at 56-58)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (App-State Rename Surface table + Pitfall 2)
  </read_first>
  <action>
    At each of the three independent TURINGDB_HOME reads (server.py:130, server.py:223, document_job_manager.py:370 — no shared constant, each function calls os.environ.get separately, so touch all three), change the key to BERTONI_HOME and the default to /bertoni. At server.py:129 change the key TURINGDB_GRAPH to AGENTMEMORY_GRAPH and keep the default agent_memory exactly, so index_prefix and self.graph telemetry are unchanged (rename, not deletion — RESEARCH Pitfall 2). In provider_config.py delete the dead TURINGDB_EMBED_DIMENSIONS fallback branch (lines ~56-58); leave EMBED_DIMENSIONS as the sole read. Rewrite or delete the stale server.py:160-163 coexistence comment to reflect ArcadeDB-only reality. Do NOT change any tenant-scoping / TenantBinding / user_identifier logic — the derived registry_path, sparse_path, job-DB path, and staging path re-root under /bertoni automatically because they are built from home. Run ruff format and ruff check.
  </action>
  <verify>
    <automated>python -c "import turing_agentmemory_mcp.server, turing_agentmemory_mcp.document_job_manager, turing_agentmemory_mcp.provider_config; print('OK')"</automated>
  </verify>
  <done>The three modules import cleanly; no TURINGDB_HOME/TURINGDB_GRAPH/TURINGDB_EMBED_DIMENSIONS keys remain in src; BERTONI_HOME + AGENTMEMORY_GRAPH are present; tenant logic byte-unchanged; ruff clean.</done>
  <acceptance_criteria>
    - grep -rnE "TURINGDB_HOME|TURINGDB_GRAPH|TURINGDB_EMBED_DIMENSIONS" src/turing_agentmemory_mcp/ returns 0 matches.
    - grep -rn "BERTONI_HOME" src/turing_agentmemory_mcp/ returns 3 matches; grep -rn "AGENTMEMORY_GRAPH" src/turing_agentmemory_mcp/server.py returns >=1 match.
    - The AGENTMEMORY_GRAPH read keeps default "agent_memory" (grep the line for agent_memory).
    - python -m ruff check src/turing_agentmemory_mcp/server.py src/turing_agentmemory_mcp/document_job_manager.py src/turing_agentmemory_mcp/provider_config.py exits 0.
    - git diff shows no edit to any user_identifier / TenantBinding / predicate expression (only env keys + default path strings changed).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove the TuringDB compose services + Dockerfile and re-root app-state to /bertoni</name>
  <files>compose.yaml, .env.example, docker/turingdb.Dockerfile (delete)</files>
  <read_first>
    - compose.yaml (turingdb-volume-init service; turingdb service; turing-agentmemory-mcp service depends_on/environment/volumes; top-level volumes section)
    - .env.example (read via: git show HEAD:.env.example — direct read of this path may be permission-blocked; lines 3-6 TURINGDB block; lines 25/57/58 the /turing paths; line 28 EMBED_DIMENSIONS already correct)
    - docker/turingdb.Dockerfile (to be deleted)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (compose.yaml service-level changes + .env.example changes lists)
  </read_first>
  <action>
    In compose.yaml: delete the turingdb-volume-init service entirely and the turingdb service entirely. On the turing-agentmemory-mcp service remove the depends_on turingdb: condition: service_healthy entry, remove all eight TURINGDB_* environment lines (TURINGDB_URL, TURINGDB_GRAPH, and the five TURINGDB_*_INDEX plus any TURINGDB_HOME), rename the volume mount turing-data:/turing to bertoni-data:/bertoni, and change every /turing/data and /turing/audit env default to /bertoni/data and /bertoni/audit (AGENTMEMORY_SPARSE_PATH, AGENTMEMORY_TENANT_REGISTRY_PATH, AGENTMEMORY_DOCUMENT_JOB_PATH, AGENTMEMORY_DOCUMENT_STAGING_ROOT, audit/span JSONL paths). If the mcp service needs a graph label env, emit AGENTMEMORY_GRAPH (not TURINGDB_GRAPH). In the top-level volumes section rename turing-data to bertoni-data. Delete docker/turingdb.Dockerfile (git rm). In .env.example delete the TURINGDB_URL/TURINGDB_GRAPH/TURINGDB_HOME block and its "transitional" comment, and rename the /turing paths (tenant registry, audit jsonl, spans jsonl) to /bertoni. Do NOT touch the top-level compose name, the turing-agentmemory-mcp service/image names, other service names, or ARCADEDB_* / AGENTMEMORY_TENANT_* entries. Then run docker compose config --quiet.
  </action>
  <verify>
    <automated>docker compose config --quiet</automated>
  </verify>
  <done>compose.yaml validates; turingdb + turingdb-volume-init services absent; docker/turingdb.Dockerfile gone; bertoni-data:/bertoni mount + /bertoni paths present; no TURINGDB_* env lines remain in compose.yaml or .env.example.</done>
  <acceptance_criteria>
    - docker compose config --quiet exits 0.
    - python -c "import yaml; c=yaml.safe_load(open('compose.yaml',encoding='utf-8')); s=c['services']; assert 'turingdb' not in s and 'turingdb-volume-init' not in s; assert 'bertoni-data' in c['volumes'] and 'turing-data' not in c['volumes']; print('OK')" exits 0.
    - grep -nE "TURINGDB_|turing-data|/turing/" compose.yaml returns 0 matches.
    - test ! -f docker/turingdb.Dockerfile exits 0.
    - git show HEAD:.env.example is superseded: the working .env.example has 0 matches for TURINGDB_ and /turing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Flip tests/test_compose_config.py to assert the Bertoni-renamed reality</name>
  <files>tests/test_compose_config.py</files>
  <read_first>
    - tests/test_compose_config.py (test_product_service_enables_the_fused_temporal_pipeline asserts AGENTMEMORY_SPARSE_PATH=/turing/... and five TURINGDB_*_INDEX lines; test_product_service_locks_the_physical_tenant_database_contract asserts turing-data:/turing, turing-data volume, /turing tenant-registry path)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (tests/test_compose_config.py — REWRITE + Pitfall 4)
  </read_first>
  <action>
    In test_product_service_enables_the_fused_temporal_pipeline: change the AGENTMEMORY_SPARSE_PATH assertion to /bertoni/data/agent-memory-fts.sqlite3, and DELETE the five TURINGDB_*_INDEX presence assertions (those vars are deleted; asserting their presence would fail). In test_product_service_locks_the_physical_tenant_database_contract: change turing-data:/turing to bertoni-data:/bertoni, change the "turing-data" volume assertion to "bertoni-data", and change the AGENTMEMORY_TENANT_REGISTRY_PATH default to /bertoni/data/agent-memory-tenant-registry.sqlite3. Leave the ARCADEDB image assertion, the ARCADEDB_DATABASE-absence assertions, the healthcheck-layer test, the AGENTMEMORY_TENANT_* bounded-settings assertions, and the ARCADEDB_PASSWORD triplication test unchanged. Run ruff, then run the file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_compose_config.py -q</automated>
  </verify>
  <done>tests/test_compose_config.py passes against the renamed compose.yaml/.env.example; no assertion references TURINGDB_*_INDEX, turing-data, or /turing.</done>
  <acceptance_criteria>
    - python -m pytest tests/test_compose_config.py -q exits 0.
    - grep -nE "TURINGDB_|turing-data|/turing/" tests/test_compose_config.py returns 0 matches.
    - grep -nE "bertoni-data|/bertoni/" tests/test_compose_config.py returns >0 matches.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant request → user_identifier predicate + TenantBinding | The app-state rename must not touch the mandatory scoping/binding layer (invariant #1). |
| durable app-state → /bertoni volume | Registry, job DB, staging, audit/span JSONL live under the renamed volume; the rename must not orphan or drop them. |
| operator config (.env.example/compose.yaml) → running stack | Leftover TURINGDB_* entries could point a future deployment at a decommissioned host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Elevation of Privilege | tenant predicate / TenantBinding during path rename | high | mitigate | Task 1 renames only env keys + default path strings; acceptance criteria assert no user_identifier/TenantBinding expression changed; tenant-scoping code is out of the diff. |
| T-07-03 | Denial of Service / Repudiation | /turing to /bertoni volume + durable state | high | mitigate | Fresh-start milestone (no data to migrate); the recreate step is documented in user_setup; all app-state paths re-root together from BERTONI_HOME so no split-root orphaning. |
| T-07-08 | Denial of Service | stale TURINGDB_URL in .env.example/compose.yaml | medium | mitigate | Complete deletion (not commenting-out) of all TURINGDB_* entries; grep acceptance criteria assert zero remain. |
| T-07-SC | Tampering | supply-chain | low | mitigate | Deleting the turingdb service + docker/turingdb.Dockerfile removes one exposed local port and one container image's pip-install surface. |
</threat_model>

<verification>
- python -c import of server/document_job_manager/provider_config succeeds; no TURINGDB_* keys remain in src.
- docker compose config --quiet exits 0; turingdb services + Dockerfile gone; bertoni-data:/bertoni present.
- python -m pytest tests/test_compose_config.py -q passes.
- python -m ruff format --check src tests scripts and python -m ruff check src tests scripts pass.
</verification>

<success_criteria>
App-state env/volume/paths are Bertoni-named, the six dead TURINGDB_* vars and both TuringDB compose services + Dockerfile are gone, TURINGDB_GRAPH is preserved-as-AGENTMEMORY_GRAPH with an unchanged default, tenant isolation is provably untouched, and compose config + its test are green.
</success_criteria>

<output>
Create `.planning/phases/07-remove-turingdb-dependency-hardening/07-03-SUMMARY.md` when done.
</output>
