---
phase: 07-remove-turingdb-dependency-hardening
plan: 07
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - CLAUDE.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [ARC-10]
must_haves:
  truths:
    - "CLAUDE.md invariant #2 states ArcadeDB is the sole canonical backend (the TuringDB-canonical claim is superseded, not merely edited)."
    - "The TuringDB-era submit-before-match / load_graph rationale is retired/replaced by the ArcadeDB single managed-transaction (run_in_transaction) + reconnect/health-probe reality; #1 (tenant scope) and #3 (stable IDs) are reconfirmed as still enforced."
    - "New invariants are codified: MVCC conflict = HTTP 503 ConcurrentModificationException redone via run_in_transaction (never blind-retry); native LSM_VECTOR + Lucene are ACID-consistent with graph writes (the write-side FTS5 outbox-as-second-source-of-truth is retired, but SparseIndex still exists as a fusion-gated channel); one ArcadeDB database per tenant PLUS the mandatory user_identifier predicate + TenantBinding (physical isolation never replaces the predicate)."
    - "The stale 'TURINGDB_HOME remains its transitional env name' note is replaced by BERTONI_HOME in both files."
    - ".claude/CLAUDE.md's milestone framing reflects the completed ArcadeDB migration (no 'migrates to ArcadeDB this milestone' pending language, no 'invariant #2 TuringDB canonical' live claim)."
  artifacts:
    - "CLAUDE.md (rewritten Invariants section + Notes)"
    - ".claude/CLAUDE.md (reconciled Constraints/Durability/milestone framing)"
  key_links:
    - "The invariant text must match the actual Phase 4/5 code behavior — reconcile against 04-CONTEXT.md and the live store, do not fabricate a behavior claim."
  prohibitions:
    - "MUST NOT weaken or drop invariant #1 (user_identifier scope, fail-closed on empty) or invariant #3 (stable/deterministic IDs) — both are reconfirmed, not relaxed."
    - "MUST NOT assert a vector-ordering behavior that the ArcadeDB code does not actually exhibit — cross-check the app-layer re-sort against 04-CONTEXT.md before retiring or rewording that invariant."
---

<objective>
Rewrite the invariant lists in both CLAUDE.md and .claude/CLAUDE.md for the ArcadeDB-sole reality (D-03), using the RESEARCH Invariant Rewrite Substrate table (traced to Phase 4/5/6 sources) as the authority. Supersede the TuringDB-canonical invariant, retire the submit-before-match / load_graph rationale, reconfirm tenant-scope + stable-IDs, and codify the new ArcadeDB-era invariants (MVCC 503 handling, native ACID-consistent vector+full-text, one-DB-per-tenant + TenantBinding).

Purpose: The governance docs must describe how the system actually works now so future planning/execution does not re-assert retired TuringDB constraints.
Output: Two reconciled invariant/constraint doc sections, ready for the human review gate in Plan 08.

Note on numbering: the CURRENT CLAUDE.md invariant list is already partially ArcadeDB-worded (a prior partial migration), so its numbering no longer matches the ROADMAP's original reference numbering (#2/#4/#6). Apply each substrate change by CONCEPT to whatever invariant currently carries that rationale, not by the old number.

## Artifacts This Phase Produces (this plan)
- Rewritten CLAUDE.md Invariants section + Notes (BERTONI_HOME).
- Reconciled .claude/CLAUDE.md Constraints/Durability/milestone-status prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-remove-turingdb-dependency-hardening/07-CONTEXT.md
@.planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md
@.planning/phases/04-arcadedb-direct-port/04-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the CLAUDE.md Invariants section + Notes</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the ## Invariants section 1-9 as it stands today — already partially ArcadeDB-worded; and the ## Notes line "TURINGDB_HOME remains its transitional env name")
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (Invariant Rewrite Substrate table — the source fact + citation for each change)
    - .planning/phases/04-arcadedb-direct-port/04-CONTEXT.md (native HNSW+Lucene, deleted vector_id int-join, run_in_transaction/MVCC, reconnect/readiness)
    - .planning/STATE.md (Accumulated Context: MVCC 503 ConcurrentModificationException handling; TenantBinding.verify via derive_tenant_database_identity + hmac.compare_digest; all 18 public store methods call the binding-aware guard first)
  </read_first>
  <action>
    Apply each RESEARCH substrate row to CLAUDE.md's Invariants section by concept: (a) ensure the canonical-backend invariant states ArcadeDB is the SOLE canonical backend (TuringDB-canonical superseded); (b) retire/replace any submit-before-match or load_graph rationale with the ArcadeDB single managed-transaction + read-your-writes model (run_in_transaction) and the reconnect + /health reachability probe; (c) reconfirm the user_identifier tenant-scope invariant (fail-closed on empty) and the stable/deterministic-IDs invariant verbatim in intent; (d) codify the NEW invariants — MVCC conflict = HTTP 503 ConcurrentModificationException, which invalidates the session so run_in_transaction redoes the whole begin/body/commit (never blind-retry the same commit); native LSM_VECTOR + Lucene are ACID-consistent with graph writes so the write-side FTS5 outbox-as-second-source-of-truth is retired (but SparseIndex survives as a fusion_enabled-gated channel with a status() signal — do NOT claim SparseIndex was deleted); one ArcadeDB database per tenant PLUS the mandatory user_identifier predicate + TenantBinding, with physical isolation never replacing the predicate. For the existing app-layer vector re-sort invariant, cross-check 04-CONTEXT.md and the live store: retire the TuringDB-specific rationale, and keep the accurate part only if the code still re-sorts — do NOT fabricate an ordering claim. Replace the Notes "TURINGDB_HOME remains its transitional env name" with the BERTONI_HOME reality.
  </action>
  <verify>
    <automated>bash -c 'grep -qiE "ArcadeDB.*(sole|canonical)|(sole|canonical).*ArcadeDB" CLAUDE.md && grep -qiE "ConcurrentModificationException|503" CLAUDE.md && grep -qiE "TenantBinding" CLAUDE.md && ! grep -qi "TURINGDB_HOME remains" CLAUDE.md && echo OK'</automated>
  </verify>
  <done>CLAUDE.md invariants read ArcadeDB-sole, codify MVCC 503 handling + native ACID vector/full-text + one-DB-per-tenant/TenantBinding, reconfirm tenant-scope + stable IDs, and the transitional TURINGDB_HOME note is gone (BERTONI_HOME).</done>
  <acceptance_criteria>
    - grep -iE "ArcadeDB" CLAUDE.md shows the canonical-backend invariant naming ArcadeDB as sole/canonical.
    - grep -iE "ConcurrentModificationException|503" CLAUDE.md returns >0 (MVCC invariant codified).
    - grep -iE "TenantBinding" CLAUDE.md returns >0 (per-tenant-DB + predicate invariant codified).
    - grep -i "user_identifier" CLAUDE.md still shows the fail-closed tenant-scope invariant (invariant #1 reconfirmed).
    - grep -i "TURINGDB_HOME remains" CLAUDE.md returns 0 matches.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reconcile .claude/CLAUDE.md Constraints, Durability, and milestone framing</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - .claude/CLAUDE.md (the ### Constraints block referencing "invariant #2 (TuringDB canonical) is superseded ... migrates to ArcadeDB this milestone"; the Durability note "TURINGDB_HOME remains its transitional env name"; the Tech-stack "turingdb 1.35 - Primary graph + vector database" prose)
    - .planning/phases/07-remove-turingdb-dependency-hardening/07-RESEARCH.md (Invariant Rewrite Substrate)
  </read_first>
  <action>
    Update .claude/CLAUDE.md's invariant/constraint prose to past-tense completed state: the ArcadeDB migration is DONE (not "migrates this milestone"); ArcadeDB is the sole canonical backend (drop the "invariant #2 TuringDB canonical is superseded" pending framing — state it as fact); rename the Durability "TURINGDB_HOME remains transitional" note to BERTONI_HOME; and fix short prose that still calls TuringDB the primary database (e.g. the Key-Dependencies / Runtime line) to name ArcadeDB. Scope: the invariant/constraint/durability prose only — do NOT attempt a full rewrite of the large auto-generated tech-stack/architecture tables (that is the deferred full-rebrand phase); fix only the load-bearing false-backend claims that are short prose. Keep the product/package/skill names unchanged.
  </action>
  <verify>
    <automated>bash -c 'grep -qiE "ArcadeDB" .claude/CLAUDE.md && ! grep -qi "TURINGDB_HOME remains" .claude/CLAUDE.md && echo OK'</automated>
  </verify>
  <done>.claude/CLAUDE.md states the ArcadeDB migration as completed fact, names ArcadeDB as sole canonical backend in the constraint prose, and no longer carries the transitional TURINGDB_HOME note; product/package names preserved.</done>
  <acceptance_criteria>
    - grep -i "migrates to ArcadeDB" .claude/CLAUDE.md returns 0 (no pending-migration language).
    - grep -i "TURINGDB_HOME remains" .claude/CLAUDE.md returns 0.
    - grep -iE "ArcadeDB.*(sole|canonical|backend)" .claude/CLAUDE.md returns >0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| governance docs → future planning/execution | Invariants steer every future change; a wrong/stale invariant silently mis-guides downstream work. |
| invariant text → actual code behavior | The invariant claims must match the Phase 4/5 code, especially the tenant predicate + MVCC handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Elevation of Privilege | tenant-scope invariant wording | high | mitigate | Invariant #1 (user_identifier fail-closed) and the one-DB-per-tenant + TenantBinding invariant are reconfirmed, never weakened; acceptance criteria assert their presence. |
| T-07-10 | Repudiation | fabricated behavior claim in an invariant | medium | mitigate | The vector-ordering invariant is cross-checked against 04-CONTEXT.md and live code before retiring/rewording; the whole rewrite is human-reviewed in Plan 08. |
| T-07-SC | Tampering | supply-chain | low | accept | Docs only. |
</threat_model>

<verification>
- CLAUDE.md: ArcadeDB-sole canonical invariant present; MVCC 503 + TenantBinding codified; user_identifier + stable-ID invariants reconfirmed; transitional TURINGDB_HOME note gone.
- .claude/CLAUDE.md: migration stated as completed fact; no transitional note; ArcadeDB named as sole backend in constraint prose.
- Both files still name the product/package turing-agentmemory (rebrand deferred).
</verification>

<success_criteria>
Both governance docs describe the ArcadeDB-sole reality with the new invariants codified and tenant-scope/stable-IDs reconfirmed, with no fabricated behavior claims — ready for the Plan 08 human review.
</success_criteria>

<output>
Create `.planning/phases/07-remove-turingdb-dependency-hardening/07-07-SUMMARY.md` when done.
</output>
