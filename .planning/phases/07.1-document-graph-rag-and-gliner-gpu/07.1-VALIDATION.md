---
phase: 07.1
slug: document-graph-rag-and-gliner-gpu
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-17
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by `/gsd-plan-phase` from `07.1-RESEARCH.md` § Validation Architecture.
> Task-level rows are filled once PLAN.md files exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.2+ (`pyproject.toml`: `testpaths=tests`, `pythonpath=src`) |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `.venv\Scripts\python -m pytest tests/test_<affected>.py -q` |
| **Full suite command** | `.venv\Scripts\python -m pytest -q` |
| **Estimated runtime** | ~90s full suite (866 tests green at Phase 7 close) |

---

## Sampling Rate

- **After every task commit:** `python -m ruff format --check src tests scripts` → `python -m ruff check src tests scripts` → the narrowest affected `pytest tests/test_<affected>.py -q`
- **After every plan wave:** `python -m pytest -q` + `bash scripts/check-file-size.sh`
- **Before `/gsd-verify-work`:** full suite green + `docker compose config --quiet` + the D-17 mini-gate (`gate_diff.py` re-run showing **19/19 at 10.0**)
- **Max feedback latency:** ~90 seconds

> **Re-run `check-file-size.sh` after any format pass** — reformatting changes line counts, and
> `store_documents.py` (585/600) and `store_search.py` (559/600) have ~zero headroom (research Unknown 4).

---

## Per-Task Verification Map

Task IDs are assigned when plans land; requirement→test bindings below are fixed now.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | GRAPH-01 | — | `stable_id("ent", user, canonical_name)` yields ONE id across GLiNER type-drift variants of one surface | unit (tdd) | `pytest tests/test_temporal_graph.py -k entity_key -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-01 | — | `type_observations` increments per-type on repeated mentions; never overwrites | unit (tdd) | `pytest tests/test_temporal_graph.py -k type_observations -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-01 (D-19) | T-07.1-01 | Re-key rebuild is tenant-scoped, **fails closed on empty `user_identifier`**, and never re-keys across tenants | unit (tdd) | `pytest tests/test_store_rebuild.py -k rekey_tenant_scope -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-01 (D-19) | — | Re-key rebuild is **idempotent** — a second run converges (no duplicate entities, no orphaned edges) | unit (tdd) | `pytest tests/test_store_rebuild.py -k rekey_idempotent -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-01 (D-19) | — | Re-key rebuild **never deletes canonical rows** (`Memory` / `Chunk` / `Document` counts unchanged) | unit (tdd) | `pytest tests/test_store_rebuild.py -k rekey_canonical_intact -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-01 (D-03) | — | Hub damping `1/(1 + 0.001*(n-1)^2)` applied **before** RRF; entity in 100 chunks → ~0.09 weight | unit (tdd) | `pytest tests/test_retrieval_fusion.py -k hub_damping -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-02 | T-07.1-02 | `Chunk`→`Entity` MENTIONS write binds `user_identifier`; fails closed on empty | unit (tdd) | `pytest tests/test_store_documents_queries.py -k mentions -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-02 | T-07.1-02 | Cross-doc chunk-bridge traversal returns only same-tenant chunks under a foreign-tenant adversarial fixture (`user_identifier` bound at **every** hop) | unit (tdd) | `pytest tests/test_store_documents_graph.py -k traversal_tenant_scope -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | FIX-08 | T-07.1-03 | A document whose extracted text exceeds `MAX_TEXT_CHARS` (16,384) no longer returns HTTP 400 | integration | `pytest tests/test_store_documents.py -k http_400 -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | FIX-08 | T-07.1-03 | A document with >256 chunks is sub-batched to `MAX_TEXTS=256` per `/extract` call | integration | `pytest tests/test_store_documents.py -k sub_batch -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | FIX-08 | — | Each chunk carries its **own** `entity_extraction` metadata (not one shared copy) | unit (tdd) | `pytest tests/test_store_documents.py -k per_chunk_metadata -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | PERF-04 | — | GLiNER concurrency fan-out preserves per-text result **ordering and count** | unit (tdd) | `pytest tests/test_gliner_provider.py -k concurrent_batch -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | PERF-04 | — | CUDA execution-provider binding proof (black-box) | manual/integration | See Manual-Only Verifications | n/a | ⬜ pending |
| TBD | TBD | TBD | GRAPH-04 | — | `diversify_fused(max_per_source>1)` lets one document contribute >1 chunk | unit (tdd) | `pytest tests/test_retrieval_fusion.py -k document_diversify -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-04 | — | **Flag OFF: `search_documents` ranking preserved exactly** vs pre-port behavior on a fixed seed set | integration | `pytest tests/test_store_documents.py -k flag_off_parity -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-03 | — | `bridging_set_recall_at_k` computes correct set-recall on a synthetic fixture | unit (tdd) | `pytest tests/test_multihop_eval_scoring.py -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | GRAPH-03 (D-07) | — | The unanswerable-from-one-chunk **rejection filter** is distinct from the GO/NO-GO metric (research Pitfall 5) | unit (tdd) | `pytest tests/test_multihop_eval_scoring.py -k rejection_filter -x` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_temporal_graph.py` — entity-key + `type_observations` tests (GRAPH-01). **Verify whether this file already exists before creating it.**
- [ ] `tests/test_store_rebuild.py` — re-key rebuild tenant-scope / idempotency / canonical-intact tests (GRAPH-01, D-19). **Verify existence first.**
- [ ] `tests/test_store_documents_queries.py` **or** new `tests/test_store_documents_graph.py` — chunk-entity MENTIONS write + traversal tenant-scoping (GRAPH-02).
- [ ] `tests/test_gliner_provider.py` — concurrency fan-out test (PERF-04). **This file already exists** (per `01-05-PLAN.md`'s split) — extend, do not create.
- [ ] `tests/test_retrieval_fusion.py` — document-diversity + hub-damping tests (GRAPH-04, D-03). **Existence unconfirmed — verify in Wave 0.**
- [ ] `scripts/multihop_eval_scoring.py` + `tests/test_multihop_eval_scoring.py` — genuinely new code, not an extension (GRAPH-03).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| **CUDA binding proof** — GLiNER actually runs on GPU, not silently on CPU | PERF-04 (D-09) | **No white-box API exists.** `fast_gliner==0.2.1` is a Rust/PyO3 binding around `gline-rs`/`ort`, not the Python `onnxruntime` package — there is no `InferenceSession.get_providers()` in the call path, and silent CPU fallback is the *documented default* of the underlying `ort` crate (research Unknown 2). Requires real GPU + Docker. | **Use BOTH signals together — neither alone is conclusive.** (1) GPU delta: sample `nvidia-smi --query-gpu=utilization.gpu,memory.used --format=csv,noheader` before/after a burst of ≥50 chunk extractions; GPU utilization/memory must move measurably. (2) Throughput floor: a CUDA-bound run over spike 003's 167-chunk set must beat a *measured* CPU baseline by a large margin. Throughput alone is insufficient — the concurrency fan-out (PERF-04) also speeds up CPU. **Never** treat wheel-install success or `execution_provider="cuda"` being accepted without exception as proof. |
| **GPU contention measurement** — effect of a third GPU model on embed/rerank latency | PERF-04 (D-11) | Requires the full GPU compose stack; contention is only observable under real concurrent load. | Enable GLiNER on GPU with embed+rerank running; measure embed/rerank latency and the E2E score gate against the documented 3345 ms mean vs the 5.4 s bar. **Measure before tuning anything** (D-11). |
| **D-17 mini migration-correctness gate** | GRAPH-01, GRAPH-04 | Requires live GPU stack + real providers + the committed corpus; mirrors Phase 6's gate. | After the re-key rebuild (D-19) lands: re-run `scripts/e2e_score.py` — must read **19/19 at 10.0** (`VALIDATED_10_10`, `score >= 9.8 and check_count == 19`). Re-run the frozen-question benchmark via `scripts/gate_diff.py` (same CLI Phase 6 used) to confirm the document path is untouched with the flag OFF. Record as a `GATE.md`-shaped artifact. |
| **Multi-hop eval + GO/NO-GO verdict** | GRAPH-03 | The decision artifact itself; requires real providers, the corpus, and **user review** of surviving questions (D-07: "Claude composes, user verifies"). | Compose candidate two-hop chains; mechanically cut any question the current dense+lexical+rerank stack answers from a single chunk; user reviews survivors for plausibility. Run all three arms (`fused_base` / `entity_boost` / `fused_graph`). Apply D-15's four GO conditions — **all four required**. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] **No-skip-as-green (PROJECT.md DOCK-06):** the GPU tier must degrade to a *visible* compile/stub floor on GPU-less runners (mirroring the existing `HashingEmbedder`-stub E2E pattern) or fail loudly. **`pytest.skip("no GPU")` is forbidden.**
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
