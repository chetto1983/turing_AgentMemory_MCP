---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 14
type: execute
wave: 5
depends_on: ["07.1-11", "07.1-09", "07.1-17"]
files_modified:
  - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-GATE.md
  - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/gate-result.json
  - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-D-05-DECISION.md
autonomous: false
requirements: [GRAPH-01]
must_haves:
  truths:
    - "D-03's hub damping is VERIFIED ACTIVE on the memory graph channel BEFORE the rebuild runs — it is a required mitigation that must pre-exist the gate, not a suspect to investigate after a regression"
    - "After the re-key rebuild runs, the E2E score gate reads 19/19 at 10.0 (VALIDATED_10_10, score >= 9.8 and check_count == 19)"
    - "The frozen-question document benchmark is re-run and confirms the document path is untouched with the flag OFF"
    - "The gate artifact mirrors Phase 6's GATE.md shape and cites real measured numbers"
    - "No artifact claims the pre-existing rebuild tools re-key entities — that claim is false"
    - "Spike 001's bridge measurement is re-run after the re-key, and D-05's SAME_AS decision is made on the residual fragmentation SHAPE, not deferred silently"
  artifacts:
    - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-GATE.md
    - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/gate-result.json
    - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-D-05-DECISION.md
  key_links:
    - "rebuild_entity_identity (plan 11) -> E2E score gate 19/19 at 10.0 -> the proof the memory-side ER change is safe (D-19)"
    - "re-keyed entities -> spike 001's bridge re-measurement -> D-05's SAME_AS decision"
    - "hub_damping_factor active in store_evidence.py (plan 17, wave 2) -> the precondition this gate verifies BEFORE running the rebuild -> D-03's 'cannot ship without it'"
---

<objective>
Apply Phase 6's own discipline to the ER change: run the re-key rebuild, prove the E2E gate still
reads 19/19 at 10.0, confirm the document path is untouched with the flag OFF, and record it as a
`GATE.md`-shaped artifact. Then re-run spike 001's bridge measurement and make D-05's SAME_AS
decision on the evidence.

Purpose: D-17 exists because D-04's ER change touches the E2E-gated memory search path, and D-19
names this gate as its proof. **Precision note (D-17):** the ER fix affects **memory** search
(which uses entity/graph/community channels). `document_search` has **no entity channel today**, so
the frozen-question document benchmark **cannot move from the ER fix alone** — it becomes relevant
only once the channel flag turns on. **The E2E gate's 19 checks are the real guard** for the
memory-side change. Do not mistake a flat document benchmark for evidence the ER fix worked.
Output: `07.1-GATE.md` + `gate-result.json` + the D-05 decision record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-11-SUMMARY.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-09-SUMMARY.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-17-SUMMARY.md
@.planning/phases/06-migration-correctness-gate/GATE.md
</context>

## Artifacts this phase produces (this plan)

New artifacts created here — none exist yet, exclude from drift verification:

- `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-GATE.md`
- `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/gate-result.json`
- `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-D-05-DECISION.md`

<tasks>

<task type="auto">
  <name>Task 1: Run the re-key rebuild and prove the E2E gate still reads 19/19 at 10.0</name>
  <files>.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-GATE.md, .planning/phases/07.1-document-graph-rag-and-gliner-gpu/gate-result.json</files>
  <read_first>
    - `.planning/phases/06-migration-correctness-gate/GATE.md` and
      `.planning/phases/06-migration-correctness-gate/gate-result.json` — read BOTH in full. These
      are the **shape** D-17's mini-gate must mirror: the structure, the evidence discipline, the
      verdict format. Do not invent a new gate format.
    - `.planning/phases/06-migration-correctness-gate/06-04-PLAN.md` — how Phase 6 drove
      `gate_diff.py`. Its CLI (`--baseline-benchmark`, `--port-runs` (repeatable), `--e2e-baseline`,
      `--e2e-port`, `--corpus-root`, `--manifest`, `--frozen-questions`, `--epsilon`,
      `--derive-corrected-baseline`, `--out`) is unchanged by this phase. D-17's mini-gate is a
      RE-RUN of this SAME CLI against a fresh pre-/post-ER-fix pair of captures, not a new script.
      Note STATE.md's 06-04 finding: `--derive-corrected-baseline` is a pipeline modifier, not a
      standalone single-file CLI mode.
    - `scripts/e2e_score.py` — `run_e2e` gates on `score >= 9.8 and check_count == 19`
      (`VALIDATED_10_10`). This IS the "19/19 at 10.0" bar D-17 names; do not re-derive a threshold.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md` — D-17 in full,
      including its precision note; D-19's constraint that this gate is the re-key's proof.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-11-SUMMARY.md` — what the
      rebuild actually does and its recorded caveats.
    - `baseline/03-turingdb/BASELINE.md` — the ARC-01 yardstick and run params, so the capture is
      comparable.
  </read_first>
  <action>
    Capture a PRE-ER-fix baseline and a POST-ER-fix run, then diff them with `gate_diff.py`:

    1. **Pre-capture.** With the live GPU stack and real providers, capture the E2E score and the
       frozen-question benchmark BEFORE running the re-key rebuild. If a suitable capture already
       exists from plan 09's measurement, reuse it rather than re-running — but confirm the code
       state matches and record which capture you used.
    2. **Run the rebuild.** Execute `memory_rebuild_entity_identity` for each tenant in scope. For
       the E2E gate's synthetic tenants, either the rebuild or a fresh re-ingest is viable (they
       have source files). **For the `dvdmarchetto@gmail.com` dogfooding tenant, the rebuild is the
       ONLY option** — those memories were written via `memory_add_fact` / `memory_store_message`
       and have no source files; the store IS the source (D-19). Never drop-and-re-ingest it.
    3. **Post-capture + diff.** Re-run `scripts/e2e_score.py` — it must print `VALIDATED_10_10`.
       Re-run the frozen-question benchmark via `gate_diff.py`'s existing CLI with Phase 6's locked
       `--epsilon` (0.03) to confirm the document path is untouched with the flag OFF.

    Write `07.1-GATE.md` mirroring Phase 6's structure, and emit `gate-result.json` in the same
    schema `gate_diff.py` already produces.

    **Carry D-17's precision note prominently in the artifact:** `document_search` has NO entity
    channel today, so the frozen-question document benchmark CANNOT move from the ER fix alone. A
    flat document benchmark is therefore an expected NULL result, not evidence the ER fix worked.
    The E2E gate's 19 checks are the guard. Stating this is what stops a future reader from
    misreading the artifact in either direction.

    **Never claim the pre-existing rebuild tools re-key entities** (D-19's final constraint;
    research Unknown 8a). If the gate artifact mentions `memory_rebuild_vector_projection` /
    `memory_rebuild_communities` at all, it must say they re-embed/re-cluster existing rows and do
    NOT re-derive identity — that is why `memory_rebuild_entity_identity` exists.

    **Precondition check — run this BEFORE step 2, and stop if it fails.** D-03's hub damping is a
    REQUIRED MITIGATION that must already exist, not a diagnosis to reach for after a regression.
    D-03: the ER fix "regresses precision without this counterweight — it cannot ship without it."
    Plan 17 (wave 2) wired `hub_damping_factor` into `store_evidence.py`'s entity-expansion scoring,
    and plan 13 wired it into the document channel. Confirm the memory-side wiring is present and
    reached — `grep -c 'hub_damping_factor' src/turing_agentmemory_mcp/store_evidence.py` returns at
    least 2, and `python -m pytest tests/test_fused_memory_search.py -k hub_damping_channel -x`
    exits 0 — before running the rebuild against any tenant. If either fails, the counterweight is
    absent: **do not run the rebuild.** Running it would knowingly manufacture undamped hubs on the
    production `memory_search` path (`fusion_weights` `"graph": 0.5`), against the dogfooding tenant
    whose recall D-19 exists to protect.

    If the E2E gate does NOT read 19/19 at 10.0 after the rebuild: STOP. Do not relax the threshold,
    do not adjust epsilon, do not re-run until it passes. That is the gate doing its job — the ER
    change regressed the memory path. Record the failure, identify which of the 19 checks moved, and
    surface it at the checkpoint. Note that with the precondition check above passing, "damping was
    never wired" is already excluded as a cause — investigate the damping's *reach* (is the count
    query returning real linked-memory counts, or silently zero/absent so every factor is 1.0?)
    rather than its existence.
  </action>
  <acceptance_criteria>
    - **Precondition, asserted before the rebuild ran:** `grep -c 'hub_damping_factor' src/turing_agentmemory_mcp/store_evidence.py` outputs at least `2` AND `python -m pytest tests/test_fused_memory_search.py -k hub_damping_channel -x` exits 0
    - `07.1-GATE.md` records that D-03's hub damping was verified active on the memory graph channel BEFORE the rebuild ran, citing plan 17
    - `python scripts/e2e_score.py --out e2e-results.json` prints `VALIDATED_10_10` AFTER the re-key rebuild has run
    - `gate-result.json` exists and validates against the schema `gate_diff.py` produces (reuse `validate_gate_result_schema` per STATE.md's 06-02 entry)
    - `07.1-GATE.md` exists, mirrors Phase 6's `GATE.md` structure, and records: the pre/post E2E scores and check counts, the frozen-question metrics vs epsilon=0.03, the tenants rebuilt, and the verdict
    - `07.1-GATE.md` contains D-17's precision note (document_search has no entity channel today; the flat document benchmark is an expected null result)
    - `grep -ric "rebuild_vector_projection.*re-key\|rebuild_communities.*re-key" .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-GATE.md` outputs `0`
    - `git diff --stat scripts/e2e_score.py scripts/gate_diff.py` shows NO changes (the machinery is reused unchanged)
    - The dogfooding tenant's canonical `Memory` row count is unchanged before/after (asserted and recorded)
  </acceptance_criteria>
  <verify>
    <automated>python scripts/e2e_score.py --out e2e-results.json</automated>
  </verify>
  <done>The re-key rebuild has run, the E2E gate reads `VALIDATED_10_10` (19/19 at 10.0), the frozen-question benchmark confirms the document path is unmoved within epsilon=0.03, and a Phase-6-shaped `GATE.md` + `gate-result.json` record it with the precision note intact.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure cross-document bridges and decide D-05 (SAME_AS)</name>
  <files>.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-D-05-DECISION.md</files>
  <read_first>
    - `.planning/spikes/001-doc-entity-graph-substrate/README.md` — read in full, especially the
      bridge measurement methodology: `(type,name)` = 1 bridge vs name-only = 3 bridges. You are
      RE-RUNNING that measurement on real re-keyed data. Note the spike's stated premise about
      neo4j's `enrichment/` is **RETRACTED** (CONTEXT.md correction 1): `enrichment/` does no
      canonicalization and no disambiguation — it is Wikipedia/Diffbot property decoration. Do not
      port it and do not cite it.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md` — **D-05 in full.**
      It is explicitly "a recorded decision point the plan must schedule, not skip." The decision is
      shaped by evidence: land the name-only key, re-run the bridge measurement, and let the
      RESIDUAL FRAGMENTATION SHAPE decide.
    - `src/turing_agentmemory_mcp/temporal_graph.py` — `canonicalize_entity_name`. D-05 notes it
      "may already cover" alias-shaped variants (`ArcadeDB` / `Arcade DB` / `arcade-db`) — check
      what it actually normalizes before concluding a `SAME_AS` mechanism is needed.
  </read_first>
  <action>
    Re-run spike 001's cross-document bridge measurement against the re-keyed graph and record the
    number next to the spike's original (1 bridge under `(type,name)`, 3 under name-only). Report
    what actually happened on real data at real scale, not the spike's projection.

    Then inspect the RESIDUAL fragmentation and classify its shape:
      - **Alias-shaped** (`ArcadeDB` / `Arcade DB` / `arcade-db` surviving as separate entities) —
        this is the ONLY shape that justifies reaching for D-05's `SAME_AS`. First check whether
        `canonicalize_entity_name` already collapses these; if it does, there is nothing to build.
      - **Not alias-shaped** (genuinely distinct surfaces) — then `SAME_AS` is not the answer and
        D-05 closes as NOT NEEDED.

    Write `07.1-D-05-DECISION.md` recording: the measured bridge counts (before/after re-key), the
    residual fragmentation shape with examples, and an explicit decision — BUILD `SAME_AS`, or CLOSE
    as not needed. Cite the evidence for whichever you choose.

    **If the decision is BUILD:** do NOT build it in this plan or this phase. Record it as a
    follow-up with its constraints already captured: HippoRAG 2's embedding-threshold `SAME_AS` (no
    LLM — we already have the embedder + native `LSM_VECTOR`); start at a **tight** threshold
    (mem0's *surviving* value is **0.95**; its deleted graph used 0.7); and if the tombstone-merge
    shape is ported, add the `Chunk` label to the transfer leg (neo4j's only matches `(m:Message)`),
    run inside one `run_in_transaction` (invariant #5), carry `user_identifier` on every leg
    (invariant #1), and **never delete the node** — set `merged_into` and have readers filter
    tombstones. That is the only merge shape compatible with deterministic IDs (invariant #4).

    Do not defer this silently. D-05 explicitly requires the decision be scheduled, and a
    measurement without a recorded decision is exactly the deferral it forbids.
  </action>
  <acceptance_criteria>
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-D-05-DECISION.md` exists
    - It records a measured cross-document bridge count on the re-keyed graph, alongside spike 001's original 1-vs-3 figures
    - It classifies the residual fragmentation shape with concrete example entity names from the real graph
    - It states explicitly whether `canonicalize_entity_name` already covers the observed alias variants, with evidence
    - It contains an explicit decision line: `SAME_AS`: BUILD (deferred, with constraints) or CLOSE (not needed)
    - If BUILD: it records the 0.95 starting threshold, the tombstone/never-delete constraint, and the `Chunk`-label transfer-leg requirement
    - It does NOT cite neo4j's `enrichment/` as a canonicalization/disambiguation reference (that claim is retracted)
    - No `SAME_AS` implementation code was written in this phase (`grep -rc 'SAME_AS' src/` outputs `0`)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-D-05-DECISION.md && grep -Eq "SAME_AS.*(BUILD|CLOSE)" .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-D-05-DECISION.md && test "$(grep -rc SAME_AS src/ | grep -v :0 | wc -l)" -eq 0 && echo D05_DECIDED'</automated>
  </verify>
  <done>The bridge measurement is re-run on real re-keyed data, the residual fragmentation shape is classified with real examples, and D-05 is explicitly decided (BUILD-deferred with constraints, or CLOSED) — not silently skipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — approve the D-17 mini-gate and the D-05 decision</name>
  <what-built>
    The D-17 mini migration-correctness gate: the re-key rebuild (D-19) run against real tenants, the
    E2E score gate re-verified at 19/19 / 10.0, the frozen-question benchmark re-run with the flag
    OFF, a Phase-6-shaped `GATE.md` + `gate-result.json`, and the D-05 SAME_AS decision record.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-GATE.md`. Confirm it
       mirrors Phase 6's shape and cites real measured numbers.
    2. Confirm the E2E result reads `VALIDATED_10_10` (score >= 9.8, check_count == 19) AFTER the
       rebuild — this is D-19's stated proof.
    3. Confirm the artifact carries D-17's precision note and does NOT claim the pre-existing
       rebuild tools re-key entities.
    4. **Verify your own tenant survived.** `dvdmarchetto@gmail.com` holds the live working record of
       this milestone and has no source files to re-ingest from. Confirm the canonical `Memory` row
       count is unchanged and spot-check recall via `memory_search` for a known prior decision (e.g.
       the Phase 6 ARC-09 gate verdict or the spike 001-003 verdicts).
    5. Read `07.1-D-05-DECISION.md` and confirm the SAME_AS decision matches the measured residual
       fragmentation shape. This is your call — D-05 deliberately left it to the evidence.
    6. Confirm the measured bridge count. If the re-key did NOT increase cross-document bridges, that
       is a material finding for the eval (plan 15): the substrate the graph channel depends on may
       be weaker than spike 001 projected.
  </how-to-verify>
  <resume-signal>Type "approved" plus the D-05 decision (BUILD or CLOSE), or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate execution -> live tenant data | The rebuild runs against real tenants, including the dogfooding tenant whose data has no source-file backup. |
| measured numbers -> phase verdict | These numbers authorize (or block) the memory-side ER change across the milestone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-27 | Tampering | Destroying the dogfooding tenant's data by drop-and-re-ingest — it has NO source files | critical | mitigate | The rebuild (plan 11) is the only path used for that tenant; re-ingest is confined to the E2E gate's synthetic tenants and the eval corpus, which have source files. Canonical `Memory` row count asserted unchanged; the blocking checkpoint has the user spot-check recall directly. |
| T-07.1-39 | Repudiation | Relaxing the threshold or re-running until green to make the gate pass | high | mitigate | The bar is `scripts/e2e_score.py`'s existing `score >= 9.8 and check_count == 19` and Phase 6's locked `epsilon=0.03`; `git diff --stat` asserts the gate machinery is unmodified; the plan explicitly instructs to STOP on failure rather than tune. |
| T-07.1-40 | Repudiation | Reading a flat document benchmark as evidence the ER fix worked | medium | mitigate | D-17's precision note is a required element of the artifact: `document_search` has no entity channel today, so the document benchmark cannot move from the ER fix alone; the E2E gate's 19 checks are the guard. |
| T-07.1-41 | Repudiation | Asserting the pre-existing rebuild tools re-key entities in a durable artifact | medium | mitigate | D-19's explicit constraint; grep-asserted absent from `07.1-GATE.md`. |
| T-07.1-55 | Tampering (quality) | Running the re-key rebuild against live tenants while D-03's mandatory counterweight is absent, manufacturing undamped hubs on the production `memory_search` graph channel and regressing the dogfooding tenant's recall | high | mitigate | The damping is wired in plan 17 (wave 2), this plan `depends_on` it, and Task 1 asserts it is present AND reached (grep + `-k hub_damping_channel`) BEFORE the rebuild runs. D-03's "cannot ship without it" is enforced as a precondition, not diagnosed after the fact. |
</threat_model>

<verification>
- `python scripts/e2e_score.py --out e2e-results.json` prints `VALIDATED_10_10` after the rebuild.
- `gate-result.json` validates against `gate_diff.py`'s schema; `07.1-GATE.md` mirrors Phase 6's shape.
- The dogfooding tenant's canonical row count is unchanged and recall spot-checks pass at the checkpoint.
- `07.1-D-05-DECISION.md` records a measured bridge count and an explicit BUILD/CLOSE decision.
- `scripts/e2e_score.py` and `scripts/gate_diff.py` are unmodified.
</verification>

<success_criteria>
The ER change is gated by Phase 6's own discipline: the re-key rebuild ran, the E2E gate still reads 19/19 at 10.0, the document path is confirmed unmoved with the flag OFF, and D-05 is decided on measured evidence rather than deferred.
</success_criteria>

<output>
Create `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-14-SUMMARY.md` when done.
Record: the pre/post E2E scores; the measured bridge count before/after re-key (this is a material
input to plan 15's eval expectations); the D-05 decision and the user's approval; and confirmation
the dogfooding tenant's canonical data survived intact.
</output>
</content>
