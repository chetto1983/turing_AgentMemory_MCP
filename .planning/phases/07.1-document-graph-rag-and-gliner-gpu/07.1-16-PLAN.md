---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 16
type: execute
wave: 7
depends_on: ["07.1-15"]
files_modified:
  - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [GRAPH-03, GRAPH-04]
must_haves:
  truths:
    - "The verdict applies ALL FOUR of D-15's GO conditions — a GO requires every one; failing any single condition is a NO-GO"
    - "The artifact states that the gate measures EFFICACY and that demand is UNMEASURED, citing spike 003's 0/15 as the only real-traffic signal we have"
    - "On NO-GO: the flag-gated channel AND the fusion port are DELETED; the substrate and the eval are KEPT"
    - "The verdict cites arXiv 2502.11371 + 2506.05690 + spike 003's 0/15 + our own measured number, so the question is not relitigated every quarter"
    - "No artifact claims the pre-existing rebuild tools re-key entities"
  artifacts:
    - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md
  key_links:
    - "multihop-eval-results.json (plan 15) -> D-15's four conditions -> GO/NO-GO -> D-16's disposition (delete channel+port, or productionize)"
    - "verdict -> ROADMAP/REQUIREMENTS status for GRAPH-03 and GRAPH-04"
---

<objective>
Apply D-15's pre-committed four-condition threshold to plan 15's measurements, write the verdict
artifact, and execute D-16's disposition.

Purpose: this is the decision the whole phase was built to buy. **The named failure mode is: build
it, measure +0.2, call it a win because it's already built.** D-15's conditions 1 and 2 exist to
prevent exactly that, and the threshold was pre-committed precisely so it could not be renegotiated
once the number was known. The eval is stacked **in the channel's favour** (multi-hop by
construction, each question verified unanswerable from one chunk). **If it cannot win here it cannot
win anywhere.** Meanwhile the literature ceiling is small — HippoRAG 2, the leading PPR system
**with an LLM write path**, gets MuSiQue +2.9 F1 and **HotpotQA +0.2**; our substrate (NER, no
triples) is weaker.
Output: `07.1-FINDINGS.md` with the verdict, and the disposition executed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-15-SUMMARY.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-13-SUMMARY.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-12-SUMMARY.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/multihop-eval-results.json
</context>

## Artifacts this phase produces (this plan)

New artifacts created here — none exist yet, exclude from drift verification:

- `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md` (the verdict)

<tasks>

<task type="auto">
  <name>Task 1: Compute the verdict against D-15's four conditions and write 07.1-FINDINGS.md</name>
  <files>.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md</files>
  <read_first>
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/multihop-eval-results.json` — plan
      15's raw measurements: per-arm mean recall@10, improved/regressed counts, per-arm latency with
      N and variance, and spike 003's 15-question yardstick re-run.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md` — **D-15 in full**
      (the four conditions and BOTH rationale paragraphs), **D-06 in full** (efficacy vs demand),
      and **D-16 in full** (the disposition). Also the `<canonical_refs>` research sources and the
      "Not verified — do NOT plan against" warning about the "GraphRAG 86% vs vector 32%" figures.
    - `.planning/spikes/003-doc-graphrag-quality-signal/README.md` — the 0/15 result and the ~10%
      latency cost for zero gain.
    - `.planning/phases/06-migration-correctness-gate/GATE.md` — the verdict-artifact discipline to
      mirror.
  </read_first>
  <action>
    Evaluate each of D-15's four conditions against the measured data. **A GO requires ALL FOUR.**
    Failing any single one is a NO-GO. Do not average them, do not weigh them, do not treat three of
    four as "mostly GO":

    1. `fused_graph` beats **the best of the two cheap arms** by **>= +0.10 mean recall@10** of the
       bridging passage set. Note carefully: the bar is the BEST cheap arm, **not** merely beating
       `fused_base`. If `entity_boost` is the best cheap arm, `fused_graph` must clear
       `entity_boost` + 0.10. mem0's name-keyed hub-damped single-hop index apparently *beat the
       graph it replaced* — multi-hop must beat that cheap baseline or we ship complexity a simpler
       mechanism already delivers.
    2. Improves **>= 1/3 of questions** with **ZERO regressions**. Zero means zero. One regression
       fails this condition regardless of the mean.
    3. **No regression > 0.03** on spike 003's existing 15-question frozen yardstick (matching Phase
       6's locked epsilon).
    4. **Latency <= +20%** vs the best cheap arm (spike 003 measured +10% for zero gain).

    Write `07.1-FINDINGS.md` recording, per condition: the measured value, the threshold, and
    PASS/FAIL — then the overall verdict. Mirror Phase 6's `GATE.md` evidence discipline.

    **D-06 is mandatory content, not a footnote.** The artifact MUST state explicitly:
      - The gate measures **EFFICACY**. A MuSiQue-style eval is multi-hop **by construction**, so it
        proves the channel works — **never that anyone will ask such questions.**
      - **Demand is UNMEASURED.** Cite spike 003's **0/15** as the only real-traffic signal we have,
        which points at single-passage lookups.
      - Therefore a GO is an efficacy result **plus a consciously-accepted demand bet**, and must be
        written down as such. Do not let the eval's construction launder into a demand claim.
    If plan 15's rejection rate was high, note it here too: a corpus that rarely yields genuine
    two-hop questions is a second demand signal pointing the same way as 0/15.

    Cite, per D-16, so the question is not relitigated every quarter:
      - arXiv **2502.11371** — RAG **beats** graph on single-hop factoid (NQ 64.78 vs 60.04 F1);
        graph wins multi-hop narrowly (HotpotQA 61.66 vs 60.04). Complementarity, not dominance.
      - arXiv **2506.05690** — graph "introduced redundant information, actually degrading
        performance" on simple fact retrieval; 40,000+ tokens/query vs 879 vanilla.
      - spike 003's **0/15** on the real yardstick, ~10% slower.
      - **our own measured number** from this eval.
    Do NOT cite the "GraphRAG 86% vs vector 32%" / "vector accuracy -> 0%" figures: they trace to
    vendor blogs with no reproducible methodology and contradict the controlled evaluations above by
    an order of magnitude.

    Never claim the pre-existing rebuild tools re-key entities (D-19; research Unknown 8a).
  </action>
  <acceptance_criteria>
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md` exists
    - It evaluates all FOUR D-15 conditions individually with measured value, threshold, and PASS/FAIL each
    - It states an unambiguous verdict line: `VERDICT: GO` or `VERDICT: NO-GO`
    - Condition 1 is evaluated against the BEST cheap arm (named explicitly), not against `fused_base` by default
    - The verdict is GO only if all four conditions read PASS (verify the logic, not just the prose)
    - It contains an explicit statement that demand is UNMEASURED and cites spike 003's 0/15
    - It cites arXiv 2502.11371, arXiv 2506.05690, spike 003's 0/15, and this eval's measured number
    - `grep -c '86%\|32%' .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md` outputs `0` (the unverified vendor figures are not cited)
    - `grep -ric "rebuild_vector_projection.*re-key\|rebuild_communities.*re-key" .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md` outputs `0`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md; test -f "$F" && grep -Eq "VERDICT: (GO|NO-GO)" "$F" && grep -qi "unmeasured" "$F" && grep -q "0/15" "$F" && grep -q "2502.11371" "$F" && grep -q "2506.05690" "$F" && echo FINDINGS_OK'</automated>
  </verify>
  <done>All four conditions are individually evaluated with measured values against their pre-committed thresholds, the verdict is GO only if all four pass, condition 1 is measured against the named best cheap arm, and the artifact states demand is UNMEASURED with the required citations.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Accept the computed GO/NO-GO verdict and authorize D-16's disposition</decision>
  <context>
    D-15's threshold was **pre-committed** so it could not be renegotiated once the number was known.
    The eval is stacked in the channel's favour by construction; if the channel cannot win here it
    cannot win anywhere. The literature ceiling is small (HippoRAG 2 — PPR **with an LLM write
    path** — gets HotpotQA +0.2; our NER-only substrate is weaker).

    Read `07.1-FINDINGS.md`. The named failure mode is: build it, measure +0.2, call it a win because
    it's already built. Conditions 1 and 2 exist to prevent that.

    Note: a GO is an efficacy result PLUS a consciously-accepted demand bet (D-06). Spike 003's 0/15
    is the only real-traffic signal we have, and it points at single-passage lookups.
  </context>
  <options>
    <option id="accept-go">
      <name>Accept GO — productionize the channel</name>
      <pros>All four pre-committed conditions passed on an eval designed to be readable. The channel earns its place on measured efficacy.</pros>
      <cons>Demand remains UNMEASURED — this consciously accepts a bet that real users will ask multi-hop questions, against the only traffic signal we have. Ongoing tax on invariants #1/#4/#5/#6.</cons>
    </option>
    <option id="accept-no-go">
      <name>Accept NO-GO — delete the channel and the fusion port, keep the substrate and the eval</name>
      <pros>Honors the pre-committed threshold. Removes a permanent tax on invariants #1/#4/#5/#6 against an upside our evidence says does not exist. The substrate still serves the ER fix and entity_boost; the eval survives as a permanent artifact so the question is settled, not relitigated.</pros>
      <cons>Plans 12 and 13's work is deleted. This is the designed outcome of a build-gated phase, not waste — the phase bought the decision, not the feature.</cons>
    </option>
    <option id="dispute-measurement">
      <name>Dispute the measurement — the eval itself is flawed</name>
      <pros>Legitimate if a real methodological defect exists (e.g. the 3-doc corpus fallback narrowed the bridge surface, or the question set is too small after a high rejection rate).</pros>
      <cons>Must be justified by a NAMED defect, not by dissatisfaction with the number. Re-running until favourable is the exact failure D-15's pre-commitment forbids.</cons>
    </option>
  </options>
  <resume-signal>Select: accept-go, accept-no-go, or dispute-measurement (with the named defect)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the disposition and update ROADMAP/REQUIREMENTS status</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md</files>
  <read_first>
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-FINDINGS.md` — the verdict and
      the user's decision from the checkpoint.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-13-SUMMARY.md` — it records the
      EXACT files/symbols to remove on a NO-GO, so the deletion is mechanical rather than
      archaeological. Read it before deleting anything.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-12-SUMMARY.md` — the fusion
      port's flag-OFF parity fixture and branch boundary.
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` — plan 01's Phase 07.1 entry and
      Traceability rows; you are updating status only.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md` — D-16 in full.
  </read_first>
  <action>
    **On NO-GO** (D-16's disposition — a real, executable outcome, not an afterthought):
      - **DELETE** the flag-gated channel (plan 13: `store_documents_graph.py`,
        `_DocumentsGraphMixin`, its `store.py` registration, `DOCUMENT_GRAPH_MAX_HOPS`) **and** the
        fusion port (plan 12: `_search_documents_fused`, the `doc_graph_enabled` flag, the `doc_`
        fusion weights, `DOCUMENT_MAX_PER_SOURCE`). `search_documents` returns to its
        `blend_hybrid_score` -> `_rerank_documents` shape. Plan 12's flag-OFF parity fixture is the
        proof the revert is clean: post-deletion results must still match it.
      - **KEEP** the substrate (plan 10: the `(:Chunk)-[:MENTIONS]->(:Entity)` edges and the query
        builders) — the ER fix path needs it and `entity_boost` may use it. **KEEP** the eval (plans
        07/15) as a permanent artifact. **KEEP** everything from GRAPH-01, PERF-04, and FIX-08 —
        those are the unconditional wins and were never contingent on this bet (D-01).
      - Dormant channel code is **rejected**: it would tax invariants #1/#4/#5/#6 forever against an
        upside our evidence says does not exist. Do not comment it out, do not leave it behind a
        flag, do not park it in a branch-and-forget. Delete it.
      - Mark `GRAPH-04` in REQUIREMENTS.md as closed-by-verdict (not "Complete" as if shipped, and
        not "Pending" as if outstanding) with a pointer to `07.1-FINDINGS.md`. Use a status word that
        reads honestly against the existing `Pending`/`Complete` vocabulary; if none fits, add one
        and note it in the coverage block — do NOT force a misleading fit. Coverage must stay 61/61
        with no orphans.
      - Run the full gate afterwards: the E2E score gate must still read `VALIDATED_10_10`, proving
        the deletion is as behavior-preserving as the flag-OFF path was.

    **On GO:**
      - Keep the channel and the port. Consider whether the flag's default should flip — but note
        D-16 does not automatically authorize that; a GO authorizes productionization, and the
        default-on decision should be recorded explicitly with the demand bet (D-06) attached.
      - Mark `GRAPH-03` and `GRAPH-04` Complete in REQUIREMENTS.md.
      - Record D-13's upgrade path (HippoRAG 2's PPR with dense-derived reset mass) as a follow-up
        IF the eval showed the channel matters and RRF is the limiter.

    **Either way:**
      - Mark `GRAPH-03` Complete — the decision artifact IS its deliverable, and it is delivered by
        either verdict.
      - Update the ROADMAP Phase 07.1 plan checklist and the Progress table row.
      - Use scoped `Edit` calls on ROADMAP.md and REQUIREMENTS.md — never `Write`. A whole-file
        rewrite would destroy unrelated phase entries.
      - Append the executed disposition to `07.1-FINDINGS.md` so the artifact records not just the
        verdict but what was actually done about it.
  </action>
  <acceptance_criteria>
    - The disposition matches the recorded verdict and the user's checkpoint decision
    - On NO-GO: `test ! -f src/turing_agentmemory_mcp/store_documents_graph.py` succeeds; `grep -rc 'doc_graph_enabled\|DOCUMENT_MAX_PER_SOURCE\|_search_documents_fused' src/` outputs `0`; `grep -rc 'AGENTMEMORY_DOC_GRAPH_ENABLED' src/ compose.yaml` outputs `0`
    - On NO-GO: the substrate SURVIVES — `grep -c 'chunk_entity_mentions_edge_statement' src/turing_agentmemory_mcp/store_documents_queries.py` outputs at least `1`, and `tests/test_store_documents_graph.py`'s substrate tests still pass
    - On NO-GO: `scripts/multihop_eval.py`, `scripts/multihop_eval_scoring.py`, and `multihop-questions.json` all still exist (the eval is kept)
    - On NO-GO: `search_documents` results still match plan 12's flag-OFF parity fixture
    - On GO: the channel and port remain; `GRAPH-04` is marked Complete
    - `python scripts/e2e_score.py --out e2e-results.json` prints `VALIDATED_10_10`
    - REQUIREMENTS.md coverage still reads 61 total / 61 mapped / 0 unmapped
    - `07.1-FINDINGS.md` records the executed disposition, not just the verdict
    - `python -m pytest -q` passes in full; `python -m ruff format --check src tests scripts` && `python -m ruff check src tests scripts` && `bash scripts/check-file-size.sh` && `docker compose config --quiet` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>The verdict's disposition is executed exactly as D-16 specifies — on NO-GO the channel and port are deleted while the substrate, the eval, and the unconditional wins survive; the E2E gate reads 19/19 at 10.0; coverage stays 61/61; the FINDINGS artifact records what was done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured numbers -> a pre-committed threshold -> shipped/deleted code | The verdict authorizes deleting or productionizing real code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-47 | Repudiation | Renegotiating the threshold after seeing the number — "build it, measure +0.2, call it a win because it's already built" | critical | mitigate | The four conditions were pre-committed in D-15 before any measurement; each is evaluated individually with measured value vs threshold and an explicit PASS/FAIL; a GO requires all four; the blocking decision checkpoint routes acceptance to the user, and disputing requires a NAMED methodological defect. |
| T-07.1-48 | Repudiation | Laundering the eval's construction into a demand claim | high | mitigate | D-06's statement is mandatory artifact content: the gate measures EFFICACY, demand is UNMEASURED, spike 003's 0/15 is the only real-traffic signal, and a GO is explicitly an accepted bet. Grep-asserted present. |
| T-07.1-49 | Tampering | Leaving dormant channel code after a NO-GO, taxing invariants #1/#4/#5/#6 forever | high | mitigate | D-16 rejects dormant code; the deletion is grep-asserted (no `doc_graph_enabled`, no `store_documents_graph.py`), and plan 13's SUMMARY records the exact removal list so it is mechanical. |
| T-07.1-50 | Tampering | Deleting too much on NO-GO — removing the substrate or the unconditional wins | high | mitigate | Acceptance criteria assert the substrate, the eval scripts, the question set, and GRAPH-01/PERF-04/FIX-08 all survive; plan 12's flag-OFF parity fixture and the E2E gate prove the revert is behavior-preserving. |
| T-07.1-51 | Tampering | A whole-file `Write` on ROADMAP.md / REQUIREMENTS.md destroying unrelated entries | medium | mitigate | Scoped `Edit` only; coverage asserted still 61/61 with 0 unmapped. |
</threat_model>

<verification>
- `07.1-FINDINGS.md` evaluates four conditions individually and states an unambiguous verdict.
- Condition 1 is measured against the named best cheap arm.
- The demand-UNMEASURED statement and all four required citations are present; the unverified vendor figures are absent.
- The executed disposition matches the verdict; on NO-GO the deletion is grep-proven and the substrate/eval survive.
- `python scripts/e2e_score.py --out e2e-results.json` prints `VALIDATED_10_10`.
- Full gate green: `ruff format --check`, `ruff check`, `check-file-size.sh`, `pytest -q`, `docker compose config --quiet`.
</verification>

<success_criteria>
The phase delivers its decision artifact: a verdict computed against a threshold that was pre-committed before the number was known, honest about efficacy vs demand, cited so it is not relitigated — and its disposition actually executed.
</success_criteria>

<output>
Create `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-16-SUMMARY.md` when done.
Record: the four condition results and the verdict; the user's decision; exactly what was deleted or
kept; confirmation that GRAPH-01 / PERF-04 / FIX-08 (the unconditional wins, D-01) are unaffected by
the verdict either way; and, on GO, whether the flag's default was flipped and with what recorded
demand bet.
</output>
</content>
