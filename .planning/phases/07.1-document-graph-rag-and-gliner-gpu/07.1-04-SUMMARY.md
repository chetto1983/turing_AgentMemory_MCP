---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 04
subsystem: retrieval
tags: [rrf, hub-damping, diversification, tdd]

requires:
  - phase: 07.1-03
    provides: Name-only entity identity whose deliberate hub creation requires D-03 damping
provides:
  - Exact reusable mem0 quadratic hub damping factor
  - Characterized document-source diversification cap and candidate-id fallback
affects: [07.1-12, 07.1-13, 07.1-15, 07.1-17]

tech-stack:
  added: []
  patterns:
    - Pure pre-fusion scoring primitive shared by graph retrieval consumers
    - Characterization tests at the source-diversification call boundary

key-files:
  created: []
  modified:
    - src/turing_agentmemory_mcp/retrieval_fusion.py
    - tests/test_retrieval_fusion.py

key-decisions:
  - "Keep hub damping as one pure reusable function; plans 13, 15, and 17 import it rather than re-derive the formula."
  - "Preserve diversify_fused unchanged and pin document behavior through explicit max_per_source call contracts."

patterns-established:
  - "Hub damping: use 1/(1 + 0.001*(n-1)^2) for n>1 and retain full weight for defensive n<=1 inputs."
  - "Document diversity: candidate_id identifies a chunk while source_memory_id identifies its document."

requirements-completed: [GRAPH-01, GRAPH-04]

coverage:
  - id: D1
    description: Exact mem0 quadratic hub damping with defensive, monotonic, and bounded behavior
    requirement: GRAPH-01
    verification:
      - kind: unit
        ref: "tests/test_retrieval_fusion.py#test_hub_damping_*"
        status: pass
      - kind: e2e
        ref: "python scripts/e2e_score.py --out e2e-results.json"
        status: pass
    human_judgment: false
  - id: D2
    description: Document-source diversification cap and empty-source candidate-id fallback
    requirement: GRAPH-04
    verification:
      - kind: unit
        ref: "tests/test_retrieval_fusion.py#TestDocumentDiversifyContract"
        status: pass
    human_judgment: false

duration: 9min
completed: 2026-07-29
status: complete
---

# Phase 07.1 Plan 04: Hub Damping and Document Diversification Summary

**Exact mem0 quadratic hub damping as a reusable pure function, with document-source diversification semantics pinned before the fusion port**

## Performance

- **Duration:** 9 min
- **Started:** 2026-07-29T09:13:30Z
- **Completed:** 2026-07-29T09:22:33Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `hub_damping_factor` with the exact `0.001` quadratic coefficient, exact full weight at `linked_count <= 1`, the D-03 worked values, monotonicity, and strict positivity.
- Characterized that `max_per_source=1` retains one chunk per document, `max_per_source=3` retains three, and an empty `source_memory_id` falls back to distinct chunk IDs.
- Preserved the retrieval-fusion implementation and confirmed the full deterministic E2E score remains `VALIDATED_10_10` with 19/19 checks.

## Task Commits

Each TDD step was committed atomically:

1. **Task 1 RED: failing hub-damping specification** - `3b028f6` (test)
2. **Task 1 GREEN: exact quadratic damping primitive** - `7bc5c84` (feat)
3. **Task 2: document diversification characterization** - `4f384ae` (test)

## Files Created/Modified

- `src/turing_agentmemory_mcp/retrieval_fusion.py` - Defines the reusable damping constant and pure factor function.
- `tests/test_retrieval_fusion.py` - Pins damping edge cases and document diversification semantics.

## Decisions Made

- The factor remains a pure arithmetic primitive. This plan defines it but does **not** activate damping in retrieval: plan 17 wires the existing memory graph channel, plan 13 wires the document graph channel, and plan 15 imports it for the `entity_boost` evaluation arm.
- `diversify_fused` remains unchanged. Document callers must use chunk IDs as `candidate_id`, document IDs as `source_memory_id`, and a cap greater than one; plan 12 owns that call-site wiring.

## TDD Gate Compliance

- RED `3b028f6` failed during collection because `hub_damping_factor` did not exist.
- GREEN `7bc5c84` followed RED and passed all five selected hub-damping cases.
- Task 2 is an intentionally green characterization of already-correct behavior and changed tests only.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The global Python interpreter was not editable-installed, so the plan's direct import snippets initially raised `ModuleNotFoundError`. The same assertions passed as `DAMPING_OK` and `MONOTONIC_OK` under the repository's supported `.venv` interpreter.
- ArcadeDB was stopped before the E2E gate. The repository-owned service was started, reached healthy, produced `VALIDATED_10_10` with 19/19 checks, and was stopped again.

## Recall Context

Configured Turing AgentMemory recall was unavailable in this executor session. No tenant identity was guessed and no memory call was attempted; execution used checked-in plans, repository guidance, tests, and source evidence.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 12 can rely on the characterized `max_per_source=3` document behavior without changing fusion internals.
- Plans 13, 15, and 17 can import `hub_damping_factor`; re-deriving the formula would violate the shared-helper contract.
- Damping is not active on production `memory_search` until plan 17 lands, which must precede plan 11's rebuild-created hubs.

## Self-Check: PASSED

- Confirmed both modified files and this summary exist.
- Confirmed all three task commits exist in git history.
- Re-ran the complete retrieval-fusion test file and the exact damping assertions successfully.
- Validated both coverage entries as fully automated and passing.

---
*Phase: 07.1-document-graph-rag-and-gliner-gpu*
*Completed: 2026-07-29*
