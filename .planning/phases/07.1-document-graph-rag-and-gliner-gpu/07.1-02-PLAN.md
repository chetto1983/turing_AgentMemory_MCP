---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/store_documents.py
  - src/turing_agentmemory_mcp/store_documents_ingest.py
  - src/turing_agentmemory_mcp/store.py
autonomous: true
requirements: [FIX-08, GRAPH-02, GRAPH-04]
must_haves:
  truths:
    - "bash scripts/check-file-size.sh exits 0 with store_documents.py under 600 LOC and enough headroom for the FIX-08 and GRAPH-04 work that follows"
    - "The public store API is unchanged: ingest_document_text / reindex_document_text / search_documents / get_document / delete_document all resolve through TuringAgentMemory exactly as before"
    - "The full test suite is green with zero behavior change — this is a pure move"
  artifacts:
    - src/turing_agentmemory_mcp/store_documents_ingest.py
  key_links:
    - "store.py facade -> _DocumentsIngestMixin in the TuringAgentMemory MRO (mixin order must keep every existing method resolvable)"
    - "_create_document -> _embed_many / _write_many / _process_texts_for_storage resolved via the MRO from _UtilsMixin and _StoreCore"
---

<objective>
Create 600-LOC headroom in `store_documents.py` by extracting the document ingest write path into
a new `store_documents_ingest.py` mixin, following the established mixin-composed-facade pattern.
Pure move: zero behavior change.

Purpose: `store_documents.py` is at **585/600** (verified this session). `check-file-size.sh` is a
**pre-commit hook**, not a plan-time check — plan 08 (FIX-08 per-chunk extraction) and plan 12
(GRAPH-04 fusion port) both add code to this file and would fail the commit immediately. This
split must land FIRST. It is sequencing, not cleanup.
Output: `store_documents_ingest.py` mixin owning the ingest write path; `store_documents.py`
trimmed to the read/search path with real headroom.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@src/turing_agentmemory_mcp/store_documents.py
@src/turing_agentmemory_mcp/store.py
@src/turing_agentmemory_mcp/store_chunking.py
</context>

## Artifacts this phase produces (this plan)

New symbols created here — they do not exist yet, exclude from drift verification:

- `src/turing_agentmemory_mcp/store_documents_ingest.py` (new module)
- `_DocumentsIngestMixin` (new class in that module)

<tasks>

<task type="auto">
  <name>Task 1: Extract the ingest write path into store_documents_ingest.py</name>
  <files>src/turing_agentmemory_mcp/store_documents_ingest.py, src/turing_agentmemory_mcp/store_documents.py, src/turing_agentmemory_mcp/store.py</files>
  <read_first>
    - `src/turing_agentmemory_mcp/store_documents.py` — read in full (585 lines). You are moving
      code out of it; you must see its exact current state, its import block, and which helpers
      each moved method resolves via the MRO.
    - `src/turing_agentmemory_mcp/store.py` — the facade. See how existing `store_<concern>.py`
      mixins are imported and ordered in the `TuringAgentMemory` class definition.
    - `src/turing_agentmemory_mcp/store_chunking.py` — read its module docstring. This module was
      split out of `store_documents.py` in phase 01-01 for exactly this reason. It is the
      precedent to copy: docstring style, mixin naming, import shape.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-RESEARCH.md` — "Unknown 4 —
      the 600-LOC cap collision" for the verified line counts and the split recommendation.
  </read_first>
  <action>
    Create `src/turing_agentmemory_mcp/store_documents_ingest.py` defining a
    `_DocumentsIngestMixin` class. MOVE (cut, do not copy) these methods from
    `store_documents.py` into it, verbatim — no logic edits in this plan:
      - `ingest_document_text`
      - `reindex_document_text`
      - `_create_document`
      - `_update_document_metadata`

    LEAVE in `store_documents.py`: `get_context`, `get_document`, `delete_document`,
    `search_documents`. Research recommends this boundary explicitly: `search_documents` is
    plan 12's fusion-port target and needs the headroom this split creates, and
    `_rerank_documents` already lives in `store_search.py` — do NOT move document search into
    `store_search.py` (559/600, memory-search-specific by design).

    Give the new module a docstring in `store_chunking.py`'s style, stating: it is the document
    ingest write path split out of `store_documents.py` (phase 07.1-02) to create 600-LOC headroom
    for FIX-08's per-chunk extraction and GRAPH-04's fusion port; no instance state is defined
    here; all helpers resolve via the `TuringAgentMemory` MRO.

    Move only the imports each moved method actually uses; delete now-unused imports from
    `store_documents.py`'s import block (ruff F401 will catch leftovers). Expect
    `document_create_statement`, `document_edge_statement`, `chunk_create_statement`,
    `has_chunk_edge_statement`, `next_chunk_edge_statement`, `stable_id`, `sparse_vector`, and
    `IngestedDocument` to be needed by the new module; verify against the real code rather than
    assuming this list is complete or minimal.

    In `store.py`, import `_DocumentsIngestMixin` and add it to the `TuringAgentMemory` base list
    adjacent to the existing documents mixin, matching the surrounding ordering convention. Do not
    reorder any existing mixin.

    Fix-on-touch applies but SCOPE CONTROL dominates here: this is a pure move so that the next
    plans' diffs are readable. Do not refactor the moved method bodies, do not rename anything, do
    not "improve" the logic. Any genuine bug you find must be recorded in the SUMMARY and fixed in
    the plan that owns that behavior (08 or 12), not smuggled into this move.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-file-size.sh` exits 0
    - `wc -l src/turing_agentmemory_mcp/store_documents.py` reports a value <= 420 (leaving >= 180 lines of headroom for plans 08 and 12)
    - `wc -l src/turing_agentmemory_mcp/store_documents_ingest.py` reports a value <= 600
    - `grep -c 'def ingest_document_text' src/turing_agentmemory_mcp/store_documents.py` outputs `0`
    - `grep -c 'def _create_document' src/turing_agentmemory_mcp/store_documents.py` outputs `0`
    - `grep -c 'def search_documents' src/turing_agentmemory_mcp/store_documents.py` outputs `1`
    - `grep -c '_DocumentsIngestMixin' src/turing_agentmemory_mcp/store.py` outputs at least `2` (import + base list)
    - `python -m pytest tests/test_store_arcadedb_documents.py -q` passes with the same test count as before the move
    - `python -m ruff check src tests scripts` exits 0 (no F401 leftovers)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_store_arcadedb_documents.py -q</automated>
  </verify>
  <done>The ingest write path lives in `store_documents_ingest.py`; `store_documents.py` is <= 420 LOC; `check-file-size.sh` exits 0; document tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the move is behavior-preserving across the full suite and the commit gate</name>
  <files>src/turing_agentmemory_mcp/store_documents.py, src/turing_agentmemory_mcp/store_documents_ingest.py, src/turing_agentmemory_mcp/store.py</files>
  <read_first>
    - `CLAUDE.md` — the "Post-edit validation" section: the pre-commit gate order is
      `ruff format --check` -> `ruff check` -> `check-file-size.sh`, and `ruff format --check` is a
      SEPARATE gate that fails on its own. CI pins ruff `0.15.21`.
    - `src/turing_agentmemory_mcp/store_documents_ingest.py` — the module you just created.
  </read_first>
  <action>
    Run the full gate in CLAUDE.md's exact order and fix anything red:
      1. `python -m ruff format --check src tests scripts` (run without `--check` to fix)
      2. `python -m ruff check src tests scripts`
      3. `bash scripts/check-file-size.sh`
      4. `python -m pytest -q`

    Re-run `check-file-size.sh` AFTER any format pass — reformatting changes line counts and can
    push a file back over the cap. This is a documented trap in CLAUDE.md, and this plan's entire
    purpose is line counts.

    Confirm the public API is intact by asserting `TuringAgentMemory` still resolves every moved
    method through the MRO — a fresh `TuringAgentMemory` class must expose `ingest_document_text`,
    `reindex_document_text`, `search_documents`, `get_document`, and `delete_document` as
    callables. If the suite passes but any of these is missing, the mixin was not wired into
    `store.py`'s base list.

    Do not add new tests in this plan. The existing suite IS the behavior-preservation proof for a
    pure move; a new test here would be testing the move rather than the behavior.
  </action>
  <acceptance_criteria>
    - `python -m ruff format --check src tests scripts` exits 0
    - `python -m ruff check src tests scripts` exits 0
    - `bash scripts/check-file-size.sh` exits 0 AND prints `check-file-size: all tracked *.py files within the 600-LOC cap.`
    - `python -m pytest -q` passes with the same test count as the pre-move baseline (record both numbers in the SUMMARY)
    - `python -c "from turing_agentmemory_mcp.store import TuringAgentMemory as T; assert all(callable(getattr(T, n)) for n in ('ingest_document_text','reindex_document_text','search_documents','get_document','delete_document')); print('API_INTACT')"` prints `API_INTACT`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>All four gate commands are green, the suite test count is unchanged from the pre-move baseline, and every moved public method still resolves on `TuringAgentMemory`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (new) | Pure code move. No new trust boundary; the existing client->API and API->ArcadeDB boundaries are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-05 | Tampering | `_create_document` / `ingest_document_text` losing a `user_identifier` bind or a `_require_user` / `_ensure_user` call during the move | high | mitigate | Move methods verbatim (cut/paste, no edits); the existing tenant-scoping tests in `tests/test_store_arcadedb_documents.py` plus the full suite must pass with an unchanged test count. |
| T-07.1-06 | Repudiation | An `_audit(...)` call dropped during the move, silently losing the content-free audit trail | medium | mitigate | Verbatim move; `grep -c '_audit(' ` across `store_documents.py` + `store_documents_ingest.py` must equal the pre-move count in `store_documents.py` alone. |
</threat_model>

<verification>
- Pre-move vs post-move: `git stash` / compare `python -m pytest -q` counts — identical.
- `git diff --stat` shows lines moving between two files, with no net logic delta in the moved bodies (`git diff -M` should detect the move).
- Combined `_audit(` occurrence count across the two files equals the pre-move count.
</verification>

<success_criteria>
`store_documents.py` <= 420 LOC with the ingest write path in `store_documents_ingest.py`; the four-command gate is green; the suite count is unchanged; the public store API resolves identically.
</success_criteria>

<output>
Create `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-02-SUMMARY.md` when done.
Record the pre-move and post-move `store_documents.py` LOC and suite test counts, and any bug found
but deliberately NOT fixed here (with the plan that owns it).
</output>
</content>
