---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/turing_agentmemory_mcp/temporal_graph.py
  - src/turing_agentmemory_mcp/arcadedb_schema.py
  - src/turing_agentmemory_mcp/store_memory_queries.py
  - tests/test_temporal_graph.py
autonomous: true
requirements: [GRAPH-01]
must_haves:
  truths:
    - "GLiNER type-drift variants of one surface (arcadedb as product / library / framework) collapse onto ONE entity id"
    - "entity_type survives as a non-identity attribute — it is still stored and still readable, just not part of the key"
    - "type_observations accumulates per-type counts across mentions and never overwrites a prior observation"
    - "stable_id() remains the sole identifier mechanism — no signature change, no new id scheme (invariant #4)"
  artifacts:
    - src/turing_agentmemory_mcp/temporal_graph.py
    - tests/test_temporal_graph.py
  key_links:
    - "temporal_graph.py canonical_by_key dict key -> the persistent stable_id() key (the two MUST agree or in-memory dedup diverges from stored identity)"
    - "Entity.type_observations_json property -> entity_create_statement -> the UPSERT write path"
---

<objective>
Re-key entity identity on the canonical name alone (D-02), demote `entity_type` to a non-identity
attribute, and add a `type_observations` map that accumulates per-type observation counts.

Purpose: the current `(entity_type, canonical_name)` key at `temporal_graph.py:137` splits ONE
entity into MANY under GLiNER type-drift (`arcadedb` -> product / library / framework), measured
at **3x fewer cross-document bridges** by spike 001. mem0 keys on normalized name in both its LLM
graph and its NER replacement; the industrial literature calls name-only "the floor — do this
regardless." `type_observations` preserves the evidence base for a future deliberate homograph
split, which is the accepted cost of this merge.
Output: a name-only entity key on the memory projection path, a `type_observations` map on the
Entity vertex, and unit tests that pin both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@src/turing_agentmemory_mcp/temporal_graph.py
@src/turing_agentmemory_mcp/ids.py
@src/turing_agentmemory_mcp/store_memory_queries.py
@src/turing_agentmemory_mcp/arcadedb_schema.py
</context>

## Artifacts this phase produces (this plan)

New symbols created here — they do not exist yet, exclude from drift verification:

- `type_observations` field on `EntityProjection` (`temporal_graph.py`)
- `type_observations_json` property on the ArcadeDB `Entity` vertex type (`arcadedb_schema.py`)
- `type_observations_json` parameter on `entity_create_statement` (`store_memory_queries.py`)

<feature>
  <name>Name-only entity key + type_observations accumulation</name>
  <files>src/turing_agentmemory_mcp/temporal_graph.py, tests/test_temporal_graph.py</files>
  <behavior>
    Given one canonical surface observed under drifting GLiNER labels, identity is the name:
    - `plan_temporal_projection` over mentions [("ArcadeDB", label="product"), ("arcadedb", label="library"), ("Arcade DB", label="framework")] for one user yields entities whose ids are ALL EQUAL to `stable_id("ent", user_identifier, canonicalize_entity_name("ArcadeDB"))` — one entity, not three (today: three).
    - The same surface for a DIFFERENT `user_identifier` yields a DIFFERENT id (tenant scope is part of the key and stays part of it — invariant #1).
    - `entity_type` is still populated on the projection (non-identity attribute, not deleted).
    - `type_observations` for that entity equals `{"product": 1, "library": 1, "framework": 1}`.
    - Two mentions with the SAME label increment that label: two `product` mentions -> `{"product": 2}`.
    - `type_observations` never overwrites: a later single-type mention merges into the existing map rather than replacing it.
    - Highest-confidence mention still wins `display_name`/`content` (existing behavior at `temporal_graph.py:153-160`, must be preserved).
  </behavior>
  <implementation>
    Change the `canonical_by_key` dict key and the `stable_id()` argument list; accumulate a
    per-entity `Counter`-shaped map over `entity_type`; persist it as JSON. See tasks.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED then GREEN — name-only entity key + type_observations in temporal_graph.py</name>
  <files>src/turing_agentmemory_mcp/temporal_graph.py, tests/test_temporal_graph.py</files>
  <read_first>
    - `src/turing_agentmemory_mcp/temporal_graph.py` lines 100-200 — read in full. The two call
      sites you change are `canonical_by_key` (line 125, keyed `tuple[str, str]` = `(entity_type,
      canonical_name)`) and the `stable_id("ent", episode.user_identifier, entity_type,
      canonical_name)` call at line 137. Note `_normalize_identifier(mention.label)` at line 132
      and `canonicalize_entity_name(mention.text)` at line 133 — REUSE BOTH VERBATIM.
    - `src/turing_agentmemory_mcp/ids.py` — confirm `stable_id(prefix, *parts)` is variadic, so
      dropping an argument needs no signature change (invariant #4: no new id mechanism).
    - `tests/test_temporal_graph.py` (200 LOC, EXISTS — extend it, do not create a new file) — copy
      its existing fixture/assertion style for building `EpisodeContext` + `MemoryExtraction`.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md` — D-02 in full,
      including the accepted cost (true homographs merge) and the rejected alternative (neo4j
      POLE+O), so you do not reintroduce type into the key "just for safety".
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-RESEARCH.md` — the "Entity
      re-keying call-site change (D-02)" code example, which flags that `canonical_by_key` must
      change too or in-memory dedup diverges from persistent identity.
  </read_first>
  <action>
    RED first. Add tests to `tests/test_temporal_graph.py` covering every case in `<behavior>`
    above. Name them so the VALIDATION.md commands select them: the key tests must match
    `-k entity_key` and the map tests must match `-k type_observations`. Run them and confirm they
    FAIL against current code. Commit: `test(07.1-03): add failing tests for name-only entity key and type_observations`.

    GREEN. In `plan_temporal_projection`:
      - Change `canonical_by_key`'s type annotation from `dict[tuple[str, str], EntityProjection]`
        to `dict[str, EntityProjection]` and set `key = canonical_name` (drop `entity_type` from
        the key). This is the line-125 change; leaving it keyed on the pair would let one
        extraction call build two in-memory entities that then collide on one persistent id.
      - Change the line-137 call to `stable_id("ent", episode.user_identifier, canonical_name)`.
        `episode.user_identifier` STAYS in the key — this is invariant #1, not an optimization.
      - Add a `type_observations: dict[str, int]` field to `EntityProjection`. `EntityProjection`
        is `@dataclass(frozen=True, slots=True)`, and a `dict` default needs
        `field(default_factory=dict)`; because the class is frozen you must build the merged map
        and pass it through `replace(...)` rather than mutating in place. Accumulate counts per
        `entity_type` across all mentions that resolve to the same `canonical_name`.
      - `entity_type` remains a populated `EntityProjection` field. Do NOT remove it. It is
        demoted from identity, not deleted — D-02 keeps it as a rerank/filter signal.
      - Preserve the existing highest-confidence-wins `display_name`/`content` behavior at lines
        153-160 exactly. Note `content` is currently `f"{mention.text} ({entity_type})"`; keep that
        formatting as-is — changing it would silently change the entity embedding text and is out
        of scope for this phase (PROJECT.md: changing the embedding model / re-embedding semantics
        is gated separately).

    Run the tests; confirm GREEN. Commit: `feat(07.1-03): key entities on canonical name only, accumulate type_observations`.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_temporal_graph.py -k entity_key -x` exits 0
    - `python -m pytest tests/test_temporal_graph.py -k type_observations -x` exits 0
    - `temporal_graph.py` contains the literal `stable_id("ent", episode.user_identifier, canonical_name)`
    - `grep -c 'stable_id("ent", episode.user_identifier, entity_type, canonical_name)' src/turing_agentmemory_mcp/temporal_graph.py` outputs `0`
    - `grep -n 'canonical_by_key' src/turing_agentmemory_mcp/temporal_graph.py` shows the annotation is `dict[str, EntityProjection]`
    - `grep -c 'entity_type=entity_type' src/turing_agentmemory_mcp/temporal_graph.py` is >= 1 (entity_type still populated as a non-identity attribute)
    - The RED commit precedes the GREEN commit in `git log --oneline` for this plan
    - `python -m pytest tests/test_temporal_graph.py -q` passes in full
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_temporal_graph.py -q</automated>
  </verify>
  <done>One canonical surface under three drifting labels yields ONE entity id scoped to the tenant; `type_observations` reads `{"product": 1, "library": 1, "framework": 1}`; `entity_type` is still populated; RED preceded GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist type_observations on the Entity vertex</name>
  <files>src/turing_agentmemory_mcp/arcadedb_schema.py, src/turing_agentmemory_mcp/store_memory_queries.py, tests/test_temporal_graph.py</files>
  <read_first>
    - `src/turing_agentmemory_mcp/arcadedb_schema.py` — find the `CREATE PROPERTY <type>.<name> IF
      NOT EXISTS <datatype>` idempotent-DDL idiom (research cites it at lines 22-23) and the
      `Entity` vertex type's existing property declarations. NOTE the recorded live finding
      (STATE.md): `CREATE PROPERTY` and `CREATE VERTEX/EDGE TYPE` DO support `IF NOT EXISTS`;
      `CREATE INDEX` does NOT. You are adding a property, so `IF NOT EXISTS` is available.
    - `src/turing_agentmemory_mcp/store_memory_queries.py` around lines 30-50 —
      `entity_create_statement` and the `tags_json` / `metadata_json` convention. This is the
      pattern you mirror.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-RESEARCH.md` — "Unknown 7 —
      `type_observations` storage shape". The resolved answer: a single JSON-serialized STRING
      column via the existing `self._json_dumps` / `self._json_loads` helpers. There is NO
      precedent for a native ArcadeDB `EMBEDDED MAP` in this codebase and none is needed.
  </read_first>
  <action>
    RED first: extend `tests/test_temporal_graph.py` (matching `-k type_observations`) with a test
    that `entity_create_statement` binds a `type_observations_json` param carrying the
    JSON-serialized map, and that an entity with no observations binds `"{}"` (not `null`, not an
    omitted key — `fuse_rankings`-adjacent readers and the `_json_loads(..., default)` helper both
    expect a parseable string). Confirm RED. Commit:
    `test(07.1-03): add failing test for type_observations_json persistence`.

    GREEN:
      - In `arcadedb_schema.py`, declare `type_observations_json` on the `Entity` vertex type using
        the existing `CREATE PROPERTY Entity.type_observations_json IF NOT EXISTS STRING` idiom,
        placed with the other `Entity` property declarations. Follow the exact datatype spelling
        the neighbouring `tags_json`/`metadata_json`-style properties use — read them, do not guess
        between `STRING` and `TEXT`.
      - In `store_memory_queries.py`, add a `type_observations_json` keyword parameter to
        `entity_create_statement` and bind it as a param exactly like `tags_json`/`metadata_json`
        (bound param, never string-interpolated — `store_retrieval_queries.py`'s module docstring
        makes this a hard convention).
      - Update the call site(s) that build entity statements from an `EntityProjection` to pass
        `self._json_dumps(entity.type_observations or {})`. Find them with
        `grep -rn 'entity_create_statement' src/` — do not assume there is exactly one.

    Do NOT write the accumulation-on-UPSERT logic here. Merging `type_observations` when a mention
    collapses onto an ALREADY-STORED entity is the re-key rebuild's job (plan 11, D-19) and the
    chunk-substrate write path's job (plan 10, D-12). This task persists the map the projection
    already computes in-process for one extraction call. Keeping that boundary is what makes plan
    11's idempotency test meaningful.

    Confirm GREEN. Commit: `feat(07.1-03): persist type_observations_json on the Entity vertex`.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_temporal_graph.py -k type_observations -x` exits 0
    - `grep -c 'type_observations_json' src/turing_agentmemory_mcp/arcadedb_schema.py` outputs at least `1`
    - `arcadedb_schema.py` contains the literal `Entity.type_observations_json IF NOT EXISTS`
    - `grep -c 'type_observations_json' src/turing_agentmemory_mcp/store_memory_queries.py` outputs at least `2` (signature + bound param)
    - `store_memory_queries.py` binds the value as `:type_observations_json` and contains no f-string or `.format(` interpolation of it
    - `python -m pytest -q` passes in full
    - `python -m ruff format --check src tests scripts` && `python -m ruff check src tests scripts` && `bash scripts/check-file-size.sh` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_temporal_graph.py -q</automated>
  </verify>
  <done>`type_observations_json` is declared idempotently on the Entity vertex, bound as a parameter in `entity_create_statement`, populated from the projection at every call site, and defaults to `"{}"`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extracted entity text -> entity identity | GLiNER output (derived from untrusted document/memory content) determines `canonical_name`, which is now the whole identity key besides the tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-07 | Spoofing / Information Disclosure | `stable_id("ent", user_identifier, canonical_name)` — dropping `user_identifier` from the key would collapse entities ACROSS tenants | critical | mitigate | `episode.user_identifier` stays the first key part; a dedicated test asserts the same surface under two tenants yields two distinct ids. Invariant #1 is not weakened by D-02 — only `entity_type` is dropped. |
| T-07.1-08 | Tampering | Adversarial document text crafting a canonical name that collides with an unrelated entity (homograph merge) | low | accept | D-02 explicitly accepts homograph merging: the corpus is Italian legal/technical where measured fragmentation is the real problem and homographs are rare. `type_observations` preserves the evidence to split later; neo4j's POLE+O collapse is the recorded fallback (D-02) if name-only proves too coarse. |
</threat_model>

<verification>
- `python -m pytest tests/test_temporal_graph.py -q` green.
- The three-label drift case yields exactly one distinct entity id; the two-tenant case yields exactly two.
- `git log --oneline` for this plan shows RED (`test(...)`) commits strictly before their GREEN (`feat(...)`) counterparts.
- Full gate: `ruff format --check`, `ruff check`, `check-file-size.sh`, `pytest -q` all green.
</verification>

<success_criteria>
Entity identity is `stable_id("ent", user_identifier, canonical_name)`. Type drift no longer fragments an entity. `entity_type` survives as a non-identity attribute. `type_observations` accumulates per-type counts in-projection and persists as `type_observations_json`.
</success_criteria>

<output>
Create `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-03-SUMMARY.md` when done.
Explicitly record: this change affects NEWLY-INGESTED data only. Already-stored entities remain
fragmented under the old key until plan 11's re-key rebuild (D-19) runs. Do not claim otherwise —
`memory_rebuild_vector_projection` / `memory_rebuild_communities` do NOT re-key (research Unknown 8a).
</output>
</content>
