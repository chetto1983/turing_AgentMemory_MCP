---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 09
type: execute
wave: 2
depends_on: ["07.1-05", "07.1-06"]
files_modified:
  - scripts/gliner_cpu_probe.py
  - tests/test_gliner_cpu_probe.py
  - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-PERF-04-MEASUREMENT.md
autonomous: false
requirements: [PERF-04]
must_haves:
  truths:
    - "The current pinned FastGLiNER2 sidecar remains on CPU and keeps the existing /extract endpoint"
    - "Throughput is measured after plan 06's fan-out with the same code and the same 167-chunk corpus"
    - "The measurement records run count, provider configuration, wall time, throughput, and variance"
    - "No CUDA, GLiREL, alternative ONNX adapter, or model swap is introduced before the device checkpoint"
    - "The user makes the permanent CPU-versus-renewed-CUDA decision from measured CPU evidence"
  artifacts:
    - scripts/gliner_cpu_probe.py
    - tests/test_gliner_cpu_probe.py
    - .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-PERF-04-MEASUREMENT.md
  key_links:
    - "scripts/gliner_cpu_probe.py drives the existing agentmemory-gliner /extract endpoint"
    - "07.1-PERF-04-MEASUREMENT.md records the exact pinned model and GLINER_BATCH_SIZE used"
---

<objective>
Measure the same-code CPU GLiNER path after plan 06's bounded fan-out, then make the
CPU-versus-CUDA decision from evidence.

Purpose: plan 05's CUDA build attempt was stopped and the user redirected execution to CPU
first. This plan replaces the stale GPU-binding work with the required CPU measurement. It
does not add a service, change an endpoint, introduce GLiREL, or treat the two recorded ONNX
repositories as drop-in FastGLiNER2 models.

Output: a reusable CPU probe, deterministic tests for its result contract, a measured record,
and a blocking device/corpus decision.
</objective>

<execution_context>
@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@CONTRIBUTING.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-05-SUMMARY.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-06-SUMMARY.md
@compose.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a reproducible CPU throughput probe and result-contract tests</name>
  <files>scripts/gliner_cpu_probe.py, tests/test_gliner_cpu_probe.py</files>
  <read_first>
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-05-SUMMARY.md`
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-06-SUMMARY.md`
    - `.planning/spikes/003-doc-graphrag-quality-signal/README.md`
    - `scripts/real_document_benchmark.py`
    - `src/turing_agentmemory_mcp/gliner_provider.py`
    - `src/turing_agentmemory_mcp/gliner_provider_extraction.py`
  </read_first>
  <action>
    Create `scripts/gliner_cpu_probe.py`, a small CLI that drives the existing GLiNER
    `POST /extract` contract with a JSON corpus and writes machine-readable JSON. The result
    must include model, device, concurrency width, input count, run count, per-run wall times,
    mean wall time, standard deviation, and mean chunks/second.

    Default the endpoint to the existing Compose service when invoked inside the network, but
    require an explicit URL or environment value from the host. Do not publish a new port and do
    not add another provider. Keep transport and aggregation helpers pure enough for unit tests.

    Use repeated runs over the same ordered texts. Reject empty corpora, non-positive run counts,
    malformed provider responses, and result-count mismatches. Do not add `pytest.skip`; a missing
    live provider is a visible measurement failure, while unit tests use a deterministic local
    stub.

    Add focused tests for the JSON schema, timing aggregation, request batching/order, and
    fail-closed response validation.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gliner_cpu_probe.py -q</automated>
  </verify>
  <done>The CPU probe reproduces the existing /extract workload and emits validated, machine-readable timing data without changing runtime topology.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the pinned CPU sidecar on the 167-chunk corpus</name>
  <files>.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-PERF-04-MEASUREMENT.md</files>
  <read_first>
    - `.planning/spikes/003-doc-graphrag-quality-signal/README.md`
    - `.planning/spikes/003-doc-graphrag-quality-signal/run.py`
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md`
    - `compose.yaml`
  </read_first>
  <action>
    Run the probe against the healthy existing CPU sidecar after plan 06. Use spike 003's
    ordered 167-chunk set, at least three timed runs, the pinned
    `lion-ai/gliner2-base-v1-onnx` revision, and the checked-in concurrency width. If the exact
    167 texts cannot be reconstructed from committed spike inputs, stop and report the missing
    corpus instead of substituting or fabricating data.

    Store raw JSON under `.benchmarks/` and write
    `07.1-PERF-04-MEASUREMENT.md` with machine/CPU information, exact model revision, endpoint,
    concurrency width, run count, all timings, mean, standard deviation, throughput, and any
    warm-up policy. State clearly that this is a CPU result only and makes no CUDA or alternative
    model claim.

    Run the official `make e2e` score gate after measurement and record its verdict. Do not tune
    Compose, swap models, add GLiREL, or attempt CUDA in this task.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gliner_cpu_probe.py -q &amp;&amp; make e2e</automated>
  </verify>
  <done>The same-code CPU path has a reproducible N>=3 measurement on the committed 167-chunk corpus, with raw evidence and a green E2E gate.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide the permanent GLiNER device and plan 15 corpus</name>
  <what-built>
    A measured CPU baseline for the current pinned FastGLiNER2 sidecar after bounded fan-out,
    with no provider, endpoint, CUDA, GLiREL, or model-topology changes.
  </what-built>
  <how-to-verify>
    Read `07.1-PERF-04-MEASUREMENT.md` and confirm the 167-chunk corpus, N>=3, exact provider
    configuration, variance, and E2E verdict are present.
  </how-to-verify>
  <decision>
    - `cpu-3-doc`: keep CPU for Phase 07.1 and use spike 003's three-document corpus in plan 15.
      Update the PERF-04 requirement and roadmap wording to the measured CPU+fan-out contract.
    - `cpu-12-doc`: keep CPU for Phase 07.1 but accept the measured cost of the full 12-document
      corpus in plan 15. Update PERF-04 and roadmap wording to the measured CPU+fan-out contract.
    - `resume-cuda`: leave PERF-04 pending and stop before plan 15 so a separately scoped CUDA
      binding/contestion plan can be prepared from the measured CPU baseline.
  </decision>
  <resume-signal>Choose cpu-3-doc, cpu-12-doc, or resume-cuda.</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_gliner_cpu_probe.py -q`
- `python scripts/gliner_cpu_probe.py --help`
- `make e2e`
- `python -m ruff format --check src tests scripts`
- `python -m ruff check src tests scripts`
- `bash scripts/check-file-size.sh`
- `docker compose config --quiet`
- `git diff --stat compose.yaml docker/` is empty for this plan
</verification>

<success_criteria>
The CPU sidecar has a reproducible same-code measurement on the committed 167-chunk corpus,
the E2E gate remains green, and the user has made the permanent device/corpus decision without
an unmeasured CUDA or model change.
</success_criteria>

<output>
Create `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-09-SUMMARY.md` only
after the decision checkpoint is resolved. Record the measured CPU results, E2E verdict,
device decision, corpus decision, and whether PERF-04 was completed or remains pending.
</output>
