---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 03
subsystem: database
tags: [arcadedb, entity-resolution, temporal-graph, stable-id, tdd]

requires:
  - phase: 04-arcadedb-direct-port
    provides: Tenant-scoped ArcadeDB entity vertices and bound-parameter memory writes
provides:
  - Name-only stable entity identity scoped by exact user_identifier
  - Immutable in-projection accumulation of per-label type observations
  - JSON STRING persistence for type observations on Entity vertices
affects: [07.1-10, 07.1-11, 07.1-14, 07.1-17, GRAPH-01]

tech-stack:
  added: []
  patterns:
    - Canonical-name-only entity identity with tenant scope retained as the first stable-ID part
    - Counter-shaped type evidence serialized through the existing canonical JSON helper

key-files:
  created: []
  modified:
    - src/turing_agentmemory_mcp/temporal_graph.py
    - src/turing_agentmemory_mcp/arcadedb_schema.py
    - src/turing_agentmemory_mcp/store_memory_queries.py
    - src/turing_agentmemory_mcp/store_memory_write.py
    - tests/test_temporal_graph.py
    - tests/test_store_arcadedb_retrieval.py

key-decisions:
  - "Keep internal-whitespace handling in canonicalize_entity_name unchanged; D-02 removes type from identity without introducing a second canonicalization rule."
  - "Persist type_observations as a canonical JSON string in an ArcadeDB STRING property, using bound parameters and the existing _json_dumps helper."

patterns-established:
  - "In-memory entity dedup keys and persistent stable-ID inputs must match argument-for-argument."
  - "Frozen EntityProjection updates copy and replace observation maps instead of mutating them."

requirements-completed: [GRAPH-01]

coverage:
  - id: D1
    description: "Key newly projected entities on canonical name plus exact tenant identity, independent of drifting entity labels."
    requirement: GRAPH-01
    verification:
      - kind: unit
        ref: "tests/test_temporal_graph.py#test_entity_key_uses_canonical_name_without_type_and_remains_tenant_scoped"
        status: pass
      - kind: e2e
        ref: "python scripts/e2e_score.py --out %TEMP%/turing-agentmemory-07.1-03-e2e.json: 10.0, 19/19"
        status: pass
    human_judgment: false
  - id: D2
    description: "Accumulate every normalized entity label as a per-type count without overwriting earlier observations."
    requirement: GRAPH-01
    verification:
      - kind: unit
        ref: "tests/test_temporal_graph.py -k type_observations: 5 passed"
        status: pass
    human_judgment: false
  - id: D3
    description: "Persist a parseable type_observations_json value through idempotent Entity schema and bound write parameters."
    requirement: GRAPH-01
    verification:
      - kind: unit
        ref: "tests/test_temporal_graph.py#test_type_observations_json_is_bound_on_entity_create_statement"
        status: pass
      - kind: integration
        ref: "tests/test_arcadedb_schema.py tests/test_tenant_query_scope.py: 75 passed"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-07-29
status: complete
---

# Phase 07.1 Plan 03: Name-Only Entity Identity and Type Observations Summary

**Tenant-scoped name-only stable IDs collapse GLiNER label drift while preserving every observed type as canonical JSON evidence on ArcadeDB Entity vertices**

## Performance

- **Duration:** 15 min
- **Started:** 2026-07-29T08:53:01Z
- **Completed:** 2026-07-29T09:07:27Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Re-keyed temporal entity projections from `(entity_type, canonical_name)` to canonical name alone while retaining exact `user_identifier` scoping in `stable_id`.
- Added immutable `type_observations` accumulation for drifting and repeated labels without changing highest-confidence display/content selection.
- Added idempotent `Entity.type_observations_json` schema plus canonical JSON serialization and bound-parameter persistence at the sole write call site.
- Preserved the deterministic retrieval/store gate at 10.0 with all 19 E2E checks passing.

## TDD Cycle

- **RED 1:** `6e4bf9a` proved label drift produced three IDs and no observation map existed.
- **GREEN 1:** `94c8b05` aligned in-memory and persistent identity and accumulated immutable counts.
- **RED 2:** `d7bf9ac` proved the entity statement rejected and omitted `type_observations_json`.
- **GREEN 2:** `3dea9df` added schema, bound persistence, and the canonical serialization call site.
- **REFACTOR:** No separate refactor commit was needed; the minimal GREEN implementation passed formatting, lint, file-size, focused, full-suite, Compose, and E2E gates.

## Task Commits

Each TDD phase was committed atomically:

1. **Task 1 RED: Name-only identity and accumulation behavior** — `6e4bf9a` (test)
2. **Task 1 GREEN: Name-only key and immutable observation counts** — `94c8b05` (feat)
3. **Task 2 RED: Entity observation persistence contract** — `d7bf9ac` (test)
4. **Task 2 GREEN: Entity schema and bound JSON persistence** — `3dea9df` (feat)

## Files Created/Modified

- `src/turing_agentmemory_mcp/temporal_graph.py` — Uses canonical-name-only entity keys and accumulates normalized label counts.
- `src/turing_agentmemory_mcp/arcadedb_schema.py` — Bootstraps the idempotent Entity observation JSON property.
- `src/turing_agentmemory_mcp/store_memory_queries.py` — Binds `type_observations_json` in entity creates with a parseable empty-object default.
- `src/turing_agentmemory_mcp/store_memory_write.py` — Serializes projection observations with `_json_dumps` at the sole entity-statement call site.
- `tests/test_temporal_graph.py` — Pins tenant isolation, type drift, repeated counts, non-overwrite behavior, confidence selection, and JSON persistence.
- `tests/test_store_arcadedb_retrieval.py` — Aligns the direct entity-expansion fixture with the new stored stable ID.

## Decisions Made

- Preserved `canonicalize_entity_name` exactly, including its internal-whitespace behavior. This plan changes whether type participates in identity; it does not introduce a second or migration-sensitive name canonicalizer.
- Stored observations as canonical JSON text in an ArcadeDB `STRING` property rather than introducing a native embedded-map shape with no repository precedent.
- Kept `entity_type` populated as the first observation's non-identity attribute while highest-confidence display/content behavior remains unchanged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Verification correctness] Preserved the established internal-space canonicalization contract**
- **Found during:** Task 1 RED
- **Issue:** The behavior example grouped `"ArcadeDB"` and `"Arcade DB"`, but the same task requires reusing `canonicalize_entity_name` verbatim; that established function preserves internal spaces, so those are not one canonical surface.
- **Fix:** Tested GLiNER type drift over case-equivalent forms that resolve to the same canonical name, keeping D-02 scoped to removing type from identity.
- **Files modified:** `tests/test_temporal_graph.py`
- **Verification:** The expected ID is derived directly from `canonicalize_entity_name("ArcadeDB")`; entity-key and full temporal tests pass.
- **Committed in:** `6e4bf9a`

**2. [Rule 1 - Test contract] Updated a direct retrieval fixture that derived the retired entity key**
- **Found during:** Task 2 full-suite gate
- **Issue:** `test_expand_entity_evidence_runs_two_hop_traversal_on_match_surface` looked up Bob with the old `(tenant, type, name)` ID after the write path correctly began storing `(tenant, name)` IDs.
- **Fix:** Derived the fixture ID with the new name-only stable-ID contract.
- **Files modified:** `tests/test_store_arcadedb_retrieval.py`
- **Verification:** Focused retrieval test passes; full suite passes with 864 tests and the E2E gate remains 19/19.
- **Committed in:** `3dea9df`

---

**Total deviations:** 2 auto-fixed verification/test-contract issues.
**Impact on plan:** Both changes keep verification aligned with the intended D-02 scope. No new identity scheme, tenant relaxation, or retrieval behavior was introduced.

## Issues Encountered

- The first E2E attempt reported ArcadeDB unavailable because the Compose service was stopped. The executor started the repository-owned service, waited for health, obtained `VALIDATED_10_10` with 19/19 checks, and stopped the service again.
- Ruff identified formatting normalization after each source/test edit; formatting was applied and every lint/file-size gate then passed.

## Recall Context

Configured Turing AgentMemory recall was unavailable in this executor session. No tenant identity was guessed and no memory call was attempted; execution used only checked-in plans, repository guidance, tests, and source evidence.

## Migration and Compatibility

This change affects **newly ingested data only**. Existing Entity vertices remain fragmented under the retired `(entity_type, canonical_name)` key until plan 11's tenant-scoped re-key rebuild runs. `memory_rebuild_vector_projection` and `memory_rebuild_communities` only re-embed or re-cluster existing rows and do not re-key them.

Plan 17 remains responsible for aligning the existing memory graph channel's query-side seed derivation with this write-side key and for activating mandatory hub damping before the re-key rebuild is eligible to run.

## Known Stubs

None. Added empty dict/list values are test inputs or initialized accumulation containers, not UI/runtime placeholders; no TODO/FIXME, skipped test, or unwired data path was introduced.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 07.1-10 can reuse the name-only Entity identity and persisted observation map for Chunk-to-Entity substrate writes.
- Plan 07.1-11 can re-derive already-stored entities from canonical tenant text under this exact identity contract.
- Plan 07.1-17 must make memory graph query seeds match this key and apply the already-planned hub counterweight before the rebuild lands.

## Self-Check: PASSED

- Confirmed all six modified source/test files exist and remain below 600 lines.
- Confirmed task commits `6e4bf9a`, `94c8b05`, `d7bf9ac`, and `3dea9df` exist in strict RED/GREEN order.
- Confirmed focused temporal tests (15), schema/tenant audits (75), full suite (864 passed), Ruff format/check, file-size, Compose, and E2E 10.0/19-of-19 gates pass.
- Confirmed `stable_id("ent", episode.user_identifier, canonical_name)` remains the sole projection identity mechanism and `type_observations_json` is bound rather than interpolated.

---
*Phase: 07.1-document-graph-rag-and-gliner-gpu*
*Completed: 2026-07-29*
