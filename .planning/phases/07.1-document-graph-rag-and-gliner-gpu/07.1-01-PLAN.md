---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, PERF-04, FIX-08]
must_haves:
  truths:
    - "REQUIREMENTS.md registers GRAPH-01..04, PERF-04, FIX-08; coverage reads 61 total with 0 orphans"
    - "ROADMAP.md Phase 07.1 carries the real goal from CONTEXT.md <domain>, the six requirement IDs, and success criteria derived from D-15's four GO conditions + D-16's disposition"
    - "No pre-existing requirement ID loses its phase mapping"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "REQUIREMENTS.md Traceability rows -> Phase 07.1 -> ROADMAP.md Phase 07.1 Requirements line (both must name the same six IDs)"
    - "ROADMAP.md Progress table row 7.1 -> plan count"
---

<objective>
Replace ROADMAP.md's `Goal: [Urgent work - to be planned]` / `Requirements: TBD` stub with the
real Phase 07.1 scope, and register the six new requirement IDs in REQUIREMENTS.md while
preserving its no-orphans coverage invariant (D-18).

Purpose: this phase currently has ZERO registered requirement IDs. Every other plan's
`requirements` frontmatter references IDs that do not yet exist in REQUIREMENTS.md. This plan
creates them. It is planned work, not bookkeeping that happens by magic.
Output: updated REQUIREMENTS.md (55 -> 61) and ROADMAP.md (Phase 07.1 entry + Progress row +
closing coverage line).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md
</context>

## Recorded planning decision: spec-less probe fallback SKIPPED

**Visible and recorded, not silent.** This phase has no `-SPEC.md`, so there was no
`## Edge Coverage` or `## Prohibitions` section to lift into `must_haves`. The spec-less probe
fallback ALSO skipped: it derives predicates from requirement text, and at planning time this phase
had **zero requirement IDs registered** — ROADMAP.md read `Requirements: TBD` and REQUIREMENTS.md had
no `GRAPH` category. The IDs are created by D-18, which is *this plan's own work*, so no
probe-derived predicates could exist for this run.

**Consequence:** every plan's `must_haves` in this phase is derived instead from (a) the phase goal
in `07.1-CONTEXT.md`'s `<domain>` section, (b) D-15's four GO conditions, and (c) D-19's constraints
— not from probe predicates. A future re-plan of 07.1, run after this plan lands, WOULD have the six
IDs available and could generate probe predicates.

## Artifacts this phase produces (this plan)

New symbols/rows created here — exclude from drift verification, they do not exist yet:

- REQUIREMENTS.md: new `### Document Graph RAG (GRAPH)` category section
- Requirement IDs: `GRAPH-01`, `GRAPH-02`, `GRAPH-03`, `GRAPH-04`, `PERF-04`, `FIX-08`
- REQUIREMENTS.md: six new `Traceability` rows mapped to `Phase 07.1`
- REQUIREMENTS.md: new `- Phase 07.1 (Document Graph RAG and GLiNER GPU): ... — 6` line under `**By phase:**`
- ROADMAP.md: Phase 07.1 `Success Criteria` block (does not exist today)

<tasks>

<task type="auto">
  <name>Task 1: Register GRAPH-01..04, PERF-04, FIX-08 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read in full. Note the exact shapes you must mirror:
      the `### Performance (PERF)` category (already has PERF-01/02/03), the
      `### Tech-Debt & Bug Fixes (FIX)` category (already has FIX-01..07), the
      `## Traceability` table at lines 124-181, and the `**Coverage:**` / `**By phase:**`
      blocks at lines 182-201.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md` — D-18 for the
      exact ID definitions; the `<domain>` section for phase scope.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-RESEARCH.md` — "Unknown 8b"
      for the verified mechanics of every edit below.
  </read_first>
  <action>
    Add a NEW `### Document Graph RAG (GRAPH)` category section, placed after the existing
    `### ArcadeDB Backend Replacement (ARC)` category and mirroring its header + `- [ ] **ID**: text`
    bullet style. Add these four unchecked bullets verbatim in intent:

    - `GRAPH-01`: Entity resolution — name-only entity key (`stable_id("ent", user_identifier,
      canonical_name)`) plus a `type_observations` map, applied to BOTH the memory and document
      paths, with mandatory hub damping, and derived projections re-keyed from canonical in-database
      text via a tenant-scoped re-key rebuild (D-02, D-03, D-04, D-19).
    - `GRAPH-02`: Chunk entity substrate — `(:Chunk)-[:MENTIONS]->(:Entity)` edges written from
      per-chunk extraction, tenant-scoped at every hop (D-12).
    - `GRAPH-03`: Multi-hop eval with a pre-committed GO/NO-GO gate — the decision artifact
      (D-06, D-07, D-08, D-15, D-16).
    - `GRAPH-04`: Flag-gated (default-OFF) document graph channel plus the `document_search`
      fusion port (D-13, D-14).

    Add `PERF-04` under the EXISTING `### Performance (PERF)` category: GLiNER GPU — CUDA-capable
    sidecar image, a black-box CUDA execution-provider binding proof, and a concurrency fan-out
    extraction path (D-09, D-11).

    Add `FIX-08` under the EXISTING `### Tech-Debt & Bug Fixes (FIX)` category: whole-document
    GLiNER extraction returns HTTP 400 — fix via per-chunk extraction after chunking plus
    `MAX_TEXTS=256` sub-batching (D-10).

    Add six new rows to the `## Traceability` table, each `| <ID> | Phase 07.1 | Pending |`.
    Place `GRAPH-01..04` as a contiguous block; put `PERF-04` immediately after the `PERF-03` row
    and `FIX-08` immediately after the `FIX-07` row so each ID sits with its category.

    Update the `**Coverage:**` block: `v1 requirements: 55 total (...)` becomes `61 total` and the
    parenthetical category counts gain `GRAPH 4` and become `FIX 8`, `PERF 4`. `Mapped to phases:
    55 ✓` becomes `61 ✓`. `Unmapped: 0 ✓` is unchanged.

    Add to `**By phase:**`, between the Phase 7 and Phase 8 lines:
    `- Phase 07.1 (Document Graph RAG and GLiNER GPU): GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, PERF-04, FIX-08 — 6`

    Do NOT renumber, reword, or re-map any existing requirement. This is additive only.
  </action>
  <acceptance_criteria>
    - `grep -c '^| GRAPH-0[1-4] | Phase 07.1 | Pending |$' .planning/REQUIREMENTS.md` outputs `4`
    - `grep -c '^| PERF-04 | Phase 07.1 | Pending |$' .planning/REQUIREMENTS.md` outputs `1`
    - `grep -c '^| FIX-08 | Phase 07.1 | Pending |$' .planning/REQUIREMENTS.md` outputs `1`
    - `.planning/REQUIREMENTS.md` contains the literal `61 total` and `Mapped to phases: 61`
    - `.planning/REQUIREMENTS.md` contains a `### Document Graph RAG (GRAPH)` heading
    - `.planning/REQUIREMENTS.md` contains the literal `Unmapped: 0`
    - The Traceability table has exactly 61 requirement rows; every one of the 55 pre-existing IDs
      still appears with its original phase mapping unchanged (verify with
      `git diff .planning/REQUIREMENTS.md` — no pre-existing row may show as modified)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "Phase 07.1" .planning/REQUIREMENTS.md | grep -qx 7 && grep -q "61 total" .planning/REQUIREMENTS.md && grep -q "Mapped to phases: 61" .planning/REQUIREMENTS.md && echo REQS_OK'</automated>
  </verify>
  <done>REQUIREMENTS.md registers all six new IDs, maps each to Phase 07.1, reads 61/61 with zero orphans, and shows zero modifications to any pre-existing requirement row.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the ROADMAP.md Phase 07.1 stub with the real goal and success criteria</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 287-310 — the Phase 07.1 stub and the Phase 8 entry immediately
      after it (copy Phase 8's `**Goal**` / `**Depends on**` / `**Requirements**` /
      `**Success Criteria** (what must be TRUE):` / `**Plans**:` structure exactly).
    - `.planning/ROADMAP.md` lines 369-390 — the `## Progress` table; row `7.1` is at line 384.
    - `.planning/ROADMAP.md` line 441 — the closing `*Coverage: 55/55 v1 requirements mapped...*`.
    - `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md` — the `<domain>`
      section IS the authoritative goal; D-15 (four GO conditions) and D-16 (NO-GO disposition)
      are the source of the success criteria.
  </read_first>
  <action>
    Replace the Phase 07.1 entry's `**Goal:** [Urgent work - to be planned]` and
    `**Requirements**: TBD` lines. Use scoped `Edit` calls — never rewrite ROADMAP.md wholesale.

    Set `**Goal**:` to a single outcome-shaped sentence derived from CONTEXT.md `<domain>`:
    the phase delivers the unconditional wins (correct entity resolution on both paths, GLiNER on
    GPU, the whole-document HTTP 400 ingest fix), plus a MuSiQue-style multi-hop eval with a
    pre-committed GO/NO-GO threshold that decides whether a flag-gated document graph channel
    ships at all.

    Set `**Requirements**: GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, PERF-04, FIX-08`.

    Keep `**Depends on:** Phase 7`.

    Add a `**Success Criteria** (what must be TRUE):` block with these numbered items:
      1. Entity identity is keyed on `stable_id("ent", user_identifier, canonical_name)` with
         `entity_type` demoted to a non-identity attribute plus a `type_observations` map, on BOTH
         the memory and document paths; mem0's quadratic hub damping `1/(1 + 0.001*(n-1)^2)` ships
         in the same change as its required counterweight (D-02, D-03).
      2. A tenant-scoped re-key rebuild re-derives entity identity from canonical in-database text
         (`Memory.content`, `Chunk.text`), fails closed on an empty `user_identifier`, is
         idempotent, and never deletes canonical rows; after it runs the E2E score gate still reads
         19/19 at 10.0 (D-19, D-17).
      3. Per-chunk entity extraction runs AFTER chunking in `MAX_TEXTS=256` sub-batches, so a
         document whose extracted text exceeds `MAX_TEXT_CHARS` (16,384) no longer returns HTTP
         400, and every chunk carries its own `entity_extraction` metadata (D-10).
      4. `(:Chunk)-[:MENTIONS]->(:Entity)` edges ship as production substrate, tenant-scoped at
         every hop (D-12).
      5. The GLiNER sidecar runs on GPU, proven by a BLACK-BOX binding check (an `nvidia-smi`
         GPU-utilization delta AND a throughput floor together — never either alone, and never a
         silent skip on a GPU-less runner); GPU contention against embed/rerank is measured before
         anything is tuned (D-09, D-11).
      6. A multi-hop eval of 30-50 mechanically-verified two-hop questions measures three arms
         (`fused_base`, `entity_boost`, `fused_graph`) on recall@k of the bridging passage set, and
         a GO requires ALL FOUR of D-15's conditions: (a) `fused_graph` beats the BEST of the two
         cheap arms by >= +0.10 mean recall@10; (b) improves >= 1/3 of questions with zero
         regressions; (c) no regression > 0.03 on spike 003's 15-question frozen yardstick;
         (d) latency <= +20% vs the best cheap arm.
      7. The verdict is recorded as an artifact that states demand is UNMEASURED (the eval proves
         efficacy only) and cites spike 003's 0/15 as the only real-traffic signal (D-06); on
         NO-GO the flag-gated channel and the fusion port are DELETED while the substrate and the
         eval are KEPT (D-16).

    Update `**Plans:** 0 plans` to `**Plans:** 17 plans` and replace the
    `- [ ] TBD (run /gsd-plan-phase 07.1 to break down)` line with the 17-item plan checklist
    (`- [ ] 07.1-NN-PLAN.md — <objective>`), reading each plan's `<objective>` from the PLAN.md
    files in this phase directory. Verify the count against
    `ls .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-*-PLAN.md | wc -l` rather than
    trusting this number.

    Note the checklist runs `07.1-01` through `07.1-17` with **no gaps** — plan 17 (the memory-side
    hub-damping wiring, D-03/D-19) was added during plan revision and is numbered last while
    executing in **wave 2**. Plan numbers in this phase are identifiers, not an execution order;
    `wave` in each plan's frontmatter is what `/gsd-execute-phase` reads.

    Update the Progress table row for `7.1` from `0/TBD` to `0/17`.

    Update the closing line at the end of the file from
    `*Coverage: 55/55 v1 requirements mapped to phases (no orphans, no duplicates)*` to
    `*Coverage: 61/61 v1 requirements mapped to phases (no orphans, no duplicates)*`.

    Leave Phase 8's `**Depends on**: Phase 7` UNCHANGED. Rationale to record in the SUMMARY
    (research Assumption A4, a judgment call left to planning): ROADMAP.md line 372 already fixes
    numeric execution order `7 -> 7.1 -> 8`, so 07.1 is already sequenced before Phase 8 without an
    explicit edge. Phase 8's document-ingestion reliability work does not require the D-12 substrate
    to be present or absent; adding a hard dependency would falsely narrow Phase 8's
    parallelizability off the port, which ROADMAP.md line 373 explicitly preserves.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` does NOT contain the literal `[Urgent work - to be planned]`
    - `.planning/ROADMAP.md` does NOT contain the literal `Requirements**: TBD` within the Phase 07.1 entry
    - The Phase 07.1 entry contains the literal `GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, PERF-04, FIX-08`
    - The Phase 07.1 entry contains a `**Success Criteria** (what must be TRUE):` block with 7 numbered items
    - `grep -c '7.1. Document Graph RAG and GLiNER GPU (INSERTED) | 0/17' .planning/ROADMAP.md` outputs `1`
    - The Progress-table plan count equals the number of PLAN.md files on disk: `test "$(grep -oP '(?<=INSERTED\) \| 0/)\d+' .planning/ROADMAP.md)" -eq "$(ls .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-*-PLAN.md | wc -l)"` exits 0
    - The `**Plans:**` line, the Progress-table row, and the checklist item count all state the SAME number (17)
    - `.planning/ROADMAP.md` contains `*Coverage: 61/61 v1 requirements mapped to phases (no orphans, no duplicates)*` and does not contain `55/55`
    - Phase 8's `**Depends on**` line is byte-identical to its pre-edit value (`git diff` shows no change to it)
    - Every other phase entry (1-7, 8-14) is unmodified in `git diff .planning/ROADMAP.md`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -q "Urgent work - to be planned" .planning/ROADMAP.md && { echo STUB_STILL_PRESENT; exit 1; }; grep -q "Coverage: 61/61" .planning/ROADMAP.md && grep -q "GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, PERF-04, FIX-08" .planning/ROADMAP.md && echo ROADMAP_OK'</automated>
  </verify>
  <done>ROADMAP.md's Phase 07.1 entry carries the real goal, the six IDs, seven success criteria traceable to D-15/D-16, a 17-plan checklist, a `0/17` Progress row matching the PLAN.md file count on disk, and a 61/61 coverage line — with every other phase entry untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan edits planning documents only; no runtime code, no data path, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-04 | Tampering | ROADMAP.md / REQUIREMENTS.md whole-file rewrite destroying unrelated phase entries or requirement mappings | medium | mitigate | Scoped `Edit` calls only, never `Write`, on both files; the acceptance criteria assert via `git diff` that every pre-existing requirement row and phase entry is unmodified. |
</threat_model>

<verification>
- `git diff .planning/REQUIREMENTS.md` shows additive changes only — no pre-existing row modified.
- `git diff .planning/ROADMAP.md` shows changes confined to the Phase 07.1 entry, the `7.1` Progress row, and the closing coverage line.
- The six IDs in ROADMAP.md's Phase 07.1 `**Requirements**` line exactly match the six IDs mapped to `Phase 07.1` in REQUIREMENTS.md's Traceability table.
</verification>

<success_criteria>
REQUIREMENTS.md reads 61/61 mapped with 0 orphans and registers GRAPH-01..04, PERF-04, FIX-08 against Phase 07.1. ROADMAP.md's Phase 07.1 stub is gone, replaced by the CONTEXT.md `<domain>` goal, the six IDs, and seven D-15/D-16-derived success criteria.
</success_criteria>

<output>
Create `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-01-SUMMARY.md` when done.
Record the Phase-8-`Depends on` judgment call (research Assumption A4) and its rationale.
</output>
</content>
</invoke>
