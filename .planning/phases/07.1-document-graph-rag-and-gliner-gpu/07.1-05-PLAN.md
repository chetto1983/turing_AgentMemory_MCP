---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "The checked-in GLiNER sidecar remains an exactly pinned, non-root CPU image and compose keeps GLINER_DEVICE=cpu"
    - "The CPU image builds and imports fast_gliner==0.2.1 successfully"
    - "The abandoned CUDA source-build experiment leaves no production or test diff"
    - "No CPU-vs-GPU performance winner is claimed before plan 06's concurrency fan-out enables a fair same-code measurement"
    - "GLiREL remains future research: it is not installed or wired into this runtime"
    - "GLiNER_ITA_BASE and gliner_multi-v2.1 ONNX conversions remain later CPU benchmark candidates, not FastGLiNER2 production drop-ins"
  artifacts:
    - docker/gliner-provider.Dockerfile
    - compose.yaml
  key_links:
    - "Pinned CPU Dockerfile -> compose GLINER_DEVICE=cpu -> measurable post-plan-06 CPU baseline"
    - "Plan 06 concurrency fan-out -> same-code CPU measurement -> later device decision"
---

<objective>
Retain and validate the hardened CPU GLiNER sidecar as the phase's current experimental baseline.

Purpose: the original CUDA source-build attempt did not produce a usable image. Ubuntu package
fetches returned HTTP 403 responses and BuildKit then stalled while materializing the CUDA devel
layer. The user explicitly redirected the work to CPU-first experimentation because GLiNER, and
possibly a future GLiREL integration, may be faster on CPU. This is an experiment-order decision,
not a measured CPU win and not a permanent rejection of CUDA.

Output: the pre-existing pinned CPU image and Compose contract are restored exactly, built, and
smoke-tested. The device verdict is deferred until plan 06 lands concurrency fan-out and the CPU
path can be measured fairly with the same code. PERF-04 remains pending.
</objective>

<execution_context>
@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md
@CLAUDE.md
@docker/gliner-provider.Dockerfile
@compose.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore and validate the pinned CPU GLiNER sidecar</name>
  <files>docker/gliner-provider.Dockerfile, tests/test_docker_hardening.py</files>
  <action>
    Remove the uncommitted CUDA experiment and restore the checked-in CPU Dockerfile and its
    hardening test contract exactly. Keep the pinned `python:3.12-slim` digest, exact
    `fast_gliner==0.2.1` pin, uid/gid 10001, non-login user, cache paths, non-root entrypoint,
    and Compose `GLINER_DEVICE=cpu`.

    Build the image and run a provider import smoke check. Validate Compose and the focused
    Docker hardening test. Do not run another CUDA build.
  </action>
  <acceptance_criteria>
    - `git diff --exit-code -- docker/gliner-provider.Dockerfile tests/test_docker_hardening.py` exits 0
    - The Dockerfile has one digest-pinned `python:3.12-slim` `FROM`, exact `fast_gliner==0.2.1`, `USER app`, uid/gid 10001, and the provider entrypoint
    - The `agentmemory-gliner` Compose service contains `GLINER_DEVICE=cpu`, keeps its non-root/read-only/no-new-privileges/tmpfs hardening, and does not request a GPU
    - `docker build -f docker/gliner-provider.Dockerfile -t turing-agentmemory-gliner-provider:local .` exits 0
    - `docker run --rm --entrypoint python turing-agentmemory-gliner-provider:local -c "import fast_gliner; print('IMPORT_OK')"` prints `IMPORT_OK`
    - `docker compose config --quiet` exits 0
    - `python -m pytest tests/test_docker_hardening.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>docker build -f docker/gliner-provider.Dockerfile -t turing-agentmemory-gliner-provider:local .; docker run --rm --entrypoint python turing-agentmemory-gliner-provider:local -c "import fast_gliner; print('IMPORT_OK')"; docker compose config --quiet; python -m pytest tests/test_docker_hardening.py -q</automated>
  </verify>
  <done>The original hardened CPU image contract is restored with no production/test diff, builds successfully, imports the pinned provider, and Compose remains CPU-only.</done>
</task>

<task type="auto">
  <name>Task 2: Record the CPU-first experiment order without deciding the winner</name>
  <files>.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md, .planning/ROADMAP.md</files>
  <action>
    Record the failed CUDA attempt and the user's CPU-first override in durable phase context.
    Update the Phase 07.1 roadmap and plan-05 description so neither claims a CUDA image shipped.
    State explicitly that:

    - plan 06 must land before a fair CPU throughput measurement;
    - CPU-vs-GPU remains undecided;
    - PERF-04 remains pending;
    - GLiREL is not installed or wired and remains future research.
    - `onnx-community/GLiNER_ITA_BASE` and `onnx-community/gliner_multi-v2.1` are
      Transformers.js-oriented ONNX conversions that require a separate adapter/model benchmark
      against the same Italian corpus; they are not production switches in this plan.
  </action>
  <acceptance_criteria>
    - CONTEXT.md records the CUDA attempt, CPU-first override, deferred device verdict, GLiREL scope boundary, and both later ONNX CPU candidates
    - ROADMAP.md describes plan 05 as a validated CPU baseline rather than a delivered CUDA image
    - ROADMAP.md keeps PERF-04 in Phase 07.1 and states that its full contract remains pending
    - REQUIREMENTS.md still shows PERF-04 as Pending
  </acceptance_criteria>
  <verify>
    <automated>rg -n "CPU-first|PERF-04 remains pending|GLiREL" .planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-CONTEXT.md .planning/ROADMAP.md; rg -n "PERF-04.*Pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The execution history and changed experiment order are durable, while the unmeasured device decision and unchanged PERF-04 requirement remain open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build -> PyPI | The image installs the already-approved exact `fast_gliner==0.2.1` wheel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-12 | Spoofing | An unmeasured CPU run being reported as faster than CUDA | high | mitigate | Make no device-performance claim; measure after plan 06 with identical fan-out code and parameters. |
| T-07.1-SC | Tampering | Provider supply chain | medium | mitigate | Retain the exact approved package version and digest-pinned base image; add no GLiREL dependency. |
</threat_model>

<verification>
- CPU Dockerfile/test contract matches HEAD exactly.
- CPU image builds and imports `fast_gliner==0.2.1`.
- `docker compose config --quiet` and the focused Docker hardening test pass.
- Phase context and roadmap record the CPU-first override without completing PERF-04.
</verification>

<success_criteria>
The pinned and hardened CPU GLiNER sidecar is buildable and smoke-tested; no CUDA experiment
diff or GLiREL scope expansion remains; the device-performance verdict is explicitly deferred
until post-plan-06 measurement; PERF-04 remains pending.
</success_criteria>

<output>
Create `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-05-SUMMARY.md`.
Record the CUDA build attempt evidence, CPU build/smoke results, absence of a performance claim,
the GLiREL scope boundary, the two later ONNX CPU candidates, and the deferred post-plan-06
device decision.
</output>
