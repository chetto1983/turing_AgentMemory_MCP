---
phase: 07.1-document-graph-rag-and-gliner-gpu
plan: 02
subsystem: database
tags: [arcadedb, documents, mixins, tenant-isolation, file-size]

requires:
  - phase: 04-arcadedb-direct-port
    provides: Tenant-scoped ArcadeDB document ingest, lifecycle, and hybrid search paths
  - phase: 01-stabilization-foundation
    provides: Mixin-composed store facade and the no-allowlist 600-LOC gate
provides:
  - Dedicated document-ingest write-path mixin with verbatim ingest behavior
  - 327 lines of headroom in store_documents.py for FIX-08 and GRAPH-04
  - Behavior-preservation evidence for the public document store API
affects: [07.1-08, 07.1-12, FIX-08, GRAPH-04]

tech-stack:
  added: []
  patterns:
    - Concern-specific store mixins composed through the TuringAgentMemory MRO
    - Pure structural extraction proven by AST identity and unchanged audit coverage

key-files:
  created:
    - src/turing_agentmemory_mcp/store_documents_ingest.py
  modified:
    - src/turing_agentmemory_mcp/store_documents.py
    - src/turing_agentmemory_mcp/store.py

key-decisions: []

patterns-established:
  - "Document writes live in _DocumentsIngestMixin; reads, lifecycle deletion, and search remain in _DocumentMixin."
  - "Pure store-module moves preserve method ASTs, tenant guards, audit calls, and facade resolution before behavior work continues."

requirements-completed: [FIX-08, GRAPH-02, GRAPH-04]

coverage:
  - id: D1
    description: "Extract ingest_document_text, reindex_document_text, _create_document, and _update_document_metadata verbatim into a dedicated document-ingest mixin."
    requirement: FIX-08
    verification:
      - kind: unit
        ref: "AST comparison of HEAD store_documents.py methods against store_documents_ingest.py"
        status: pass
      - kind: other
        ref: "bash scripts/check-file-size.sh; store_documents.py 258 LOC and store_documents_ingest.py 343 LOC"
        status: pass
    human_judgment: false
  - id: D2
    description: "Preserve the public TuringAgentMemory document API, explicit tenant scoping, audit calls, and managed write behavior."
    requirement: GRAPH-02
    verification:
      - kind: unit
        ref: "tests/test_store_arcadedb_documents.py: 14 passed"
        status: pass
      - kind: unit
        ref: "python -m pytest -q: 858 passed, 11 skipped"
        status: pass
      - kind: other
        ref: ".venv/Scripts/python API callable assertion: API_INTACT"
        status: pass
    human_judgment: false
  - id: D3
    description: "Leave document search in _DocumentMixin with enough headroom for the later GRAPH-04 fusion port."
    requirement: GRAPH-04
    verification:
      - kind: other
        ref: "grep acceptance gates: search_documents count 1; moved-method counts 0; _DocumentsIngestMixin facade count 2"
        status: pass
      - kind: other
        ref: "ruff format/check, 600-LOC gate, and Docker Compose configuration"
        status: pass
    human_judgment: false

duration: 17min
completed: 2026-07-29
status: complete
---

# Phase 07.1 Plan 02: Document Ingest Mixin Extraction Summary

**Verbatim document-ingest write-path extraction reduces `store_documents.py` from 585 to 258 lines while preserving tenant-scoped APIs, audits, and transaction behavior**

## Performance

- **Duration:** 17 min
- **Started:** 2026-07-29T08:31:27Z
- **Completed:** 2026-07-29T08:48:27Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Moved the four ingest/write methods into a 343-line `_DocumentsIngestMixin` without changing their ASTs or tenant-scoped behavior.
- Reduced `store_documents.py` from 585 to 258 lines, leaving 342 lines below the hard cap and 327 lines of new headroom.
- Preserved all five public document methods through the `TuringAgentMemory` MRO, all three audit calls, and the complete green validation gate.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract the ingest write path into store_documents_ingest.py** — `54c634e` (refactor)
2. **Task 2: Prove the move is behavior-preserving across the full suite and commit gate** — `7704832` (test, verification-only empty commit)

## Files Created/Modified

- `src/turing_agentmemory_mcp/store_documents_ingest.py` — Owns document ingest, reindex, create, and metadata-update writes.
- `src/turing_agentmemory_mcp/store_documents.py` — Retains context, get, delete, and search behavior with 327 lines of new headroom.
- `src/turing_agentmemory_mcp/store.py` — Composes `_DocumentsIngestMixin` adjacent to `_DocumentMixin` without reordering existing bases.
- `.planning/phases/07.1-document-graph-rag-and-gliner-gpu/07.1-02-SUMMARY.md` — Records the structural and verification evidence.

## Decisions Made

None - followed the plan's verbatim-move and MRO-ordering instructions.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Verification correctness] Corrected the impossible unchanged-suite-count expectation**
- **Found during:** Task 2 (full-suite behavior proof)
- **Issue:** The plan expected the same pre/post test count, but `tests/test_vector_id_absent.py` parametrizes two invariant tests over every `store*.py` module. The required new module therefore raises the green count from 856 to 858 without adding or changing tests.
- **Fix:** Verified both added cases are the expected vector-ID and ArcadeDB-RID guards for `store_documents_ingest.py`; retained the stronger coverage instead of suppressing or editing tests.
- **Files modified:** None beyond the planned new store module.
- **Verification:** Pre-move 856 passed / 11 skipped; post-move 858 passed / 11 skipped; both new cases pass.
- **Committed in:** `7704832`

---

**Total deviations:** 1 auto-handled verification correction.
**Impact on plan:** No behavior or scope change. The higher test count is deterministic evidence that the new module participates in existing store-wide invariants.

## Issues Encountered

- The plan's raw `python -c` API assertion used the global interpreter, which does not have this `src/`-layout project installed. The repository-supported `.venv\Scripts\python.exe` resolved the installed package and printed `API_INTACT`.
- The first format check identified newline/format normalization in the two edited existing modules. Ruff 0.15.21 reformatted them, after which formatting, lint, file-size, focused tests, the full suite, and Compose validation all passed.

## Recall Context

The configured Turing AgentMemory skill was available, but its live MCP recall tools were not exposed in this executor session. No tenant identity was guessed and no memory call was attempted; execution relied on checked-in planning context and repository evidence.

## Known Stubs

None. The changed modules contain no TODO/FIXME markers, placeholder UI values, skipped tests, or unwired data paths.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 07.1-08 can add FIX-08 per-chunk extraction to the dedicated write-path mixin.
- Plan 07.1-12 can port GRAPH-04 document fusion in the 258-line read/search mixin.
- The cross-phase windows ledger records the explained +2 parametrized-test deviation for ship-time visibility; no production blocker remains.

## Self-Check: PASSED

- Confirmed `store.py`, `store_documents.py`, and `store_documents_ingest.py` exist.
- Confirmed task commits `54c634e` and `7704832` exist.
- Confirmed `store_documents.py` is 258 lines, `store_documents_ingest.py` is 343 lines, and the combined `_audit(` count remains 3.
- Confirmed all focused, full-suite, formatting, lint, file-size, Compose, and public-API checks pass.

---
*Phase: 07.1-document-graph-rag-and-gliner-gpu*
*Completed: 2026-07-29*
